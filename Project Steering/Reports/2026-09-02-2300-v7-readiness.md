# v7 launch-distance report — 2026-09-02, ~23:00 Berlin

**Provenance.** Compiled by a read-only analyst agent on the Master Mind's brief, from primary sources in this
precedence: `MODEL_REGISTRY.md` → raw JSON/logs → `GOALS_AND_CLAIMS.md` (register rows) → `V7_LAUNCH_GATE.md` →
everything else (INHERITED). Line references are to those files at HEAD `e69039c`. The Master Mind reviewed the
report but did not re-run the reads; treat every line as INHERITED-from-the-agent until the cited row is opened.
Class + tier on every number. Nothing here is a capability claim.

## A. What "v7 ready" means, in the programme's own words

- PI directive (BINDING): *"we should not start training of v7 until the remaining problems are solved"* —
  gate:3–4, register D-V7-GATE. Closure rule: *"no item below may be marked closed by me. I can report that a
  criterion is met; the PI closes"* — gate:12–15.
- The gate's five items and their closing criteria: **P1** a v7 arm beats hold-action at T1 across the four
  families, paired bootstrap, echo clean (gate:34–35); **P2** the model-does-not-use-actions causes (a)(b)(c)
  each measured, or one confirmed with a material action-ratio rise, plus a PI call on comma2k19 geometry
  (gate:162–165); **P3** the one-variable freeze ablation reads ≈0.199 or ≈0.669 (gate:222–226); **P5** an arm
  clears L3 (`zhat` beats `z_t` paired, |t| ≥ 2.9) while `splitp30k` still fails it (gate:267–269); **P4** the
  recipe reaches the tactical band AND the strategic level has a mechanism — a temporal-abstraction ladder, not a
  longer flat rollout (gate:280–282, 307–311).
- The proof ladder L0–L5 (`V7_RECIPE_AND_SCALEUP.md:263–270`): L4 = T1, S-curve above hold-action, ADE below the
  CV floor 0.5352 m, four families — *"no v7 arm has ever reached it"* (:296). Tier law: T0 is never driving
  performance; capability needs T1 (`EVAL_DOCTRINE.md:10–17`).
- Launch hygiene: no launch before the B1 manifest is banked and content-verified (`B1_TRAINING_PREP.md:18–19`);
  per-run prereg with committed outcomes, done-marker, four-family T1 plan with echo control (:56–59); nav token
  mandatory at all three layers (`SPEC_NAV_CONDITIONING_ALL_LAYERS.md:3–11`); **every new training runs one full
  epoch** (register D-ONE-EPOCH, PI 2026-09-02) — this contradicts `BINDING_TRAINING_IMPROVEMENTS.md:16–33`
  (*"There is no epoch … Raising steps to reach one epoch is forbidden"*); the newer PI ruling supersedes, the
  doc is stale.

## B. Gate table

| item | status | MEASURED evidence (tier) | what closes it |
|---|---|---|---|
| **P1** no arm beats hold-action | **FAILED at the only read; OPEN** | registry:3784–3802 — T1, 3 arms, 6,924 windows, episode-cluster bootstrap: `emao14_30k` ADE cl/ha 14.069/13.879, all three `LOSES_TO_HOLDV0`, heading ~95°; `copy_detector=CLEAN`, echo 0.0000. First parity T1: ADE 14.52 m [11.55, 17.58] vs CV floor 0.5352 (registry:3868). Scope: ~19 M v7-tiny, S-W only, planner objectives at zero (registry:3798) | gate:34 |
| **P2** model does not use actions | **OPEN** — (a) horizon: NOT the lever (T0); (b) command channel: never tested, corpus has none; (c) untested; (d) teacher-forced target: untested | MM-E10 ratio 0.00408–0.00595 (register MM-E10, T0); MM-E11 O1 → 0.40×; MM-E19 k=60 → 0.50×; k=8 clip-0.5 control 1.04× ⇒ one-variable: action 0.83×, scene 1.71× (MM-E19-K8); E-DEC-57 r 0.9988; O11 breakout at step 5,400 (gate:127–141) unattributed, 4 config diffs + a scene-matching shortcut (gate:142–152) | gate:162–165; the 4-min actdiv read on `o11p30k` decides the 8.6 h re-run (gate:154–160, 395–398) |
| **P3** drift 3.3× unexplained | **Criterion MET by rows the gate does not cite — the registry wins** | `postrain30k_freeze` = `postrain30k` + `--freeze-encoder` only, drift **0.3905** (registry:3818–3820, 3826) — exactly the gate's "one variable, matched otherwise" arm (gate:222–224), read 2026-08-27. E-DEC-64: DEGENERATE, nrmse 0.9301 vs 0.8115 (+14.6 %); E-DEC-66: freeze×k4 interaction 0.3905→0.199, *"C164's leftover fully resolves"*. T0 | Nothing to run: freezing is the main effect and it is degenerate ⇒ the lever dissolves (gate:212–213). The gate's queue item #3 (gate:392) duplicates a banked arm (md5 `5f5e5c92…`). Remaining half is P5's L3 read on those checkpoints (probe, no training) |
| **P5** predictor adds nothing over `z_t` (L3) | **OPEN**; L1/L2 PASSED | L1: participation 3.80/3.62 → 25.58/26.96 (registry:3907); L2: `splitp30k` `n_agents` +0.3881 > DINOv3 +0.2754 (registry:3958), held-out +0.1220 vs +0.0998. L3: `splitp30k` predictor delta −0.0008/−0.0320/−0.0158, t −3.69/−5.62/−6.26 (`V7_RECIPE…:104`). Census: 0/32 arms move >0.1 % under action shuffle (registry:4017–4025). T0 | gate:267–269 |
| **P4** horizon ladder short of labels | **OPEN**; k=60 answered "no" | bands 0–2/2–6/8–30 s, median manoeuvre 12.5 s; trained horizon 0.8 s at k=8; k=60: gnorm 2.1e9, killed at 9,000 (`PREREG_MM_E19…:298–309`); the clip-0.5 successor spiked to 1.24e10 at 18k; Lab: no primary back-props 60 steps, the fix is a curriculum (`…/2026-09-01-longhorizon-bptt-stability/RESULT.md:14–34`, PUBLISHED); MM-E16 stride prereg written, unrun; the corpus reaches K=9 (18 s) (D-EPISODE-LENGTH) | gate:280–282; the L-1 curriculum arm judged on the action-side SMAS statistic (`…/2026-09-02-scene-matched-action-criterion/RESULT.md:133–150`) |
| closed 08-31 | nav join, dead horizons, v7.2 md5 keying, 6 s cost 2.90 s/step / 24.2 h / 6.27 GB | gate:372–379 | — |

**Three prerequisites not on the gate, MEASURED from source:**
1. The v7 trainer still cannot read the v7.2 tactical/strategic labels: `--s2-labels` help reads *"s2-strategic-v1
   label artifact"* (`stack/scripts/train_v6_staged.py:7431–7432`); `load_v7_labels` is imported only for nav
   (:5525–5549); the schema sniff of `SPEC_V7_LABEL_TRAINER_WIRING.md:27–33` is absent at two probes.
   `intrain_eval.py` (`resolve_v72`, :381) is imported by nothing but its own test. ⇒ **v7f S-W can launch without
   labels (`V6F_PLANNER_DESIGN.md:484–488`); S-T/S-S cannot consume the mandated labels.** D-V72-WIRING is still
   open on the trainer side.
2. No B1 parity key: `stack/tanitad/data/parity_manifest.json` carries exactly four keys (train/val
   `e438721ae894` / `0c5f7dac3b11`, ± `-w120-256x640cyl`); `--require-parity` cannot enforce four-arms-same-corpus.
3. No k=60 stabiliser in the v7 trainer: `rollout_transitions` is *"not truncated BPTT"*
   (`stack/tanitad/models/metric_dynamics.py:264`; `train_v6_staged.py:7940–7945`), no curriculum/AGC/truncation
   flag among 38 hits — refav1 got `bptt_truncate=15`, v7 did not. Step-stamped checkpoints ARE implemented
   (`train_v6_staged.py:6354–6387`); the `V7_RECIPE…:201` line saying otherwise is stale.

## C. Proven / refuted for the v7 recipe (class · tier)

- Collapse SOLVED: participation ×7 at parity (`rdw8p30k`) — MEASURED · T0 (registry:3907; H-RANK-11).
- Representation carries environment (L2) — MEASURED · T0; target-specific, loses on free space/side occupancy.
- Predictor beats a constant only by temporal extrapolation, never by action — MEASURED · T0 (registry:4017–4025,
  4061–4065).
- Action is realised motion, not a command (r 0.9988) — MEASURED, PI-raised; action redundant for the future scene
  (E-DEC-48b) but NOT redundant given the latent (MM-E12); ego marginal over drift −0.0006, t −0.48 (E-DEC-59) — T0.
- Sensitivity is acquired from ~0, 20–50× slower than needed (MM-E13); h≥2 heads never trained (MM-E14);
  attenuation 56×/128× located in FiLM (MM-E17); gain converged, not starved (MM-E18) — MEASURED · T0.
- O1 inert (0.40×); k=60 horizon: action 0.83×, scene 1.71× against the one-variable control (MM-E19-K8) —
  MEASURED · T0, no interval (L-13), window-count confound open (L-10). The prereg bar floated (E1).
- Drift: training manufactures it (E-DEC-61); init is NOT the lever (E-DEC-60, C164); freeze degenerate (E-DEC-64);
  k4 inert on trainable (E-DEC-66); O14 absorbs pixel content, not drift (E-DEC-67); EMA teacher = the largest
  prediction gain, fixed τ, ramp neutral (registry §13.10 `emao14_30k_tauramp`) — MEASURED · T0.
- Velocity at cycle time admissible; GT future never (D-VELOCITY-AT-CYCLE-TIME, PI ruling).
- Nav: PI-reviewed, usable; provenance `ego-future` on 100 % ⇒ route accuracy admissible only under nav-shuffle.
- HYPOTHESIS, untested: P2(d) teacher-forced target (UWM-JEPA, PUBLISHED-PRIMARY, gate:53–83); LeWM independently
  measured action-insensitive on the same diagnostic (INHERITED — `2026-09-02-LAB-RUN-008.md:13–30`, via a critic).

## D. The reference arms (no registry row exists for either — 0 hits at 10 patterns, two probes)

| | **refcv3** (A40 pod) | **refav1** (Thor) |
|---|---|---|
| run | `/workspace/experiments/refcv3-b1-v72-30k`, re-armed to **40,284 steps** = 1 epoch @b20 (D-ONE-EPOCH); base rung 107,032,901 params, PI override of the ≤80 M budget | `/home/nvidia/experiments/refav1-b1-v72-1ep-21109`, launched 16:02:49Z, **21,109 steps** = 1 epoch @b8, ~20.6 s/step |
| state (T0 in-training monitor, never driving) | train loss 27.1→8.1, eval loss 19.1→8.6, eval traj 2.43→1.09 on 160 fixed windows; 7 restarts (5 OOM kills, diagnosed tonight); 4.56–4.74 s/step | step 750: op loss 1.38→0.50 against a pinned target, `adapter_std` 0.478→0.68 then flat, tactical target OSCILLATING (share 5→36 % row to row), grad_norm 1.3–7.6 under clip 1.0 |
| confounds | nav source switched refb→v7.2 token at **S = 16,500** (41 % of the run on a 94.6 %-`follow` signal, 65.5 % agreement); eval-prior leak (~7.7 %/eval × 34 evals; gated in `1f9d08d`, live from 17,500); strategic width rebalanced d_ctx 64→256 | tactical/strategic targets unanchored (inflation ×104, now oscillation); anti-collapse attribution not one-variable; route/nav same source ⇒ echo hazard; val600 ∩ B1-train = 47 clips — any val600 number is contaminated; one fp8 episode missing (being rebuilt) |
| baseline admissibility | **UNAVAILABLE today**: no taniteval HIER adapter (two-probe absence); the F arm of the H−F dominance pair has not run (50–58 h pod); needs the nav-shuffle control and S quoted | **UNVERIFIED**: T1 adapter with hold-action + nav-shuffle + lead block exists but is untested on a real checkpoint; the first read is the step-1000 checkpoint with `cl_oraclegoal` beside `cl` |

Both train on B1, so same-corpus comparability with v7f holds in principle — but windowing differs 4.6×
(170.9 vs 36.9 windows/episode), so "one epoch" is not one sample budget.

**MEASURED from raw logs tonight:** the TSC-2 dev-box probe's deliberate-regression arm B′ did **not** reproduce
the inflation (`tgt_std_tac` 0.0201 @1 → 0.0197 @250; `…/2026-09-02-refav1-ema-inflation/raw/B_prime.log`) ⇒
per its own R1 branch the rig is insensitive, VOID; and R7 shows a `--resume` with `--ema-targets` fails strict
load on 20 missing `ema.*` keys (`raw/R7_resume.log`) ⇒ a mid-run EMA switch is blocked as implemented.

## E. Blockers → cheapest discriminating experiment (costs ESTIMATED unless noted)

| blocker | experiment | cost | decides |
|---|---|---|---|
| P2 lever | actdiv on `o11p30k/ckpt.pt` | ~4 min GPU (gate:390); runnable on the 4060 | whether the 8.6 h O11 re-run runs at all |
| P2(b) | CAN steering vs curvature proxy on comma2k19 | 0 GPU; data not on Thor (gate:109) | whether a command channel deserves an arm; geometry is a PI call |
| P2(d) control | measure the dataset's future-action horizon, then same-clip negatives | 0 GPU, then one 8.6 h arm | whether O11 discrimination is dynamics or scene-matching (gate:439–452) |
| P5/L3 (+P3 dual read) | `zhat` vs `z_t` on banked `postrain30k` / `postrain30k_freeze` / `splitp30k` | probe only, hours on the 4060 | replaces gate queue #3 without training |
| P4 stability | L-1 horizon curriculum at k=60, SMAS-judged; L-10 explicit `--max-horizon` control | ~24 h Thor each (2.90 s/step MEASURED) | whether 6 s is trainable at all on this rig |
| instrument | GS-1 audit: endpoint-decoding probes confounded by construction | 0 GPU | may reopen eliminated P2 branches (INHERITED, LAB-RUN-008:23–27, 74) |
| v7f code | v7.2 sniff + intrain_eval hook + B1 parity key + curriculum/truncation | 0 GPU | S-T/S-S launchability |

Compute reality: Thor is held by refav1 to ~09-08 (or ~09-05 under the pre-registered bf16 restart), the A40 by
refcv3 H then F to ~09-06; only the 4060 is free now.

## F. Corpus and data readiness

| artifact | state | consumer need |
|---|---|---|
| B1 4,719 clips, 26.2 h, manifest sha256 `5feda062a72a32ad`, all joins 4719/4719 | VERIFIED (D-CORPUS-B1) | all four |
| parity-overlap exclusion: 6 val40 clips ⇒ **4,713** enter any train cache (`--exclude-parity-overlap`) | BUILT: Thor epcache ~178 GB; pod build 4,719 | v7f, refcv3 read `.v2ep.pt` directly; refav1 needs features |
| DINOv3 fp8 features 4,713 (291 GB) + val600 (37 GB); fp8 gate PASS | BUILT, content-verified; Thor ~98 GB free | refav1 only |
| v7.2 labels: train 4,572, eval 147; canonical md5 `0ff90213…`/`aa12c948…` (`intrain_eval.py:346–352`) | DELIVERED; **D-V72-SPLIT's md5s `0a586fe8…`/`bffc9df5…` are the pre-schema-fix blobs** (`intrain_eval.py:327–331`) — that register row is stale | consumer `v7_labels.py` — wired into refav1/refcv3, NOT into the v7 trainer (§B) |
| eval split with pixels | **141 of 147** (6 val40 clips have labels, no pixels) — a register row says "2 of 147 dropped": conflicts with 4,713 − 4,572 = 141; needs reconciliation | four-family T1; the 6 need a separate eval-side cache |
| val40 | trajectory-only; 6/40 labelled | never for label families |
| nav token | joined, count-verified, `NavEmitter` | all layers, mandatory |
| REF-D | "does not exist" (`V7_RECIPE…:199`) vs "heads wired; model never trained" (`B1_TRAINING_PREP.md:52`) — INHERITED conflict | fourth consumer undefined |

## G. PI decisions (default in bold)

1. P3: **accept the criterion as met by `postrain30k_freeze` (registry), retire queue #3; the open item becomes L3.**
2. GPU order when Thor frees: **actdiv `o11p30k` (4060, now) → L3 probe (4060) → L-1 curriculum arm → O11 re-run
   only if actdiv clears it.**
3. comma2k19 geometry: **letterbox with explicit mask, correlation first, no arm.**
4. refav1 inflation: **the pre-registered clean-epoch restart (E-ARCH-REFAV1-EPOCH-2) is the EMA test, since the
   slice rig is VOID; the mid-run switch is blocked by the strict-load failure until the load path initialises
   missing `ema.*` keys from the student.**
5. v7f scale/horizon: config E is 336.5 M (`HANDOVER_TO_LOCAL_2026-08-15.md:76–79`, INHERITED) vs "sub-300 M"
   (`ROADMAP.md:24`) vs the Lab's "beat ~40 M" — **pin the budget in the v7f prereg; no flat k=60 without
   curriculum/truncation.**
6. One-epoch rule vs A1 "no epoch": **retire A1.**
7. Label provenance: **canonise `intrain_eval.V72` md5s, fix D-V72-SPLIT.**
8. Eval count 141/147 and REF-D: **141; declare REF-D not a consumer until a trainer exists.**
9. refcv3 F arm: **run after H, sequential, or the dominance claim is unsupported.**

## H. Distance and risks

Decisions: the nine above. GPU: the tiny-ladder gate work is ~3–4 Thor-days (ESTIMATED: two ~24 h arms, two
~8.6 h arms, probes) and cannot start before Thor frees; the v7f run itself, config E on B1 for one epoch at k=60,
has **no MEASURED cost** — the only anchors are 2.90 s/step on a 19 M tiny rig and a config-E OOM at k=60 before
checkpointing, so no day count is quoted. Substantively, the gate is not schedule-bound: **no measured intervention
has raised action sensitivity** (two moved it the wrong way), and P1 cannot close until one does. Earliest honest
v7f launch is a tiny-ladder positive on P2 plus the trainer wiring in §B — mid-September at best (ESTIMATED).

Top three risks: (1) P2 has no positive lever — the O11 breakout is unattributed and may be scene-matching; (2)
k=60 is untrainable as implemented and the v7 trainer carries no stabiliser; (3) neither reference arm has an
admissible number and both carry confounds (nav switch + prior leak; inflation + echo hazard), so "v7 beats the
references" may be unmeasurable when v7f finishes.
