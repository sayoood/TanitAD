# X1 — Red-team of `00_PROGRAMME_REVIEW.md` before it goes to the PI

**Date:** 2026-09-25 · **Model:** Fable 5.1 (one bounded pass) · **Method:** static; every check below is against the primary source named in the row (code at file:line at `main` `467ce8a`, `MODEL_REGISTRY.md`, raw JSON, the stream files). No pod, no torch. 27 claims were checked; the 15 most load-bearing are in §5, the rest are folded into the findings.

**Headline for the orchestrator.** The synthesis is sound in its centre of gravity (the headline is WM fidelity; the hierarchy's own decision paths lose; the selector is the lever; the data usage is a sliver) and every number I could trace to a raw JSON reproduced. Six things would embarrass the PI or misdirect compute if left as written, and each has a one-line fix: (1) the verdict says the programme "has not built a driver that beats a simple baseline" when its own REF-C clears every floor; (2) the "1.9×" bound rests on a deprecated split-mean of a CEM planner that was handed a *future* speed target; (3) PhysicalAI-AV has **7** camera streams, not 6; (4) the Thor tick is described as "one rollout of given actions" when the runbook says it is the 9-manoeuvre `TacticalSelector` decision — a path that has **no ADE anywhere**; (5) §4.4 quotes two "speed bias" numbers 5× apart from two instruments under one metric name, and RR-20 is not in the registry; (6) the "R² 0.9999" goal claim has no source and is true by construction. Fixes are given verbatim.

---

## 1. MUST-FIX

### M1 — §1 verdict: "It has not yet built a driver that beats a simple baseline."
**Problem.** False as written and unfair to the programme. REF-C-base (0.4728) and REF-C-XL (0.4714) clear the CV floor separated and clear CTRV (0.523) and best-of-3 (0.5005) on the point estimate (MEASURED, `MODEL_REGISTRY.md` §6 rows 1= and the "trivial bars" note). What the review means is that the **hierarchy's own** decision paths (1.9028, 3.3839) do not clear CV (0.8377). The sentence as written hands the PI a claim any reader of the leaderboard can refute.
**Replacement (§1, line "It has not yet built …"):**
> It has not yet built a *hierarchical* driver that beats its own flat reference: REF-C clears all three trivial floors (CV separated; CTRV and best-of-3 on the point estimate, `MODEL_REGISTRY.md` §6), while the hierarchy's two decision paths (1.9028 plan-tracked, 3.3839 direct) do not clear CV (0.8377). The review now explains why, and the explanation changes the plan.

### M2 — §1 claim 1 and §2 scorecard: "beats it by 1.9× to 7.2× (0.4728 vs P2-over-WM 0.893 legacy …)"
**Problem.** Two rule breaches in one number. (a) 0.893 is the DEPRECATED `legacy_split_mean` (registry §6 row 10: "NOT RECOMPUTABLE — no raw JSON and no `windows_*.pt`"); comparing it with the full-set 0.4728 mixes estimators, which CLAUDE.md forbids. (b) P2 is a CEM planner over the frozen WM fed a **future-derived speed target** (85th-percentile free-flow speed over the next 10–20 s; R1 table row f) — it is neither a hierarchy path nor future-blind, so it cannot anchor "paths where the hierarchy itself must decide". R1 carried the caveat ("legacy, *and* P2 has a privileged future speed target"); the synthesis dropped it. On full-set numbers the ratios are 1.9028/0.4728 = **4.0×** and 3.3839/0.4728 = **7.2×**.
**Replacement (§1 claim 1, last sentence):**
> On the two paths where the hierarchy itself decides, the flat 104 M REF-C-base beats it by 4.0× to 7.2× (0.4728 vs plan-tracked 1.9028 and tactical head 3.3839, all full-set, `closedloop_flagship-30k.CORRECTED.json`). The P2 CEM planner's 0.893 is a deprecated split-mean of a path handed a future speed target and is not comparable (registry §6 row 10).
**Replacement (§2 row "Core-thesis validation", "why" cell):** `1.9–7.2×` → `4.0–7.2×`.

### M3 — §1 claim 5, §2 "Data strategy", §7.1, §10.6: "1 of 6 cameras" / "6 camera views"
**Problem.** PhysicalAI-AV ships **seven** camera streams: `camera_front_wide_120fov`, `front_tele_30fov`, `cross_left_120fov`, `cross_right_120fov`, `rear_left_70fov`, `rear_right_70fov`, `rear_tele_30fov` (MEASURED in-repo, `…/Data Engineering/Implementation/incoming/2026-07-26-physicalai-feature-probe/PHYSICALAI_FEATURE_PROBE.md:70-76`; row 4 there: "per-clip × 7 cams"). R3 listed six and omitted `rear_tele`. The PI knows this dataset; a wrong camera count in the first section costs the review its credibility on data.
**Replacement:** every `1 of 6 cameras` → `1 of 7 camera streams`; `6 camera views` → `7 camera views (front-wide 120°, front-tele 30°, cross L/R 120°, rear L/R 70°, rear-tele 30°)`; §7.1 evidence class → `MEASURED (in-repo feature probe) + PUBLISHED (HF card, via search)`. The 306,152 clips / 1,700 h figure is confirmed by the same probe (`:42`, `:125`, with the HF split train 153,625 / val 90,928 / test 61,599 — see S7).

### M4 — §4.8, §1 verdict bullet 3, §2 "Embedded deployment": the Thor tick
**Problem.** §4.8 says "The timed tick is one rollout of *given* actions, not a decision." The runbook says the opposite: the 60.3 ms tick is "encoder + strategic head + tactical head + **9-candidate fan** + `step_readout` decode + SE(2) + **scoring**" (`THOR_DEPLOYMENT_RUNBOOK.md:286-288`), and its 2026-08-03 annotation (`:134-137`) states that the deployed `TacticalSelector` "loops over NINE" manoeuvres (`config.py:95`: 3 steer × 3 accel; `fourbrain.py:571`). So the tick *is* a decision — over a hand-built 9-primitive vocabulary scored by "predicted ego displacement toward sub-goal + comfort" (`fourbrain.py:566-568`). The finding the review misses is larger than the wording: **that path has no ADE anywhere.** `eval_behavior.py:23-25` records that `TacticalSelector` is "not wired into the trained model, goal-conditioned on an externally supplied sub-goal, scored over hand-built action primitives"; no `taniteval/results/*.json` scores it. The 60.3 ms and the 0.4271 (or any leaderboard number) belong to **different paths**, and the dossier must not put them side by side as one system.
**Replacement (§4.8, second bullet):**
> **The timed tick is v1's `TacticalSelector`: a 9-manoeuvre (3 steer × 3 accel) imagine-and-select over the world model with a heuristic displacement-plus-comfort score (`fourbrain.py:566-571`, runbook `:288`). It is a decision, but a decision on a path that has never been scored open- or closed-loop (`eval_behavior.py:23-25`; no results JSON). A world-model-scored decision over a REF-C-class 256-candidate fan would be about 32 saturated Thor batches × ~70 ms ≈ 2.2 s, roughly 22× over budget (ESTIMATED, R1 §8).**
**Replacement (§1 verdict bullet 3):** `The planning tick on Jetson Thor is 60.3 ms p50 / 63.1 ms p95 against a 100 ms budget` → `A v1 tick (encoder, heads, a 9-manoeuvre imagined fan, scoring) runs in 60.3 ms p50 / 63.1 ms p95 on Jetson Thor against a 100 ms budget — on a selector path that has no open-loop score yet`.
**Add to §13 P0:** `| P0 | The Thor-timed path (TacticalSelector, 9 primitives) has no ADE on the canonical val | eval_behavior.py:23-25; runbook :134-137 | score it once on val-40 (four families) so the dossier's latency and accuracy refer to one path, or label the tick "latency of the v1 selector path, accuracy not measured" | ≤ 0.2 A40-day |`

### M5 — §4.4 (two "speed bias" values), §1 claim 4, §11 #4, §6.6: RR-20
**Problem A — internal inconsistency.** §4.4's table says the flagship's WM-fidelity path "runs fast vs the human **+0.1911 m/s** [+0.0922, +0.2846]" (distance-keeping artifact). Six lines later, cause 4 says the K = 20 fine-tune removed a bias of **+0.9397 → −0.0092 m/s**. Both are "speed bias" on the same path, 5× apart, unexplained. The source of the split: `fourfam_rrctl.json`/`fourfam_rr20.json` report `speed_bias_mps` **0.9397 → −0.0092** (`four_families.py:185`), while `rrctl.json`/`rr20.json` in the *same folder* report `speed_bias_mps` **+0.1879 → −0.0018** (`driving.py:342`), and the driving-harness value matches the +0.1911 of 2026-08-04. Same key name, two instruments, two speed derivations (mechanism UNVERIFIED here — the definitions differ in how speed is taken from the rollout). A PI will ask "which is it?" and the review currently answers both.
**Problem B — registry status and evidence class.** RR-20 / RR-CTL appear nowhere in `MODEL_REGISTRY.md` (grep `RR-20|rollout.?recovery|rr20|rrctl`: none). The raw JSON exists (`…/Evaluation/Implementation/incoming/2026-08-02-rollout-recovery-verdict/rr20.json`: 0.3485 [0.2911, 0.4110]; `rrctl.json`: 0.4244 [0.3678, 0.4826]; n 881/40, cluster bootstrap), so the numbers are MEASURED-by-path, not INHERITED — but they are **unregistered**, and CLAUDE.md makes the registry the only quotable source. §11 #4 ranks the lever "proven in-house" while §6.6 says the same run "has no admissible verdict" under its own pre-registration (primary `CR_k` never computed). Both cannot stand, and Δ7 (0.9 A40-days) is funded on it. The raw ab file also shows the trade the prose softens: RR-20 wins on straight windows (win-rate 0.70, n = 634) and **loses on sharp curvature** (win-rate 0.43, n = 122; `ab_rrctl_vs_rr20.json` `by_curvature`), and the deep review records miss@2m 0.043 → 0.056 and yaw-rate worse.
**Replacement (§4.4 cause 4):**
> **The world model is trained at K = 4 and scored at K = 20.** The K = 20 fine-tune (RR-20) cut WM-fidelity ADE 0.4244 → 0.3485 (paired, separated; MEASURED `…/2026-08-02-rollout-recovery-verdict/rr20.json`, `rrctl.json`, `ab_rrctl_vs_rr20.json`, 881/40) and removed the speed bias on both instruments that measure it — driving harness +0.1879 → −0.0018 m/s (`rr20.json`), four-family +0.9397 → −0.0092 m/s (`fourfam_rr20.json`; the two instruments derive speed differently and disagree 5× on the *pre*-fix bias, which must be reconciled before either is quoted alone). Cost: curvature MAE 2.2× worse, miss@2m 0.043 → 0.056, and a loss on sharp-curvature windows (win-rate 0.43, n = 122). ⚠️ RR-20 is not in `MODEL_REGISTRY.md` and its pre-registered primary `CR_k` was never computed (§6.6).
**Replacement (§1 claim 4, last bullet):** `(+0.94 → −0.009 m/s) … (INHERITED, V5_FLAGSHIP_DEEP_REVIEW.md §1 P5)` → `(driving-harness +0.19 → −0.002 m/s; four-family +0.94 → −0.009 m/s) … (MEASURED, raw JSON in …/2026-08-02-rollout-recovery-verdict/, not yet registered; primary CR_k pending)`.
**Replacement (§11 #4 "maturity" cell):** `proven in-house` → `measured in-house on a secondary endpoint (ADE); primary CR_k pending; lateral family worsened`.
**Add to §13 P0:** `| P0 | RR-20 / RR-CTL unregistered; two speed-bias instruments disagree 5× under one key | …/2026-08-02-rollout-recovery-verdict/ | register both arms in MODEL_REGISTRY with all five JSONs; state which speed_bias_mps is canonical (four_families.py:185 vs driving.py:342) | 1 hour |`

### M6 — §4.3 fix 4, §10.1, §11 #6/#10 context: "the goal carried no information beyond (v, ax_fd) (R² 0.9999)"
**Problem.** The number is untraceable (not in `EGOAL_4.md`; R1 §3.3 gives no path), and the finding is true **by construction**: E-GOAL-4's goal feature `g_along` *is* E-GOAL-3's `T_OOF|H_v0_ax`, a fitted function of `v` and `ax_fd` (`EGOAL_4.md:50`; audit G-6: a refit of `GBM(v, ax_fd)` reproduces it to max |Δ| = 0.0). A goal defined as f(v, ax_fd) cannot carry information beyond (v, ax_fd); no vision-derived or route goal was tested. The blanket "do not fund a goal supplier" therefore rests on a tautology plus the oracle-route fusion failure (§7.8), not on a test of the geometric goal point the literature and the PI's 2026-08-03 ruling both point at. This matters because D3 and the v6 STRATEGIC line are built on it.
**Replacement (§4.3 fix 4):**
> 4. Predicted goal-point conditioning as an inductive bias (Δ4). Do not fund a goal *supplier* yet: the only goal tested so far (E-GOAL-4) was itself a fitted function of `(v, ax_fd)` (`EGOAL_4.md:50`, audit G-6), so its gain (0.3917 → 0.3040, 600-episode, out-of-fold within val) is a re-parameterisation of ego kinematics, not new information; and the oracle route command failed at the fusion (§7.8). A vision-predicted geometric goal point is untested here; Δ4 is where it earns or loses its place.
**Replacement (§10.1 GoalFlow bullet):** `On this corpus, however, a goal adds nothing beyond (v, ax_fd) at 2 s (R1 §3.3).` → `No goal tested on this corpus has added information beyond (v, ax_fd) — but the only one tested was constructed from (v, ax_fd) (§4.3). Treat the geometric goal point as an untested inductive bias, not a refuted lever.`
Delete `(R² 0.9999)` wherever it appears unless a raw path is added.

### M7 — D5: "the leaderboard closes 2026-10-31" stated as fact in the decision table
**Problem.** V1 #28: "closing date 2026-10-31 not explicitly confirmed"; R4 took it from a search snippet; §8.6 correctly marks it UNVERIFIED. The decision table — the part the PI acts on — states it as fact. A wrong date costs the entry.
**Replacement (D5 "decision" cell):** `the leaderboard closes 2026-10-31. Confirm front-camera eligibility this week` → `the closing date (reported 2026-10-31, UNVERIFIED) and front-camera-only eligibility (default config is 4 cameras) must both be confirmed this week`.

---

## 2. SHOULD-FIX

### S1 — D2 asks for a 6–12 A40-day commitment on an INHERITED, in-val-trained result
CLAUDE.md: a claim that decides a GPU-day must be MEASURED or PUBLISHED, never INHERITED. E-GOAL-4 is INHERITED in §1.3; its selector was trained **inside the 600 val episodes** (5-fold out-of-fold; `EGOAL_4.md:91`, `:139-140`, `:409`), never on the parity train corpus; and 0.3040 is the future-blind `sel` background (0.3102 under `parent_resampled`). The review's own Δ2 is the replication. Stage the decision.
**Replacement (D2):** `D2a (now, 0.5 A40-day): Δ2 — retrain the expected-cost selector on parity-train fan dumps and score it paired on val-40 and val-600; E-GOAL-4 graduates only if the gain survives. D2b (on graduation): make REF-C the driving baseline-of-record and build v6 around the decision layer (…as written…), ≈ 6–12 A40-days to a gated first result.` Mark E-GOAL-4 in §1.3 as `MEASURED-by-path (…/2026-07-28-egoal-4-joint/raw/e4_select_sel.json), out-of-fold within val-600, never trained on the train corpus`.

### S2 — §1.3 places a 600-episode number beside 40-episode numbers without the deployment warning
0.5015 / 0.3917 / 0.3040 sit two paragraphs under 0.4271 / 0.4518 / 0.4728. The registry (§6 deployment stamp; §1.2a) forbids cross-deployment comparison: the 600 is an easier corpus (CV floor 0.6917 vs 0.8377). §8.4 says so; §1 does not.
**Add after the E-GOAL-4 sentence in §1.3:** `These are 600-episode numbers (a different, easier deployment: CV floor 0.692 vs 0.838; REF-C-XL's own pick there is 0.5015) and are not comparable with the 40-episode figures in claim 1.`

### S3 — D3 contradicts §4.9 on when hierarchy-vs-flat is testable
D3: "Pre-register hierarchy-vs-flat only once a strategic information source exists." §4.9: "The tactical level is falsifiable now" (Δ4). Both are right about different levels; the decision text conflates them and reads as deferring the July ask again.
**Replacement (D3 decision cell, last sentence):** `Pre-register the tactical-level test now (Δ4, 0.3 A40-day, frozen dumps); pre-register the strategic-level test only once a corpus with a route or goal source exists (AlpaSim routes, L2D).`

### S4 — v6 diagram (§4.9) vs the vision-only ruling and the goal/classifier disjointness rule
The diagram feeds `ego: v0, ax_fd, 8-step speed history, yaw rate` into the decoder block that also hosts `TACTICAL = factorised lateral(3) × longitudinal(3) posterior, kinematic labels`. §7.4 itself records the PI's 2026-08-03 ruling: labels derived from ego dynamics make any ego-fed classifier read its own label source — inference must be vision-only. The tactical posterior's inference inputs are not stated. Likewise the ruling requires that when one trunk feeds both the goal and the situation/manoeuvre path, the review says so and justifies why it is not a back door; the diagram shares the patch tokens and says nothing.
**Add under the diagram:** `Inference inputs, stated per the 2026-08-03 rulings: the tactical posterior reads patch tokens and z only (vision-only at inference; its kinematic labels are used at label time only); the goal head reads patch tokens, z and ego kinematics and never the tactical posterior; both share the encoder trunk, which is admissible because neither head's output is an input to the other — Δ4's R² test against the flat inputs is the check that the shared trunk has not re-created the leak.`

### S5 — §4.9: "The existing evidence already leans this way: REF-B v2 … 0.5913 against flat REF-C's 0.47"
REF-B v2 vs REF-C differ in decoder family (unimodal heads vs a 128/256-anchor diffusion decoder), label version (registry R7: v2.1 vs v1 labels) and trainer; the gap says nothing about hierarchy per se. The review criticises exactly this class of confound elsewhere (C6, v2corpus).
**Replacement:** `The one existing hierarchical-vs-flat pair (REF-B v2 0.5913 vs REF-C-base 0.4728, separated) leans this way but is confounded by decoder family and label version, so it is a prior, not evidence.`

### S6 — §14.1: the ten-day plan needs a cut line, a dependency and an alignment statement
(a) **Dependency.** Days 2–5 run Δ2/Δ3/Δ8 "on val-40 and the 600-episode deployment". The val-40 fan dumps are in-repo (`taniteval/results/fan_refc-{base,xl}-30k.pt`, `windows_*.pt`); the **600-episode fan dump is not** (search found none; E-GOAL-4 notes "the poses live on pod2", `EGOAL_4.md:81`, and pod2 is recorded as terminated in the registry). Regenerating it needs the REF-C-XL checkpoint, the val-600 corpus and a GPU — say so and cost it, or run days 2–5 on val-40 only (MDE 0.06–0.16 m, §8.4) and defer the 600 arm to Phase A.
(b) **Load.** Days 3–7 (envelope v0 on Thor) overlap days 5–9 (paired closed loop on Thor for three arms) and days 1–2 (runner wiring) for a small team whose fleet has been dark 52 days. Give the dossier a priority order so a slipped item still leaves a defensible deliverable: 1 registry split (D1) → 2 four-family panel on val-40 for v1/REF-C → 3 Δ2 on val-40 → 4 Thor tick stated honestly (M4) → 5 existing 12-scene closed-loop table as-is → 6 envelope v0 → 7 new closed-loop runs.
(c) **Alignment with what 05.10 scores.** `Mission Plan.md:181` defines Phase 0 as "a running architecture with the most important hypotheses like the 4B architecture, world model based approach … ideally both in open loop and closed loop". The review's v6 keeps four *roles* but removes the four *brains* as built (Δ5). Tell the PI this plainly in §14.3 and ask which reading the 05.10 evaluation uses; P8 ("be honest, admit failures") supports the review's stance, but the choice is the PI's.
**Add to §14.3:** `Whether Phase 0's "4B architecture" is scored as the four brains as built (v1 satisfies it and loses to a flat planner) or as four measurable roles (v6). This review recommends the second and says so in the dossier.`

### S7 — Bet C / D4 / §10.4: "the unused 99 %" must exclude the HF val/test splits and the closed-loop scenes
PhysicalAI-AV's own split is train 153,625 / val 90,928 / test 61,599 clips (`PHYSICALAI_FEATURE_PROBE.md:125`). Label-free pretraining on "the unused 99 %" would sweep in the official val/test clips and any clip whose NuRec reconstruction is in the 12-scene suite or the AlpaSim challenge — contaminating the one external number the review wants (same class as the REF-A I-JEPA leak, CLAUDE.md). Admissible material is the HF **train** split (≈ 853 h), minus scene clips.
**Replacement (Bet C first bullet):** `Pretrain the world model label-free on the unused part of PhysicalAI-AV's *train* split (≈ 153 k clips / ≈ 850 h; never the HF val/test splits, never a clip whose NuRec scene is in the closed-loop suite or the challenge — register the exclusion list before the run), front-wide + front-tele, and later on front-camera web video (LFG-style).`

### S8 — §1 verdict: "integrates driving controls as accurately as a physics model does"
The comparison is unpaired, the bicycle's CI is wide ([0.3097, 0.6174]) and the paired delta was never computed (§4.1 says so). "As accurately as" is a tie claim from overlapping unpaired intervals; the honest form is "not separated from". The bicycle floor is also not a clean oracle — it integrates steer labels built with a 2.9 m wheelbase against true 2.73–3.22 m (§7.4).
**Replacement:** `A camera world model whose latent integrates given driving controls about as well as a kinematic bicycle model (0.4271 [0.3675, 0.4871] vs 0.4518 [0.3097, 0.6174]; unpaired, not separated, paired delta not yet computed).`

### S9 — §1 verdict / §3: "NuRec scenes render and drive closed-loop on the same board"
The videos README (`…/Evaluation/Videos/alpasim-closedloop-thor-2026-08-03/README.md`) records four closed-loop videos (flagship v1 × REF-C base × traffic/empty) **rendered on Thor** and md5-matched; it does not say the driving policy's inference ran on Thor, and `ALPASIM_ON_THOR_PATH.md:65` lists "driver=tanitad on Thor" as a planned step. Say "rendered on Thor; the closed-loop driver's inference location is not recorded" unless a log shows otherwise.

### S10 — §4.3: "recovered about 32 % of the gap" vs E-GOAL-4's own "+35.62 %"
Two gap definitions (oracle-in-fan 0.1547 vs `R_goal2s` 0.1933 as the floor). Name the definition once, or the "4× disagreement" with v1.2's 8.4 % (which the registry defines against the *oracle* gap, in-sample, 40 episodes) is partly a units artefact. Also note the registry's own standing claim that "the oracle gap is ~92 % irreducible" (`MODEL_REGISTRY.md:1355-1357`) — E-GOAL-4's 35–64 % recovery directly contradicts it, which strengthens the review's call to reconcile before funding.

### S11 — §14.2 Phase A gate: "non-inferior on the rest, and non-inferior in closed loop"
Non-inferiority needs a pre-registered margin; at n = 40 the MDE is 0.06–0.16 m (§8.4), so a val-40 non-inferiority claim is unpowered by construction. State the margin and the deployment (val-600) in the gate.

---

## 3. NIT

- **N1 §4.4 table** "87 % of v1's WM-fidelity squared error is longitudinal | 0.8733 | MEASURED" has no path; it is `…/2026-07-26-closedloop-artifact-rerun/latlon_decomposition.json`. Likewise "45.4 %" (§4.3, §5) lives in `…/2026-08-03-esel-verdict/raw/full/esel_probe_refc-base-30k.json`, not the scaleab file the row cites; "9.68 %" is `…/2026-08-03-refc-corpus-and-labels/CORPUS_LABEL_MANIFEST.json`.
- **N2 §2 "Core-thesis validation D".** The review's own words are "not refuted … unpaid-for"; a letter grade for an untested thesis reads as "failed". Consider "D (untested; the decisive experiments were not run)".
- **N3 §1 "A measurement culture better than most funded labs'."** Unsupported comparative; keep the praise, drop the comparison.
- **N4 §4.6** DINO-WM (arXiv 2411.04983) is not in V1's 31 — mark "not V1-checked" like the other unchecked citations (the ID is correct to my knowledge, but the review's own rule is to say so).
- **N5 §14.4 / Appendix C** prices match the API reference cached 2026-06-24 (Haiku 4.5 $1/$5, Sonnet 5 $2/$10, Opus 5.5 $4/$20, Fable 5.1 $10/$50). The same reference lists Opus 5.5 as *launching*; name the fallback (Opus 5, $5/$25) for the orchestrator/adjudicator rows.
- **N6 D2 / §4.9 / Bet A / §14.2** quote four different costs for "a first decision-layer result": ≈ 1 A40-day (Bet A first gate), 5.8 (the ladder), 6–12 (D2), 10–20 (Phase A). Reconcile in one line: first gate ≈ 1, full ladder ≈ 6, Phase A ≈ 10–20.
- **N7 §4.2 seams** — all three verdicts HOLD (nav echo 1.0000 per CLAUDE.md; ctx→tactical +0.0148 full-set per CLAUDE.md; 0/3 in `V5_FLAGSHIP_DEEP_REVIEW.md:73`). Note the v2corpus 0/3 is on 19 leak-free episodes, n = 418 (`:203`), not the canonical 40.
- **N8 §9.8 "≥ 5 streams should be conditional"** is a proposal to change a binding rule; label it as a proposal for the PI, which §14.4 rule 1 then already applies correctly.
- **N9 Bet C "R² ≥ 0.8 as CLAUDE.md requires"** — CLAUDE.md's R² rule is about learning-curve exponents vs step; extending it to a data-fraction slope is sensible but is the review's extension, not the rule.

---

## 4. Logic, rules and strategy — what is *not* wrong

Checked and consistent: evidence-class discipline is applied throughout §4–§8 (the only unlabelled numbers are those in M5/N1); estimator use is correct everywhere except M2; the four-family rule is honoured in the plan (§14.1 day 1–2) and correctly flagged as unenforced (§8.1); the vision-only ruling is respected in §10.5–10.7 (train-time-only structure, label-time teachers); parity is not touched; the AlpaSim citation trap (§8.5) and the registry's stale "TanitEval uncommitted" (§8.8; 263 tracked files, verified) are real; the follow-through table (§3) and commit timeline (149 commits over 9 days, 49 on 2026-08-03, silence since 08-04) reproduce from `git log`; the registry-integrity suite is red exactly as O1 states (re-run today: 2 MISSING; `tools/tests` 237 pass / 2 fail / 1 skip).

---

## 5. Spot-check table (15 load-bearing claims; verdicts on the review's text as written)

| # | claim (section) | source checked | verdict | correction |
|---|---|---|---|---|
| 1 | §1.1 0.4271 is scored with the expert's future controls; harness labels it `expert_future` / `wm_fidelity_ade_2s` | `taniteval/taniteval/rollout.py:146-147` (`fa = ep.actions[t+window : t+window+fwd_k]`), `:181-185` | HOLDS | — |
| 2 | §1.1 registry records the fact at `:192` yet §6 ranks "1=" | `MODEL_REGISTRY.md:188-197`; §6 rows at `:1803-1805` (three 1= rows) | HOLDS | — |
| 3 | §1.1/§4.1 bicycle 0.4518 [0.3097, 0.6174]; plan-tracked 1.9028 [1.732, 2.0745]; tactical head 3.3839 [2.8336, 3.9722]; re-plan −0.1711 [−0.2615, −0.0838] | `…/2026-07-26-closedloop-artifact-rerun/closedloop_flagship-30k.CORRECTED.json` (881 windows / 40 episodes, episode_cluster_bootstrap, B = 2000) | HOLDS | — |
| 4 | §1.1/§2 "beats it by 1.9× to 7.2× (… P2-over-WM 0.893 legacy …)" | registry §6 row 10 (`legacy_split_mean`, NOT RECOMPUTABLE); R1 row f (P2 fed a future speed target) | WRONG (estimator mix; not a hierarchy path) | M2: 4.0×–7.2× on full-set paths only |
| 5 | §1.2 cadence configured, read by no code | `config.py:118` (`cadence = 5`), `:140` (`cadence = 20`); only docstring refs `fourbrain.py:376-377` | HOLDS | — |
| 6 | §1.2/§6.3 22 M H15 parameters never trained in the v4/v5f line | `train_flagship_v4.py:390,395,999`; `config.py:154` (`enabled = True`), `:349`; `fourbrain.py:456` (built), no `self.imagination(` call in forward; `train_flagship4b.py:655-658` (v1 computes the loss); registry §1.8 v5f `--from-scratch`; `eff_flagship-30k.json` imagination 22.0557 M | HOLDS | — |
| 7 | §1.3 E-GOAL-4 0.5015 → 0.3917 → 0.3040 (600-episode) | `EGOAL_4.md` §4 table; n = 600, 5-fold out-of-fold **inside val-600** (`:91`, `:139-140`); 0.3040 = `sel` background, 0.3102 = `parent_resampled`; raw `raw/e4_select_sel.json` | HOLDS as numbers; class/caveats incomplete | S1, S2 |
| 8 | §4.3 "goal carried no information beyond (v, ax_fd) (R² 0.9999)" | `EGOAL_4.md:50` (`g_along` = f(v, ax_fd) by definition), audit G-6; 0.9999 absent | UNTRACEABLE / by construction | M6 |
| 9 | §1.4/§4.4 RR-20 0.424 → 0.348; speed bias +0.9397 → −0.0092 | `…/2026-08-02-rollout-recovery-verdict/rr20.json` 0.3485 [0.2911, 0.4110], `rrctl.json` 0.4244 [0.3678, 0.4826]; `fourfam_*.json` +0.9397 → −0.0092 vs `rr20/rrctl.json` +0.1879 → −0.0018; not in registry | ADE HOLDS; bias OVERSTATED/ambiguous; unregistered | M5 |
| 10 | §1.5/§7.1 "1 of 6 cameras", 306,152 clips | `PHYSICALAI_FEATURE_PROBE.md:59,70-76` (7 camera streams), `:42,:125` (306,152) | WRONG on cameras; clips HOLD | M3 |
| 11 | §1/§4.8 Thor tick 60.3/63.1 ms ±13 %; "one rollout of given actions, not a decision" | `THOR_DEPLOYMENT_RUNBOOK.md:286-304`, annotation `:134-137`; `config.py:95`, `fourbrain.py:566-571`; `eval_behavior.py:23-25`; artifact `…/2026-08-03-thor-batch9-engine/thor_d6_tick_intent_K20.json` in repo | numbers HOLD; characterisation WRONG; path unscored (missing finding) | M4 |
| 12 | §4.2 180.6 M of 276.9 M on the scored path; ≈ 85 M off-path | `eff_flagship-30k.json` `by_module_m`: encoder 87.02 + predictor 91.36 + readout 0.10 + step_readout 2.11; tactical_pred 26.54 + tactical_policy 22.74 + imagination 22.06 + strategic 8.39 + inv_dyn 5.25 = 84.97 | HOLDS | — |
| 13 | §4.3 v5f probe tokens have no candidate axis; hard-argmin CE | `flagship_v15.py:654` (`IMAGINATION_HAS_CANDIDATE_AXIS = False`), `:864-866` (`r_star = fan_err.argmin`, CE on `sel_score`) | HOLDS | — |
| 14 | §4.4 cross-track +0.772 [0.4166, 1.1914]; along +0.2543 [−0.0278, 0.5304]; 639 steady windows 0.4231 vs 0.2109 | `taniteval/results/driving_flagship-30k.json` | HOLDS | — |
| 15 | D5 "the leaderboard closes 2026-10-31" | `V1_citation_check.md:37` (#28: "closing date 2026-10-31 not explicitly confirmed"); R4 `:81` (from search) | OVERSTATED in D5 (§8.6 is correct) | M7 |

Also checked, all HOLD: `runner.py:341-342` flagship-only gate; `four_families` has no functional caller (docstring mentions only) and 0 of 103 result files carry `_binding_rule`; `refc.py:413` `ego_dropout = 0.5`; `dino_precompute.py:44` last-frame only; `flagship_losses.py:227-238` v0 broadcast; `train_flagship4b.py:229` no stop-gradient on the H15 target, `:794` `--op-fwd-k 4`; `closedloop.py:265-271` re-plan per tick, `:782` paired bootstrap; `config.py:395` `free_dims = 64`, `:253` `v2_invdyn_gradscale = 1.0` (0.25 only under a flag, `train_flagship4b.py:368`); registry v1.6 canary 0.452 → 1.1022 / 144 % (`:612,:626`); v1.2 "at most 8.4 % of the ORACLE gap" (`:1356-1357`); wheelbase +8.6 % (`:70`); `IDM_DIAGNOSIS.md:15` r = 0.434; `DISTANCE_KEEPING_ARMS.md:160,370` +0.1911 [+0.0922, +0.2846], LEAD 270 / NO_LEAD 551 / NO_LABEL 60; `HPP0_CONFOUND_AUDIT.md:457` (3.3839/0.4271 = 7.9×); `Mission Plan.md:143` P7 05.10.2026; no hooks in `.claude/settings*.json`, no `.github/workflows`, no pre-commit; `LOOP_STATE.md` 1,493 lines / 202,832 bytes; scaleab K ladder 12.0153 (K = 1) → 0.164 oracle; scorecard prices vs the API reference (cached 2026-06-24).

---

## 6. Deliverable manifest

| artifact | location | note |
|---|---|---|
| This red-team report | `repo:Project Steering/Reviews/2026-09-25-programme-review/streams/X1_redteam.md` | staged; verified with `git ls-files --stage` |
| Spot-check evidence | inline above (file:line and JSON keys); no separate files written | — |

Nothing lives only in this container. No other file was edited. Integration needed: the orchestrator applies M1–M7 (verbatim replacements above) to `00_PROGRAMME_REVIEW.md` and decides S1–S11; two P0 rows (M4, M5) are proposed for §13.
