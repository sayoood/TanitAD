# Weekly Progress Report — 2026-W31

> Orchestrator & Project Steering synthesis. Run **2026-07-11** (scheduled Friday 14:23 slot; fired
> 15:29 UTC — the program's **fourth** orchestrator cycle). Covers **2026-07-11 ~12:00 → 2026-07-11 ~17:30**
> across BOTH streams (research hub + core MVP) — i.e. everything since the W30 report (`da85a3d`).
> Honest per P8 — every claim carries a number or a repo/source pointer, or is explicitly labeled
> preview / confounded / un-passed gate.
>
> **Label note (unchanged convention):** ISO week of 2026-07-11 is still **W28**
> (`date(2026,7,11).isocalendar()` → 2026-W28, weekday 6). The program uses the report label as a
> **run-sequence counter, not strict ISO** (W28 = bootstrap 07-09, W29 = 07-10, W30 = 07-11 morning);
> this cycle continues the sequence as **W31**. Cadence is compressed to roughly one orchestrator cycle
> per day (sometimes more) during the Phase-0 evidence sprint.

---

## Executive summary (≤10 lines, honest)

1. **All three architecture arms now physically exist (Phase-0 day 7/42).** Main WM training at
   **25,750 / 30,000 (86 %)**; **REF-A frozen-DINO reference 30k COMPLETE** (loss 1.19→**0.465**, roll
   0.076, no collapse, ~4.5 h); **REF-B vision-action E2E reference built + merged** (rev1 260.7 M −0.82 %;
   rev2 = strategic transformer d384×4 + route-derived nav commands). The comparison scaffold the whole
   Phase-0 verdict rests on is assembled.
2. **First head-to-head number arrived — and it is a caution, not a win (honest).** REF-A probes
   **ADE@1s ≈ 14.2 m** vs the main model's **≈ 7.5 m** at matched protocol (`refa_probe.py`, `ecbad24`).
   The frozen encoder is currently **worse**, but the read is **confounded**: the v1 adapter *mean-pools*
   the 256 DINO tokens (destroys the spatial structure the DINO-WM lineage depends on), and REF-A is
   comma-val-only vs the main rows' comma+PhysicalAI mix. **Does NOT yet arbitrate H4** — two de-confounders
   queued (grid-adapter REF-A + comma-only main re-run + CV kinematic-floor baseline).
3. **Sayed review sharpened the D1 instrument twice:** "expected better from DINO" → grid-adapter variant;
   **"7.51 is bad too"** → a constant-velocity kinematic-floor baseline row, to decide whether D1's sub-metre
   threshold is even meaningful or must be redefined baseline-relative (gate honesty).
4. **The W30 P1 instrument is now real code:** D1 route-resampled protocol implemented in the gate runner
   (`e9b2491`, `gates.py` — ADE/FDE as mean over `n_splits=8` episode splits + 95 % CI). Ready for the 30k eval.
5. **Decisions closed:** **D-028 ADOPTED** (was `proposed` in W30 — Sayed confirmed, `7cc210f`);
   **D-022 numbering gap resolved** (backfilled, default HOLD); **REF-B $40 envelope CONFIRMED by Sayed in
   chat** (`5dd08c2`, attribution corrected from an earlier "pre-approved" over-claim). **H16** (active depth
   interrogation, Sayed's idea) banked into the hypothesis ledger with F1–F3 falsifiers.
6. **Hygiene debt from W30 P3 is CLEARED** (`1285524`): the stranded Prod-Opt `int8_quant` +
   `contract-windowing-failloud` dirs and the loose Opponent catalogs are committed; working tree is clean.
   **Data-Eng ran this cycle** (15:14 UTC, semantic-datasets survey) — reversing the 2-cycle no-show.
7. **The one thing still NOT done: the step-30k decision-grade verdict.** ETA slipped to **~22:15** (a ~2 h
   pod1 crawl episode that escaped the watchdog threshold). The first program *verdict* remains pending —
   for the third report running. **New blocker: pod3 volume quota mis-sized (≈50 GB, not 300 GB)** → REF-B
   training-data staging blocked; needs a Sayed console action.

---

## 1. Agent health check (2026-07-11 wave)

Scheduler truth via `list_scheduled_tasks` (times UTC). Worktree/branch truth via `git worktree list` +
git log. The Mon–Sat discipline wave fired together **09:34–09:39**; **Data-Eng additionally fired 15:14**;
orchestrator fired **15:29** (this run).

| Agent | Sched lastRun (UTC) | Committed this cycle? | QUALITY | Note |
|---|---|---|---|---|
| Tools&DevEnv | 07-11 09:34 | ❌ (empty branch) | no output | last real delivery W29 `ci.ps1` (`9b0ba24`); STATE LAST_RUN still 07-09 |
| Data Engineering | 07-11 **15:14** | ⚠️ backlog-only | ran (recovered) | **no-show reversed** — semantic/strategic-label dataset survey (nuPlan/DriveLM/CoVLA/L2D/Talk2Car/Bench2Drive, Sayed directive) went to the DE backlog; no intake pkg this run |
| Architecture & Inference | 07-11 09:35 | ❌ (empty branch) | no output | but the arch *content* (REF-A/REF-B/D1-protocol/H16) landed on main via the loop — see §2 |
| Benchmarks & Eval | 07-11 09:35 | ❌ (empty branch) | no output | still owns the D-028 dataset/benchmark seam (AUTOPILOT-VQA screening) |
| Opponent Analyzer | 07-11 09:38 | ✅ (swept to main) | complete | D-028 catalog refresh; the 4 loose catalog files W30 flagged are now **committed** (`1285524`) |
| Production & Optimization | 07-11 09:39 | ✅ (swept to main) | resolved | `int8_quant` + `contract-windowing-failloud` **now committed** (`1285524`) — W30 carryover cleared |
| **Orchestrator** | 07-11 **15:29** | ✅ (this run, main) | — | this report |

**Diagnosis (honest — mixed, net-improving).**
- **The two W30 hygiene failures are cleared.** `1285524` (11:57) swept the stranded Prod-Opt packages and the
  loose Opponent catalogs into a proper commit, added the refA/refB ledger rows, and adopted D-028 in the log.
  **Working tree is now clean** (only `.claude/settings.local.json`).
- **Data-Eng is no longer a no-show** — it fired at 15:14 and produced the semantic-datasets survey directive.
  (It committed to the backlog, not a full intake pkg, so it's a partial recovery, not a full G-E run — but the
  2-cycle silence is broken.)
- **The discipline-branch pattern persists (3rd cycle):** the Tools / Arch / Bench 07-11 worktree branches are
  still empty at the main tip; the session-end ritual (G-F) did not complete in them. **But** the substantive
  work this window — REF-A train+probe, REF-B rev1+rev2, D1 route-resampled protocol, H16 dossier — was real
  and landed on main via the daily loop. The honest framing: the *loop* is carrying the program; the *weekly
  discipline-agent framework* contributed little tracked output again, though its cost this cycle was zero (no
  lost work, clean tree). The W30-proposed session-end guardrail is still not built → carried to P3, **but
  de-escalated** from "top risk" to "process debt" because nothing was lost this cycle.

### 1b. Intake-queue audit (D-011)

No new intake package committed this cycle. The two W30 "uncommitted" items are now **committed** (they became
tracked packages, still awaiting MVP triage). Queue = the standing set:

| Package | State |
|---|---|
| tools/testsuite-io-profiling + ci-script | **pending** — additive, tests-green → safe-integrate (pair) |
| opponent/stop-arm-gate-scenario | **pending** — additive, 11 tests → safe-integrate |
| data/physicalai-r1-selection | **pending** — feeds training mix (1,926/2,000 urban clips, 74 short of R1) → MVP review |
| prod/models-predictor-failfast | **pending** — closes a `python -O` silent-wrong-inference hole, 8 tests → MVP review |
| prod/int8_quant | **now committed** (`1285524`) — INT8 weight-only: predictor 0.9531 / encoder 0.9844 agree, **heads collapse 0.4844 (33 flips, 1.67 m)** → heads stay ≥fp16 → MVP triage |
| prod/contract-windowing-failloud | **now committed** (`1285524`) — fail-loud windowing guard → MVP triage |
| tools/metadrive-frontcam | **dead code** (D-014) — remove `stack/tanitad/data/metadrive_frontcam.py` |

**Escalation check:** by wall-clock no committed verdictless package crosses the >3-day bar. The W30 hygiene
overdue item is resolved. No integration into `stack/` by the orchestrator (D-011 — an MVP-stream act).

---

## 2. Synthesis

### Research + core stream — top movements this cycle

- **REF-A frozen-DINO reference trained to 30k** (`cda93df` precompute → feature-level trainer `d741bd9`,
  17:57 report): loss 1.19→**0.465**, pred 0.080, **roll 0.076** (K=4 rollout healthy), SigReg 3.44 active,
  adapter_std 0.86 monotonically rising (no collapse), 60 checkpoints, ~4.5 h wall-clock ($0 incremental,
  rides the approved A40). The frozen reference finished BEFORE the main model reaches 30k — the
  feature-level speed dividend is real.
- **First REF-A ↔ main head-to-head** (`refa_probe.py`, `ecbad24`, gate-matched d1_probe_capacity protocol):
  **REF-A ADE@1s ≈ 14.2 m** vs **main ≈ 7.5 m**. *Confounds recorded in the artefact itself:* (a) v1 adapter
  mean-pools the 256 DINO tokens → discards spatial structure (DINO-WM/EponaV2 keep patch tokens); (b)
  comma-val-only vs the main rows' comma+PhysicalAI mix. **Verdict: no H4 arbitration yet** — this is a first,
  deliberately-confounded read, not the capacity-ceiling-vs-training-time answer. De-confounders queued in the
  Architecture backlog (`a7563e0`): grid-adapter REF-A retrain (features cached, ~4.5 h) + comma-only main
  re-run + CV kinematic-floor baseline + a camera-unit-vs-metres audit.
- **REF-B vision-action E2E reference** (the D4 *learned opponent*): rev1 merged (`e616b23`, 260.7 M, −0.82 %
  vs main, 204 tests green, verified on pod3); **rev2** (`38cf9ca`, Sayed review) upgrades the strategic layer
  to a real transformer d384×4 with route-derived nav commands + own aux loss — fixing a **real defect** (the
  strategic head had been training on a constant `follow` command) — tactical 4→6, encoder 27→25, budget
  −0.124 %. Training GO held pending Sayed's look + the pod3 quota fix.
- **D1 route-resampled protocol is now implemented** (`e9b2491`, `gates.py`): ADE/FDE = mean over `n_splits=8`
  episode splits + 95 % CI — turning the W30 P1 *design* into runnable gate code, ready before the 30k eval.
- **H16 — active depth interrogation** (`7cd9d7b`, Sayed's idea): tactical-commanded, σ-triggered ROI depth
  queries composing H15 σ-trigger + H2 scheduler + a ZipDepth-class specialist. Dossier + F1–F3 pre-registered
  falsifiers + hypothesis-ledger row; Phase-1 H2 window (~Sep). An offline trigger-probe (cheap F1
  falsification on existing SC-01 + D8 telemetry) is queued.

**Hypothesis-ledger changes:** **H16 added** (open, Sayed 2026-07-11). **H4** gains its first *measured* arm
(REF-A 14.2 m) but the row is **not upgraded** — the read is confounded (P8). No other status change; the core
WM verdict stays gated on the imminent 30k full-val eval.

### Core MVP stream — gate status table (D1–D9)

Last **decision-grade** eval remains **step-14k** (full val, 39 train / 9 val eps). The 30k full-val eval is
imminent (~22:15 tonight). New this cycle: the REF-A reference row and the CV-baseline row (queued) that will
recalibrate how D1 is read.

| Gate | Status | Value (step-14k full-val unless noted) | Trend | Source |
|---|---|---|---|---|
| D1 (probe ADE) | **FAIL** | ADE@1s **5.18 m** (bar < 1.0). Probe-capacity: main **≈7.5 m**; **REF-A ≈14.2 m** (confounded). CV kinematic-floor baseline queued to test if the bar itself is meaningful. | 10.94→5.18 (−53 %); monotone re-confirmed (W30) | `gates_step14000.json`, `d1_probe_capacity.json`, `refa_probe.py` |
| D2 (imagination ranking) | **PASS** | dir-acc P1 **0.917** / P4 **1.000** (bar 0.7) | 0.872/0.940 → 0.917/1.000 | `gates_step14000.json` |
| D3 (imagined-ADE ratio) | **BLOCKED** (I4 doctrine) | I4 **3.05** @0.4 s (floor ~1.5) | 3.83 → 3.05 (closing) | `gates_step14000.json` |
| D4 (tactical lift) | not run | REF-B (learned opponent) now built — arbitrates once trained | — | REF-B rev2 `38cf9ca` |
| D5 / D6 (routing / generalization) | not run | needs CARLA closed-loop (pod2) | — | — |
| D7 (latent-RAG continual) | not run (end P0/P1) | — | — | — |
| D8 (OOD AUROC) | preview | 16/23 same-scene clear→degraded pairs higher imag-error (p≈0.047) @6.5k; 30k paired re-run queued | — | `p0-d8-preview` |
| D9 (H15 hidden-sector) | not run | — | — | — |

> Read: still **1 PASS / 1 FAIL / 1 BLOCKED / 6 not-run**. The ladder did not move; what moved is the
> **comparison scaffold** — all three arms exist, and the first REF-A number tells us the frozen-encoder path
> is (so far) *behind*, not ahead, of end-to-end training. **The first program *verdict* is still the step-30k
> full-val eval — tonight ~22:15.** (P8: D1 still FAILs the sub-metre floor; the CV-baseline row may show the
> floor needs redefining as baseline-relative.)

### KPI / benchmark trends

- **D1 head-to-head (NEW this window, ADE@1s, matched d1_probe_capacity protocol):** main **≈7.5 m** ·
  **REF-A frozen-DINO ≈14.2 m** → frozen encoder currently ~**1.9×** worse, **confounded** (mean-pool adapter +
  comma-only). First cross-architecture data point; de-confounded re-run queued.
- **REF-A training curve:** loss 1.19→**0.465**, roll 0.076, adapter_std 0.86↑ (no collapse), 60 ckpts, ~4.5 h.
- **Main trainer:** 23,700 → **25,750 / 30,000 (86 %)**; 3-min pace ~1,000 steps/h; 30k ETA slipped ~20:15 →
  **~22:15** after a ~2 h crawl episode (honest — escaped the <50-steps/15-min watchdog threshold, self-recovered).
- **Budgets (REF-B):** rev1 260.7 M (−0.82 %), rev2 −0.124 % — both **inside** the 261 M envelope (test-pinned).
- **Efficiency (unchanged):** decision tick **15.07 ms p50 / 1.08 GB** (fp32, 4060); CNCE median
  **2.02×10⁵ m/(s·B-params)**; **261 M** vs Alpamayo-2 **32 B** / GAIA-3 **15 B** (~60–120× envelope wedge).
- **Precision (unchanged):** fp16 decision-safe 95.3 % / bf16 unsafe 67.2 %; INT8 weight-only heads collapse
  0.4844 → heads stay ≥fp16 (curve now committed, `1285524`).
- **External board:** navhard EPDMS DrivoR 56.3 / DriveFuture 55.5. **TanitAD board rows still —** (Phase-1
  first entry; honest — no comparable published number yet).

### Resource burn vs plan

| Item | Planned $ | Actual (settled) $ | Note |
|---|---|---|---|
| p0-sB01-realmix (RunPod, 30k) | 40 | — | run of record; 25.75k/30k, finishing ~22:15 tonight |
| p0-pod2-carla+bakeoffs (A40) | 40 | — | REF-A trained ($0 incremental) + probed; grid-adapter re-run next |
| refa-30k (rides A40) | 0 incr | — | ledger row added `1285524` — Sayed pod2 approval |
| refb-post30k (envelope) | 40 | — | **CONFIRMED by Sayed in chat 2026-07-11** (`5dd08c2`); gated on 30k verdict |
| Local RTX 4060 + Colab-free | 0 | 0 | D1 probe + REF-A probe here |
| **Phase-0 total** | **$120 planned** | **$0 settled** | on plan; ≤$50/wk guardrail honoured |

Burn **on plan, $0 settled**. Base $80 (two W28–29 pods) + $40 REF-B envelope = $120 planned; REF-A at $0
incremental. **New utilization/infra risk:** pod3 volume quota mis-sized (~50 GB vs planned 300 GB) →
REF-B data staging blocked at 34/88 GB since 14:55; needs a Sayed console action (§4).

### Opponent delta digest

No new competitor gate-relevant number this cycle. D-028 (recency-first scan + seam ownership) is now
**adopted** in the decision log — the process fix that closed the AUTOPILOT-VQA / ZipDepth blind spot is
permanent. Data-Eng's new semantic-label dataset survey (DriveLM / CoVLA / Talk2Car / nuPlan / Bench2Drive)
is the forward-looking competitive-data move — one Phase-1 ingest recommendation to come. Standing thesis
unchanged: "world model" is table-stakes; the moat is **hierarchy + CNCE + imagination + self-monitoring**.

---

## 3. Proposals — next cycle's MVP focus (max 3, prioritized)

**P1 — Execute the step-30k flagship decision-grade gate tonight (~22:15), with the new instruments.**
*(highest leverage — carries from W30, still unmet.)* Run the first *verdict* eval on the full val set:
**D1 route-resampled mean±CI** (now real code, `e9b2491`) **+ the CV kinematic-floor baseline row** (so the
7.5 m FAIL is read against what constant-velocity extrapolation alone achieves — this decides whether the D1
threshold is honest or needs baseline-relative redefinition), D2 (must hold ≥0.9/1.0), D3 I4 (toward ~1.5),
and a local paired `d8_preview.py` re-run (clear 16/23). **Impact: HIGH** — converts D1/D2/D3/D8 into
decision-grade verdicts, closes the Phase-0 core evidence cycle ~2 weeks early. **Cost: ~$0** + a few hours.

**P2 — De-confound the REF-A comparison, then read H4 honestly.**
The 14.2-vs-7.5 gap is currently un-interpretable (mean-pool adapter + corpus mismatch). Do the two cheap
de-confounders before drawing any capacity-ceiling conclusion: **(a) grid-adapter REF-A retrain** (literature-
faithful patch-token readout; features already cached, ~4.5 h A40, $0 incremental) and **(b) comma-only main
re-run** of d1_probe_capacity for the apples-to-apples row. Only *then* is H4 (frozen-encoder deficit real vs
adapter artefact) answerable. **Impact: MEDIUM-HIGH** — the single most important open architectural question,
and W30 over-hoped a frozen encoder would rescue D1; this keeps that hope honest. **Cost: ~$0** (rides A40).

**P3 — Unblock pod3 (Sayed action) + land the session-end guardrail as process debt.**
**(a) Sayed:** expand pod3 volume 50 → 300 GB (RunPod console, pod 69.30.85.16) — REF-B training-data staging
is blocked until this; default-if-not-done = REF-B GO stays blocked. **(b)** Build the still-missing D-026
session-end guardrail (fail-loud if an agent wrote files but committed nothing; forbid agent writes to the
primary tree) and add a `dd`-write quota probe to the pod-provisioning checklist (the pod3 mis-size cost 3.5 h).
**Impact: MEDIUM** (P3a unblocks a $40-envelope arm; P3b prevents future silent loss). **Cost: LOW** — one
console click + a small hook/script. *De-escalated from W30's "top risk": nothing was lost this cycle and the
hygiene debt is cleared, so this is now process hardening, not fire-fighting.*

---

## 4. Risks & drift

- **[TOP — infra] pod3 volume quota mis-sized (~50 GB vs 300 GB).** Bulk writes fail since 14:55
  ("Disk quota exceeded"); REF-B data staging stuck at 34/88 GB. Sayed console action required (P3a). Also a
  process miss: mis-read as a network stall, download restarted twice (~3.5 h lost) — checklist fix queued.
- **[carries] Step-30k decision-grade verdict still pending — 3rd report running.** ETA slipped to ~22:15 after
  a ~2 h crawl episode that escaped the watchdog's <50-steps/15-min threshold. The whole Phase-0 core-evidence
  claim rests on this eval; until it runs, every gate above step-14k is preview/directional (P8).
- **[NEW — read-honesty] Do not over-read REF-A 14.2 vs main 7.5.** It is confounded (mean-pool adapter +
  comma-only corpus). It is NOT yet evidence that frozen encoders lose to end-to-end — P2's de-confounders must
  land first. Recording it as a *caution against premature H4 closure*.
- **[open] D1's 7.5 m may itself be "bad" against a trivial baseline.** Sayed's "7.51 is bad too" is the reason
  the CV kinematic-floor row is P1: if constant-velocity extrapolation also lands ~5–7 m on comma highway, the
  linear-probe gap is smaller real work than it looks and the gate threshold needs redefining (gate honesty).
- **[de-escalated] Agent-framework produced little tracked output (3rd cycle).** But hygiene debt is cleared,
  Data-Eng ran, and the loop carried all real work with zero loss → process debt, not top risk. Guardrail → P3b.
- **[watchdog gap] The ~2 h pod1 crawl escaped the <50-steps/15-min threshold.** Borderline; self-recovered.
  If a second episode precedes 30k, the standing remedy (restart-from-checkpoint) applies without further ask.
- **Open decisions unchanged:** **D-021** (latent-dim k measured) `proposed`, default holds; **D-022**
  (public-claim firewall) `proposed`, default HOLD — the numbering gap is closed but the substantive legal
  reclassification still needs an explicit Sayed/legal accept.

**Decisions captured this cycle:** **D-028 ADOPTED** (`7cc210f` — was `proposed` in W30). **D-022 numbering gap
resolved** (backfilled). **REF-B $40 envelope confirmed in chat** (`5dd08c2`). **H16 added** to the hypothesis
ledger. No unlogged decisions found in the commit stream.

---

## Schedule audit

All **9 scheduled jobs present, enabled, and fired** (via `list_scheduled_tasks`, times UTC): **7 hub jobs** —
6 discipline agents Mon–Sat + orchestrator (Fri 14:23 → this run, fired 15:29) — plus **pod-training-monitor**
(6-hourly, last 07-11 10:41, next 16:41) and **evening-report** (daily 17:53, next 07-11 16:01). Discipline
wave fired 09:34–09:39; **Data-Eng additionally fired 15:14** (recovery run). `nextRunAt` correct for the
standard weekly cadence (next discipline runs 07-13…07-18). **No missing job to re-register.**

Doc-drift still open (minor): `orchestrator-agent.md` duty 4 says "confirm all **six** scheduled jobs" — there
are **seven** hub jobs since the Saturday Production & Optimization agent (D-020). Flagged for a one-line fix.
**D-025 session-cron drumbeat** is not re-armed in this synthesis run (avoiding duplicate crons); the persistent
scheduled-tasks are the healthy source of truth — the session-cron re-arm is owned by the next loop iteration.
