# Weekly Progress Report — 2026-W29

> Orchestrator & Project Steering synthesis. Run 2026-07-10 (Friday 14:23 slot — the program's **second**
> orchestrator cycle, first on the regular schedule). Covers **2026-07-09 20:52 → 2026-07-10** across BOTH
> streams (research hub + core MVP). Honest per P8 — every claim carries a number or a repo/source pointer,
> or is explicitly labeled preview / design-oracle / un-passed gate.
>
> **Label note:** ISO week of 2026-07-10 is also W28; the bootstrap report already occupies `2026-W28.md`,
> so this cycle is filed as **W29** (the program uses the report label as a run sequence, not strict ISO —
> the cadence is compressed to roughly one orchestrator cycle per day right now).

---

## Executive summary (≤10 lines, honest)

1. **Last week's TOP RISK is RESOLVED.** The trainer restart that was blocked on Sayed since 06:15 was
   **superseded by a root-cause fix** (`7b5faa6`): the mmap dataloader's working set was unbounded (spanned
   the 88 GB cache inside the 57 GB cgroup); binding it to `n_episodes` took `data_s` from **73 % → nominal**.
   Trainer is now healthy at **~21,000 / 30,000 steps (~70 %)**, no interventions, riding to 30k on its own.
2. **First real gate movement since step-5k** (step-14k, full val): **D1 ADE@1s halved 10.94 → 5.18 m**
   (still FAIL vs sub-metre floor); **D2 PASS improved to near-perfect — P1 0.917 / P4 1.000** (was 0.872 /
   0.940); **D3 BLOCKED but I4 closing 3.83 → 3.05** (floor ~1.5). All admissible (I1–I3 green). **Caveat (P8):**
   a later **step-21k preview on a small sample (20/4 eps vs 39/9)** drifted back (D2 0.857/0.940 — still PASS;
   D1 11.52 m; D3 flat) → the 5k→14k gain is real but **not yet a clean monotone trend; the decision-grade read is the 30k eval on the full val set.**
3. **Strongest architectural read of the week — D3 decomposition** (`c0b22b7`): in the **direct multi-horizon
   heads, relative error FALLS with k** (comma 12.1→8.4→5.1; PhysicalAI 6.9→4.0→2.55) → **no error-compounding**;
   **recursive 1-step×4 is 2–4× WORSE** (20.5 / 9.5) → **keep direct heads, do not recurse.** The K=4-trained
   arm confirms it: 1-step imag_rel **8.13 → 1.03**, recursive-×4 **14.5 → 1.11** at matched compute (step-10.5k).
4. **Agent-health regression (honest):** only **3/6** discipline agents committed this cycle (Tools&DevEnv,
   Architecture, Opponent — all `QUALITY: full/complete`, worktree-isolated per D-026). **Data-Eng produced
   no committed output; Benchmarks and Prod-Opt left no branch commit** (Prod-Opt stranded two intake dirs
   uncommitted in the *main* tree). First real failure mode of D-026 worktree isolation.
5. **Efficiency/precision anchors unchanged & healthy:** 15.07 ms tick / 1.08 GB (fp32, 4060); fp16
   decision-safe 95.3 % / bf16 unsafe 67.2 %; 261 M vs field 15–32 B. New instrument: orthogonality/isotropy
   admissibility (iso_ratio 0.250 @6.5k → **NOT-YET-ADMISSIBLE**, correctly tempering the D-021 optimal-planning reading).
6. **Three Sayed strategic directives banked** (dashcam Y0–Y2, REF-A/REF-B reference stacks, multi-cam encoder
   optimization) + **D-026** (workflows/monitors/worktrees/skills) and its Sonnet-5 addendum — all captured & logged.

---

## 1. Agent health check (2026-07-10 agent wave)

Scheduler truth via `list_scheduled_tasks` (all times UTC). Committed agents work on **isolated worktree
branches** (D-026) — their 2026-07-10 STATE updates live on the branch, **not yet merged to main**.

| Agent | Sched lastRun | Committed? | Branch tip | QUALITY | Headline |
|---|---|---|---|---|---|
| Tools&DevEnv | 07-10 05:03 | ✅ | `worktree-agent+tools-devenv-20260710` (`9b0ba24`) | **full** | `ci.ps1` CI gate — I2 tripwire → suite + timing budget; measured warm **11.2 s / 189 tests / exit 0**, falsifier → exit 1 |
| Data Engineering | 07-10 05:03 | ❌ | branch empty (no commit) | **incomplete** | fired ~15 h ago; **no branch commit, no STATE update, no output in main** → session-end ritual (G-F) not completed this cycle |
| Architecture & Inference | 07-10 05:03 | ✅ | `worktree-agent-arch-inf-20260710` (`5f0c316`) | **full** | orthogonality/isotropy admissibility instrument — iso_ratio_active **0.250** @6.5k → **NOT-YET-ADMISSIBLE** (blocks premature D-021 optimal-planning claim); HamJEPA/PGSA theory-watch |
| Benchmarks & Eval | 07-10 19:59 | ❌ | branch empty (no commit) | **unknown** | fired ~recently (19:59 UTC); no branch commit at report time — just-finished-without-commit or still in its 4 h loop; two worktrees exist (dup: `agent-bench-eval` + `agent-benchmarks-eval`) |
| Opponent Analyzer | 07-10 05:04 | ✅ | `worktree-agent-opponent-20260710` (`874f78e`) | **complete** | SC-13 stationary-lead scenario — collision **0.000 vs 0.429**, brakes **3.10 s earlier**, min-TTC 4.40 vs 0.77 s; FMVSS-135 NPRM H11 regulatory tailwind |
| **Orchestrator** | 07-10 12:26 | ✅ (this run, main) | — | — | this report |
| Production & Optimization | 07-10 20:00 | ❌ (uncommitted) | `worktree-prod-opt-20260710` empty; 2 dirs untracked in **main** | **partial/uncommitted** | INT8 quant curve + contract-windowing fail-loud produced, but left **uncommitted in the main tree** (`int8_quant/`, `2026-07-10-contract-windowing-failloud/`) — G-F miss / possibly in-flight (fired 20:00) |

**Diagnosis (honest — a regression from last week's 6/6).**
- **3/6 clean:** Tools, Architecture, Opponent committed worktree-isolated branches with `full/complete`
  gates — D-026 worktree isolation working as designed for these three.
- **3/6 not clean:** **Data-Eng** fired 05:03 and, ~15 h later, has no output anywhere → a genuinely
  incomplete run (not still executing). **Benchmarks** and **Prod-Opt** fired ~20:00 UTC → recent; may still
  be in their loops, but **Prod-Opt already stranded work uncommitted in the main tree** — the new failure
  mode D-026 must close (agents must commit *in their worktree*, never leave artifacts loose in main).
- **New guardrail needed:** a session-end check that (a) fails loud if an agent wrote files but committed
  nothing, and (b) forbids writes to the primary tree from an agent session. Filed as a fix task (see §3 P3).
- **Unmerged branches:** the 3 clean agent branches await MVP-loop triage/merge (D-026). None merged this cycle.

### 1b. Intake-queue audit (D-011)

**13 packages** in `*/Implementation/incoming/` + **3 new** on unmerged agent branches + **2 uncommitted** in
main. Verdict-writeback into `INTAKE.md` is **inconsistent** — integrated packages are recorded in git/PROJECT_STATE
but their INTAKE verdict sections were often left on the template (a minor process gap, flagged).

| Package | State |
|---|---|
| arch/gate-runner-d1-d3, spectral-sizing-p0, bakeoff-harness | integrated (`cb58820`/`88c47d0`/`8dfbc25`) |
| data/cosmos-drive-dreams-loader | integrated (`9e8ab7e`) |
| opponent/work-zone-phantom | integrated (`8a27476`) |
| bench/eval-metric-suite, lal-v2-anticipation | integrated (`67eee5b`/`3784e34`) |
| prod/data-cluster-compliance | integrated (`fee08cf`/`8dfbc25`) |
| tools/metadrive-frontcam-perturbation | **integrated 2026-07-06 — now DEAD CODE** (D-014 retired MetaDrive); action = remove `stack/tanitad/data/metadrive_frontcam.py`, not a verdict flip |
| tools/testsuite-io-profiling | **pending** — additive, 9 tests, stdlib → safe-integrate (paired with new `ci-script`) |
| data/physicalai-r1-selection | **pending** — feeds training mix (1,926/2,000 urban clips) → MVP review |
| opponent/stop-arm-gate-scenario | **pending** — additive, 11 tests → safe-integrate |
| prod/models-predictor-failfast | **pending** — closes a `python -O` silent-wrong-inference hole in `stack/models`, 8 tests → MVP review |
| **NEW** tools/ci-script (`ci.ps1`) | on branch, verdictless, <1 day — pair with testsuite-io-profiling |
| **NEW** arch/orthogonality-instrument (8 tests) | on branch, verdictless, <1 day |
| **NEW** opponent/stationary-lead-scenario (13 tests) | on branch, verdictless, <1 day |
| **NEW** prod/int8_quant + contract-windowing-failloud | **uncommitted in main** — commit + triage |

**Escalation check:** by wall-clock every verdictless package is **≤ 1–2 days old → none overdue (>3 days)**;
no escalation. I did **not** integrate into `stack/` (an MVP-stream act, D-011). Two carryover packages
(testsuite-io-profiling, stop-arm-gate) remain safe-additive and are recommended for integration next MVP pass.

---

## 2. Synthesis

### Research stream — top finding per discipline (this cycle)

- **Architecture & Inference (H3/D-021):** shipped the **orthogonality/isotropy admissibility instrument**
  (from LeJEPA-identifiability 2605.26379). Measured on step-6500: `active_k=21` / `cov_effective_rank=24.93`
  **exactly reproduce** the independent spectral numbers (cross-instrument sanity), but `iso_ratio_active=0.250`
  / `cond_active=246` / `rms_offdiag=0.428` → **NOT-YET-ADMISSIBLE**. **Honest tempering (P8):** the D-021
  over-provisioning finding stays *descriptive*; its *optimal-latent-planning* reading is **not licensed** yet —
  the instrument blocks a premature claim (D-004). Theory: HamJEPA (2605.20107) scopes isotropy to
  rotation-invariant cost; PGSA (2606.12471) — statistical-WM error "grows monotonically with time" under
  non-Gaussian dynamics = the mechanism behind K-step horizon degradation (reinforces H1 hierarchy).
- **Opponent Analyzer (H11/H15):** **SC-13 stationary-lead** design-oracle — H15 `imagination_forward` vs
  `classifier_react`: collision **0.000 vs 0.429** over {8…25} m/s; at 15 m/s brakes **3.10 s earlier**, min-TTC
  **4.40 vs 0.77 s**, OKRI **7 vs 18,220**; honest falsifier built in (edge decays to −2.90 s as detect-range
  grows 20→120 m → the edge is *specifically* acting-before-classification). **FMVSS-135 NPRM (2026-06-26)**
  proposes an ADS "be aware of the operational status of each safety-critical system … respond to degradations"
  → **regulation-native tailwind for H11**. Avride's NHTSA PE wording is **verbatim** the SC-13 failure.
- **Tools&DevEnv (infra):** `ci.ps1` CI gate (I2 collapse tripwire → full suite + timing budget), warm
  **11.2 s / 189 tests / exit 0**, falsifier → exit 1. Envelope sweep: Thor FP8 on ViT ≈ +20 % only + needs
  ONNX Q/DQ surgery → keep deploy = **ONNX→TensorRT FP16 static-shape** (reinforces bf16-unsafe / INT8-gated).
- **Data Engineering / Benchmarks & Eval:** **no new committed run this cycle** (see §1). Data-Eng's prior
  (2026-07-09) run's deferred shared-file HANDOFF rows (G-D ledger / SCENARIO / PROJECT_STATE §5) remain
  un-merged — a small carryover to reconcile.

**Hypothesis-ledger changes:** H3 (isotropy admissibility instrument — *tempers*, does not upgrade), H1
(PGSA monotone-error mechanism), H11/H15 (SC-13 + FMVSS-135). **No status upgrades** — every delta is external
theory, a design-oracle number, or an un-passed gate (P8). The core WM claim remains gated on the 30k read.

### Core MVP stream — gate status table (D1–D9)

| Gate | Status | Value (step-14k unless noted) | Δ vs step-5k | Source |
|---|---|---|---|---|
| D1 (probe ADE) | **FAIL** | ADE@1s **5.18 m** (bar < 1.0) | 10.94 → 5.18 (**−53 %**, monotone) | `gates_step14000.json` |
| D2 (imagination ranking) | **PASS** | dir-acc P1 **0.917** / P4 **1.000** (bar 0.7) | 0.872/0.940 → 0.917/1.000 | `gates_step14000.json` |
| D3 (imagined-ADE ratio) | **BLOCKED** (I4 doctrine) | I4 **3.05** @0.4 s (floor ~1.5) | 3.83 → 3.05 (closing) | `gates_step14000.json` |
| D4 (tactical lift) | not run | — | — | — |
| D5 (routing edge) | not run | — | — | — |
| D6 (generalization slope) | not run | — | — | — |
| D7 (latent-RAG continual) | not run (end P0/P1) | — | — | — |
| D8 (OOD AUROC) | preview | 16/23 same-scene clear→degraded pairs higher imag-error (p≈0.047) @6.5k; 14k re-run queued | — | `p0-d8-preview` |
| D9 (H15 hidden-sector) | not run | — | — | — |

> Read: still **1 PASS / 1 FAIL / 1 BLOCKED / 6 not-run**, but **all three decision gates improved 5k→14k**
> — the first real gate movement of the program. **Honest caveat (P8):** the later **step-21k preview used a
> small sample (20 train / 4 val eps vs 39/9 at 14k)** and drifted back — D2 P1 0.917→**0.857** (still PASS),
> D1 5.18→**11.52 m**, D3 I4 essentially flat (3.05→3.04); oracle-decode ade@2s 1.57→3.43. This is small-sample
> noise (directional, not decision-grade), **not** a regression verdict. The **first decision-grade read is the
> step-30k eval on the full val set** (imminent, ETA ~2026-07-11–12).

**D3 decomposition (architectural read, `c0b22b7`):** direct multi-horizon heads show rel-err **falling** with
k (no compounding); recursive-×4 is **2–4× worse**. The **K=4-trained bake-off arm** (matched compute, step-10.5k)
confirms: 1-step imag_rel **8.13 → 1.03**, recursive-×4 **14.5 → 1.11** vs baseline; direct-k4 only modestly
better (3.71 → 2.89) → **K-training mainly repairs recursive composition; keep direct heads and match K to the
decode horizon.** Artifacts: `p0-d3-decompose/d3_decompose_14k.json`, `p0-kstep-arms/{d3_arm_base,d3_arm_kstep}.json`.

### KPI / benchmark trends

- **Gate trend (new):** first real movement 5k→14k — D1 **−53 %**, D2 to near-perfect, D3 I4 closing; the
  step-21k small-sample preview drifted back (noise — see gate-table caveat), so the trend is promising but
  not yet clean-monotone until the 30k full-val read.
- **Collapse-health (step-21k):** erank steady **~28** (intrinsic highway-dynamics rank — spectral eff-rank ≈35,
  knee ≈22 → expected, **not a collapse signature**); dim_std ~0.92–1.04 (variance preserved); step_ratio
  ~0.09–0.11 (healthy); H15 NLL ≈ **−4.3**. Intervention flags: **none**.
- **Throughput:** mmap fix (`7b5faa6`) restored nominal pace (`data_s` 73 % → nominal; ~750 steps/h recovered);
  30k ETA ~2026-07-11–12.
- **Efficiency (unchanged):** decision tick **15.07 ms p50 / 1.08 GB** (fp32, un-optimized, 4060); CNCE median
  **2.02×10⁵ m/(s·B-params)**; **261 M** vs Alpamayo-2 **32 B** / GAIA-3 **15 B** (~60–120× envelope wedge).
- **Precision (unchanged):** fp16 decision-safe **95.3 %** / bf16 unsafe **67.2 %** → deploy TRT-fp16, never bf16;
  INT8/FP8 measured-and-surgery-gated (Prod-Opt `int8_quant` curve pending commit/triage).
- **New instrument:** orthogonality/isotropy admissibility — iso_ratio 0.250 @6.5k (NOT-YET-ADMISSIBLE);
  convergence tripwire armed for 15k/30k.
- **External board (open-loop ⊥ closed-loop, standing footnote):** navhard EPDMS DrivoR 56.3 / DriveFuture 55.5;
  AlpaGym closed-loop RL now open. **TanitAD board rows still —** (Phase 1 first entry; honest — no comparable published number yet).

### Resource burn vs plan

| Item | Planned $ | Actual (settled) $ | Note |
|---|---|---|---|
| p0-sB01-realmix (RunPod, 30k steps) | 40 | — | training run of record; now ~21k/30k healthy |
| p0-pod2-carla+bakeoffs (RunPod A40) | 40 | — | CARLA harness + K-step/arm bake-offs |
| Local RTX 4060 + Colab-free bursts | 0 | 0 | all agent experiments this cycle ran here |
| **Phase-0 total** | **$80 planned** | **$0 settled** | on plan; guardrail ≤ $50/wk (2 pods, explicit Sayed approval) |

Burn **on plan**. Every measured experiment this cycle (orthogonality instrument, SC-13 oracle, ci.ps1,
D3 decomposition, K-step arms, INT8 curve) ran on the free local/Colab tier. **Open:** post-30k / Stage-1
spend is **not yet pre-approved** (see §3 P1, §4).

### Opponent delta digest

**SC-13 stationary-lead** shipped (Avride NHTSA PE verbatim; 16 crashes, operator intervened 1/16). **FMVSS-135
NPRM (2026-06-26, comments → 07-27)** = regulatory tailwind for H11 self-knowledge; AV STEP withdrawn. **Waymo**
sixth recall confirmed. **NVIDIA AlpaGym** closed-loop RL on AlpaSim+NuRec open (closed-loop now table stakes).
**Wayve** Stellantis-on-Uber (CLAIM $2.8 B); **Pony** 4 driver-out cities + Singapore; **Tesla** eyeing ~5,000
Las Vegas slots. Industry WM substrate trending to **expensive explicit 4D-occupancy** → our latent
consequence-forward path is the efficiency counter (moat = hierarchy + CNCE + imagination + self-monitoring).

---

## 3. Proposals — next cycle's MVP focus (max 3, prioritized)

**P1 — Ride to step-30k and run the first decision-grade D1–D3 + D8 gate.** *(highest leverage)*
The trainer is unblocked and ~9k steps from the finish. The 30k read is the first *verdict* (not preview) of
the program: D2 must hold ≥0.9/1.0; D1 ADE should keep falling below ~5 m; D3 I4 toward the ~1.5 floor; then a
local `d8_preview.py` paired re-run (paired-D8 fraction should clear 16/23). **Impact: HIGH** — converts D1/D2/
D3/D8 from mid-training previews into decision-grade verdicts and unblocks D4 arbitration. **Cost: ~$0** (within
the $40 plan) + a few hours wall-clock — **but the post-30k / Stage-1 spend needs Sayed's pre-approval** (§4).

**P2 — Fold the D3-decomposition + K=4-arm result into the architecture (D-018 config change).**
The evidence is now decisive and matched-compute: **direct multi-horizon heads (no recursion)**, with **K matched
to the decode horizon** (K≈4 for the 2-s D3 claim). Update the D3 claim path (0.4 s → 2 s) and the bake-off lever
set accordingly. **Impact: MEDIUM-HIGH** — a measured architecture decision squarely on the D3/D4 critical path.
**Cost: $0** — results in hand; a config/trainer change under the D-018 execution class.

**P3 — Close the agent-commit gap and clear the intake wave.**
(a) Diagnose the Data-Eng no-show and add a **session-end guardrail** (fail-loud if an agent wrote files but
committed nothing; forbid agent writes to the primary tree) — the first D-026 failure mode. (b) Commit + triage
the two stranded Prod-Opt dirs (`int8_quant`, `contract-windowing-failloud`). (c) Merge the 3 clean agent
branches; integrate the two safe-additive carryovers (testsuite-io-profiling + ci-script as a pair, stop-arm-gate).
(d) Remove dead `stack/tanitad/data/metadrive_frontcam.py` (D-014). **Impact: MEDIUM** — hub hygiene + a real
`python -O` safety fix lands + queue stays clean. **Cost: LOW** — ~30–45 min triage + one full-suite run.

---

## 4. Risks & drift

- **[RESOLVED — carried-forward top risk] Trainer restart is no longer blocked.** The 2026-07-09 06:15 "restart
  trainer" escalation was **superseded** by the mmap root-cause fix (`7b5faa6`); trainer healthy at ~21k/30k.
  The corresponding item in PROJECT_STATE §4 is now stale and is being cleared this run.
- **[NEW — top] Agent-health regression: 3/6 agents did not commit this cycle.** Data-Eng produced no output;
  Prod-Opt stranded work uncommitted in the *main* tree; Benchmarks committed nothing. First real failure mode
  of D-026 worktree isolation — needs the P3 guardrail before it recurs and silently loses agent work.
- **D1 still FAIL (5.18 m vs sub-metre floor).** The trend is steep, but the sub-metre bar is far — the 30k read
  and the REF-A/REF-B encoder arms must answer whether it's a probe/decode-capacity ceiling or just training time.
- **Unmerged agent branches + INTAKE verdict-writeback gap.** 3 clean branches await merge; integrated packages
  often don't get their `INTAKE.md` verdict filled (recorded in git/PROJECT_STATE instead) — a traceability gap.
- **Directive sequencing open.** Dashcam (Y0–Y2), REF-A/REF-B, and multi-cam encoder optimization are all banked
  (`811df73`, `REFERENCE_ARCHITECTURES.md`) but **unsequenced.** Recommendation: **REF-A/REF-B first after 30k**
  (unblocks the D4 opponent-baseline arbitration) unless the dashcam pilot is time-sensitive.
- **Post-30k compute spend not yet pre-approved** — nothing blocked today, but the next stage needs a ledger row.
- **D-021 remains `proposed`** and is now further tempered by the orthogonality instrument (optimal-planning
  reading not licensed at 6.5k) — default holds (keep 2048, keep measuring).

**Decisions captured:** DECISIONS.md logged **D-026** (workflows + monitors + agent worktrees + project skills)
and its **Sonnet-5 addendum** this cycle; the **three Sayed strategic directives** are banked in
`REFERENCE_ARCHITECTURES.md` + commit `811df73`. All visible in commits and logged. **No unlogged decisions**
found in the cycle's commit stream. D-021 correctly remains `proposed`.

---

## Schedule audit

All **9 scheduled jobs present, enabled, and fired on 2026-07-10** (via `list_scheduled_tasks`): 6 discipline
agents (Mon–Sat), orchestrator (Fri 14:23 → this run), pod-training-monitor (6-hourly, last 07-10 10:42) and
evening-report (daily 17:53, last 07-10 16:02). `nextRunAt` correct for the normal weekly cadence (next
discipline runs 07-13…07-16). **No missing job to re-register.**

Two nominal-day drifts noted (compressed cadence, not a fault): the **Thursday** Benchmarks agent and **Saturday**
Prod-Opt agent both fired **Friday 2026-07-10 ~20:00 UTC** (off their nominal day). Their cron expressions remain
correct for the standard weekly clock going forward. Session-scoped CronCreate drumbeat (D-025) is empty in this
fresh orchestrator session — expected; the persistent scheduled-tasks are the source of truth and are healthy.
