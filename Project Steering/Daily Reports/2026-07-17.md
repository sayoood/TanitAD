# Daily Report — 2026-07-17 (Fri)

*Evening reporter. 2-minute read. Honest (P8): today was a synthesis + monitoring day, not a new-result day.*

## Executive summary (what moved TODAY)

1. **Weekly synthesis 2026-W32 shipped** (`93f0419`) — the week's story is the **speed/scale RESET (2026-07-14)**: no arm was fed current ego-speed `v0`, so each tried to infer absolute speed from vision (impossible for a frozen encoder, probe R²=0.61). Fix = `v0` as a 3rd action channel → REF-A **3.73 → 2.14 m** held-out, speed decodability **0.61 → 0.965 R²**. All 3 arms restarted from scratch. This **supersedes the entire pre-reset W31 narrative** (the 14.2-vs-7.5 head-to-head is now void; H4 stays OPEN).
2. **Honest kinematic floor is the headline number:** best-of-3 CV/CTRV on 26,132 anchors = **0.056 m@1s**. Flagship's 6.44 m = **~115× the trivial floor** → neither arm beats constant-velocity extrapolation open-loop yet. **D1 FAIL now stands on an honest denominator.**
3. **First genuinely positive signal:** flagship **@5k already beats REF-A @30k at 1 s on every stratum** (0.63/0.98/1.01 vs 1.85/1.41/1.13) — the trained-encoder thesis has a pulse, but is **not yet proven over constant-velocity**.
4. **Training is healthy across all 3 arms** (see below). The decision-grade verdict is gated on **flagship @30k (~Jul-19–23)**.
5. **Ops honesty:** the pod monitor is on **day 3 of pointing at a dead log** — it can't see the two arms that matter (pod2 flagship, pod3 REF-A). Retarget is a 3-day-pending Sayed action. No compute was lost; it's a blind-spot risk, not an outage.

## Training status

| Arm | Pod | Step | Status | Held-out fwd-ADE |
|---|---|---|---|---|
| **Flagship** (trained ViT, 4-brain) | pod2 | ~6.35k / 30k | rising, op fwd-ADE 0.114 m in-train | 6.44 m (@5k, ~115× floor) |
| **REF-A** (frozen DINO) | pod3 | 30k **DONE** | plateaued — frozen ceiling, final | **2.14 m** |
| **REF-B** (from-scratch ViT BC) | pod1 | **8,750 / 30k** (29%) | healthy, ETA **~Jul-23** | — (warming) |

- **REF-B live metrics** (the only arm the monitor can actually reach, via `tanitad-pod`): loss bounded 2.0–4.0, man_acc ~0.67–0.72, route_acc 1.00, nav_follow ~0.93–0.96, gnorm bounded, `aux_accel_r2` ~0 (still warming), steps monotonic no-dupes, ckpt fresh. ~150 steps/h (step_s ~24 s, ~half data-bound). No intervention.
- **Collapse-health:** REF-B nominal. Flagship/REF-A collapse rows are on pod2/pod3 logs the monitor can't tail (blind spot). The `p0-sB01-realmix` log the monitor *does* reach is **frozen at step 28600 since Jul-12** — a superseded pre-reset run (erank ~24–30, dim_std ~0.87–1.01 there are **stale**, ignore).
- **Intervention flags:** none today. Monitor correctly did **not** relaunch the dead sB01 run (would resurrect a pre-reset experiment and contend REF-B's GPU).

## Gates / experiments delta

No new gate numbers today — the meaningful change is the **denominator**, not the gates:
- **D1:** FAIL (unchanged verdict) but now measured against the honest 0.056 m CV floor (`e8fca8e`) instead of an abstract sub-metre bar. Flagship 6.44 m = ~115× floor.
- **D2 / D3:** no fresh decision-grade read; both await the flagship @30k eval.
- Fallback capability-proof surface is **closed-loop D4–D6** (open-loop ADE ⊥ Drive Score; CV is near-unbeatable on highway open-loop).

## Hub agents + intake queue

- **Week health:** 5/5 weekday agents ran + committed measured work (per W32 synthesis). Today's wall-clock commits: the orchestrator weekly report + the pod monitor flag.
- **D-026 debt persists:** unmerged branches, stale main-tip STATE.md (Data/Bench/Opp behind their own commits), and uncommitted work sitting in the tree since Jul-12/13 — Bench backlog-#3 + cosmos-robustness first-pass, Data `OWN_DATASET_PLAN.md`, opponent stationary-lead scenario, and 3 stack scripts (`eval_metric_rollout.py`, `validate_data.py`, `test_physicalai_rig.py`). None are today's work; all are **untriaged intake debt**.
- **Intake:** verdict write-back chronically skipped; ~3 genuinely-untriaged packages.
- **Schedule:** 9/9 jobs enabled + fired. Burn **$0 / $120** on plan.

## DECISIONS NEEDED FROM SAYED

1. **Pre-approve the graphics-capable CARLA pod (D4–D6 closed-loop + real D8).** This is the fallback capability-proof surface if flagship @30k doesn't beat CV open-loop — and open-loop ADE is known to be ⊥ closed-loop Drive Score, so we likely need it regardless. Low $, MED-HIGH value. *(W32 proposal P2.)*
2. **Retarget the pod monitor** (`tanitad-pod-training-monitor/SKILL.md`) at all 3 live arms — log path, experiment dir, and process pattern all changed at the reset (`refb_train` not `train_worldmodel`). **3 days pending.** The monitor is currently structurally blind to a real flagship/REF-A crash. ~5 min edit.

*(Standing/no-action-needed: D-021 latent-dim — default keep 2048, nothing blocked; D-022 public-claim firewall — default HOLD, legal call.)*

## Tomorrow's plan (top 3)

1. **Ride flagship → 30k** and run the decision-grade gate the moment it lands (~Jul-19–23): D1 route-CI **vs the 0.056 m floor and vs REF-A 2.14 m**, re-gate D2/D3. This is the pending verdict for the whole 4-brain trained-encoder thesis.
2. **Clear the D-026 hygiene debt** — merge stranded branches, commit/triage the Jul-12/13 intake sitting loose in the tree, refresh the stale main-tip STATE.md.
3. **Unblock closed-loop** — get the monitor retargeted and (pending Sayed) stand up the graphics CARLA pod so D4–D6 is ready as the capability-proof fallback.
