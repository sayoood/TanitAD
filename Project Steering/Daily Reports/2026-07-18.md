# Daily Report — 2026-07-18 (Sat)

## Executive summary (what moved TODAY, honestly)
1. **Flagship v1 is the FIRST SUB-FLOOR ARM.** Final 30k TanitEval (n=881/40 eps): ade_0_2s **0.4522±0.031** — below *every* trivial bar for the first time (best-of-3 floor 0.5005, CTRV 0.523, ridge ego-status ceiling 0.5735). Turns now beat floor; high-speed (1.785) is the last stratum above floor. Pushed to HF.
2. **This morning's REF-B crash is root-caused and fixed.** The ~step-500 DataLoader "bus error / insufficient shm" was a MooseFS mmap slice crossing the worker boundary. Fix = materialize (clone) mmap window tensors in `getitem` → unblocks `--workers>0`. REF-B relaunched as **refb-refbpatch-v2-30k**, now live at **step 7550/30000**, `data_s=0.0` (fix holds).
3. **H4 closed NEGATIVE, then REFRAMED.** Frozen DINOv2-B/14 lacks generalizable yaw (distill pre-gate RED, episode-split) — but the RED was the *expected* result: the encoder was **mis-wired, not unfit**. New arm **refa-dynin** = frozen DINO + yaw-rate as input (same trick as the v0 speed fix) + anti-shortcut ego-dropout. Code committed; awaiting a GPU slot.
4. **REF-C redesigned to a DiffusionDrive anchored decoder** (replaces the wrong 2022 tiny-TCP unimodal build), set up as a 3-size scaling study: **small 55M / base 104M / XL 252M** on the identical 2376-ep data.
5. **H25 (vision-decoupling) + H26 (hierarchical cross-alignment) proposed;** H26 got its first partial confirmation — the ctx→tactical seam flipped to load-bearing at 30k.

## Training status
- **Live arm:** `refb-refbpatch-v2-30k` (DiffusionDrive-anchored REF-B, relaunched post-fix). GPU 100% / ~21 GB. Latest ckpt 19:52.
- **Step:** 7550 / 30000. **~3.9 s/step** (step_s 193.5 over log-every=50), `data_s=0.0` → mmap fix confirmed, no dataloader stall. ETA ~24 h to 30k.
- **Health (this arm's schema — no erank/dim_std/step_ratio here; those live in the flagship/SigReg arm):** man_acc ~0.50–0.64, route_acc 1.0, nav_follow_frac ~0.94, anchor_acc ~0.12–0.28 (early mode-cls warmup), n_modes 21–23, aux_accel_r2 / aux_yaw_r2 ≈ 0 (early). Loss 33–47 (multi-task sum). No intervention flags.
- **Note on the scheduled target:** `p0-sB01-realmix.log` (what this report is scripted to tail) is a **6-day-obsolete pre-reset run** — log frozen Jul-12 04:38 @ step 28600, ckpt Jul-12 04:16, no metrics.json. Ignore it; the live signal is refb-v2. The monitor/report SKILLs should be retargeted (4th day this has bitten).

## Gates / experiments delta
| Item | Before → After today |
|---|---|
| Flagship v1 | 19k 0.640 → **30k 0.4522 (sub-floor, FINAL)**; win 81.2%, miss@2m 0.180→0.060 |
| H4 (frozen DINO) | open → **CLOSED NEGATIVE → REFRAMED** (fix = yaw-rate input, arm refa-dynin) |
| REF-C | tiny-TCP (wrong) → **DiffusionDrive decoder, 3-size 55/104/252M** |
| REF-B | crashed @step 500 → **relaunched v2, live @7550** |
| H26 ctx→tactical seam | 0/3 inert → **1/3 load-bearing (content_matters=true)** |
| NaN sweep | kamm_circle sqrt-relu 0*inf trap **fixed** (H14) + witness test |

New files: `refc_small_config`, `rebuild_pai_rolling.py`, `test_anchor_tactical.py`, `test_gated_intent.py`, refa-dynin toolkit (`smoke_dynin.py`, `eval_refa4b_grounded.py`, `refa_plus.py`).

## Hub / intake status
- 12 commits today (00:07 → 12:08 CEST), all landed on the tip agent branch. Ledger updated with H4 close+reframe, REF-C redesign, flagship v1 FINAL, H25/H26. Test suite 394 green after the refc redesign.
- No new intake packages pending triage flagged today; the day was research-decision + recovery heavy, not ingestion.

## DECISIONS NEEDED FROM SAYED
1. **Next GPU allocation.** One live GPU is on refb-v2 (≈24 h to 30k). Three arms now queue behind it: **refa-dynin** (H4-reframe test, ready), **REF-C 3-size scaling** (55/104/252M), and any **flagship-v3** (H25 vision-decouple) start. Which goes next when refb-v2 finishes? (Recommend refa-dynin first — cheapest, directly closes the reframed H4.)
2. **Confirm the flagship-v3 target.** v1 is FINAL and sub-floor; high-speed (1.785) and route-from-vision (route_skill 0.0) are the named open weaknesses. Greenlight v3 to attack those via H25 decoupling?

(Nothing blocking — training is live and healthy; these are scheduling calls.)

## Tomorrow's top 3
1. Babysit refb-v2 to 30k; run gate eval at the next milestone (15k) — watch anchor_acc / aux_accel_r2 start to lift out of warmup.
2. Launch **refa-dynin** on the freed GPU (or in parallel if a second slot opens) — the direct H4-reframe test.
3. Retarget the evening-report + pod-monitor SKILLs off the dead `p0-sB01-realmix` and onto the live-arm auto-discovery (stop the recurring stale-target miss).
