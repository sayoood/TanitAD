# Daily Report — 2026-07-20 (Mon)

Written 19:30 UTC. Sources: `git log` (18 h), pod1/pod2 live probes, raw `metrics.json`
files, `MODEL_REGISTRY.md`. No number below is copied from prose.

---

## 1. Executive summary — what actually moved today

1. **v1.5 (diffusion head on the frozen v1 WM) failed all three gates, but produced the
   sharpest diagnostic of the program so far: a double dissociation.** Conditioning buys
   **ranking**; better labels buy **proposal coverage**. Neither alone reaches the gates.
2. **The decisive `ab` ablation finished at 17:17 UTC and has not been reported anywhere
   yet — I read it off pod2. It resolves the open question from this afternoon: the
   imagination earns its place; the goal conditioning does not.**
   `a` 0.6759 → `ab` 0.5404 (**imagination, −0.1355 m**) → `abc` 0.5341 (**goals, −0.0063 m,
   i.e. nothing**).
3. **REF-C v1.0 (hand-written cost re-rank) recovers 0.0 %** — a GT-perfect speed-matcher
   is *worse* than baseline (1.1236). That whole path is dead; the surviving idea is a
   learned residual over the top-K.
4. **REF-C-XL final @29,999 = ADE@2s 0.458**, which re-ranks the reference arms — REF-C,
   not REF-B, is the strong reference. Registry corrected twice today (I had quoted
   single-clip numbers as corpus-wide).
5. **Process: 45 pod-only files (~3,650 lines) rescued into git**, and a boot hook that
   *would have double-launched a trainer onto a live GPU* was caught and fixed. The
   agent operating standard ("stage, never push") is now in force.

Honest counterweight: **no arm improved today.** v1.5 is ~0.09 m short of the weakest gate,
and the day's value is entirely diagnostic — we now know *which* of two levers to pull, not
that either works.

---

## 2. Training status

### pod1 (`tanitad-pod`, A6000) — `flagship4b-v3enc-30k` — LIVE

| field | value |
|---|---|
| step | **4,900 / 30,000** (16 %) |
| elapsed | 13 h 55 m |
| pace | **10.2 s/step** (`step_s` 504.8 ÷ 50; data 1.3 s/step — not data-bound) |
| **ETA** | **≈ 71 h remaining → ~2026-07-23 18:30 UTC** |
| erank | 27.5 → 32.2 (from 21.6 at step 0) — **healthy, rising** |
| dim_std | 0.769–0.794 (from 0.119 at step 0) — **healthy** |
| ego_r2 | 0.819–0.862 |
| man_acc / route_acc | 0.44–0.56 / 0.40–1.00 (noisy, small batch) |
| RAM guard | 60–62 GB, sweeping to 2,376 files — holding, no OOM |

`step_ratio` is not emitted by the v2 log schema; erank + dim_std are the live collapse
guards and both are in good shape. **No collapse signature.**

🚩 **Intervention flags**
- **ETA is 3 days.** At 10.2 s/step this run occupies the only training GPU until Friday.
  Nothing else can train on pod1 in that window.
- **`/workspace/experiments/p0-sB01-realmix.log` — the file this report is scheduled to
  read — is stale since 2026-07-12 04:38.** Anyone tailing it sees step 28,600 and
  concludes a run is nearly finished. It is a dead run's log. Scheduled task fixed to read
  the live `train_log.jsonl` instead.

### pod2 — **IDLE since 17:17 UTC**
All four v1.5 arms complete; GPU at 0 %, 0 MiB, no trainer process. Free capacity tonight.

---

## 3. Gates / experiments — today's delta

v1.5 arms, all at step 7,999, n = 881, like-for-like (full-set `metrics.json`):

| arm | conditioning | ADE@2s | oracle-in-fan | frac_sel_2x_worse | miss@2m |
|---|---|---|---|---|---|
| `a` | states only | 0.6759 | 0.2990 | 0.403 | 0.234 |
| **`ab`** | **+ imagination** | **0.5404** | 0.3073 | 0.318 | 0.160 |
| `abc` | + imagination + goals v2.1 | 0.5341 | 0.3377 | 0.235 | 0.135 |
| `abc_legacy` | + imagination + goals (legacy) | 0.5674 | 0.3960 | 0.184 | 0.160 |

Gates: G1 < 0.458 (REF-C-XL) · G2 < 0.4522 (flagship v1) · G3 miss@2m ≤ 0.10.
**`abc` misses all three** — G1 by 0.096, G2 by 0.102, G3 at 0.145 heldout.
(Jackknifed heldout: `abc` 0.5543 ± 0.0512. All arms beat CV 0.8248.)

**Reading the `ab` row — the new result.** Imagination carries **96 %** of the
`a`→`abc` gain; goal conditioning nets **−0.006 m**, far inside the ±0.051 CI. But goals are
not inert: they keep improving *ranking* (frac_2x 0.318 → 0.235) while *degrading the
proposal fan* (0.307 → 0.338), and the two cancel. That is the double dissociation showing
up a third time, now cleanly separated.

Reference arms (registry, corrected today): flagship-v1 **0.4522** > REF-C-XL **0.458** >
REF-B v2 0.826 (@10k) > REF-A 2.92.

---

## 4. Hub agents + intake queue

- **41 intake packages** on disk; 6 touched today (`pod-corpus-build`, `vlm-labeling-pilot`,
  `vtarget-validation`, `ci-gate`, `curve-rebalance`, `zod-loader`). The orchestrator's
  05:00 run reported **19 of 23 verdictless for the third consecutive report** — triage is
  the standing bottleneck, unchanged today.
- **Branch drift:** current tip `agent/tools-devenv-20260717` is **109 commits ahead of
  `main`**; 6+ unmerged agent branches remain. Working tree is clean apart from 2 doc edits.
- **HYPOTHESIS_LEDGER's newest entry is 2026-07-19.** Today's v1.5 / REF-C findings are in
  commit bodies but **not yet in the ledger** — that is a gap, since the ledger is what the
  next agent reads.

---

## 5. DECISIONS NEEDED FROM SAYED

1. **v3enc: let it run to 30k (finishes Fri ~18:30 UTC) or stop it early?** It holds the
   only training GPU for 3 days. Health is good, but there is no intermediate gate planned
   before the end. *My recommendation: let it run, but add a 10k gate eval so we learn
   something on Tuesday instead of Friday.*
2. **Where does the v1.5 line go now that the ablation is in?** The evidence says drop the
   goal conditioning (buys nothing net) and attack the **proposal fan**, which is where the
   0.196 m to the gates actually sits. Confirm before I spend pod2 on it.
3. **pod2 is idle tonight.** Approve a use, or it burns unused. *My recommendation: the
   learned top-K residual on joint along+cross error — the one selection idea REF-C v1.0
   did not refute (oracle within top-8 = 0.2026 = 87 % of the gap).*
4. **H26 swamping monitor fired** on `abc`: the ROUTE seam's contribution norm is 2.80× the
   measurement norm despite `rt_gate` 0.10. It partially confounds the conditioning result.
   Cap the norm before reusing the pattern? (Low cost, my default is yes.)

---

## 6. Tomorrow — top 3

1. **Write today's findings into HYPOTHESIS_LEDGER** (v1.5 double dissociation, `ab`
   ablation, REF-C v1.0 refutation) and into MODEL_REGISTRY as the v1.5 row. Commit bodies
   are not a source of truth for the next agent.
2. **Run the `ab`-arm result through the same 8-split jackknife** the other three got, so
   the "imagination carries it, goals don't" claim is CI-stated rather than point-estimated.
3. **Stand up the top-K learned residual on pod2** (pending decision 3), and add a **10k
   intermediate gate** to v3enc so Friday is not the first signal.

---

*Instrument notes for whoever reads this next: `step_s` is accumulated over `--log-every`
(÷50). The `p0-sB01-realmix.log` path is dead — the live log is
`/workspace/experiments/flagship4b-v3enc-30k/train_log.jsonl` on `tanitad-pod`.*
