# Daily Report — 2026-07-10 (evening)

> Breakthrough day. The trainer blew past the block that stalled everything and ran clean to
> **step ~21k / 30k** — and the first real gate movement landed: **D1 halved, D2 near-perfect, D3
> closing**. The overnight I/O thrash is root-caused and fixed for good. 2-minute read.

## (a) Executive summary — what moved TODAY (honest, P8)

1. **Trainer unblocked and healthy — ~21,000 / 30,000 steps (~70 %).** The page-cache thrash that
   capped throughput at ~10–60 steps/h is **root-caused and fixed** (`7b5faa6`): the mmap dataloader's
   working set was unbounded (spanned the whole 88 GB cache inside the 57 GB cgroup); it's now bound to
   `n_episodes`. `data_s` was **73 %** of step time — now back to nominal. No interventions; run is
   riding to 30k on its own.
2. **First real gate movement (step-14k, the slipped "15k preview" landed):** **D1 ADE@1s halved
   10.94 → 5.18 m** (still FAIL vs the sub-metre floor, but the trend is now steep and monotone);
   **D2 PASS *improved* to near-perfect — P1 0.917 / P4 1.000** (was 0.872 / 0.940); **D3 BLOCKED but
   I4 closing 3.83 → 3.05** (floor ~1.5). Verdict triple: **D1 FAIL / D2 PASS / D3 BLOCKED**, all
   admissible (I1–I3 green).
3. **D3 decomposition experiment answers Sayed's compounding concern (`c0b22b7`):** in the **direct
   multi-horizon heads, relative error FALLS with k** (comma 12.1 → 8.4 → 5.1; PhysicalAI 6.9 → 4.0 →
   2.55) — **there is no error-compounding**. **Recursive 1-step×4 is 2–4× WORSE** (20.5 / 9.5) →
   keep direct heads, do **not** recurse. Residual highway-normalization artifact noted (not a model
   defect). This is the strongest architectural read of the week.
4. **Three Sayed strategic directives banked (`811df73`)** for scheduling: **YouTube dashcam strategy**
   (Y0–Y2 pipeline + pilot), **REF-A / REF-B reference-architecture build plans**, and a **multi-cam
   encoder-optimization investigation**. Directives are captured; sequencing is the open question (§e).
5. **Steering: D-026 addendum (`a49e159`)** — Sonnet 5 is the default workflow model, **except the
   stack audit** (kept on the stronger tier), per Sayed.

## (b) Training status — p0-sB01-realmix (A40 pod)

Latest step: **~21,000 / 30,000 (~70 %)**. Throughput recovered to nominal after the mmap fix; `ckpt`
written at step 21000. ~9k steps to the 30k finish (a few hours at current rate).

Collapse-health trend (the numbers that gate everything):

| step | erank | dim_std | step_ratio | note |
|---|---|---|---|---|
| 14200 | 28.2 | 1.04 | 0.096 | yesterday's read |
| 19100 | 27.7 | 0.95 | 0.090 | post-mmap-fix, nominal |
| 19850 | 29.8 | 0.94 | 0.112 | |
| 20350 | 28.7 | 0.93 | 0.106 | |
| 20850 | 28.2 | 0.96 | 0.100 | |
| 21000 | 28.1 | 0.96 | 0.091 | current ckpt |

**Read:** erank steady at **~28** (the intrinsic highway-dynamics rank — spectral effective-rank ≈35,
knee ≈22 → expected, **not a collapse signature**). dim_std ~0.92–1.04 (variance preserved), step_ratio
~0.09–0.11 (healthy). H15 NLL steady ≈ −4.3 (0.0 on masked steps). **Intervention flags: none.** The
overnight page-cache pollution is resolved at the root, not just restarted.

## (c) Gates / experiments delta

**Real gate movement today** (first since step-5k):

| Gate | step-5k | step-14k | Δ |
|---|---|---|---|
| **D1** ADE@1s | 10.94 m FAIL | **5.18 m FAIL** | halved, steep trend |
| **D2** dir-acc P1 / P4 | 0.872 / 0.940 PASS | **0.917 / 1.000 PASS** | improved to near-perfect |
| **D3** I4 (rel err) | 3.83 BLOCKED | **3.05 BLOCKED** | closing toward ~1.5 floor |

New experiment artifacts today: `stack/experiments/p0-d3-decompose/d3_decompose_14k.json`
(direct-vs-recursive horizon study), `stack/experiments/p0-kstep-arms/{d3_arm_base,d3_arm_kstep}.json`,
pod `gates_step14000.json`. D3 horizon under test = 0.4 s (path toward the 2-s claim).

## (d) Hub agent + intake-queue status

- **Trainer is the story today** — the fix + gate read came from the training/eval track, not the
  weekly discipline agents (those fired yesterday). No new discipline-agent runs logged since the
  07-09 wave.
- **Intake queue unchanged from yesterday** — ~6 verified packages still awaiting promotion/triage
  (lal-v2, physicalai-r1-selection, models-predictor-failfast, stop-arm-gate, cosmos-drive-dreams-loader,
  metadrive-frontcam[stale/retire]). Two new untracked local dirs appeared under Prod-Opt
  (`int8_quant/`, `contract-windowing-failloud/`) — not yet committed/triaged.
- Stack suite last recorded green (181–188); today's changes were pod-side (mmap fix) + eval JSON.

## (e) DECISIONS NEEDED FROM SAYED

1. **Ride to 30k + next-stage budget.** Trainer is ~9k steps from the 30k finish. Confirm it rides to
   30k and that the **next stage's spend is pre-approved** (Stage-1 / bake-off arms). *(The 07-09 06:15
   "restart trainer" decision is now RESOLVED — the mmap fix superseded it; if §4 still lists it, stale.)*
2. **Sequence the three banked directives.** YouTube dashcam (Y0–Y2), REF-A/REF-B reference builds, and
   multi-cam encoder optimization are all captured (`811df73`). **Which goes first after 30k?** My
   recommendation: REF-A/REF-B (D4 opponent baseline unblocks decision-grade gate arbitration) unless
   the dashcam pilot is time-sensitive.
3. **Intake triage (light, mostly orchestrator).** ~6 verified packages await promotion; flag any to
   hold or fast-track. Two new Prod-Opt dirs (int8_quant, contract-windowing) need a commit/triage pass.

## (f) Tomorrow's plan (top 3)

1. **Drive to step-30k and run the full D1–D3 gate at 30k** — the first decision-grade read of the
   run. Watch: D2 must hold ≥0.9/1.0; D1 ADE should keep falling below ~5 m; D3 I4 toward the floor.
   Then local `d8_preview.py` paired re-run (paired-D8 fraction should clear 16/23).
2. **Fold the D3 decomposition result into the architecture** — direct multi-horizon heads confirmed
   over recursion; update the D3 claim path (0.4 s → 2 s) and bake-off lever set accordingly.
3. **Clear the intake wave + commit the two new Prod-Opt dirs** (int8_quant, contract-windowing-failloud),
   and prep the next-stage config per Sayed's directive sequencing.

---
*Sources: `git log --since=18h` (4 commits), PROJECT_STATE.md §3/§5, pod `p0-sB01-realmix.log`
(tail step-21000 + trend samples), pod `gates_step14000.json` (summary D1 FAIL / D2 PASS / D3 BLOCKED),
`stack/experiments/p0-d3-decompose` + `p0-kstep-arms`. Pod reachable; training live at ~21k; overnight
I/O thrash root-caused and fixed (`7b5faa6`).*
