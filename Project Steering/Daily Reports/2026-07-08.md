# Daily Report — 2026-07-08 (evening)

> Autonomous-loop day. Single trainer live all day on the pod; MVP orchestrator ran measured
> experiments on top. Written for a 2-minute read.

## (a) Executive summary — what moved TODAY (honest, P8)

1. **First directional positive for the familiarity signal (H11).** The naive relative-imagination-error
   probe *inverted* (AUROC 0.34 — a confound, not a win). The **matched-pairs redesign** is the real
   result: **16/23 same-scene clear→degraded pairs show higher imagination error (p≈0.047)**. Honest
   bounds: step-6500 ckpt (~22% trained), small n, pre-registered re-runs locked at 15k/30k before any
   claim.
2. **Data-integrity bug caught + fixed:** 28/60 extracted Cosmos clips were chunk-1 but getting chunk-0
   actions. Temporal semantics settled (121-frame chunks @30 Hz, T=39 exact), verified on real bytes →
   Cosmos cleared for the D-010 real+sim mix.
3. **Latency proof (I8):** decision tick **15.07 ms p50 / 1.08 GB VRAM**, fp32, un-optimized, on the 4060
   → **~66 Hz vs the 10–20 Hz requirement**. Comfortable headroom before any INT8/TRT work. First real
   CNCE (median 2.02×10⁵) + TMS expert band logged.
4. **Scenario→telemetry→metric wiring PASSED** end-to-end on the work-zone oracle (all 5 discriminative
   checks) — the eval spine is now proven from oracle to suite.
5. **Training healthy all day**, step ~8800/30k, ~300 steps/h, no interventions. Stack **149 tests green**.

## (b) Training status — p0-sB01-realmix (A40 pod)

Latest step: **~8,800 / 30,000** (~29%). Rate ~300 steps/h.

Collapse-health trend (the numbers that gate everything):

| step | erank | dim_std | step_ratio | note |
|---|---|---|---|---|
| 2500 | 7.7 | 0.75 | 0.044 | early |
| 4000 | 16.5 | 0.87 | 0.090 | climbing |
| 5000 | 13.9 | 0.82 | 0.052 | |
| 6500 | 26.9 | 0.96 | 0.112 | D8/latency ckpt |
| 8000 | 26.2 | 0.85 | 0.100 | |
| 8800 | 24.3 | 0.92 | 0.078 | current |

**Read:** erank climbed 7.7 → ~27 and **plateaued near the intrinsic highway-dynamics rank** (spectral
diagnostic: effective rank ≈35, knee ≈22). Sitting at ~27 is **expected and healthy — NOT a collapse
signature**. dim_std ~0.9 (variance preserved), step_ratio ~0.10 (healthy). H15 NLL steady around −3.3
to −3.6 (occasional 0.0 = masked steps). **Intervention flags: none.**

## (c) Gates / experiments delta

**No new gate numbers today** — last measured gate is still **D2 PASS at 17% training** (P1 0.872 /
P4 0.940 @ step ~5k). Today's work was instrumentation + measured probes feeding the *next* gate read.
Next delta: the **15k-step preview** (D1–D3 + paired D8), due ~2026-07-09 midday.

New experiment artifacts: `stack/experiments/{p0-d8-preview, p0-latency-baseline, p0-scenario-wiring}/`;
scripts `d8_preview.py`, `cosmos_pairs.py`, `latency_cnce_baseline.py`, `scenario_suite_dryrun.py`.

## (d) Hub agent + intake-queue status

- **Intake queue: 6 packages still in `incoming/`, pending orchestrator triage** (gate-runner-d1-d3,
  spectral-sizing-p0, metadrive-frontcam-perturbation, cosmos-drive-dreams-loader, work-zone-phantom,
  eval-metric-suite). All 6 were verified LIVE in the D-020 deep-screen — they work; they just aren't
  promoted out of `incoming/` yet.
- Weekly discipline agents scheduled and firing (Opponent, Benchmarks, DataEng, Architecture, Tools&DevEnv,
  new Production&Optimization Saturday agent). Per-discipline `BACKLOG.md` seeded ×6.

## (e) DECISIONS NEEDED FROM SAYED

1. **RunPod budget for continued/next stage.** The run is capped at 30k steps (~ another ~20 h at current
   rate). Confirm whether to let it ride to 30k and whether the next stage's spend is pre-approved.
2. **MetaDrive source install (~10 min, supervised, optional).** PyPI no-go on py3.13; unblocks the D-010
   sim-mix live rollout and D5/D6/D9 scenarios. Not blocking training — do it whenever convenient.
3. **D-021 (latent dim k=2048): no action needed now.** Default stands — keep 2048 for Phase 0, resize
   only Phase-1 with a trained-checkpoint spectral result. Logged as "keep measuring," nothing blocked.

## (f) Tomorrow's plan (top 3)

1. **15k-step preview** (the day's headline event): pod `evaluate_checkpoint.py` on the 15k ckpt → D1–D3;
   then local `d8_preview.py` paired re-run. Watch: D2 must not regress (P1 0.872 / P4 0.940 floor);
   D1 ADE trend; **paired-D8 fraction should rise above 16/23**.
2. **Stage-0 bake-off** (overdue W2 item): residual+change-weighted vs MSE; grid readout vs pooling;
   probe_imag vs probe_real.
3. **Triage the intake queue** (promote the 6 verified packages) and/or begin **PhysicalAI-AV Stage R0
   ingestion** (DataEng standing duty).

---
*Sources: `git log --since=18h`, PROJECT_STATE.md §3/§5, pod `p0-sB01-realmix.log` (tail + trend samples),
`stack/experiments/`, intake-queue scan. Pod reachable; training live.*
