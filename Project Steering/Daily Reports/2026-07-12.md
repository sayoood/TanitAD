# Daily Report — 2026-07-12 (evening)

> **Pivot day: "don't spend 4 days on a bad run" became a program.** After two expensive runs
> under-delivered (OOM-crawl; a grounding fine-tune that never moved the oracle ceiling 1.65→1.60),
> Sayed instituted a **6-axis PRE-FLIGHT GATE** that must be all-GREEN before the from-scratch 4-brain
> flagship launches. **Axis-2 (geometry/VLM3) went GREEN today** — the 1.62× focal error that wasted
> the prior run is measured-fixed. Main WM 30k run is at **~28.6k / 30k (~95%)**, healthy. 2-min read.

## (a) Executive summary — what moved TODAY (honest, P8)

1. **The 4-day flagship is now GATED, not scheduled** (`9085fdd`, Sayed). New `PRE_FLIGHT_VALIDATION.md`
   defines 6 axes (data · geometry/VLM3 · architecture · training · heads · empirical-lever) each with a
   concrete check + pass criterion. **Rule: no 4-day run until every axis is GREEN with numbers.** This is
   the response to the two prior runs that burned compute for a flat result.
2. **Axis-2 GREEN — the headline (`d21a7d6`).** The f-theta fix now yields a SHARED canonical focal:
   **f_eff = 266 on BOTH corpora** (comma 266.54, physicalai 265.97; old nominal 434.76 = a **1.63× zoom
   skew**). Directly measured on 500-clip per-clip intrinsics, decode-confirmed to 0.01 px. **The specific
   1.62× focal error that wasted the prior run is FIXED.** Two known residuals (camera-height ~1.19×
   ground-scale; default-crop barrel curvature) are explicitly **deferred to D-016 R1**, not this gate.
3. **The full 4-brain flagship is built + merged** (`7507176`→`4279641`): trained tactical policy +
   strategic transformer + hierarchical grounding + SIGReg-position relaxation, **shared with REF-A** for
   structural parity (Sayed's Axis-3 requirement). Axis-6 reduced-but-real config + grounded-rollout eval
   plumbing landed (`ac8f05c`); goal-loss latent-encoding fixed (`b5801f1`).
4. **Axis-1 + Axis-5 validation harness written today** (uncommitted, in progress): `validate_data.py`
   (Axis-1 five-check data-consistency gate over the corrected caches — cache integrity, temporal align,
   units, mix ratio/split-leakage, corrected-vs-old identity) and `eval_metric_rollout.py` (the *emergent*
   rollout-trajectory eval — decode the trajectory the model would actually drive, vs the post-hoc probe
   ladder). These turn Axes 1/5 from AMBER toward GREEN.
5. **Two new hypotheses banked** (Sayed, ledger H17/H18): H17 unified-FOV masked-periphery training;
   H18 hierarchical action grounding (operative grounding **ships now**, tactical/strategic next).

## (b) Training status — p0-sB01-realmix (A40 pod)

Latest step: **~28,600 / 30,000 (~95%)**, run live, resumed cleanly from the step-27001 stall
intervention logged in PROJECT_STATE §5. ~1,400 steps to the 30k finish.

Collapse-health trend (fresh tail):

| step | loss | erank | dim_std | step_ratio | note |
|---|---|---|---|---|---|
| 28450 | −1.30 | 29.4 | 0.92 | 0.116 | real step |
| 28500 | −1.18 | 30.0 | 0.96 | 0.106 | ckpt saved |
| 28550 | +1.81 | 24.2 | 0.87 | 0.087 | masked step (h15=0.0, sigreg carries) |
| 28600 | +0.88 | 28.5 | 1.01 | 0.088 | real step |

**Read:** erank steady **~24–30** (intrinsic highway-dynamics rank, knee ≈22 → expected, **not
collapse**). dim_std ~0.87–1.01 (variance preserved). The positive-loss rows are **masked steps** (H15
imagination term drops to 0.0 by design and SIGReg carries — the normal masked/real alternation, seen all
run), not an anomaly. Data-path is **~67% of step time** (data_s ~450 / step_s ~667 — the known,
already-root-caused loader bottleneck). **Intervention flags: none.**

## (c) Pre-flight gate status (the new decision surface)

| Axis | What | Status | Evidence |
|---|---|---|---|
| 1 Data consistency | corrected-cache integrity / temporal / units / split | **AMBER** — gate script written, not yet run/committed | `validate_data.py` (untracked) |
| 2 Geometry (VLM3) | shared canonical f_eff both corpora | **GREEN ✅** | `d21a7d6`, `validate_geometry.py`, `Benchmarks & Eval/axis2_geometry_gate/` |
| 3 Architecture parity | 4-brain wiring + flagship↔REF-A parity | **AMBER** — 4-brain built, parity/sensitivity checks pending | `4279641` |
| 4 Training setup | loss assembly / relaxation / schedule / loader | **AMBER** — awaits 20-step real-data smoke | `ac8f05c` |
| 5 Heads & eval | maneuver/route heads, metric decode, protocol pinned | **AMBER** — rollout eval plumbing written | `eval_grounded_rollout_4b.py`, `eval_metric_rollout.py` (untracked) |
| 6 Empirical levers (decisive) | SIGReg-relax / grounding-from-scratch / geometry A/B move the oracle at small scale | **AMBER** — config + eval ready, pre-checks not yet run | `ac8f05c` (branch `validate/axis6-precheck`) |

No fresh full D1–D3 gate today — movement was the **pre-flight program + geometry proof**, not gate numbers.

## (d) Hub agent + intake-queue status

- Work today is on branch **`validate/axis6-precheck`** — the validation program, not the weekly hub cycle.
- Uncommitted in tree: `validate_data.py`, `eval_metric_rollout.py` (today's Axis-1/5 harness),
  `PROJECT_STATE.md`, `.claude/settings.local.json`. Nothing stranded >1 day; these are live WIP.
- Intake queue otherwise unchanged since the W31 debt sweep (`1285524`).

## (e) DECISIONS NEEDED FROM SAYED

**Nothing blocking tonight** — the big decision (4-day flagship GO/NO-GO) is exactly what the pre-flight
gate exists to make *evidence-driven*, and it isn't ready: only Axis-2 is GREEN, Axes 1/3/4/5/6 are AMBER.
The GO is **auto-pending the gate**, not a nudge for you. Standing carry-overs (unchanged, none urgent):

1. **D-022 substantive reclassification** — legal call, held at conservative default (firewall kept); won't
   move without an explicit accept from you/legal.
2. **MetaDrive source install** (~10 min, supervised, your machine) — unblocks D-010 sim-mix; standing ask.
3. **D-021** (informational) — latent dim k as a measured variable; default keep 2048 for Phase 0.

## (f) Tomorrow's plan (top 3)

1. **Turn Axis-1 GREEN**: run + commit `validate_data.py` over the corrected caches (five checks →
   PASS/number each); this is the cheapest remaining axis and clears the data question outright.
2. **Axis-4 + Axis-3**: 20-step real-data smoke of the 4-brain (every loss term finite/O(1), SIGReg
   relaxation exempts free-dims while erank holds) + the flagship↔REF-A structural-parity assertion.
3. **Set up Axis-6 pre-checks** (the decisive ones) and **let the main WM 30k run finish** (~1.4k steps) →
   its route-resampled D1–D3 gate is the last decision-grade read of the *old* recipe before the pivot.

---
*Sources: `git log --since=18h` (8 commits, all 07-12), `PRE_FLIGHT_VALIDATION.md`, PROJECT_STATE §3/§5,
HYPOTHESIS_LEDGER (H17/H18), pod `p0-sB01-realmix.log` (fresh tail step-28600 + trend 28450–28600),
untracked `validate_data.py` / `eval_metric_rollout.py`. Pod reachable, run live ~95%, no interventions.
Axis-2 GREEN by direct 500-clip measurement; the other five axes AMBER with harness in flight.*
