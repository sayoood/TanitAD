# Daily Report — 2026-07-11 (evening)

> Reference-architecture day. **All three arms now physically exist and the first head-to-head landed.**
> Main WM crawled from 21k → **~26k / 30k (~87%)**, healthy. **REF-A frozen-DINO reference is 30k
> COMPLETE**; its first probe against main says frozen-encoder is *worse* — but the number is
> **confounded**, so it does NOT arbitrate H4 yet (P8). Sayed cleared a stack of decisions today. 2-min read.

## (a) Executive summary — what moved TODAY (honest, P8)

1. **All three architecture arms are live for the first time** (Phase-0 day 7/42). Main world-model
   **~26,050 / 30,000 (~87%)**, healthy, no interventions. **REF-A frozen-DINO reference 30k COMPLETE**
   (loss 1.19 → 0.465, roll K=4-healthy, adapter_std 0.86↑ no-collapse, $0 incremental). **REF-B E2E
   vision-action reference built + merged** — rev1 (`e616b23`, 260.7 M, −0.82%) then rev2 (`38cf9ca`)
   swapping the strategic layer to a real transformer (d384×4) + route-derived nav commands.
2. **First head-to-head — and we're being honest about it (`ecbad24`).** On a matched
   `d1_probe_capacity` protocol, **REF-A ADE@1s ≈ 14.2 m vs main ≈ 7.5 m** → the frozen encoder is
   currently **WORSE**. **But it's CONFOUNDED:** REF-A's v1 adapter mean-pools all 256 DINO tokens
   (killing the spatial structure the DINO-WM lineage needs), and it was scored comma-val-only vs
   main's comma+physicalai. **H4 is NOT upgraded** — this does not arbitrate the frozen-vs-E2E question.
3. **Sayed's review turned the confound into two concrete experiments.** "Expected better from DINO"
   → a **grid-adapter REF-A variant** (`29c1b62`, `SpatialGridReadout` on DINO tokens, state 2048,
   mirrors main geometry). "7.51 is bad too" → a **CV kinematic-floor baseline row** (`a7563e0`) —
   test whether the sub-metre D1 bar is even meaningful vs constant-velocity extrapolation.
4. **The step-21k D1 "regression" was a false alarm, root-caused (`0284a5c`).** A probe-capacity
   discriminator shows the later checkpoint decodes *better* at every capacity (ridge α10 8.54→7.02,
   MLP 11.19→8.40) — the 5.18 → 11.52 m jump was **route-split sensitivity, not information loss**.
   D1 is now a **route-resampled mean ± 95% CI** in the gate runner (`e9b2491`, n_splits=8). D1
   improvement is positively monotone again (still FAILs the sub-metre floor — 30k + REF-A arbitrate).
5. **Sayed cleared the decision backlog + unblocked pod3.** D-028 **accepted**; REF-A + post-30k
   spend **pre-approved** ($0 incremental + $40 REF-B envelope); D-022 numbering gap resolved (HOLD
   default); **pod3 quota 50 → 300 GB** (`08ab8bb`) unblocking REF-B staging. New directive tonight
   (`535148e`, 17:43): **REF-A + REF-B both train on full realmix** (physicalai DINO precompute added
   to the chain); the comma-only grid run is relabeled an adapter-ablation.

## (b) Training status — p0-sB01-realmix (A40 pod)

Latest step: **~26,050 / 30,000 (~87%)**. Pod reachable, run live, `ckpt` at step 26000. A ~2 h crawl
earlier today slipped the 30k ETA (~20:15 → ~22:15) but stayed above the watchdog floor and
self-recovered — no intervention. ~4k steps to finish.

Collapse-health trend (the numbers that gate everything):

| step | erank | dim_std | step_ratio | note |
|---|---|---|---|---|
| 21000 | 28.1 | 0.96 | 0.091 | yesterday's finish |
| 25700 | 26.6 | 0.94 | 0.086 | |
| 25850 | 30.0 | 1.00 | 0.104 | |
| 25950 | 29.9 | 0.99 | 0.087 | |
| 26000 | 28.1 | 0.87 | 0.099 | current ckpt |
| 26050 | 29.2 | 0.95 | 0.115 | latest |

**Read:** erank steady at **~28–30** (intrinsic highway-dynamics rank; knee ≈22 → expected, **not a
collapse signature**). dim_std ~0.87–1.00 (variance preserved), step_ratio ~0.09–0.11 (healthy).
H15 NLL steady ≈ −4.2 to −4.7 on real steps (0.0 on masked). **Intervention flags: none.**

## (c) Gates / experiments delta

No fresh full-gate run today (the decision-grade read is the 30k gate, ~4k steps out). Movement was
in **protocol + reference arms**, not gate numbers:

| Item | Before | Today | Δ |
|---|---|---|---|
| **D1 protocol** | single-split ADE (21k spike artifact) | route-resampled mean ± 95% CI, n_splits=8 | `e9b2491` — spike root-caused, monotone restored |
| **REF-A** | trainer merged | **30k COMPLETE** (loss → 0.465) + first probe ≈14.2 m | `d741bd9` / `ecbad24` — worse-but-confounded |
| **REF-B** | none | built + merged, rev2 strategic transformer | `e616b23` → `38cf9ca` |

New artifacts today: REF-A DINO precompute (`cda93df`), feature-level trainer (`d741bd9`),
`refa_probe.py` head-to-head (`ecbad24`); grid-adapter variant (`29c1b62`); route-resampled `gates.py`
(`e9b2491`); H16 active-depth-interrogation dossier (`7cd9d7b`).

## (d) Hub agent + intake-queue status

- **Weekly synthesis 2026-W31 fired** (`73b55fe`, orchestrator run #4) — covers everything since the
  W30 report. Hub is current.
- **Reference-architecture track was the story today** (REF-A/REF-B build + first probe), plus the
  D1-protocol fix from the eval track.
- **Debt sweep committed** (`1285524`): two stranded Prod-Opt packages landed, REF-A/REF-B ledger rows
  added ($0 incremental), Opponent catalogs committed. Intake queue otherwise unchanged.

## (e) DECISIONS NEEDED FROM SAYED

Light tonight — you cleared most of the backlog today (D-028, REF-A/post-30k spend, pod3 quota all
resolved). Remaining:

1. **D-022 substantive reclassification (legal, not auto-adopted).** The numbering gap is backfilled
   at its conservative default (HOLD — public-claim firewall kept to comma + Cosmos). The actual
   reclassification of WorldModel-Synthetic outputs needs an **explicit** accept from you/legal — it
   won't move on its own.
2. **MetaDrive source install (~10 min, supervised, your machine).** PyPI is a no-go on py3.13;
   Tools&DevEnv has the command sequence ready. Unblocks D-010 sim-mix live rollout + D5/D6/D9
   scenarios. Standing ask, no urgency.
3. **D-021 (informational, nothing blocked).** Latent dim k as a measured design variable — default
   is keep 2048 for Phase 0, resize only Phase-1 with a spectral result. Flag only if you want it
   revisited sooner.

## (f) Tomorrow's plan (top 3)

1. **Drive main WM to the 30k finish and run the full route-resampled D1–D3 gate** — the first
   decision-grade read of the run. Watch: D2 holds ≥0.9/1.0, D1 ADE keeps falling below ~5 m (mean±CI
   now), D3 I4 toward the ~1.5 floor.
2. **Re-probe REF-A with the grid-adapter variant** (`SpatialGridReadout`, `29c1b62`) — this is the
   experiment that settles whether frozen-DINO is genuinely worse or just crippled by the mean-pool
   confound. The single most informative result queued.
3. **Stage REF-A + REF-B full-realmix training** per directive `535148e` (physicalai DINO precompute
   in the chain; pod3 300 GB now available), and add the **CV kinematic-floor baseline row** so the
   D1 sub-metre bar is calibrated against constant-velocity extrapolation.

---
*Sources: `git log --since=18h` (22 commits, all 07-11), PROJECT_STATE.md §3/§4/§5, pod
`p0-sB01-realmix.log` (fresh tail step-26050 + trend rows 25700–26050), weekly report 2026-W31
(`73b55fe`). Pod reachable; main WM live at ~26k/30k; REF-A 30k complete; first head-to-head landed
but confounded (H4 not arbitrated, P8).*
