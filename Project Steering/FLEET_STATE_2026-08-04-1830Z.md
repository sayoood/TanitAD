# Fleet state — MEASURED 2026-08-04 18:30 UTC, from the pods themselves

Read over the RunPod **web terminal** (`stack/ops/gotty_shell.py`), not inherited.
⚠️ **Evidence class: TRAINER LOG.** Per CLAUDE.md only `eval_*.py` output is quotable as a
result, and trainer val ran ~10 % optimistic before. Admissible here for **curve shape and
liveness**, and for nothing else. Both are ADE-family only, so neither satisfies the
binding four-family rule.

## pod4 — `flagship-v1arch-v2bal-30k` (`00b05f408e35`, A40 100 %, 13 663 / 46 068 MiB)

**Step 26 500 / 30 000 — 88 % complete.** Checkpoints at 5k / 15k / 20k + live `ckpt.pt`.

| step | `g_op_fwd_ade_m` | `g_tac_fwd_ade_m` | `g_str_fwd_ade_m` |
|---|---|---|---|
| 0 | 1.7401 | 6.9758 | 7.9007 |
| 6 600 | 0.1136 | 0.3250 | 0.5749 |
| 12 850 | 0.0499 | 0.2334 | 0.3943 |
| 19 450 | 0.0445 | 0.1500 | 0.2307 |
| 24 400 | 0.0305 | 0.1072 | 0.1706 |
| 26 450 | **0.0289** | **0.1351** | **0.1824** |

All three hierarchy levels descend. `g_op_fwd_ade_m` **0.0289** is better than the
INHERITED "~0.11", which was an earlier point on this same curve.

⚠️ **`route_acc` = 1.0000 at step 1 650 and every step since — it has never moved.**
That is the **nav-echo defect**, not skill: CLAUDE.md records flagship v1's route head as an
exact bijection of the nav it is fed (369/369, 81/81) scoring 1.0000. A metric pinned at
ceiling from 5 % of training is an echo of its own input. **Do not quote it as strategic
capability.** `nav_valid_frac` is only 0.1875–0.25, so it is also a thin denominator.

⚠️ `man_acc` oscillates 0.1875 → 0.8125 with no trend — the 5-way head that mixes lat+lon,
the programme's largest known defect. `erank` 16.7, `h15` −4.71.

🔴 **NO SUPERVISOR IS RUNNING.** `ps` shows the trainer (pid 9076, up 1 d 12 h) and its
workers, and **no `supervise_run.sh`**. The programme's best curve has ~8 h of unattended
tail: if it dies at step 27 k nothing restarts it. Same class as `0cf3c03` ("v5f had NO
supervisor"). **PI decision — starting one is a write to a DO-NOT-TOUCH pod.**

**ETA ≈ 8 h** (finishes early 2026-08-05 CEST). Derived from checkpoint mtimes —
`ckpt_step15000.pt` 08-03 15:44 → `ckpt_step20000.pt` 08-04 03:20 = 5 000 steps / 11 h 36 m
= **8.35 s/step**; 3 500 steps remain. ⚠️ Deliberately NOT derived from `step_s`, which is
ACCUMULATED over `--log-every`, nor from process uptime, which spans a resume
(`ckpt_step5000.pt` predates the current pid).

Health: `memory.failcnt = 0`, rss 9.60 GB.

## tanitad-new — `flagship-v5f-w120-30k` (`e98a867e7c55`, A40 83 %, 16 071 MiB)

**Step 8 000, not 2 300.** Trainer pid 30599 up 21 h 05 m; supervisor IS attached.

| step | 1 500 | 2 000 | 3 500 | 4 000 | 4 500 | 5 500 | 6 000 | 7 500 | 8 000 |
|---|---|---|---|---|---|---|---|---|---|
| `val ade@2s` | 1.1566 | **1.7122** | 0.9043 | 0.7577 | 0.7145 | 0.5991 | 0.7165 | 0.9100 | **0.6018** |

⭐ **The "degrading 0.31 → 0.64–1.02, awaiting a kill call" alarm is REFUTED.** It was a
local excursion peaking at step 2 000 and it recovered. Killing on that basis would have
been wrong.

⚠️ **But it is not improving either** — 0.60–0.91 with no trend since step 4 000; 5 500 and
8 000 are the same number 3 000 steps apart. That is a *different* decision than the one
queued, and it is the live one.

**The error is in the SELECTOR, not the fan.** At step 8 000 `oracle_ade@2s` 0.3225 against
`ade@2s` 0.6018 ⇒ `sel_gap` 0.2793 = **46 % of total error**. Per-step,
`frac_sel_2x_worse_than_oracle` swings 0.25 → 1.0 and `rank_acc` reaches **0.0**.
Consistent with `e895e28` ("the real defect is the SELECTOR not the curve").

⭐ **`memory.failcnt = 0`** (rss 5.76 GB) — the container has **never** hit its limit,
confirming the three restarts that cost ~40 min were on a **false OOM**. Disk 430 MB/s.
`supervisor.log` also shows the documented `flock` race live at 21:08:28
(*"another supervisor holds … exiting"*), recovering at 21:11:20.

`ckpt_step5000.pt` exists ⇒ a real 5k gate (`eval_*.py`, four families) is runnable, on a
GPU that is not this one.

## Nothing was touched
Reads only, plus one 64 MB `dd` write test and two helper scripts in `/tmp`. No process was
signalled; no training config was changed.
