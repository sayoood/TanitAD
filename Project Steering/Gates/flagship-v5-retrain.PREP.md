# flagship-v5 retrain — PREPARATION CARD (not yet a launch card)

**Written 2026-07-27 on the PI's instruction: *"do what necessary to prepare v4 retrain after validated
improvements."*** The conditional is the whole design. **This document does not authorise a run.** It
states what is validated, what is not, what must land first, and the bars — all fixed **before** any
checkpoint exists, because a threshold chosen after seeing a number is the forking-paths failure
`GATE_PROTOCOL` §0.3 forbids.

**Restart budget: 0 of 2 used.** This would spend one.

---

## 0. Why the previous run failed — the four mechanical causes, all now fixed or fixable

| # | cause | MEASURED | status |
|---|---|---|---|
| 1 | **No held-out early-stop signal** | **~29.5 GPU-h — half the run — spent training past the best checkpoint**; held-out selection was separated-*worse* while every training term improved | ⛔⛔ **RE-OPENED 2026-07-28 — THE FIX LIVES IN A TRAINER THAT CANNOT READ v5's CORPUS.** VERIFIED IN CODE: `train_flagship4b.py:421` reads `ds_val = None  # this trainer runs no val loop`, and `ds_val` is **never read on EITHER branch** — while `train_flagship_v4.py` carries **16** `ds_val`/`val_loss` references, i.e. **the mid-run gate of §4 lives in v4's trainer.** But `train_flagship_v4` has **NO v2 support at all**, and v5 MUST train from a v2 compressed cache (raw epcache at 120°/256×640 is **697 GB for the train split alone** and fits on no host). ⇒ **as it stood, v5 had to give up EITHER parity-capable storage OR its early-stop — and giving up the early-stop is repeating cause #1 verbatim.** Two live docs already disagreed on the trainer (this card presumed v4; `WIDE_FOV_BUILD.md` §7 gives a `4b` command). ✅ **CLOSED 2026-07-27 — `…/incoming/2026-07-28-v5-trainer/V5_TRAINER.md`. `train_flagship_v4` NOW READS v2 (`--v2-train-cache` / `--v2-val-cache` / `--require-parity` + the four geometry flags), so v5 keeps BOTH parity-capable storage and the mid-run gate. THE DISAGREEMENT IS RESOLVED IN FAVOUR OF THIS CARD: v5 runs on `train_flagship_v4`; `WIDE_FOV_BUILD.md` §7's `train_flagship4b --v2-cache` command is NOT the v5 launch.** The alternative was measured, not assumed: `train_flagship4b`'s selected plan is 4 points at 0.5 s spacing and the gate's pseudo-simulation **raises `NonDensePlanError`** on it, so "a val loop in 4b" means ≥586 code lines of new planner machinery on the critical path vs **0** for the chosen route. Proven: the gate probes the **val** providers by object identity, a deliberately-degraded arm is STOPPED at −0.0647 [−0.0844, −0.0517] (paired episode-cluster bootstrap), and `train_flagship4b.py` is byte-identical so **pod1's run is untouched**. ~~🔴 **Residual blocker is DATA, not code: the 120° VAL split (600 clips, ~24 GB) does not exist yet, and the trainer now refuses to start without it.**~~ ⏹ **CLOSED 2026-08-16 — blocker CLEARED; the split was built and the run it gated is FINISHED.** `flagship-v5f-w120-30k` is ✅ **COMPLETE at 30,000** (2026-08-09T19:23Z) and its final eval ran **"on the full 600-episode w120 val corpus (881 windows, frame parity …)"** — i.e. exactly the 600-clip 120° VAL split declared missing here. Evidence (MEASURED): `Project Steering/MODEL_REGISTRY.md:993-996` §1.8. Note the internal proof too: this card states the trainer **refuses to start without** the split, and the trainer ran to 30,000 — so the split necessarily existed. Swept by the 2026-08-16 stale-blocker sweep. |
| 2 | **The four diagnostics that would have caught it were discarded** | computed **601×** and thrown away by the row-writer | ⚠️ **THE "FIXED" WAS INERT — NOW REALLY FIXED.** `v4_loss_step` never merged them out of `plan_l`, so the row-writer's `if k in log` dropped all three **every step** — the same DOUBLE-FILTER shape as the original bug. Caught by a new written-row test (RED before, GREEN after) |
| 3 | **No grounding instrument at all** | all three v4 logs carry **no `g_*` key**; the real-vs-imagined gap is *unmeasurable* on v4 | 🔄 being added (~20 GPU-min) |
| 4 | **The selector's target is nearly noise** | trained against "closest to the single realised future"; an **in-sample** re-scorer with zero generalisation gap bottoms out at **0.4907** vs v1's 0.4271 | ⛔ **T3 REFUTED — the composite gave the same answer; selector change struck** |

⚠️ **And a fifth, which is not a bug but a wrong axis:** the run was gated on `ade_0_2s`. **Two
independent lines now say that is the wrong target** — the **ADE-optimal pick collides 4.7× more often
than the rule-optimal pick** (3.36 % vs 0.71 %, separated), and published **L2/ADE vs closed-loop
Driving Score is ρ = −0.36, p = 0.43** while **Ego Progress is ρ = 0.83**.

---

## 1. VALIDATED — changes with measured evidence, admissible for the run

| change | evidence | cost |
|---|---|---|
| **Reachability clamp on the offset head** | 72.08 % of candidates removed, **oracle survives 100 %**, paired **Δ exactly 0.0000**, **3.58× cheaper**. Anchors are **bitwise identical to real human windows** (256/256) — **the vocabulary is fine; the unbounded offset head emits 171.5 km/h** (p99 159.6 vs a val GT max of 132.4) | free |
| ⛔ ~~**Vision enters at rank ≈ 16**~~ **STRUCK 2026-07-27 — THE LADDER DOES NOT SURVIVE A PAIRED TEST.** | **Every rung was an UNPAIRED point estimate**: the source artifact bootstraps each arm **against chance** and contains **not one contrast between two rungs**. The raw held-out scores were banked, so the missing test cost seconds — and **the "peak at k=16" is NOT separated from ego alone: +0.00085 [−0.02204, +0.02299], a CI 27× wider than the effect. It cannot distinguish "16 dims of vision help" from "vision contributes ZERO."** ⭐ **And the image-ONLY ladder is FLAT** — `img_pca16/64/256/raw2048` identical to **5 d.p.**; if 16 were the visual state's information content, image-alone would peak at 16 and fall. It does not. *(The decline is separated — `+k16 − +k256 = +0.05142 [+0.01701, +0.08792]` — but **only under concatenation**.)* **Three propagation defects:** the ladder **splices two instruments** (rungs 1–4 a linear ridge, rung 5 a 2.17 M-param attention head on a different baseline); it measures a **linear probe on a binary anticipation target, not the predictor**; and the "replication by two independent streams, all ten arms selecting r=16" — which I quoted repeatedly — **ran three arms that do not read the PCA rank at all.** ⇒ **The CHOICE of rank 16 survives (it is free, and the ladder is flat); the REASON does not, and it may no longer justify anything.** | — |
| **Calibrated readout selection (C8)** | `op` for lead ≤ 0.5–0.8 s, `str`/`tac` beyond — **Pareto-dominant** over the flat swap (0.8597 vs 0.8710 at identical `T_blind`). ⚠️ **Fitting the switch to ADE costs 3.1× of `T_blind` to buy 0.7 % of ADE** — do not fit it to ADE | free |
| **`imagine_candidates`** | the old `imagine_probes` returned **32 tokens invariant to candidate count** — no candidate axis existed, making E-V5-1's failure over-determined | ~0, paid for by the clamp |
| **Emit the grounding instrument** | without `g_*` keys v4 cannot be diagnosed at all | ~5 lines |
| **Emit the four selection diagnostics** | already computed every step | log-only |

## 2. NOT VALIDATED — must not enter the run on current evidence

- **C1 (inverse-dynamics consistency on imagined transitions)** — X1 scoped it to **k ≤ 2 only**: the head is mis-attached at 0.1–0.2 s but **at or above the frozen-latent ceiling from 0.4 s** (at k=20 the head *beats* every probe by 19.4 %). **A rollout-quality guard is mandatory** — PlaNet Fig. 7 is on record as counter-evidence that overshooting *hurt* their RSSM.
- **C3 (teach the encoder to perceive ego-motion)** — X1 calls this *the real item*, and it is not started. A **one-scalar `v0` integrator beats every latent probe by up to 24.8×**, and the second latent slot adds ~nothing.
- **λ/τ prior strength** — the curve **was not measured**; we have **two points on the soft side** and cannot locate the optimum.
- **The action filter** — ⚠️ at the headline α = 0.75 the model supplies only 25 % of the command and **pure hold-last (0 % model) already reaches 11.5 s with a *better* `de@2s`.** The genuinely model-driven point is **α = 0.25 → 8.5 s**. This is an **inference-time** filter and does not belong in a training card at all.
- **R3 / scheduled sampling** — **condemned**: the free filter recovers **101.1 %** of its lever, and no method in its family attacks an oscillation.

## 3. GATING LIST — what must land before this becomes a launch card

1. ~~**T3's verdict**~~ — ⛔ **LANDED 2026-07-27: REFUTE on both pre-registered legs.** `BCE_RULE − CE_CONTROL` on **PDMS-lite** (the pre-registered primary, not ADE) is **+0.0002 [−0.0025, +0.0031], not separated**; at-fault collision **+0.0000 [−0.0035, +0.0035]**. ⭐ **And neither fitted arm beats doing nothing** — `AS_TRAINED` holds the best PDMS-lite (0.6100), the **lowest collision rate** (0.0361) *and* the best ADE. **Bar A refuted a re-scoring lever on ADE, the standing objection was that ADE is the wrong axis, we changed the axis to the composite — and the answer did not change.** ⇒ **THE SELECTOR CHANGE IS NOT VALIDATED AND IS STRUCK FROM §1.** ⚠️ Read with the composite's resolution: random scores 0.3968 and the three trained arms occupy **0.6096–0.6100 — 0.2 % of the distance to random**; DAC is missing and **comfort is a literal constant on v4's fan (100.0000 % violation over 1,708,288 candidates)**, so two PDMS terms carry no information. The refutation is sound; the instrument's selector-vs-selector resolution is weak, and both are stated.
2. ~~**v4's grounding instrument**~~ — ✅ **LANDED 2026-07-27, and the premise was MINE and WRONG.** v4 **already carries** the instrument: `train_flagship_v4.py:95` → `flagship_losses.py:359 grounding_losses(...)` → `:425 **g_log`, with `grounding.parameters()` in the optimizer at `:861`. **All six numbers are computed every step; `:159` forwards exactly one to disk — a LOGGING gap, not an instrument gap.** *(Operating-Standard rule 2 firing inside a report written to honour it.)* Outcome A is **unanimous**: on identical 967 windows / 44 clusters the imagination:perception ratio is **op 20.14× · tac 21.93× · str 14.72×** (v1: 36.98 / 44.12 / 32.62), all inside the pre-registered [⅓, 3] band ⇒ **the registry row widens beyond v1**, and the arms are **indistinguishable on perception but separated on imagination at all three levels — v4's smaller ratio is a WORSE IMAGINATION, not a better encoder.** ⚠️ Pre-registered control V3 **FAILED** on v4 (1.47× vs a 1.5× bar; the shuffle was within-episode). A cross-episode version passes on both (2.06× / 2.48×) but is **not pre-registered and was declared as such, not substituted.**
3. ~~**The clamp's zero-change property on v4's own fan**~~ — ✅ **LANDED, and the guard FIRED.** Zero-change reproduces on v4 (**75.29 % removed, oracle 100 %, pick moved on 0 of 967, paired Δ = 0.000000, 4.05× cheaper**). **But FIX 2's own both-directions test requires a tight band to make ADE WORSE, and on v4 it makes it BETTER, separated (−0.0818 [−0.1498, −0.0265])** ⇒ by its own criterion the clamp is a **TUNED SELECTOR on v4, not inert physics. The test was NOT relaxed; the default stays OFF.** A sibling called this "the cheapest remaining win" — the guard outranks it. ⚠️ **And it corrects this card: 94.36 % of removed candidates have an ALREADY-UNREACHABLE ANCHOR** — the "blameless vocabulary" line is a REF-C-XL statement that **does not transfer to v4**, where the lever is state-conditioned anchors (→ item 6).
4. ~~**The latent-ablation verdict**~~ — ✅ **LANDED, and it is decisive.** Zeroing `v0` degrades the **imagined** decode **×93.7 (v1) / ×39.4 (v4)** while the **perceived** decode is **bit-exactly unchanged** (max |Δ| = 0.0 over 6 ablations × 3 levels × 2 arms). **DISCRIMINATOR: zeroing BOTH CAN action channels while KEEPING `v0` costs only ×1.32 / ×1.07 — a 71× / 37× separation.** ⇒ **the imagined metric decode is very nearly a PURE INTEGRATOR OF THE HANDED SCALAR, near-indifferent to the commands.** *(The earlier cross-arm ×6.87 was an order-of-magnitude under-estimate — it had two encoders in it.)* **This is the item that said the world-model objectives need re-aiming before a GPU-week, and it now says so with a mechanism. §1 and §4 must answer it explicitly.**
5. **The pseudo-simulation arm panel** (running) — v4-30k is currently **indistinguishable from constant velocity** and **worse than never steering on recovery**. A retrain must state what it expects to move on *that* instrument.
6. ⛔ **LANDED 2026-07-27 — REFUTE, and it strikes the item it was written to fund.** `…/incoming/2026-07-27-fan-conditioning/FAN_CONDITIONING.md`, DECISION-GRADE, replicated on 4 fans; independently reproduced by `…/incoming/2026-07-27-fan-clip-local/` the night before. **The premise below is TRUE and the inference from it was FALSE — and the inference was MINE.** The fan is indeed not `v0`-conditioned (candidate speed tracks ego speed at slope **−0.129** vs ground truth **+1.0003**) — but ⭐ **100.0 % of windows ALREADY contain a candidate within 0.5 m/s of the speed the car actually took** (mean gap **0.0525 m/s**), and restricting the oracle to speed-matched candidates only moves it **+0.0000 m [0.0000, 0.0000]**. ⇒ **THE FAN IS WIDE, NOT MIS-PLACED**; marginal over-dispersion and exact conditional coverage are the same fact from two sides, and **neither converging stream measured the conditional** *(new class: MARGINAL MISTAKEN FOR CONDITIONAL; sibling CORRELATION-WITHOUT-SLOPE — the correlation is −0.974 and misleading, the slope carries the physics)*. Both halves fail: every `v0`-conditioning transform is separated-worse (up to +1.62 m), realised pick tops out at **0.7968** vs CONFIRM 0.4907; and where conditioning *does* move a static anchor set's ceiling, the equal-storage control shows it is **anchor COUNT, not conditioning** (`A_cond(16,16)` separated-worse than `A_fixed(256)`, **+0.0555 [+0.0308, +0.0809]**). The guard was shown able to return the opposite verdict on a starved fan.
   ⭐⭐ **AND THE NUMBER THAT REDIRECTS THE PROGRAM: an in-sample anchor set with a PERFECT ceiling (0.0000) still realises only 0.4167 — 100 % of the residual is SELECTION.** ⇒ **DO NOT FUND CoverNet-style anchor sets, longitudinal admissibility filtering, or any further re-scoring of the frozen 2 s fan. That surface is exhausted.**
   ⛔ **UPDATE 2026-07-27 — THE GOAL INPUT WAS TESTED AND THE 88 % IS AN ORACLE ARTIFACT.** The **tautology test is a NULL**: the as-trained selector already predicts the 2 s endpoint to **1.0061 m** and the best out-of-fold learned head to **1.0028 m** — paired **−0.0034 [−0.1169, +0.1232]**, needing **≈ 5 × 10⁴ episodes** to separate. **Predicting the endpoint IS the picking problem.** Break-even needs **σ₀ = 0.955 m** (0.721 m for a realistic biased regressor); **achieved 1.330 m ⇒ recovery −10.4 %**, and the **latent-only head a strategic brain would actually have is separated-WORSE (+0.0464 [+0.0164, +0.0792])** on all three REF-C fans. **Do not fund a goal head on current features.** ⚠️ Scope: this refutes **one** argument for the hierarchy, the cheapest, **not the hierarchy** — goal conditioning does work below 0.955 m; what is refuted is that our features reach it.
   ⭐⭐ **AND IT RELOCATES THE LEVER AGAIN, PROFITABLY: the 88 % is 83.7 % LONGITUDINAL.** Oracle **along-track recovers +83.7 % (separated)**; oracle **cross-track +2.9 % (not separated)** — *even a perfect cross-track buys 2.9 %*. ⇒ **"we need AlpaSim or an external mapped corpus" targets the 2.9 % axis and is the WRONG BLOCKER. The +83.7 % axis is UNBLOCKED and its signal is already in PhysicalAI-AV** (`obstacle.offline`, **97.44 % coverage, 53.6 agents/window**, in the ~~32-of-36 features our ingest does not read~~ [⏹ **CORRECTED 2026-08-16 by the stale-blocker sweep — BOTH HALVES ARE NOW WRONG.** (a) The complement is **30 of 36**, not 32, and the bare subject *"our ingest"* was never defined, which is why this count has gone stale four times — state the LAYER (MEASURED from source): **r0 selection** `stack/scripts/physicalai_r0.py:36-38` = **2** · **episode build** `stack/tanitad/data/physicalai.py:232-235` = **5** · **program-wide incl. the pod-side obstacle join** = **6**; pinned by `stack/tests/test_physicalai_feature_readset.py`. (b) More important: **`obstacle.offline` is now READ, not unread** — wired via `taniteval/tools/build_lead_block.py` + `taniteval/taniteval/lead_source.py`, and distance-keeping is MEASURED (`status "OK"`, n 2846, `_families_unavailable []`) in `TanitAD Research Hub/Benchmarks & Eval/Implementation/incoming/2026-08-05-v1arch-oodval-four-families/raw/v1arch_oodval_q90_4fam_LEAD.json`. ⇒ **This item's core claim — that the +83.7 % longitudinal axis is UNBLOCKED and its signal already sits in PhysicalAI-AV — is CONFIRMED and has since been acted on.**]). → **E-GOAL-1 RETURNED 2026-07-27 — and it corrects the supplier named above, which was MINE and WRONG.** ⛔ **The lead-vehicle hypothesis REFUTES** (41.65 % of windows have no vehicle ahead within 50 m; RMS 0.9305 → 0.8983, +0.0322 [−0.0040, +0.0969] **not separated**; worth **+2.3 points**). ⭐⭐ **But run through the ACTUAL rule, a head with the measured error structure recovers +23.6 % of the fan's headroom FROM EGO KINEMATICS ALONE (−0.0638 [−0.1271, −0.0008], separated), +25.9 % with lead — while the parent's head through the IDENTICAL construction is separated-WORSE at −33.1 %. THE DIFFERENCE IS SPEED HISTORY: `dv_*`/`v_lag_*` are worth +0.1428 m [+0.0686, +0.2516], 4.4× the entire lead block; the parent's head had `v0` and NO HISTORY.** ⇒ **the earlier −10.4 % REFUTE was a missing-feature result, not a ceiling — and the supplier is a nearly-free ego feature, NOT an agent-track ingest.** ✅ **TIER: CONFIRMED — E-GOAL-2 at n = 600 (2026-07-28).** Recovery **+25.4 % [−0.0960, −0.0606]**, and **`by_speed`, which would not separate at n = 40, now separates at +26.2 % [−0.0987, −0.0631]** — both resamplers, all three backgrounds; the −0.0008 bound was **UNPOWERED, NOT REFUTED**. **Plan against +25.4 %, not the +40.8 % friendlier backgrounds return.** ⚠️ **But it is a RESAMPLED RESIDUAL, not a trained head — E-GOAL-3 is the decisive test and no v5 commitment may outrun it.** ⛔ **And E-GOAL-1's +23.6 % is RE-SCOPED: its registered bridge FAILED, deviating +5.6 recovery points and FLIPPING `by_speed` to separated at n = 40 — using it would have MANUFACTURED a CONFIRM. At fixed n and fixed along-track error, recovery spans +13.3 %…+29.2 % PURELY on the cross-track background (a 15.9-point swing), so EVERY recovery number in this program must now carry its background.** ⛔ A second self-caught defect: **the pre-registered predicate STOPS DISCRIMINATING at n = 600** (an information-free arm separates at +9.1 %); the claim survives on a direct contrast instead — history vs noise-in-the-same-columns **−0.0504 [−0.0519, −0.0490]** vs a tight null for dropped-vs-fake, with **64 % of the recovery being speed history and the lead block 7.9× smaller**. ⚠️ **The 0.813 / 0.439 m bars were computed on the WRONG FAMILY** — the heads are near-unbiased (α ≈ 0.996) and heavy-tailed (RMS/MAE 1.867), so the correct σ₀ is **1.1434 m**, **1.41× more forgiving**: *reading the `ISO` bar literally returns REFUTE; running the rule returns +25.9 %*. **Every bar here now carries its family.** ⚠️ And its positive control **fails to separate on RMS while passing on MAE (−61.2 %)** ⇒ **at n = 612 the along-track RMS axis cannot separate even a near-perfect oracle** — a C13 guard-that-cannot-fail found in the stream's own primary.
   *(Superseded, kept because the oracle bound itself stands:)* ⭐ **FUND INSTEAD: the goal input.** The true 2 s goal position — **2 of 8 target scalars** — moves the realised pick **0.4714 → 0.2009 [0.1689, 0.2351]**, paired **−0.2705, separated: 88.0 % of the fan's headroom, clearing BOTH bars — the first thing in the program to do so.** A **pure interaction**, replicated ×3 (along-track alone −31.4 %, cross-track alone −5.8 %, both +88.0 %). ⚠️ **It is an ORACLE: a bound, not a capability**, and ⛔ **PhysicalAI-AV carries no route or goal signal at all**, so the instrument must come from **AlpaSim or an external corpus** — *that is now the binding constraint on the hierarchy.* ⭐ It also **measures the confound `CLAUDE.md` already flagged**: REF-C evaluates with `nav_cmd=None`, so the *"strategic choice is a ~2 % lever"* reading came from a decoder that never had a working route input. **With one wired, it is worth 88 %.**
   *(Superseded framing, kept because its measurements stand:)* ⭐ **the fan-conditioning verdict** (`…/incoming/2026-07-27-fan-conditioning/`). **Two independent streams converged on this in one week:** the selection ladder discharged its advance commitment with *"the limitation is the fan's own action-sequence information, not the scorer"* — **the 256 candidates span 108.7 m of 2 s along-track travel per window against a 25.40 m ground truth, i.e. 181 km/h plans are IN THE FAN, and the world model obediently simulates them rather than vetoing them** — while item 3 independently found 94.36 % of clamp-removed candidates already unreachable. **A retrain that keeps the current fixed 256-anchor vocabulary is spending a GPU-week on a proposal set with a measured, unrepaired defect.** The free longitudinal-admissibility filter is measured first, and **both the ceiling and the realised pick** must be reported — *a better ceiling the selector cannot reach is not a win.*
7. ✅🔴 **DECIDED BY THE PI 2026-07-27 — v5 TRAINS ON THE ADAPTED GEOMETRY. This is no longer a question; it is the run's definition.** *"proceed with the adjustment of the setup for 100 or 120 degree from the av dataset… Also we will consider all new trainings with the larger hfov after a small validation, the v5 should train with the adapted setup."* **Target 100–120°** *(PhysicalAI front-wide spans ±60.3°, so 120° is its physical ceiling and 100° has margin)*. *"from the av dataset"* settles that **PhysicalAI runs wide**; what remains open is **comma2k19's role** (65.2° total field, in the flagship mix) — per-corpus vs dropped, to be **costed, not assumed**.

   ⛔ **THE CONSEQUENCE THAT MUST NOT BE DISCOVERED LATE: every bar in §4 was registered against OLD-GEOMETRY numbers, and a new-geometry v5 CANNOT BE COMPARED TO THEM.** v1's **0.4271** is not a valid comparator for a wide-FOV arm — v1's encoder was *trained* at 51.4°, so feeding it 100–120° frames is out-of-distribution and the number would be meaningless in either direction. The three ways out are not equal:
   - ❌ re-evaluate v1 at the new geometry — **invalid**, for the reason above;
   - ❌ train v5 at both geometries — **two GPU-weeks**, which defeats the point;
   - ✅ **the PI's own "small validation" is the answer, and it is why he asked for it**: matched **short** runs (hours, not a week) old-geometry vs new on identical episodes, which isolates the geometry effect *before* anything expensive, and licenses the change. **v5 then trains once, at the winner.**

   ⇒ **Registry consequence: `MODEL_REGISTRY.md` needs an explicit GEOMETRY column, and every pre-2026-07-27 row becomes a 51.4° / 256 px historical number.** Comparing across the column is inadmissible without restating it — the same discipline the `heldout`-vs-`full_set` split already forced on us. **Owner: the small-validation stream (`2026-07-27-encoder-tokenization/`), which pre-registers the validation, its bar, its n and its MDE** *(⚠️ a validation whose MDE exceeds the effect it must detect is a guard that cannot fail — class C13, and we have shipped several)*.

   **Three streams now execute this:** `2026-07-27-fov-crop-audit/` (which FOV/resolution, and the comma cost) · `2026-07-27-geometry-configurable/` (the plumbing; **no default changes until the validation passes, and then as a separate declared change**) · `2026-07-27-encoder-tokenization/` (encoder size/architecture, tokenization efficiency, and the small validation itself).

   ⭐ **And one internal finding may reshape the target before any of it is built.** Vision enters the predictor best at **rank ≈ 16** (monotone dose-response; 16 PCs = 97.0 % of variance; replicated by two streams, all ten arms selecting r=16). That cuts two ways, with **opposite architectural conclusions**: either **(a)** the predictor genuinely needs ~16 dimensions ⇒ paying 6.25× for tokens then compressing to 16 is waste, and the right shape is **encoder-pays-once → fixed small latent set → dynamics cost FLAT** (Perceiver-style resampling, which fits a fixed-latent world model unusually well); or **(b)** the 51.4° features are information-**poor** and a wider field yields **16 better components**, in which case rank-16 is a property of the crop and must be re-measured after widening. **Re-measuring the dose-response on wider-crop features is cheap and decides the architecture — it is priority 1 of that stream.**

   ### ✅ The plumbing LANDED 2026-07-27 — and it corrected the card in three places

   `…/incoming/2026-07-27-geometry-configurable/`. Fidelity **90/90 BIT-EXACT** vs `HEAD` (both rigs, both centerings, native + half-res) plus **11 deliberate failures**; `stack/` **1200 → 1245** passed, `taniteval/` 559 → 559. **No default moved.**

   1. ⛔ **100° IS IMPOSSIBLE AT 256×256 — IT SILENTLY DELIVERS 67.1°.** The crop would need **1595 px** and clamps at the sensor's **1080 px** height, so it **ZOOMS INSTEAD OF WIDENING**. ⇒ **the "256×256 @ 100°" row previously on this card was never an option**, and widening *requires* more columns — the resolution increase is not a preference, it is a **precondition**. Now a permanent test, and `build_pai_cache.py` **aborts before a multi-hour decode**. *(Second silent clamp producing a plausible wrong number this week — cf. class C14.)*
   2. ✅⭐ **STORAGE: SOLVED, WITH NO COMPROMISE — AND WIDENING NOW *SHRINKS* THE CORPUS.** *(Superseded reading below, kept for the record.)* The v2 format's scalar `image_size` is unblocked (payload carries `image_h`/`image_w`/`frame`/`projection_mode`/`codec`; manifest **v3**; older square payloads fall back **exactly**, not by guess), and a **lossless PNG codec** was added in the same container. MEASURED on real clips, train+val, cross-checked against torchvision:

      | geometry | raw today | **PNG lossless** | JPEG q90 |
      |---|---:|---:|---:|
      | deployed 256² @ 51.4° | 349 GB | **44.8 GB** | 7.7 |
      | ⭐ **120° 256×640 cylindrical** | 873 GB | **112.9 GB** | 17.3 |
      | 120° 384×960 cylindrical | 1965 GB | **221.9 GB** | 32.4 |

      ⭐⭐ **120° LOSSLESS IS 112.9 GB — LESS THAN A THIRD OF THE 349 GB WE OCCUPY TODAY AT 51.4°. TRIPLING THE FIELD OF VIEW SHRINKS THE CORPUS 3×, BIT-EXACTLY.** Even **384×960** (120° *and* ~1.5× the angular resolution) is **221.9 GB — still below today's footprint.** **The provisioning request is WITHDRAWN.**
      ⚠️ **Two of my framings died here.** *(a)* I said the codec trades into the decode axis that is already the bottleneck. **Wrong axis:** the rebuild is **mp4-decode-bound** and the codec is paid by the **training dataloader, per window, forever** — and measured, **PNG DECODES FASTER THAN JPEG** (1.60 vs 2.96 ms/frame; ~16 ms per trainer window on one worker). PNG's extra cost is **build-side only** (~3× mp4 decode, ≈1.2 h at 16 workers, one-off). *(b)* The **"~466 GB quota" is FALSIFIED** — pod3 holds **487 GB**, writing at **415 MB/s** by real `dd`. *(JPEG is ruled out anyway: max abs error **68/255 even at q95**, and lossless already fits. WebP ruled out on build cost, ~60 s/episode.)*
   2b. *(Superseded, 2026-07-27:)* 🔴 **STORAGE, NOT COMPUTE, IS THE BINDING CONSTRAINT — AND IT MAY NOT FIT AT ALL.** Wall-clock grows only **1.00 → 1.23×** (the pipeline is decode-bound), but the deployed cache is **349 GB** and every **640-wide candidate is 873 GB**, against a `/workspace` MooseFS quota of **~466 GB that `df` does not show**. **873 GB does not fit; neither does 384×256 (~524 GB).** Buying volume **stops the pod and reassigns its SSH port**; shrinking the corpus is **REFUSED — parity is sacred**. ⇒ **The mitigation is the JPEG cache, blocked by v2's scalar `image_size` — being unblocked now.** ⚠️ It trades into the axis that is *already* the bottleneck (decode), so the trade must be **measured, not assumed**. **If JPEG is a bad deal, this becomes a provisioning decision for the PI and it blocks scheduling, not engineering.**
   3. ✅ **Selection-vs-cache: MEASURED, and my inherited reading was right.** `discover_r0_clips`/`split_clips` take no geometry argument; episode identity is *position* in the ordered source list. Building the same sources at 256² and 256×640 gave **identical uids, identical skip indices, identical T — only the pixels differed**, and the canonical path **reproduced the real on-disk cache key `14231cd29c74`**. ⚠️ **But one thing my framing hid: `parity.corpus_key_of()` matches on the DIRECTORY NAME, so a re-cropped cache reads NON-PARITY and `train_flagship_v4` (`require=True`) REFUSES TO TRAIN ON IT.** v5 stalls unless a new key is registered. `parity.register_geometry_sibling()` mints one **only if** uid digest + count + skip indices match `e438721ae894` exactly — **the registration IS the proof.**
      ✅ **CLOSED 2026-07-27 for the format v5's corpus is actually in** (`…/incoming/2026-07-28-v5-trainer/`, on top of `…/2026-07-28-v2-parity-enforcement/`). The wide cache is **v2 compressed**, not a raw epcache, so the registration path is `register_v2_geometry_sibling` (clip-id membership) and the trainer guard is `assert_v2_parity_cache` behind `--require-parity`, now wired into **`train_flagship_v4` as well as `train_flagship4b`**. ⭐ **Plus a binding `register_geometry_sibling` never had: the run's declared frame is checked against the raster size ON DISK and against the geometry the manifest recorded** — so omitting `--frame-h/--frame-w/--frame-hfov/--projection` on a wide cache is now a refusal, not a silently mis-shaped encoder. ⛔ **It still does not hash pixels**; see `V5_TRAINER.md` §7 for what each layer proves.
   ⇒ **v5 RUNBOOK, binding: rebuild → register → commit manifest → train.**
   4. ⭐ **`state_dim` stays 2048 at every candidate — the readout is a geometry firewall**, so the predictor, policies and grounding heads are untouched. **This is what makes the wide-FOV work COMPOSABLE with the goal-input and selection streams**: widening does not invalidate them.
   5. Also closed: a **pre-existing silent-collision hole** — build params carried `size` but never `f_ref`, so changing `F_REF` produced different pixels **under the same cache key**.
   ⚠️ **Carried forward:** `CORPUS_META` × 4 + the lake schema **must move WITH the default flip** (most likely place for a stale default to survive; being made mechanical). **REF-C cannot run wide** (private `advect` copy + `image_size % 32`, ~30 lines).

   *(The original framing, whose measurements all still stand:)* 🔴 **the geometry-and-resolution decision.** *"I think we need at least 100 degree, we need to urgently change our setup to consider this… by the way it's high time we reviewed the 256 px resolution we chose, probably a higher resolution will improve the performance of our models."* (PI, 2026-07-27.) **A v5 trained at 51.4° / 256 px is discarded the moment this lands, so it strictly precedes the GPU-week.** Four things it must settle, all now under measurement:
   - ⛔ **comma2k19 CANNOT SUPPLY 100° AT ANY RESOLUTION** — 1164×874 at f = 910 px ⇒ its *entire* horizontal field is `2·atan(582/910) = **65.2°**`; its square crop is 51.3°, which is **exactly where `F_REF = 266` came from** (`calib.py:14`). And the flagship trains on a **comma2k19 + PhysicalAI mix** (`train_flagship4b.py:3,25`), so this is load-bearing. ⇒ **≥100° is simultaneously a decision about comma2k19's role**: per-corpus geometry · letterbox (⚠️ teaches a corpus-identifiable border, and this model demonstrably eats shortcuts — `v0` alone moves the imagined decode **×93.7**) · drop it from the mix. **Needs the PI's call; the cost of losing comma must be measured, not assumed.** *(PhysicalAI itself is fine: front-wide spans ±60.3°, so ±50° is inside the sensor.)*
   - **The FOV ask and the resolution ask are ONE ask.** At 256 px, 100° forces `f_eff` 266 → `128/tan(50°) = **107.4**`, i.e. angular resolution **4.64 → 1.88 px/deg — 2.48× coarser**. More world, resolved 2.5× worse. The PI's instinct to reopen 256 px is the *enabling condition*, not a separate item.
   - ⭐ **A SQUARE FRAME IS PROBABLY THE WRONG SHAPE.** Driving is horizontally wide; a square crop buys 100° of *vertical* field we have no use for. Encoder is our own ViT at `image_size=256, patch_size=16` ⇒ 256 tokens (`dynamics_encoder.py:223-224`). Token counts **exact**, compute **ESTIMATED and under measurement**:

     | option | HFOV | angular res | tokens | encoder compute |
     |---|---:|---:|---:|---:|
     | today 256×256 | 51.4° | 4.64 px/deg | 256 | 1× |
     | 256×256 @ 100° | 100° | **1.88** ⚠️ | 256 | 1× |
     | 640×640 @ 100° | 100° | 4.64 | 1600 | ~10× |
     | ⭐ **640×256 @ 100°** | **100.6°** | **4.64** | **640** | **~3×** |

     ⚠️ A non-square input requires relaxing `encoder.py`'s square assumption (`assert image_size % patch_size == 0`; `grid_hw` is one dim today).
   - **f-theta at a 50° half-angle is far from paraxial** — a *pinhole* rectification to 100° stretches the edges and spends the new pixels on smeared periphery. **Cylindrical/equidistant horizontal projection must be evaluated against `pinhole_rectify`;** it may decide whether 100° is worth it at all.

   ✅⭐ **DECIDED BY THE PI 2026-07-28: v5's GEOMETRY IS 256×640 CYLINDRICAL (the full 120°, 640 tokens, 112.9 GB PNG-lossless).** *"let take 256x640 first step."* The 2,400-clip train build is finishing on pod2 with the 600-clip val build armed behind it (waiter PID 576967). **This is no longer an open decision.**
   ⚠️ **CORRECTION 2026-07-27 — THE CACHE IS 120°; THE ARM IS NOT.** The PI's decision above names the **CACHE**, and it stands. But the rig-clean fix that landed after it (`284c591`) makes both published launch commands carry **`--v2-subframe 176x624`**, and preflight now *refuses* a non-deployed v2 frame without a sub-frame. MEASURED on pod2 through the trainer's own `resolve_v2_frames`: that slice is **176×624, HFOV 117.000° × VFOV 32.131°, 429 tokens**, rows `[40:216]`, cols `[8:632]`. ⇒ **v5 trains at 117°, not 120° — the rig-clean fix costs 3° of field**, and "v5 is a 120° arm" is wrong wherever it describes the run rather than the cache. *(`…/incoming/2026-07-27-stale-import-guard/STALE_IMPORT_GUARD.md` §4 judges every occurrence individually.)*
   ⭐ **AND THE PI ORDERED THE TWO FOLLOW-UPS, BOTH NOW RUNNING:**
   - **The resolution question, made CHEAP** (`…/incoming/2026-07-28-resolution-gain/`): *"we need to investigate in a smart cheap way which gain do we have in using the higher resolution."* 384×960 delivers the SAME 120° — it differs only in **angular resolution** — and storage does not decide it (221.9 GB is still under today's 349 GB); **compute does**, at ~1440 tokens ≈ 8–9× the encoder cost. ⇒ the ladder is run **DOWNWARD on the cache we already have**: degrade angular resolution (low-pass then resample — ⚠️ **without a low-pass this measures ALIASING, not resolution**) and measure the cost. **If the model is insensitive to LOSING resolution it will almost certainly not benefit from GAINING it** — which bounds the upside with **no rebuild and no new corpus**. ⚠️ **This BOUNDS, it does not settle** (insensitivity to loss is strong evidence, not proof, of insensitivity to gain), and the probe must **demonstrate** resolution sensitivity before any null is admissible. ⭐ Free calibration point: today's 51.4° crop is **4.643 px/deg** and 256×640 @ 120° is **4.686** — so a degradation step should reproduce a KNOWN prior result.
   - **The clean rig fix** (`…/incoming/2026-07-28-rig-clean-fix/`): *"do the clean fix for rig."* Cylindrical replaces replicate-padding with a **mask**, but ⛔ **a rig-correlated BLACK REGION IS STILL A RIG-CORRELATED SIGNAL** — masking converts fabrication into *identifiable absence*, which a shortcut-hungry model uses just as well. The fix is **a vertical field BOTH rigs fully observe**. ⚠️ **The cost-deciding question is answered FIRST: can it be a ROW-SLICE of the frames already built, or does it need a rebuild?** ⚠️ And it is **not free in field** — shrinking to the intersection discards real pixels for the **majority** rig (rig B is **72.93 %** of the corpus), so the trade is quantified against decision-relevant content rather than assumed worth paying.

   ✅⭐ **RESOLUTION QUESTION ANSWERED 2026-07-28 — `NO GAIN`. DO NOT BUILD 384×960.** (`…/incoming/2026-07-28-resolution-gain/`.) **Three independent probes returned the pre-registered `NO GAIN`; not one returned `GAIN`** — and every other branch was a **refusal** (`INSTRUMENT-BLIND` / `UNPOWERED`), never a silent null.
   **Probe sensitivity was DEMONSTRATED before any null was admitted:** the 6× rung (0.889 px/deg) separates hard — **−0.01544 AP [−0.02479, −0.00624]** and **−0.04025 R² [−0.05130, −0.03020]** — with registered dynamic ranges **5.71 / 14.93 / 4.28** against a `DR ≥ 2` bar.
   **The primary contrast `D_1p5`, the exact mirror of the 384×960 step, costs NOTHING:** −0.00150 AP [−0.00402, +0.00139] · −0.00080 R² [−0.00353, +0.00186] · −0.00824 R² [−0.02044, +0.00464]. **The knee sits 2.3–3.0× BELOW the v5 frame** (first separated rung 1.778 px/deg; an independent handicap-free ladder puts it at 2.321). ⇒ **mirrored, the most 384×960 could buy is +0.0014 AP (1.7 % of the probe's entire above-chance margin) and +0.0019 R² (0.27 %)** — against **2.587× encoder FLOPs and +109 GB.**
   ⭐ **AND THE UPWARD ARM WAS BUILT ANYWAY, closing the bound's own asymmetry caveat:** a real **384×960 / 1440-token** arm — the registered override that could have flipped this — **won nowhere**: not separated on intersection AP, and **separated-WORSE on speed, yaw and lane-change AP.**
   **Per situation, never pooled:** intersection `NO GAIN` on both probes; **lane change `INSTRUMENT-BLIND` and roundabout `UNPOWERED` — no verdict claimed for either.**
   ⭐⭐ **AND IT SEPARATES TWO EFFECTS WE HAD CONFLATED: the wide 120°/640-token frame is separated-BETTER than today's deployed frame (+0.04246 AP, +0.02774 R²) — and 98.1 % OF THAT SURVIVES AT MATCHED px/deg, while the resolution step alone is a null on all four probes. WHATEVER WIDENING BUYS, IT IS NOT ANGULAR RESOLUTION.** That vindicates the field decision on its own terms.
   ⚠️ **COUNTER-CASE, recorded at equal prominence: on EGO YAW RATE the wide frame is separated-WORSE (−0.03546 R²)** — and §2 of this card calls teaching the encoder to perceive ego-motion *"the real item"*. **Under investigation before v5 trains.**
   ⛔ **Two figures I gave were wrong and both propagated:** the v5 frame is **5.3333 px/deg, not 4.686** (4.686 is the FOV audit's *100° pinhole letterbox*), and *"8–9× the encoder cost"* is **6.97×**, the decision-relevant ratio against the chosen 640-token frame being **2.587×**. *(Fixing the first produced a free calibration rung at today's deployed 4.6426 px/deg — a null on every probe.)* ✅ The 176×624 rig slice does **not** stale this: a centred slice cannot move `f_ref`.
   🔴 **Still unmeasured, and it blocks sizing anything: the encoder's SHARE of a full training step.** Every compute ratio here is encoder-only.

   **Program cost the PI must sign off on:** the feature cache is invalidated ⇒ every arm rebuilds and **every existing cross-arm number is incomparable until re-evaluated under the new geometry**. ✅ **Episode-selection parity SURVIVES** — skip-hash `f09e44db` governs *which* episodes, not how they are cropped — so it is a **re-cache, not a re-selection** *(to be verified in code by the running stream; if that is wrong it is the most important correction available)*.

   ⚠️ **The PI stated a hypothesis — *"this will improve the performance… because they are considering more parts of the world"* — and our job is to TEST it, not confirm it. A wider FOV that measures WORSE on the probe is a fully admissible outcome and must be reported as cleanly as a win.**

   *(Original scope, still wanted:)* ⭐ **NEW, raised by the PI 2026-07-27 — the FOV verdict** (running, `…/incoming/2026-07-27-fov-crop-audit/`). **`calib.py` canonicalises every corpus to `F_REF = 266` at 256 px, so the retained half-angle is `atan(128/266) = 25.697°` — the encoder sees 51.4° of a camera measured at 120.5°, discarding 57 % of the front camera's own horizontal field before the model ever looks.** (MEASURED, `calib.py:16-17,38,89-96`; crop applied at `physicalai.py:358`.) This is the deliberate D-016 cross-corpus fix, **not** a bug — but ⚠️ **it is a GATING item because it is an ENCODER-GEOMETRY decision: a v5 trained at 51.4° is discarded if we later widen the crop.** Sequence it before the GPU-week, not beside it. ⭐ **And it bears on the sensor-attention efficiency claim: of agent-frames outside the model crop, 12.65 % are *cropped-away* — already in the front camera's own pixels, recoverable by a wider crop with NO new sensor — against only 11.69 % genuinely beyond 60.3°.** Roughly half of what a second camera pass would buy is already in pixels we discard. ⚠️ The confound the audit must control, not assume: at a fixed 256 px a wider FOV means **lower angular resolution**, and the **rig-dependent principal point must be honoured per clip** (cy is bimodal ≈534/≈751 on exactly the three 120° f-theta cameras, the same 29.1 % of clips in all three; a geometric-centre crop is **~215 px wrong** for the second rig).

## 4. THE BARS — registered now, before any checkpoint exists

**Primary: the map-free composite, NOT `ade_0_2s`.** ADE is reported as a **diagnostic only**, for the reasons in §0.

**Mid-run held-out gate (this is the fix for cause #1):** probe the **deployable surface** on held-out
episodes at a fixed step cadence; **stop the run when the held-out primary is separated-worse for two
consecutive probes.** This converts a 59-hour loss into ~20 hours.

**Kill secondaries carried forward verbatim** from the pre-registered v4 card — no threshold below is
new: `wm_canary_ade_2s ≤ 0.55` · `miss_at_2m ≤ 0.10` · `seam_norm_ratio_max ≤ 1.0` ·
`encoder_touching_levers ≤ 2`.
⛔ **CORRECTED 2026-07-27 — "NO EMITTER" WAS A FALSE ABSENCE, AND IT WAS MINE.** Both emitters exist
in `gate_emitters.py` and were **RUN**: **`deploy_tick_p99_ms` = 18.7641 (PASS)** and
**`speed_benefit_recovered_frac` = 0.8184 (PASS)**, at **zero GPU**. The v4 `null` came from
**`n_arm_rows: 0`** — the starved log filter — not from a missing emitter. **DO NOT STRIKE THEM: the
gate really is 8 metrics, and last time we simply failed to feed two of them.** *(Absence found at
one probe, written into a card whose whole purpose is to stop that.)*
⚠️ **`nonav_route_beats_majority` remains VOID BY CONSTRUCTION** → INSTRUMENT-FAIL, never MODEL-FAIL,
and **must be printed** in the verdict.

**Horizon:** register **K = 60 (6.0 s) primary, K = 70 hard maximum**. Above K=70 the junction stratum
falls below 200 clusters on the maximum-possible corpus, so **HP-2 is permanently unmeasurable there.**
⚠️ **Every sequential closed-loop number carries EXTRAPOLATION *and* lateral infidelity.** Use
**pseudo-simulation** for the closed-loop reads — it is **0.00 % out-of-envelope**.

**Estimator:** paired episode-cluster bootstrap (`taniteval/ci.py`, B=2000), unit = **episode cluster**.
`overlapping_holdout_se` is refused. **A 40-episode "not separated" is UNPOWERED, not refuted** — use
the 600-episode deployment and state the power.

## 5. What is deliberately NOT changed

- **λ_plan stays 1.0** — it is a **gradient scale, not a loss weight**, and `lambda_plan == 1` is a
  documented **no-op**. It was never a lever.
- **The anchor vocabulary** — bitwise identical to real human windows. **Clamp the refinement instead.**
- **`closedloop.py`'s 2.7 → 2.9** — it would move every future closed-loop number and break
  comparability with published suites. **PI call, documented at the constant.**
- **Parity** — `physicalai-train-e438721ae894`, skip-hash `f09e44db`. Anything that re-selects episodes
  is refused.

## 6. Status

**NOT LAUNCHABLE YET.** Five gating items are in flight (§3). When they land, this document becomes a
launch card by adding the measured bar values — **and the PI approves the spend, not an agent.**
