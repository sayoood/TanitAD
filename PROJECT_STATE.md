# TanitAD — PROJECT STATE

> **This is the single entry point for every new working session (human or agent).**
> Read this file first. It tells you where the project stands, what is decided, and what to do next.
> Update it at the end of every working session (see `Project Steering/CONTINUATION_PROTOCOL.md`).

- **Last update:** 2026-07-20 (Orchestrator weekly report **2026-W33**, covers 2026-07-18 → 2026-07-20.
  **THE CENTRAL QUESTION CLOSED POSITIVE: flagship-v1 @30k FINAL = ADE@2s 0.4522 ± 0.031 m — the FIRST
  arm below EVERY trivial bar** (best-of-3 floor 0.5005, CTRV 0.523, no-vision ego-status ceiling 0.5735,
  CV 0.838), n=881/40 held-out eps, metric-BEV TanitEval. W32's TOP RISK ("the trained-encoder 4-brain
  thesis is not yet proven over constant-velocity") is **PROVEN — open-loop, in-distribution.** Backed by
  a **causal genuine-prediction proof**: on high-CTRV-divergence windows the model beats the CTRV *oracle*
  by +0.796 m on 94 %, and mean-replacing the scene INVERTS it → **vision-effect +1.325 m, CI [1.04,1.64]**.
  **BUT it does not generalize yet:** OOD it LOSES to the floor (comma2k19 0.849 vs 0.372; cosmos 0.583 vs
  0.358; anticipation advantage collapses 0.80→0.15 m; path feasibility 97.8 %→62.8 %) — vision-ablation
  still hurts OOD (0.27 m, CI-sep) so it reads the scene but is **partly distribution-fit**. **Arm order
  now:** flagship-v1 **0.452** > REF-C-XL@16k **0.565** (beats CV in all 3 speed terciles) > REF-B@10k
  **0.826** > REF-A dyn-in@30k **2.92** (loses to CV; 94 % longitudinal → **H4 closed NEGATIVE twice**:
  frozen DINO fails, and yaw-rate-as-input does not rescue it). **flagship-v2 @6k = 6.18 m** diagnosed
  mechanism-A but NOT converging (exponent −0.50 vs v1 −0.84) → **restarted as `flagship4b-v3enc-30k`**.
  Only open in-dist weakness: **high-speed longitudinal** (skill-vs-floor 1.785; loses to CTRV −0.617 m on
  top-10 % speed). **🔴 TOP RISK IS NOW OPERATIONAL, NOT SCIENTIFIC:** at the 2026-07-20 05:01 UTC probe
  **2 of 4 GPUs were idle behind DEAD trainers** — pod1 REF-B v2 stopped at 22,600/30,000 (~9 h, no
  process); pod2 flagship-v3enc **crashed in a checkpoint write** at 1,950/30,000 (overlay `/` **98 % full**,
  stale 3.36 GB `ckpt.tmp`); only pod3 REF-C-XL live (25k/30k). **The 6-hourly monitor ran at 04:55 and
  caught neither — 4th recurrence of the stale-target bug.** Health: **6/6 agents ran**, zero
  `QUALITY: partial`, but 3 fired 1–2 days late (host-availability catch-up, breaking the Mon→Sat read
  order) and **D-026 strand debt is at its worst: 9 unmerged branches + ~45 uncommitted files
  (+1613/−101)**. Intake: **19 of 23 packages still carry no verdict** (3rd report unfixed).
  `LEADERBOARD.md` is stale + in the wrong units (newest row = 27k camera-frame, 2026-07-12). Closed-loop
  walled twice — **AlpaSim NO-GO** (eval pod is an unprivileged container, NuRec renderer is image-only) +
  CARLA pixels host-blocked; **one fix unblocks both plus the VLM-labelling GPU: a docker/graphics-capable
  GPU host.** Suite measured today **529 passed / 2 skipped / 52 s** (the 07-17 RED-suite blocker is fixed).
  Burn **$0 settled / $120 planned**. Schedule 9/9 enabled.)
- **Superseded context (2026-07-17, weekly report 2026-W32):** the SPEED/SCALE RESET
  (2026-07-14) supersedes the W31 pre-reset narrative below**. Root cause: none of the 3 arms fed the
  **current ego-speed `v0`** as input → each tried to infer absolute speed from vision, which a frozen
  encoder can't (speed-probe R² 0.61). Fix = `v0` as a 3rd action channel → REF-A **3.73 → 2.14 m** held-out,
  speed-decodability **0.61 → 0.965 R²**; all 3 arms **restarted from scratch** with speed+jerk+aux-accel.
  **3-arm bake-off now:** flagship (trained ViT, pod2) **~6.35k/30k** rising, op fwd-ADE 0.114 m in-train;
  **REF-A** (frozen DINO, pod3) **30k DONE, plateaued 2.14 m** (frozen ceiling, final); **REF-B** (from-scratch
  ViT BC, pod1) **8,750/30k healthy**, ETA ~Jul-23. Encoder isolation is now clean (identical 4-brain + speed
  data; only DINO-frozen vs trained-ViT differs). **First positive signal:** flagship @5k **beats REF-A @30k at
  1s on every stratum** (0.63/0.98/1.01 vs 1.85/1.41/1.13). **Honest kinematic floor shipped** (`e8fca8e`):
  best-of-3 CV/CTRV on 26,132 anchors = **0.056 m@1s** → flagship 6.44 m = **~115× floor**; neither arm beats
  the trivial baseline open-loop yet → **D1 FAIL reinforced with an honest denominator**. **TOP RISK: the
  4-brain trained-encoder thesis is NOT yet proven over constant-velocity** — verdict gated on **flagship @30k**
  (~Jul-19–23); fallback capability-proof surface is **closed-loop D4–D6** (open-loop ADE ⊥ DS, CV near-
  unbeatable on highway). Health: **5/5 weekday agents ran+committed**; recurring D-026 debt (unmerged branches,
  stale main-tip STATE.md, monitor blind to pod2/pod3). Burn on plan $0/$120. H4 evidence RESET — the confounded
  W31 14.2-vs-7.5 head-to-head is now void; H4 stays OPEN.)
- **Pod2 plan of record (Sayed-approved):** Phase A DONE (repo+stack+178-test parity; CARLA 0.9.16
  server+client version-matched; step-8k ckpt relaying). Phase B: **CARLA RUNS in `-nullrhi`**
  (Town10HD, sync stepping ~1400 ticks/s) — camera RENDERING is host-blocked (compute-only vulkan:
  driver GIPA returns NULL; all userspace libs verified present/matching — not fixable in-container).
  Milestone 1 (work-zone scene + scripted-policy LIVE telemetry → LAL/OKRI/LOPS) needs NO pixels →
  proceeding under nullrhi. Checkpoint-driven ego (needs camera) → pod recreation with a
  graphics-capable template when required (Sayed decision, not urgent). Phase C: bake-off arms need
  the train-cache copy — pod-to-pod key approval still pending (else subset relay overnight).
- **Current phase:** Phase 0 — foundation & first edge proofs
- **Constitution:** `Project Steering/Mission Plan.md` (owned by Sayed, never edited by agents)
- **Final evaluation date (P7):** 2026-10-05

---

## 1. Where we are (one paragraph)

Project kicked off 2026-07-05. Prior-experiment repos (ALPS-4B, 4B-HRM, ACRE, RSRA-4B) analyzed; initial
deep research across H0–H15 done and documented; master plan and Phase 0 plan written; the `stack/`
Python package scaffolded with the 4B world-model skeleton, instrument checks (I1–I4), and a smoke test
that runs on the RTX 4060; research hub concept written and weekly agents defined; repo pushed to
https://github.com/sayoood/TanitAD.

## 2. Key documents (read in this order)

| Priority | Document | Purpose |
|---|---|---|
| 1 | `Project Steering/Mission Plan.md` | Constitution: vision, goals, hypotheses H0–H15, principles P1–P8 |
| 2 | `PROJECT_STATE.md` (this file) | Current truth |
| 3 | `DECISIONS.md` | Decision log (ADR style) — what is decided and why |
| 4 | `Project Steering/Master Plan.md` | Refined execution plan, phases 0/1/2, gates |
| 5 | `Project Steering/Phase 0 Plan.md` | Detailed current-phase plan, week by week |
| 6 | `TanitAD Research Hub/INITIAL_RESEARCH_SYNTHESIS.md` | Research baseline across all hypotheses |
| 7 | `Project Steering/CONTINUATION_PROTOCOL.md` | How to resume/continue work across sessions |
| 8 | `stack/README.md` | The implementation: how to run, test, train |

## 3. Current focus (Phase 0)

The active week-by-week schedule lives in `Project Steering/Phase 0 Plan.md` §6.
Summary of the immediate next actions:

- [x] W1: smoke training runs on RTX 4060 (`stack/experiments/p0-s000-kickoff-smoke`: loss 3.34→1.89,
      I2 pass 3.6e-7, I4=6.17 — untrained baseline, correctly blocking predictive claims)
- [x] W1: MetaDrive wrapper (WP2) — contract-identical adapter merged + CI-green (`stack/tanitad/data/metadrive_env.py`);
      live rollout needs a supervised source-install of MetaDrive (PyPI pkg no-go on py3.13; see Tools&DevEnv STATE)
- [x] W1: I1–I4 instrument checks implemented + in test suite (10/10 tests pass)
- [x] W1: D-008 executed — TanitAD-4B-M at **261.1 M params** instantiated (budget enforced by test);
      H15 ImaginationField (advection + refine + epistemic σ) wired into training; D9 gate defined;
      exact Phase-0 data spec in Phase 0 Plan §2.2; 24 tests green + 1 sim-skip
- [x] W1: D-009 executed — real camera data first: comma2k19 loader (`stack/tanitad/data/comma2k19.py`,
      HF mirror, route-level splits, real CAN actions), `base250cam` config, Windows-safe extractor;
      Chunk_1 (8.7 GB) downloading locally; 36 tests green
- [x] W1: comma2k19 loader (D-009) **real-data-validated** (DataEng agent): `av` decodes real HEVC →
      [200,3,256,256]@~105 fps, A8 real≈0.053; contract reconciled into shared `_contract` (channels=1|6|None);
      comma2k19 exported + `test_comma2k19_contract.py`; data card + H7 note; 40 tests green
- [x] W1: p0-sB00 real-camera pipe-proof DONE on 4060 — pipeline validated (I2 9.5e-7 pinned; bf16 +
      horizon-subset encoding fixed the 22 GB OOM; Chunk_1 = 188 segments local). Learning at batch 2
      invalid by design: **F-2 SigReg starvation** (n=32/step → erank 23/2048; I4=98.9 caught it);
      live collapse-health rows (erank/dim_std/step_ratio) now in every training log.
- [ ] W1: **Sayed**: start the A40 pod per `stack/RUNPOD_RUNBOOK.md` — p0-sB01 at batch 64 (SigReg
      n=1024) is the first run whose learning signal counts (planned $25)
- [x] W2: **Data strategy v1.0** (`DataEng/DATA_STRATEGY.md`, D-012): PhysicalAI-AV = first RICH
      corpus (access verified, clip_index enables urban filtering; staged R0 500 clips → R1 2 000 →
      R2 multi-view; usage tagged, license resolved at Phase-0 exit); comma2k19 = license-clean
      public anchor; composition ~60/25/15; research synthesis updated to v1.1 (theory grounding)
- [ ] W2: PhysicalAI-AV Stage R0 ingestion (DataEng agent top duty Tuesday; MVP assists)
- [x] W2: D-010 **sim arm unblocked** (Tools&DevEnv): MetaDrive front-camera RGB path renders 6ch/256
      2-frame stacks (comma2k19-identical) + perturbation policy + occluder/blocked-route scenarios —
      intake pkg `Tools&DevEnv/Implementation/incoming/2026-07-13-metadrive-frontcam-perturbation/`
      (17 tests, 0 new deps), pending orchestrator triage. The merged 1ch BEV adapter is (correctly)
      rejected by `MixedWindowDataset` — proven by test.
- [x] W2: **D-014 — MetaDrive retired** (Sayed: too old / Python pinning). Sim arm split: training
      mix ← NVIDIA synthetic corpora (WorldModel-Synthetic-Scenarios: emergency/lanechange/nudging/
      pedestrian/weather + Cosmos-Drive-Dreams CC-BY-4.0, both ungated); closed loop (D5/D6, G0.5)
      ← CARLA-in-Docker on RunPod, W31–32 (Tools&DevEnv top backlog). Training never blocked on sim.
- [x] W2: **Cosmos-Drive-Dreams loader** (DataEng, D-014 sim arm): CC-BY-4.0 → first *publicly-claimable*
      RICH AV corpus (the license review excluded real PhysicalAI-AV from public claims). Intake pkg
      `Data Engineering/Implementation/incoming/2026-07-14-cosmos-drive-dreams-loader/` — derives
      steer/accel from 4×4 `vehicle_pose` geometry, D-015 9-ch, `CORPUS_META`≡comma2k19 (D-017 I7 →
      admissible in D-010 mix), CLIP-split (I3); 9 tests, stack 73✓/1s. `DATASET_LANDSCAPE.md` created
      (D-012 duty). Pending orchestrator triage.
- [x] W2 (autonomous loop iter 1–2): **gate runner + spectral-sizing packages INTEGRATED** with the
      D-017 rework (P4 path, imag-rel→diagnostic incl. A13 test, I7 rows); 97 tests green.
      **Spectral DIAGNOSTIC on the live ckpt (step 3000, pod GPU): fit R²=0.997, operator effective
      rank ≈35, energy knee ≈22, k*≈11 → task-relevant dynamics rank is LOW (tens) — NOT a collapse
      signature.** Reframes the step-10k erank question: low live-erank may reflect intrinsic
      highway-dynamics rank; judge the checkpoint by ACTION DISCRIMINATION (D2/P1-P4), not raw rank.
      Decision-grade sizing still awaits the trained ckpt. Local R0 fetch complete: 500/500 urban clips.
- [ ] W2: Stage-0 bake-off (residual+change-weighted vs MSE; grid readout vs pooling; probe_imag vs probe_real)
- [ ] W2–3: D1–D3 gates measured (see Phase 0 Plan §4) — **step-15k preview due ~2026-07-09 midday**
      (ETA at ~300 steps/h). Turnkey commands: pod
      `python scripts/evaluate_checkpoint.py --ckpt /workspace/experiments/p0-sB01-realmix/ckpt.pt
      --cache-dirs /workspace/data/comma2k19/_epcache /workspace/data/physicalai/_epcache
      --out /workspace/experiments/p0-sB01-realmix`; then local D8 paired re-run
      `python stack/scripts/d8_preview.py --ckpt <pulled ckpt> --comma-cache
      C:/Users/Admin/tanitad-data/eval/comma2k19-val-61c46fca8f7f --cosmos-root
      C:/Users/Admin/tanitad-data/eval/extracted --pairs-root C:/Users/Admin/tanitad-data/eval/pairs
      --out <json>`. Watch: D2 must not regress (P1 0.872 / P4 0.940 at 5k); D1 ADE trend; paired
      D8 fraction should rise from 16/23.

## 4. Open questions / blocked items

- ~~NVIDIA PhysicalAI-AV license review~~ **RESOLVED 2026-07-07 (DataEng):** real AV sets = NVIDIA
  internal-dev-only/confidential/12-mo → **excluded from public claims**; comma2k19 (MIT) is the public
  corpus; Cosmos-Drive-Dreams (CC-BY-4.0) is the publicly-safe AV asset. Internal use needs Sayed+legal
  sign-off. See `TanitAD Research Hub/Data Engineering/Research/2026-07-07-physicalai-av-license-review.md`.
- RunPod budget approval per training stage (owner: Sayed; default budget in Master Plan §7)
- ~~**DECISION NEEDED (2026-07-09 06:15): RESTART TRAINER on pod1.**~~ **RESOLVED 2026-07-10** — the
  restart was **superseded by a root-cause fix** (`7b5faa6`): the mmap dataloader's working set was
  unbounded (spanned the 88 GB cache inside the 57 GB cgroup); binding it to `n_episodes` took `data_s`
  from **73 % → nominal**. Trainer recovered to ~750 steps/h and is healthy at **~21k/30k** with no
  interventions. No Sayed action needed.
- **DECISION NEEDED (2026-07-10): pre-approve post-30k / Stage-1 compute spend.** The 30k run finishes
  in a few hours; the next stage (bake-off arms / REF-A/REF-B) needs a `RESOURCE_LEDGER.md` row.
  | default: hold at 30k finish until Sayed approves the next ledger entry.
- **STEERING (2026-07-10): sequence the three banked directives.** Dashcam (Y0–Y2), REF-A/REF-B reference
  builds, multi-cam encoder optimization are all captured (`REFERENCE_ARCHITECTURES.md`, `811df73`).
  Orchestrator recommendation: **REF-A/REF-B first after 30k** (unblocks D4 opponent-baseline arbitration)
  unless the dashcam pilot is time-sensitive. | default: REF-A/REF-B first.
- ~~K-step adoption~~ **DECIDED (D-027, 2026-07-10): Sayed adopted rollout_k=4 for all post-30k
  training** (evidence: matched-compute A/B, imag_rel -87%/-92%). Config default flips after the
  record run terminates; explicit --rollout-k 4 until then.
- ~~**WATCH: D1 ADE regressed 5.18→11.52 m at step-21k**~~ **RESOLVED 2026-07-11 (`0284a5c`):** the 21k
  number was **route-split sensitivity of the D1 protocol, not information loss** — a probe-capacity
  discriminator shows the later checkpoint (23.5k) decodes BETTER at every capacity (ridge α10 8.54→7.02,
  α100 12.60→9.47, MLP 11.19→8.40). D1 becomes a **route-resampled mean±CI** in the gate runner before the
  30k eval. D1 improvement is now positively monotone; D1 still FAILs the sub-metre floor (30k + REF-A arbitrate).
- ~~D-028 proposed~~ **CONFIRMED by Sayed 2026-07-11:** recency-first literature scan + benchmark/edge seam
  ownership adopted (operationally live in `_common-protocol.md`, `2d58b69`; DECISIONS.md D-028 → accepted).
- ~~DECISION NEEDED: pre-approve REF-A/post-30k spend~~ **PRE-APPROVED by Sayed 2026-07-11:** REF-A trainer
  (A40, rollout_k=4) rides the approved pod (+$0 incremental); post-30k/REF-B continuation carries a $40
  envelope (`RESOURCE_LEDGER.md`, rows `refa-30k`/`refb-post30k`), gated on the 30k verdict — no further ask.
- **D-022 numbering gap RESOLVED 2026-07-11:** the missing ID is backfilled in DECISIONS.md (WorldModel-Synthetic
  public-claim firewall — a 07-09 DE-agent proposal never logged). Recorded at its **conservative default (HOLD,
  firewall kept to comma+Cosmos)**; the substantive public-claim reclassification is a legal call that still
  needs an **explicit** accept (not auto-adopted by closing the gap). | default: HOLD until Sayed/legal confirm.
- **DECISION NEEDED (D-021, proposed 2026-07-08):** latent dim k as a *measured* design variable
  (spectral knee) — keep 2048 for Phase 0, but Phase-1 resizing only with a trained-checkpoint
  spectral result + gate impact. | default: keep 2048, keep measuring (nothing blocked).
- **Sayed task (~10 min, supervised):** MetaDrive source install on the dev machine (PyPI no-go on
  py3.13) — unblocks D-010 sim-mix live rollout, D5/D6 blocked-route and D9 occluder scenarios.
  Command sequence prepared by Tools&DevEnv (their BACKLOG P1.3).
- **DECISION NEEDED / ESCALATION (Data-Eng 2026-07-18): request ZOD dataset access** — the ONE blocker on
  the FLEET_REVIEW P0#1 / OWN_DATASET_PLAN §7#1 unlock (the ZOD loader is DONE + geometry falsifier PASS,
  `2026-07-18-zod-loader/`). ZOD (CC-BY-SA-4.0) needs a signed access agreement (`opendataset@zenseact.com`;
  HF `Zenseact/ZOD` is a code-loader, no plain download) — a license/external-action call only Sayed makes.
  Proposal: **accept CC-BY-SA for a SEPARATE public ZOD shard** (= OWN_DATASET_PLAN §9 open-Q #1; the permissive
  `tanitad-own-core` stays proprietary-capable). On grant, the runnable job card (pod3-idle/Colab, M-1.3) does the
  5-drive real-bytes verify + precompute. | **default if no answer:** loader stays intake-ready + access-blocked;
  the rest of the corpus-diversity backlog (lake-at-scale P0#2, curve-rebalance P0#3) proceeds meanwhile.

## 5. Session log (newest first, keep last ~15 entries)

| Date | Session | What happened | Artifacts |
|---|---|---|---|
| 2026-07-20 (Mon, 05:00 UTC) | Orchestrator & Project Steering agent (run #6) | **Weekly synthesis 2026-W33** (3-day period 07-18 → 07-20; the Fri slot misfired to Monday — flagged, not hidden). **HEADLINE: the program's central question closed POSITIVE — flagship-v1 @30k FINAL 0.4522 ± 0.031 m ADE@2s is the first arm below EVERY trivial bar** (floor 0.5005 / CTRV 0.523 / no-vision ego-status 0.5735 / CV 0.838), plus a **causal genuine-prediction proof** (vision-effect **+1.325 m, CI [1.04,1.64]**; beats the CTRV *oracle* +0.796 m on 94 % of high-divergence windows). **Honest counterweight: it does NOT generalize** — OOD it loses to the floor (comma 0.849 vs 0.372, cosmos 0.583 vs 0.358) → partly distribution-fit. Arms: flagship 0.452 > REF-C-XL@16k 0.565 > REF-B@10k 0.826 > REF-A dyn-in@30k 2.92 (**H4 closed NEGATIVE twice** — 94 % longitudinal). flagship-v2 restart executed as v3enc. **🔴 Live read-only fleet probe found 2 of 4 GPUs idle behind DEAD trainers** (pod1 REF-B @22.6k no process ~9 h; pod2 v3enc crashed *in the ckpt write* @1,950, overlay `/` 98 % full + stale 3.36 GB `ckpt.tmp`) **and the 6-hourly monitor ran at 04:55 catching neither — 4th recurrence.** Deliberately did NOT restart/delete (write actions on shared training infra outside this task's remit; a naive pod2 restart re-crashes on the full disk) — escalated with turnkey recovery instead. Health **6/6 agents ran, zero partial**, but 3 fired 1–2 days late (host-availability catch-up broke Mon→Sat read order) and **D-026 strand debt is at its worst: 9 unmerged branches + ~45 uncommitted files (+1613/−101)**. Intake **19/23 verdictless (3rd report)**; `LEADERBOARD.md` stale + wrong units. AlpaSim **NO-GO** (unprivileged container, image-only NuRec renderer) → closed-loop walled twice, **one shared fix: a docker/graphics-capable GPU host**. Measured myself: full suite **529 passed / 2 skipped / 52.16 s** (07-17 RED-suite blocker RESOLVED). Schedule **9/9 enabled**. Burn $0/$120. **Proposals: P1 restore the fleet + retarget the monitor onto live-arm auto-discovery with a "no trainer PID while step<total" alarm (HIGH, ~1 h, $0) · P2 make the sub-floor result GENERAL — hold the v3enc recipe but pre-register the OOD panel as the acceptance gate (target: beat comma floor on ≥35 % of windows, up from 17.5 %) + attack high-speed longitudinal + re-gate D2/D3 post-reset (HIGH, 1 run, $0 incremental) · P3 pay the D-026 debt with the just-built `session_guard` — merge 9 branches, commit 45 files, wire the gate in, clear 19 intake verdicts, refresh LEADERBOARD (MED, 2–3 h).** 4 decisions visible in commits but unlogged, filed as `proposed` for Sayed (REF-C DiffusionDrive redesign; v2 restart; milestone ckpt archiving; v3 hierarchical-planning direction). | `Project Steering/Progress Reports/2026-W33.md`, `TanitAD Research Hub/Project Steering/Research/STATE.md`, `PROJECT_STATE.md` |
| 2026-07-20 (Mon) | Tools & DevEnv agent | **The CI gate was blind to the GPU, and the shared tree is holding 135 uncommitted tests.** 2 measured experiments, local RTX 4060 + CPU, ~2.3 h, $0. **(1) Measured before building:** `grep -rl cuda stack/tests` is **empty** — the entire 396/531-test suite is **CPU-only** while every trainer, eval and deploy tick runs on a GPU, so device/dtype/CUDA-NaN regressions were structurally invisible to CI. Shipped **`tools/gpu_tripwire.py`**: 4 device-parity probes on the real model (4060, torch 2.11+cu128, **1.7 s**) — encode CPU-vs-CUDA **9.54e-07**, imagine **7.15e-07**, **I2 run on-device** 1.66e-07, 0 non-finite grads; batch-1 encode **0.85–1.43 ms** (I8 proxy). A `tol=0` falsifier proves the probes can fail. **(2) `tools/ci_gate.py` v2** (backlog P0#2): **SUITE_MANIFEST** — 16 load-bearing modules pinned to a collected-count floor, because a named-node tripwire only guards nodes somebody thought to name while whole modules vanish silently — plus `--min-total 390`, `--gpu-smoke`, `--json`. Both trees **GATE PASS**: **396 / 39.0 s** (off-Drive worktree), **531 / 60.2 s** (Drive). **Sharding NOT needed — 5× under the 5-min ceiling.** **(3) The 396-vs-531 discrepancy had no branch explanation → root cause: the shared Drive tree carries 40 uncommitted `stack/` paths, 22 UNTRACKED — 12 test modules (~135 tests), 9 `tanitad/lake/*` + `eval/ckpt_compat.py` + `train/decorr.py`, 18 modified core files (`config.py`, `fourbrain.py`, `predictor.py`, `refa.py`, `flagship_losses.py`) — in no commit, on no branch, anywhere.** Strictly worse than D-026's unmerged branches (those are at least pushed); `session_guard` v1 called that tree clean because it only checked hub prefixes → **source check added** (untracked listed separately; `--untracked-files=all`, since git collapses an untracked dir to one row). **D-026 debt is GROWING: 30 uncommitted hub deliverables (was 5) incl. the whole Benchmarks alpasim-closedloop package with its results + Architecture's V3 design docs, 40 source paths (new class), 11 branches (was 9), 8 stale INTAKEs (was 5).** Two bugs caught pre-ship by their own falsifiers. `tools/tests/` **57 falsifiers 15.5 s**. **P1.0 AlpaSim retired** — answered NO-GO 07-19 (eval pod has no nested container runtime; NuRec is image-only) → it is now an **infra ask: a docker-capable GPU host**. Own stranded intake `2026-07-17-ci-gate` **withdrawn as SUPERSEDED** (tooling belongs in `tools/`, not intake). Lit: Rerun **0.34.1 Viewer MCP** (agents can verify their own renders — GO), **JetPack 7.2 corrects our 7.1 Orin target**, L0–L4 world-model-as-oracle ladder (2607.07196), DynaDreamer (2607.13410), Orbis 2 (2607.15898), TerraZero (2607.13028, no code → WATCH). Branch `agent/tools-devenv-20260720`. | `tools/{ci_gate.py,gpu_tripwire.py,ci.ps1,session_guard.py,README.md}`, `tools/tests/` (57✓), `Tools&DevEnv/Research/2026-07-20-ci-gate-v2-suite-manifest-gpu-tripwire-and-the-uncommitted-stack.md`, KB (+12), BACKLOG, STATE |
| 2026-07-18 (Sat, 04:43 UTC) | Pod training monitor | **REAL REF-B CRASH — the exact failure prior entries warned the blind monitor couldn't catch (ESCALATED, no execution).** Pod IDLE (GPU 0 % / 2 MiB). The live REF-B arm **`refb-refbpatch-30k` CRASHED at ~step 500/30000** (ckpt step-500 @ 19:21 → traceback log-write @ 22:11, dead ~6–9 h): `refb_train.py` L434 `next(data_iter)` → **`RuntimeError: DataLoader worker exited unexpectedly — bus error — insufficient shared memory (shm)`**. **shm/RAM fine NOW** (`/dev/shm` 0 % of 29 G; **294 G free of 503 G**) → transient dataloader-worker spike during training (the known physicalai-mmap working-set cost), **not** a persistent full /dev/shm → a resume with fewer workers / larger shm should hold. Only ~500/30000 steps banked (ckpt.pt @ 19:21). **Scripted target `p0-sB01-realmix` still 6-day obsolete** (log frozen Jul-12 04:38 step 28600, no `metrics.json`) — **did NOT run the playbook relaunch** (resurrects a dead pre-reset run). **Did NOT unilaterally relaunch refb-refbpatch** — not the monitor's arm; resume flag + worker count = the owning agent's tactical call (D-018); the queued axis6 wait-loops (pids 262917/262931) are now orphaned (waiting forever on files a dead trainer won't produce). **ESCALATED to Sayed** (push): live REF-B down + pod burning idle + monitor retarget now proven necessary (4th day). Recommend: (a) resume `refb-refbpatch-30k` from step-500 ckpt with `--num-workers` reduced (or raise container `--shm-size`); (b) retarget this monitor SKILL at `refb_train`/`refb-refbpatch-30k`. | crashed `/workspace/experiments/refb-refbpatch-30k/{ckpt.pt,.log}` (pod); obsolete `p0-sB01-realmix.log` |
| 2026-07-18 (Sat) | Production & Optimization agent (run #5) | **Advanced run #4's explicit next-step (A3) + closed 2 open items; 4060 exclusive, $0, ~20 min.** **(1) Combined deploy-tick MEASURED (A3, G-H/G-P2):** ran the two orthogonal levers TOGETHER in one decision tick (fp16 encoder + CUDA-graph predictor/select) on 64 real comma windows, 200 reps — **17.75 → 11.16 ms, 56.3 → 89.6 Hz, 1.59×**, selection agreement **96.9 %** (2 flips/64), wp-shift **0.7 cm mean / 1.9 cm max**. **The measured tick matches run #4's additive projection (11.21 ms) to 0.4 % → the levers COMPOSE with no interference** (falsifier <1.3× cleared: 1.59×). Both required (fp16 alone 66.3 Hz; +graph 89.6 Hz); the graph is **zero-accuracy-cost** (the 2 flips are the fp16 encoder's, identical without the graph) and **clock-robust** (graphed select 1.92× here vs 1.33× run #4 — replay is fixed ~4.4 ms while eager scales with the non-pinnable clock). **(2) P1.4c CLOSED — clean one-process VRAM:** fp32 **1.078 GB** (reproduces run #3's 1.10 GB), fp16 **0.560 GB** (1.93× smaller) → the run #3/#4 fp16 1.65 GB was fp32-reference co-residency pollution, retired. **(3) Compliance review #3 (`stack/scripts/` + training loop, G-P1) — LIVE BUG:** the milestone archive copies **non-atomically** in all 3 pod trainers (`train_flagship4b.py:337`, `refb_train.py:358`, `refa_train_plus.py:540`) — `shutil.copy2` guarded by `not arch.exists()` → a kill mid-copy leaves a truncated-but-existing `ckpt_step{m}.pt` the guard adopts forever → the gate protocol loads a corrupt milestone. Resume-write path confirmed atomic in every trainer. Fix → intake `2026-07-18-atomic-milestone-archive` (`.partial`→`os.replace`, 4 tests, failing-then-passing). No hypothesis status change (efficiency → Benchmarks CNCE handoff). Branch `agent/prod-opt-20260718` (base 9c36c95 behind fcbab02 → orchestrator merge). | `Production & Optimization/Research/2026-07-18-combined-tick-and-atomic-archive.md`, `Implementation/combined_tick/{combined_tick_harness.py,combined_tick_20260718.json,vram_{fp32,fp16}_20260718.json}`, `Implementation/incoming/2026-07-18-atomic-milestone-archive/` (+4✓ tests), KB/BACKLOG/STATE/PRODUCTION_READINESS |
| 2026-07-18 (Sat) | Production & Optimization agent (run #4) | **2 measured experiments on real compute (4060 exclusive, $0, ~25 min).** **(1) Predictor batch-1 latency ATTACK (P0 #3, G-H/G-P2):** run #3 found the predictor half is launch-bound (fp16 doesn't move it) → attacked it with manual `torch.cuda.CUDAGraph` capture on the operative pass (fp32, tf32 off, step-6500, 64 real comma windows, 200 reps): predict_1pass **6.08→2.36 ms (2.57×)**, select_K9 **5.94→4.45 ms (1.33×)**; accuracy vs eager rel-err **2.8e-7**, cosine 1.0, imagine-and-select **agreement 100 %**, waypoint shift **0.00 m** → a FREE win (same fp32 kernels, pure launch elimination). **Falsifier (>10 %) cleared 25× → launch-bound CONFIRMED.** Gain scales inversely with batch → **two orthogonal levers: encoder=compute-bound→fp16, predictor=launch-bound→CUDA graph** (additive tick projection ~9.1 ms / ~109 Hz). **Deploy finding: on this Triton-less Windows box the graph route is manual capture, NOT `torch.compile`** — inductor→`TritonMissing`, dynamo-cudagraphs→20× slower (117 ms). **(2) Numerics-safety sweep (P0 #4, G-P1):** grep-swept every learned/data `exp`/`log`/`div` in `stack/tanitad` → the class is **CLOSED** (all guarded: clamp / count-gate / neg-exponent), no new site; shipped an **11-test executable regression guard** (intake `2026-07-18-numerics-safety-sweep`, test-only → `stack/tests/`, all green) with real failing-then-passing witnesses (unclamped nll→inf; head bounds logvar; FeatureOOD count-gate). TRT-fp16 engine (P0 #1) still toolchain-blocked → job card. No hypothesis status change (efficiency evidence → Benchmarks CNCE handoff). Branch `agent/prod-opt-20260718` (off-Drive worktree, off HEAD). | `Production & Optimization/Research/2026-07-18-predictor-cudagraph-and-numerics-sweep.md`, `Implementation/predictor_latency/{predictor_latency_attack.py,predictor_latency_20260718.json}`, `Implementation/incoming/2026-07-18-numerics-safety-sweep/` (+11✓ tests), KB/BACKLOG/STATE/PRODUCTION_READINESS |
| 2026-07-18 (Wed agent) | Architecture & Inference agent | **Backlog P0.1 executed: E1+E2 re-run on the OPERATIVE `flagship-speed` @19k (eval-pod A40, PhysicalAI val, 2 seeds, $0) — drops the 07-17 pre-reset caveat.** **E1 (H15/H11/D8):** the σ-dissipation + attractor-collapse pathology **REPRODUCES on the operative model — falsifier "speed+jerk recipe fixed it" NOT met** (cos_rollout→chance by k3; σ_hidden −9.461→−9.564 = *lower* absolute σ = worse temporal calibration; attractor 0.219→**0.805**, sharper). **freeze-1 holds 0.213–0.232 flat across 8 horizons (7× persistence)** → parallel-horizon safe on the shipping model. NEW refinement: σ is *spatially* calibrated (hidden>visible +0.37; err↔var corr +0.29–0.43) but *temporally* anti-calibrated → target = **horizon-aware σ**. **E2 (H3/D-021):** iso_ratio_active **0.254→0.546** (SIGReg converging as predicted; crossed 0.5), cond 218→61, still NOT-YET-ADMISSIBLE (offdiag 0.32>0.1); active_k≈19 ≪ 2048 → **readout not the D1 bottleneck (G1) reaffirmed**. No config change (D-018). @30k re-run turnkey (only step to decision-grade). The 07-10 orthogonality instrument **still unmerged (3rd+ wk)** — re-flagged. Package 15✓ (5 new parity tests); zero stack/ files touched. Branch `agent/arch-inf-20260718`. | `Architecture & Inference/Research/2026-07-18-operative-flagship-blind-rollout-and-orthogonality.md`, `Implementation/belief_rollout_diagnostic/blind_rollout_flagship.py` (+parity tests), `Implementation/orthogonality_verification/run_orthogonality_flagship.py`, KB/BACKLOG/STATE/GOALS/HYPOTHESIS_LEDGER |
| 2026-07-18 (Tue agent, incr 2) | Data Engineering agent | **Curve-rebalance MEASURED on real bytes — FLEET_REVIEW P0#3, the data-side attack on the #1 risk** (intake `2026-07-18-curve-rebalance/`, analyzer + 12✓; local 4060 CPU, ~1.5 min, $0). 630 real eps / 125,247 windows, D1 eval strata (`\|net yaw@2s\|` <5°/5-20°/>20°, copied verbatim from `driving_diagnostic.py` + drift-guard test): **comma2k19 83.1% straight / PhysicalAI 56.0% / natural pool 63.9%**. **KEY: the "74% straight" is a comma/HIGHWAY property, not whole-corpus** — urban (PhysicalAI, incoming ZOD/PandaSet) is already at the 55-60% target; the ~74% ≈ a comma 0.65-0.70 mix. Recipe to 57.5%: source-mix (+10pp comma≈+2.7pp straight → shift to urban = ZOD rationale in numbers) + window turn-weight β=s(1-t)/(t(1-s))=1.31(pool)→2.22(comma-0.70), verified by construction. Sampler/mix wiring → **D-018 ESCALATE** (proposal, not a live flip). SEARCH lit delta (IDM/latent-action: Sensorimotor-WM 2606.20104, ACID 2607.02403, X-Lens 2607.12993 intrinsic-canon) folded into KB, no status change (P8). Branch `agent/data-engineering-20260718`. | `Research/2026-07-18-curve-rebalance-measured-and-idm-lit.md`, `Implementation/incoming/2026-07-18-curve-rebalance/` (analyzer + 12✓ + INTAKE + report JSON), KB/BACKLOG/GOALS/STATE |
| 2026-07-18 (Tue agent) | Data Engineering agent | **ZOD loader SHIPPED — the FLEET_REVIEW P0#1 / OWN_DATASET_PLAN §7#1 unlock** (intake `2026-07-18-zod-loader/`, `zod.py` + 19✓ + real-bytes job card + INTAKE; local 4060, ~1.6 h, $0). CC-BY-SA-4.0 owned real-urban: 14 EU countries, day/night/seasons/weather, **real CAN steer + OxTS RT3000 ego-motion** — the diversity the 74%-straight day-only mix lacks. **Key reuse result:** ZOD front is a **Kannala-Brandt fisheye** (3848×2168, **120° HFOV**) and KB radius `r(θ)=f(θ+k1θ³+…)` ≡ `calib.FThetaIntrinsics.poly` (odd powers) → `kb_to_ftheta` reuses the proven f-theta crop with **zero new geometry math**. **Pre-registered geometry falsifier ANSWERED — PASS** (grounded on the published spec, **robust to the real KB coeffs**): **f_eff=266.0, observed_frac=1.00, drop_in=True** → ZOD geometrically **UNBLOCKED**, needs NO calib.py R1 (fisheye path suffices; contrast PandaSet height-bound at 467; narrow-40° witness falsifies at 0.34 → gate not vacuous). OxTS heading drives yaw (offset-free) → tested `cosmos_drive.poses_to_signals`. **ESCALATION: request ZOD access** (`opendataset@zenseact.com`, CC-BY-SA; HF repo is a code-loader, no plain download) — the ONE blocker; then run the job card (pod3-idle/Colab, M-1.3) for real-bytes verify + precompute. Lit/seam: WorldLens+DrivingGen driving-WM benchmarks → Benchmarks&Eval; new urban dashcam corpus arXiv 2604.01044 → curve-rebalance probe (P1). Red-suite (Mon #1) already resolved (391 tests collect). Branch `agent/data-engineering-20260718`. | `Data Engineering/Research/2026-07-18-zod-loader-and-geometry-falsifier.md`, `Implementation/incoming/2026-07-18-zod-loader/` (zod.py + 19✓ + job card + INTAKE), KB/DATASET_LANDSCAPE/BACKLOG/STATE |
| 2026-07-17 (Wed agent, wall-clock Fri) | Architecture & Inference agent | **2 measured experiments on real comma2k19 (4060, $0, 2 seeds) + a duplication caught.** **E1 (novel, top-risk-adjacent, H15/H11/D8):** rolled the trained 1-step ImaginationField **fully blind** K-steps — it **dissipates epistemic σ** (hidden log-var −7.79→−8.55 = *more* confident as predictions decay to chance by k4) and **collapses to a common attractor** (inter-sample cos 0.21→0.57, belief energy −11×), matching the UWM-JEPA risk (07-15) + **"Biased Dreams" 2604.25416**. **Cause is the recursion, not the 1-step field:** *freezing* the k=1 imagination holds ~0.25 cosine flat across 8 horizons. **Constraint: cap the operative H15/D8 self-monitor at 1-step / parallel-horizon until a multi-step σ is validated** (D-018 escalate; no config change). Shipped as a prototype w/ 10✓ metric tests. **E2 (integrity, H3/D-021):** started a 3b orthogonality instrument, found a theoretically-superior one **already built 2026-07-10 but never merged** → **withdrew my duplicate**, verified the prior (iso_ratio_active **0.254 NOT-YET-ADMISSIBLE**; global isotropy ~0 = over-provisioning *by design*) → **D-021 = subspace ID, NOT "optimal planning"**; **flagged the stranded 07-10 pkg for orchestrator merge** (3rd week). Rules out latent capacity as a D1 bottleneck. Created `GOALS.md` (D-029). Backlog 0b split into 0b-A (train multi-step) / 0b-B (parallel-horizon, recommended). Stack suite 343✓/2s (fresh worktree). Branch `agent/arch-inf-20260717`. | `Architecture & Inference/Research/2026-07-17-blind-rollout-*.md`, `Implementation/belief_rollout_diagnostic/` (+10✓ tests), `Implementation/orthogonality_verification/`, `GOALS.md`, KB/BACKLOG/STATE/HYPOTHESIS_LEDGER |
| 2026-07-17 (Fri, 16:42 UTC) | Pod training monitor (2nd Jul-17 check) | **STALE-MONITOR PERSISTS + REF-B ARM ROTATED (no execution).** Scripted target **`p0-sB01-realmix` still superseded** (log frozen **Jul-12 04:38 at step 28600**, no `metrics.json`) — **did NOT run the playbook relaunch** (would resurrect a dead pre-reset run + contend the GPU refb actively uses). **NEW since this morning's check (`316f958`):** the REF-B arm **rotated** — `refb-speed-30k` (was pid 1261302) is **gone**, replaced by a fresh **`refb-refbpatch-30k`** (pid 1363229, up ~41 min): `refb_train.py --refbpatch --jerk-weight 0.02 --steps 30000 --grad-checkpoint`, on the same 2376-ep physicalai_phase0 set (406,099 windows). **HEALTHY** — step **100/30000**, log fresh (16:41 UTC, ~1 min before check), GPU **100 % / 25.3 GB**, loss descending 567→451→388, step_s ~23 s (~half data-bound, grad-ckpt), man_acc/route_acc/nav_follow nominal-early, aux_accel_r2 ~0 (warming). Two follow-on eval jobs **queued behind it** (axis6-relaxed diag+grounded-rollout → then axis6-baseline `--sigreg-free-dims 0` train). This is deliberate opponent/arch orchestration (fresh run + queued evals), **not a crash → no intervention**; prior `refb-speed-30k` disposition (completed vs stopped-early) is the owning agent's ledger, unconfirmed from the monitor vantage. GPU `[Not Found]` pid 974258 holds ~all used mem = the live refb CUDA context (single active consumer / cross-namespace label), not the leaked-zombie read from Jul-16 — not decision-relevant. **Retarget ask now even more warranted** (monitor tails `train_worldmodel`+`p0-sB01` log; live is `refb_train`+`refb-refbpatch-30k` dir) — **did NOT re-nudge Sayed** (a day-3 push already went out this morning; avoided same-day duplicate). | `/workspace/experiments/refb-refbpatch-30k/{ckpt.pt,.log}` (pod); superseded `p0-sB01-realmix.log` |
| 2026-07-17 (Fri) | Orchestrator & Project Steering agent (run #5) | **Weekly synthesis 2026-W32** (covers 2026-07-11 → 2026-07-17). **The week's pivot = the speed/scale RESET (2026-07-14)** — root cause: no arm fed `v0` (current ego-speed) → each inferred absolute speed from vision, impossible for a frozen encoder (speed-probe R² 0.61); fix = `v0` as 3rd action channel → REF-A **3.73→2.14 m** held-out, decodability **0.61→0.965 R²**; all 3 arms restarted from scratch. **Supersedes the entire W31 pre-reset narrative** (14.2-vs-7.5 confounded head-to-head void; H4 evidence reset, stays OPEN). **Honest kinematic floor** (`e8fca8e`): 0.056 m@1s (CTRV, 26,132 anchors) → flagship 6.44 m = **~115× floor**, neither arm beats the trivial baseline open-loop. **First positive signal:** flagship @5k beats REF-A @30k at 1s on every stratum. **3-arm state:** flagship ~6.35k/30k rising (pod2), REF-A 30k DONE/plateaued 2.14 m (pod3), REF-B **8,750/30k healthy** (pod1, ETA ~Jul-23). **Health: 5/5 weekday agents ran+committed measured work** (tools/data Jul-15 catch-up fire; arch resolved h15=0.0 false-zero + H15 latency 12.7–17 % tick; bench CV floor; opponent SC-13 collision 0.00 vs 0.60 + W-09; data PandaSet loader + D-016 blocking prereq); prod-opt Sat not in window (next Jul-18). **Recurring D-026 debt:** unmerged branches, stale main-tip STATE.md (Data/Bench/Opp behind own commits), uncommitted bench-backlog-#3 + opponent-stationary-lead, monitor blind to pod2/pod3 (retarget 3 d pending). Intake: verdict write-back chronically skipped; 3 genuinely-untriaged. Schedule 9/9 enabled+fired. Burn $0/$120 on plan. **Proposals: P1 ride flagship→30k, gate vs floor+REF-A (re-gate D2) (HIGH, ~$0) · P2 commit to closed-loop — Sayed pre-approve the graphics CARLA pod for D4–D6 + real D8 (MED-HIGH, LOW $) · P3 monitoring+integration bundle — retarget pod-monitor at all 3 arms + merge stranded branches + build D-026 guardrail (MED, LOW).** No unlogged decisions. | `Project Steering/Progress Reports/2026-W32.md`, `TanitAD Research Hub/Project Steering/Research/STATE.md`, `PROJECT_STATE.md` |
| 2026-07-17 (Thu) | Pod training monitor | **STALE-MONITOR PERSISTS (day 3, no execution) + REF-B health confirmed.** Unchanged from Jul-15/16: `tanitad-pod` still lands on the `refb-speed-30k` box; scripted target **`p0-sB01-realmix` still superseded** (log frozen **Jul-12 04:38 at step 28600**, no `metrics.json`). **Did NOT run the playbook relaunch** (would resurrect a dead pre-reset run + contend the GPU refb actively uses — my ops rule: never add compute while an arm trains). **Actual arm `refb-speed-30k` HEALTHY** (pid 1261302, up ~2 d 10 h): step **8750/30000 (29 %)**, log fresh (04:29 UTC, ~13 min before check), steps monotonic no-dupes (8600→8650→8700→8750), loss bounded 2.0–4.0, man_acc ~0.67–0.72, route_acc 1.00, nav_follow ~0.93–0.96, gnorm bounded, aux_accel_r2 ~0 (still warming), ckpt.pt refreshed 02:49. Throughput steady ~150 steps/h (step_s ~24 s, ~half data-bound) → 30k ETA **~Jul-23** (ample margin vs Oct-05). **Retarget ask now 3 days pending** — the monitor tails the wrong log/proc pattern (`train_worldmodel` vs live `refb_train`) so it is structurally blind to a real refb crash; sent Sayed a brief day-3 nudge (no re-explanation). | `/workspace/experiments/refb-speed-30k/{ckpt.pt,config.json}` + `.log` (pod) |
| 2026-07-16 (Wed) | Pod training monitor | **STALE-MONITOR PERSISTS (day 2, no execution) + REF-B health confirmed.** Same state as the 2026-07-15 flag: `tanitad-pod` still lands on the `refb-speed-30k` box; the scripted target **`p0-sB01-realmix` is still superseded** (log frozen **Jul-12 04:38 at step 28600**, no `metrics.json`; the 25 GB GPU allocation is a **leaked zombie CUDA context** — `nvidia-smi` compute-apps shows only pid **645197 `[Not Found]` 25.3 GB**, no matching process). **Did NOT run the playbook relaunch** (would resurrect a dead run + contend the GPU refb uses). **Actual arm `refb-speed-30k` HEALTHY** (pid 1261302, up 1 d 22 h): step **6950/30000 (23 %)**, log fresh (16:26 UTC), ckpt saved 13:25 today, loss noisy-bounded 1.7–4.0, man_acc ~0.60, route_acc 1.00, nav_follow ~0.95, gnorm 11–36 bounded, aux_accel_r2 ~0 (warming), steps monotonic no-dupes. **New datapoint: throughput ~150 steps/h** (step_s ~24 s, data_s ~11.5 s = ~half dataloader, grad-checkpoint) → 30k ETA **~Jul-23** (not urgent vs Oct-05 final eval, but data-bound half echoes the historical mmap working-set cost). **Retarget ask still pending from Jul-15** — the monitor tails the wrong log so it cannot detect a real refb crash; sent Sayed a concise follow-up. | `/workspace/experiments/refb-speed-30k/{ckpt.pt,config.json}` + `.log` (pod) |
| 2026-07-15 (Tue) | Data Engineering agent | **Two measured results advancing the OWN_DATASET_PLAN (owned license-clean corpus), $0, local/HF (pod off-limits: `refb-speed-30k` training).** (1) **WorldModel-Synthetic-Scenarios pose gate CLOSED = POSE-LESS** (BACKLOG P0.1, real bytes): each clip = 7-cam video + per-cam **Qwen2.5-7B caption + `{weather,tod,surface,region}`**, **no vehicle_pose/CAN/trajectory** → "cosmos-mirror loader" RETIRED; pixels = Phase-1 IDM (H7) target, **captions/metadata = usable-today semantic-label index** (P1 2d + SC-02/05/06, merged into SCENARIO_DATABASE). (2) **PandaSet loader shipped** (intake `2026-07-15-pandaset-loader/`, **16✓**, CC-BY-4.0 owned core, reuses cosmos geometry, I7≡comma) **+ grounded D-016 GEOMETRY BLOCKER**: on the REAL front calib (arXiv 2112.12610 fx=1970, 1920×1080, k1=−0.589) the centered square-crop is **height-bound** → lands **f_eff=467≠266** (~1.75× scale mismatch) + ignores distortion → loader **fails loud** (`GeometryError`), can't pollute the mix. **Rule: fx>1122 px on a 1080-tall frame is not square-croppable to 266.** → the **D-016 R1 pad-crop+undistort** (same mechanism as the in-flight two-rig cy fix) is now a **blocking prerequisite for the whole owned real-urban tier** (ZOD/Udacity hit it too), promoted P0. ZOD license corrected research/NC→**CC-BY-SA**; new `…Cosmos-Synthetic` (card-only) tracked. QUALITY full; branch `agent/data-engineering-20260715`. | `.../Data Engineering/Research/2026-07-15-worldmodel-pose-gate-and-pandaset-geometry.md`, `.../Implementation/incoming/2026-07-15-pandaset-loader/`, `DATASET_LANDSCAPE.md`, `GOALS.md`, `SCENARIO_DATABASE.md` |
| 2026-07-15 (Tue) | Pod training monitor | **STALE-MONITOR FLAG (no execution — D-018 tactical, escalated to Sayed).** `tanitad-pod` SSH now lands on the box running **`refb-speed-30k`** (the REF-B arm from the **2026-07-14 speed-fix reset**: `refb_train.py --speed-input --jerk-weight 0.02 --steps 30000`, pid 1261302), which is **HEALTHY** — step **3550/30000 (12 %)**, log fresh (Jul-15 18:04 UTC), ckpt saved at 3500, GPU **25.3 GB / 100 %**; early metrics nominal (man_acc ~0.60, route_acc 1.00, nav_follow ~0.95, loss bouncing 1.9–4.6, gnorm 15–87 bounded, aux_accel_r2 ~0 still warming). The monitor's scripted target **`p0-sB01-realmix` (old flagship run) is SUPERSEDED, not crashed** — its log froze **Jul-12 04:38 at step 28600** (no `[runner] finished`, no `metrics.json`), the box was repurposed for the reset. **Did NOT execute the playbook relaunch** — it would resurrect a dead experiment and contend for the GPU refb is actively using. **Action needed (Sayed):** retarget the monitor task file (`tanitad-pod-training-monitor/SKILL.md`) at the current arm(s) — log path, experiment dir, and process pattern all changed (`refb_train`, not `train_worldmodel`). This monitor now only reaches pod1=REF-B via `tanitad-pod`. | `/workspace/experiments/refb-speed-30k/{ckpt.pt,config.json}` + `.log` (pod); superseded `p0-sB01-realmix.log` |
| 2026-07-12 (Sun) | Pod training monitor | **INTERVENTION — trainer relaunched (resumed from ckpt).** Found `p0-sB01-realmix` **process dead at step 27000/30000** (no `train_worldmodel` proc, GPU idle 2 MiB, no `[runner] finished`, no `metrics.json`). Not an end-of-run crash — last log line was a normal step-27000 entry + `[ckpt] saved`; the only Tracebacks were early **per-item cache-load skips** (physicalai ep_00138 mmap/zip-central-directory, auto-skipped, non-fatal). Dead ~1h14m (log mod Jul-11 21:29 → pod 22:43 UTC; uptime 354 d, only idle jupyter-lab running). Confirmed `pod_launch.sh` resumes from `ckpt.pt` (script L42–44) → **EXECUTED playbook relaunch** (`git pull` + `EPISODES=500 MICRO=32 ACCUM=2 STEPS=30000 bash scripts/pod_launch.sh`). Verified healthy: log printed **`[resume] checkpoint found — resuming at step 27001`**, caches loaded clean (410/90 comma2k19 + 400/100 physicalai, **0 skipped**, realmix 0.40/0.60), **pid 214196** `--steps 30000` alive, **GPU 17786 MiB / 17 %**. No gate preview (already past ≥15k; step-27000 FLAGSHIP gates already committed `72c3f6e`). No escalation — clean auto-recoverable stall, D-018 execution action. | `/workspace/experiments/p0-sB01-realmix.log` (pod), `scripts/pod_launch.sh` |
| 2026-07-17 (Thu) | Benchmarks & Eval agent | **Put the driving-gap denominator in leaderboard-comparable units (G1 advanced; top program risk).** Data-only, $0, local CPU, held out by clip. Shipped the **nuScenes-style open-loop L2 protocol** (metric-BEV ego frame, both UniAD-`pointwise`/ST-P3-`cumulative` conventions, collision proxy) + a **no-vision learned ego-status shortcut** (`RidgeTrajectoryHead`, AD-MLP repro, arXiv 2312.03031) → intake `2026-07-17-openloop-l2-egostatus-shortcut` (**8 analytic tests green**), proposed `stack/tanitad/eval/openloop_l2.py`. **F1 (measured, 7 920 comma-hwy val anchors):** the no-vision shortcut ceiling = **avg L2 0.66 m** (0.144/0.552/1.256 @1/2/3s) ≈ CTRV → **`skill_score = model_L2 ÷ 0.66 m` now in community units**; cosmos-urban learned shortcut (1.19 m) beats the fixed kinematic floor (1.34). **F2 (methodology):** **comma highway is 73.9 % straight = nuScenes' 73.9 %** → our open-loop val inherits the exact ego-status-shortcut pathology; **aggregate open-loop L2 is a weak capability test** — verdict must be per-stratum skill_score + closed-loop, never an aggregate L2. **F3:** nuScenes L2 has two undisclosed averaging conventions (~2× apart) → LEADERBOARD gains a pre-registered open-loop reporting protocol (shortcut+skill_score+open⊥closed+unit disclosure). **P8/blocker:** model-relative number needs a post-reset ckpt decoded in metric-BEV; local `ckpt_full.pt` is pre-reset camera-frame (not comparable → NOT used, G-B1); pods training/off-limits. Branch `agent/benchmarks-eval-20260717` (worktree off e8fca8e, D-026). | `Benchmarks & Eval/Implementation/incoming/2026-07-17-openloop-l2-egostatus-shortcut/`, `.../Research/2026-07-17-openloop-l2-egostatus-shortcut.md`, `Benchmarks & Eval/LEADERBOARD.md`, KB, GOALS, HYPOTHESIS_LEDGER |
| 2026-07-15 (Thu) | Benchmarks & Eval agent | **The honest denominator — directly answers Sayed's "7.51 is bad too → CV kinematic-floor baseline" directive** (W31 row). Data-only, $0, local CPU, **26 132 real anchors** (comma2k19-val 25 110 + Cosmos-DD 1 022), 10 Hz. Shipped a **tested best-of-3 kinematic-baseline floor** (CV / go-straight / **CTRV** + `skill_score`, speed-gated curvature strata; **8 analytic-ground-truth tests green**) → intake `2026-07-15-baseline-floor`, proposed `stack/tanitad/eval/baselines.py`, plugs the D1 `extra_metrics` seam. **F1:** the diagnostic's single-CV floor (≈0.28 m@1s) is **not honest** — CV is the weakest null on curves (gentle CV 0.275 vs CTRV 0.060 = **4.6×**); the best-of-3 floor is **≈0.056 m@1s** (CTRV wins 55–58 %). → flagship held-out 6.44 m = **~115× the trivial floor** (framework's D1-FAIL verdict **reinforced**, not overturned); **D1 should report `skill_score` = ADE ÷ per-stratum best-of-3 floor**, redefining "baseline-relative" as Sayed asked. **F2:** curvature strata **must be speed-gated** (v≥2 m/s) — 12.4 % of comma anchors are standstill yaw-noise mislabeled "sharp" (κ=yaw_rate/v singular at v→0); found+fixed, appended to framework §Results. **F3:** the ungated Cosmos-DD sample is a **poor maneuver source** (0 % genuine sharp, 95.8 % straight) → Data-Eng curve-scarcity flag. Ecosystem (D-028): Occluded-nuScenes (2510.18552), Bench2Drive-Robust (2605.18059, occlusion×latency = our edge pair), IDOL (2605.31476, supports the diagnostic root cause). **P8:** baselines use privileged GT ego-state → denominator, not a model competitor; no model ADE this run (pod busy w/ refb-speed-30k). Created `GOALS.md` (D-029, was missing). Branch `agent/benchmarks-eval-20260715` (worktree off e17435e, D-026). | `Benchmarks & Eval/Implementation/incoming/2026-07-15-baseline-floor/`, `.../Research/2026-07-15-baseline-floor-honest-denominator.md`, `Benchmarks & Eval/{DRIVING_DIAGNOSTIC_FRAMEWORK,LEADERBOARD}.md`, `GOALS.md`, KB, HYPOTHESIS_LEDGER |
| 2026-07-11 (Sat) | Orchestrator & Project Steering agent (run #4) | **Weekly synthesis 2026-W31** (covers everything since the W30 report `da85a3d`). **All three architecture arms now physically exist (Phase-0 day 7/42):** main WM **25.75k/30k (86 %)**, ~1,000 steps/h, 30k ETA slipped ~20:15→**~22:15** (a ~2 h crawl escaped the <50-steps/15-min watchdog, self-recovered); **REF-A frozen-DINO reference 30k COMPLETE** (loss 1.19→**0.465**, pred 0.080, roll 0.076 K=4-healthy, SigReg 3.44, adapter_std 0.86↑ no-collapse, 60 ckpts, ~4.5 h, $0 incr); **REF-B E2E vision-action reference built+merged** — rev1 `e616b23` 260.7 M (−0.82 %, 204 tests), rev2 `38cf9ca` strategic→real transformer d384×4 + route-derived nav commands + own aux loss (**fixes a real defect: strategic had trained on constant `follow`**), tactical 4→6, −0.124 %. **FIRST HEAD-TO-HEAD (`ecbad24`, `refa_probe.py`, matched d1_probe_capacity):** REF-A ADE@1s **≈14.2 m** vs main **≈7.5 m** → frozen encoder currently **WORSE**, but **CONFOUNDED** (v1 adapter mean-pools the 256 DINO tokens, killing spatial structure the DINO-WM lineage needs; comma-val-only vs main's comma+physicalai) → **does NOT arbitrate H4 yet** (H4 row NOT upgraded, P8). Sayed review (`a7563e0`) sharpened D1 twice: "expected better from DINO"→grid-adapter REF-A variant; **"7.51 is bad too"→CV kinematic-floor baseline row** (test if the sub-metre bar is even meaningful vs constant-velocity extrapolation, else redefine baseline-relative). **D1 route-resampled protocol now real code** (`e9b2491`, gates.py — ADE/FDE mean over n_splits=8 + 95 % CI). **H16 active depth interrogation banked** (`7cd9d7b`, Sayed's idea, F1–F3 falsifiers, Phase-1 H2 ~Sep). **Decisions:** D-028 **ADOPTED** (`7cc210f`, was proposed W30), D-022 numbering gap resolved (default HOLD), REF-B **$40 envelope CONFIRMED by Sayed in chat** (`5dd08c2`, attribution corrected). **Health (net-improving):** W30 hygiene debt **CLEARED** (`1285524` — Prod-Opt int8/windowing + Opponent catalogs committed, tree clean); **Data-Eng no-show REVERSED** (fired 15:14, semantic-datasets survey → DE backlog); discipline worktree branches still empty 3rd cycle but the loop carried all real work with 0 loss → **de-escalated** to process debt; session-end guardrail still unbuilt. Schedule **9/9 enabled+fired**. Intake: 2 W30-uncommitted items now committed; none >3 days. Burn **$0 settled / $120 planned, on plan**. **Proposals: P1 run the 30k flagship gate tonight ~22:15 (D1 route-CI + CV kinematic-floor baseline) (HIGH, ~$0) · P2 de-confound REF-A — grid-adapter retrain + comma-only main re-run → read H4 honestly (MED-HIGH, $0) · P3 Sayed unblock pod3 quota 50→300 GB + build session-end guardrail (MED, LOW).** **TOP RISKS: (1) pod3 volume quota mis-sized (Sayed console action, blocks REF-B staging); (2) 30k decision-grade verdict still pending 3rd report; (3) don't over-read 14.2-vs-7.5 (confounded).** | `Project Steering/Progress Reports/2026-W31.md`, `TanitAD Research Hub/Project Steering/Research/STATE.md`, `PROJECT_STATE.md` |
| 2026-07-11 (Fri) | Orchestrator & Project Steering agent (run #3) | **Weekly synthesis 2026-W30** (covers 2026-07-10 20:52 → 2026-07-11). **D1 "regression" RESOLVED as a PROTOCOL artefact** (`0284a5c`): probe-capacity discriminator → 23.5k decodes BETTER than 14k at every capacity (ridge α1 7.56→7.51, α10 8.54→**7.02**, α100 12.60→9.47, MLP 11.19→**8.40**); the 21k 11.52 m was route-split sensitivity → **D1 becomes route-resampled mean±CI** before the 30k eval; improvement positively monotone, still FAIL vs sub-metre floor. **Trainer 21k→23.7k/30k (79 %)**, ~1,000 steps/h, data path = 67 % of step time; **30k flagship decision-grade gate TONIGHT ~19:30** → Phase-0 core evidence closes ~2 weeks early (day 7/42). **D-027 ACCEPTED** (`ff6a409`, config-flip guard keeps running recipe k=1). **REF-A stage-1 done** (`cda93df`, 490 eps → 54 GB DINOv2-B/14 grids, DINOv3-gated fp16 fallback) → REF-A trainer queued tonight (A40, k=4) — the H4 frozen-encoder arm that arbitrates D1 capacity-ceiling vs training-time. **D-028 recency-first literature scan** (`2d58b69`): closes the AUTOPILOT-VQA (arXiv 2607.08745) + ZipDepth miss; seam-ownership rule; **logged `proposed`** (was in commits + protocol, absent from DECISIONS.md). **Health-check (honest, regression PERSISTS — 2nd cycle):** the 07-11 09:34–09:39 wave produced **0/5 clean committed worktree branches** (all 07-11 branches empty at main tip); **Data-Eng no-show again**; real work bypassed the agent framework onto main (loop/monitors); **loose files in main** — Opponent 4 catalogs modified-uncommitted after `2d58b69`, **Prod-Opt `int8_quant`+`contract-windowing-failloud` STILL untracked 1+ day**. W29 P3 guardrail never built → re-escalated. Schedule: **9/9 jobs enabled + fired** (7 hub + pod-monitor + evening-report). Intake: no new pkg; 2 uncommitted Prod-Opt dirs = the overdue hygiene item (none >3 days by wall-clock). Burn **$0/$80 on plan**; **REF-A + post-30k spend NOT yet pre-approved** (needs a ledger row today). **Proposals: P1 run the 30k flagship decision-grade gate tonight — D1 mean±CI + D2 + D3 + paired D8 (HIGH, ~$0) · P2 launch REF-A frozen-encoder arm (A40, k=4) + stand up REF-B (MED-HIGH, needs ≤$40 ledger row) · P3 build the D-026 session-end guardrail + clear the 2-cycle hygiene debt (MED, LOW).** Flagged: D-022 absent in DECISIONS.md (numbering gap); `orchestrator-agent.md` says "six" jobs but 7 hub jobs exist (D-020 Sat agent); D-025 session drumbeat empty (needs weekly re-arm). Did **not** sweep loose files into the orchestrator commit (possibly in-flight) — filed for MVP loop. | `Project Steering/Progress Reports/2026-W30.md`, `DECISIONS.md` (D-028 proposed), `PROJECT_STATE.md` |
| 2026-07-11 (Thu-sched, Sat) | Benchmarks & Eval agent | **Independent-test / G-H: D1 ADE statistical-power audit** (local RTX 4060, step-6500 ckpt + comma2k19 val, 80.8 s, $0). Measured the D1 `ade@1s` estimator's sampling variance to settle whether the **step-14k→21k 5.18→11.52 m "regression"** is real: per-route ADE spans **2.31–18.75 m** (CoV 0.58); shipped single-seed `run_d1` swings **7.28 m across split seeds** at 4 val eps; fixed-probe bootstrap 95 % CI half-width **±4.51 m (n=4)** / ±3.13 (n=9) / ±2.11 (n=20). **Falsifier tripped → the 21k D1 "regression" is INSIDE the estimator's own noise band (11.52 m ∈ n=4 CI upper 13.55 m) — NOT decision-grade; even n=9 marginal.** Audited the loop's `d1_probe_capacity.py` (`0284a5c`): same small-sample fragility + corpus mixing → verdict not decision-grade as written. **Adopted rule R1: D1/D3 report mean±CI over ≥5 seeds, ≥20 val eps; single-seed points deprecated for gate-movement claims.** Shipped `d1_power_audit/` (4 tests) + intake `2026-07-11-d1-gate-bootstrap` (`run_d1_bootstrap` mean±CI wrapper, 4 tests); LEADERBOARD D1 row + power footnote; KB/BACKLOG/STATE. No H-status change (P8). Worktree `worktree-agent+bench-eval-20260711`, unmerged. | `.../Benchmarks & Eval/Research/2026-07-11-d1-ade-statistical-power-audit.md`, `.../Implementation/d1_power_audit/`, `.../Implementation/incoming/2026-07-11-d1-gate-bootstrap/`, `Benchmarks & Eval/LEADERBOARD.md` |
| 2026-07-10 (Fri) | Orchestrator & Project Steering agent (run #2) | **Weekly synthesis 2026-W29** (covers 2026-07-09 20:52 → 2026-07-10). **W28 top risk RESOLVED:** blocked trainer restart superseded by mmap root-cause fix (`7b5faa6`, `data_s` 73 %→nominal) → trainer healthy **~21k/30k**. **Gate movement (step-14k, full val):** D1 10.94→**5.18 m** FAIL, D2 0.872/0.940→**0.917/1.000** PASS, D3 I4 3.83→**3.05** BLOCKED (I1–I3 admissible) — but the **step-21k small-sample preview (20/4 eps) drifted** (D2 0.857/0.940 still PASS; D1 11.52 m; D3 flat) → directional, **decision-grade read = 30k on full val**. **D3 decomposition:** direct heads rel-err FALLS with k — no compounding; recursive 2–4× worse → **keep direct heads, match K to horizon** (K=4 arm: 1-step imag_rel 8.13→1.03, recursive 14.5→1.11). **Health-check regression (honest):** only **3/6** agents committed — Tools (`ci.ps1` CI gate), Architecture (orthogonality instrument iso_ratio 0.250 NOT-YET-ADMISSIBLE → tempers D-021), Opponent (SC-13, collision 0.000 vs 0.429 + FMVSS-135 H11 tailwind), all worktree-isolated (D-026) & **unmerged**; **Data-Eng no output, Benchmarks no commit, Prod-Opt stranded `int8_quant`+`contract-windowing-failloud` uncommitted in main** → new D-026 failure mode. Schedule: **9/9 jobs enabled + fired 07-10** (bench/prod drifted to Fri 20:00, cron correct). Intake: 9 integrated, 4 pending in-window + 3 new on branches + 2 uncommitted; none >3 days. Burn **$0/$80 on plan**; post-30k spend needs pre-approval. **Proposals: P1 ride to 30k → first decision-grade gate (HIGH, ~$0) · P2 fold D3-decomp+K=4 into arch — direct heads, K=horizon (MED-HIGH, $0) · P3 close agent-commit gap + guardrail + clear intake wave (MED, LOW).** D-026 + Sonnet-5 addendum + three Sayed directives logged; no unlogged decisions. | `Project Steering/Progress Reports/2026-W29.md`, `TanitAD Research Hub/Project Steering/Research/STATE.md` |
| 2026-07-10 (Fri) | Pod training monitor | **Gate preview at step 21000 (p0-sB01-realmix, run healthy — step 21350/30000, PID 191199 alive, GPU 19.96 GB, steps monotonic).** No intervention. Trainer auto-emitted `gates_step21000.json` → satisfies the ≥15k preview condition (no manual evaluator run, GPU uncontended). **D2 dir-acc did NOT regress below 0.7 → no escalation (D-018):** P1 0.917→0.857, P4 1.000→0.940 (step14k→21k) — mild downward drift, both well above the 0.7 floor, D2 still **PASS**. **D1 ADE trend (recorded once): 5.18 m → 11.52 m** (14k→21k, camera-frame) — regressed, but D1 is the known structural-FAIL gate (ade<1.0 m camera-unit unmeetable; D4–D6 closed-loop arbitrate per 2512.24497). D3 also drifted (I4 3.05→3.04 unchanged, but oracle-decode ade@2s 1.57→3.43, imagined 1.59→5.37) — BLOCKED throughout. **Caveat (P8):** step-21k preview used 20train/4val eps vs step-14k 39train/9val → small-sample noise inflates the D1/D3 deltas; directional not decision-grade. Summary unchanged: **D1 FAIL / D2 PASS / D3 BLOCKED.** | `/workspace/experiments/p0-sB01-realmix/gates_step{14000,21000}.json` (pod) |
| 2026-07-09 (Fri) | Orchestrator & Project Steering agent (run #1) | **First weekly synthesis report — 2026-W28.** Health check: **6/6 discipline agents ran, all `QUALITY: full/complete`, 0 partial flags, 0 uncommitted** (Tools&DevEnv Mon, Data Tue, Arch Wed, Bench Thu, Opponent Fri, Prod&Opt Sat). Schedule audit: **9/9 scheduled jobs enabled + fired 2026-07-09** (`nextRunAt` correct). Intake audit (D-011): **9 verdicted/integrated, 4 pending & in-window** (testsuite-io + stop-arm-gate = safe-additive → recommend integrate; physicalai-r1 + models-predictor-failfast = MVP-review, reach into training-mix/`stack/models`), **1 stale** (metadrive-frontcam superseded by D-014 → flip to defer/reject). Synthesis: **gate ladder = 1 PASS (D2 dir-acc 0.872/0.940) / 1 FAIL (D1 ADE 10.94 m) / 1 BLOCKED (D3, I4 3.83) / 6 not-run**, all previews mid-training; KPI anchors 15.07 ms tick / 1.08 GB, **fp16 safe (95.3 %) vs bf16 unsafe (67.2 %)**, CNCE 2.02e5, 261 M vs 32 B/15 B field; resource burn **$0 settled / $80 planned, on plan**. **Proposals: P1 unblock trainer → step-30k (HIGH, ~$0, blocked on Sayed since 06:15) · P2 operative K∈{1,2,4} sweep + spectral resize D-021 (MED-HIGH, $0) · P3 clear intake queue + retire stale MetaDrive pkg (MED, LOW).** Top risk: all decision-grade gates depend on the blocked trainer restart. No unlogged decisions (D-023/024/025 all captured). | `Project Steering/Progress Reports/2026-W28.md`, `TanitAD Research Hub/Project Steering/Research/STATE.md` |
| 2026-07-09 (Wed) | Architecture & Inference agent | **G-H measured experiment — K-step rollout bake-off, first arm** (backlog P0 #2; the lever landed mid-week as `train.rollout_k`, commit c4375f8). Matched-compute **K=2 vs K=1** (2×2000 steps, real comma2k19, RTX 4060, 11.74 M reduced-but-real probe, OFAT-verified by `lever_diff`): **(1) rollout ≈ free — +0.5 % wall-clock (749.4 vs 745.4 s), 0 extra params;** **(2) the planned falsifier (D2 dir-acc) SATURATED at 1.0 both arms** → non-discriminative, so the read moved to `imag_rel`; **(3) K=2 cuts 1-step latent-pred error vs persistence 2.914→1.049 (−64 %) but does NOT help the 4-step horizon (I4 1.451→1.645)** → **K must match the decode horizon** (K≈4 for the 2-s D3 claim; 2512.24497 Pareto). **D1 FAIL + D3 BLOCKED both ⇒ no decision-grade claim (D-004); no trained-config change executed (D-018).** Decision-grade = operative-scale K∈{1,2,4} sweep from pod2 step-8k (new backlog P0 #2b). **Theory-watch (D-013):** *When Does LeJEPA Learn a World Model?* (2605.26379, LeCun/Klindt) — LeJEPA/SIGReg → linear+orthogonal identifiability under a UNIQUE Gaussian prior → optimal latent-space planning; grounds H3 anti-collapse + the `p0-spectral-sizing` linear proxy (D-021). ACWM (2605.08567) — AdaLN≈cross-attn for low-dim (2-D) actions → bounds the `adaln_conditioning` lever's Δ. No stack code change this run (suite 188✓/1s, unchanged by me). Ledger H5+H3 evidence rows. | `.../Architecture & Inference/Research/2026-07-09-kstep-rollout-bakeoff-and-lejepa-identifiability.md`, `.../Architecture & Inference/Implementation/kstep_bakeoff_probe/` (script + `results/2026-07-09-kstep_bakeoff_result.json`) |
| 2026-07-09 (Sat) | Production & Optimization agent (run #2) | Measured experiment + models review #2. **(1) Precision policy measured (P1.4 precursor):** TensorRT-proper is toolchain-blocked (`import tensorrt`→missing, ORT CPU-only) → ran the honest precursor (fp16/bf16 casts the same weights a TRT-fp16 engine would). On 64 real comma2k19 windows (step-6500, 4060), decoding a K=9 imagine-and-select fan through one **fixed fp64 probe**: **fp16 decision-SAFE** (selection agreement **95.3 %**, enc rel-err 7.8e-4, decoded-waypoint shift **3.9 cm** mean/19 cm max); **bf16 UNSAFE** (agreement **67.2 %** — 1/3 maneuver picks flip — rel-err 7.2e-3, **47.7 cm** mean/3.58 m max), both finite. G-P2 made concrete: speed alone picks bf16 (1.8× vs 1.6×); the accuracy delta rejects it. **Deploy TRT-fp16, never bf16**; ViT tower ≥fp16; TRT-fp16 acceptance bar pre-registered. P8: 4060 was **CarlaUE4-contended** (99 % util) → absolute latency/Hz + per-precision VRAM withheld (accuracy is contention-immune); latency-hygiene finding logged. **(2) Compliance review #2 `tanitad/models/`:** operative-predictor `assert`-only guard (`predictor.py:73`, stripped under `python -O` → silent wrong-window inference; cryptic matmul on wrong dims) → intake `2026-07-09-models-predictor-failfast` (`-O`-proof named `ValueError`s, shape-checks constant-fold → export-safe, **8 tests**). Logged: `tactical_pred` same guard; `imagination_nll` unclamped `exp(-logvar)`. Review #1 (data) confirmed integrated. Backlog split P1.4→P1.4a (TRT toolchain/idle-pod build) + P1.4b (clean-GPU latency). | `.../Production & Optimization/Research/2026-07-09-half-precision-and-models-failfast.md`, `.../Implementation/half_precision/{half_precision_latency_accuracy.py,half_precision_step6500.json}`, `.../Implementation/incoming/2026-07-09-models-predictor-failfast/`, PRODUCTION_READINESS + BACKLOG + KB + STATE, `Benchmarks & Eval/LEADERBOARD.md` |
| 2026-07-24 (Fri) | Opponent Analyzer agent (run #2) | Deltas sweep + measured scenario + Metis deep-read. **New emerging player Avride** (Uber/Yandex-SDG) under **NHTSA ODI** (16 crashes: lane-change/same-lane/stationary-object) → new profile + **W-08** + **SC-13**. **G-H experiment — Stop-Arm Gate scenario** (SC-04, W-03) shipped as intake pkg (`stop_arm_gate.py` + telemetry oracle, **11/11 offline tests**): H9 **violation rate rule_barrier 0.0 / soft_prior 1.0** over the free-path temptation sweep; barrier invariant to temptation while soft prior's line-crossing speed grows 3.0→9.6 m/s; OKRI toward the occluded child **80% lower at 4 B vs 15 B** params → **SC-04 spec-drafted**. **Metis** (2606.15869) deep-read: efficient WAM (MoT + asymmetric mask, action head skips video rollout at inference) but flat/no-hierarchy/no-imagination/no-monitoring + **no param count** → not a true CNCE rival yet. Deltas: Waymo **2nd recall** + highways pulled + **Dallas red-light** (SC-14); Tesla EA **3.2 M vehicles/9 crashes/1 fatality** naming the failed "degradation-detection" feature; Momenta listed + **R7 RL WM** + X7 chip; **Autobrains→L4** (Uber Munich pilot); NVIDIA **Mercedes CLA** ships Alpamayo + **AlpaSim** open (10 B Nano tier). "World model" now table stakes → moat = hierarchy+CNCE+imagination+self-monitoring. Ledger H0/H6/H9 row (no upgrade, P8). Loop 9/25 searches. | `.../Opponent Analyzer/Research/2026-07-24-opponent-sweep-w3.md`, `WEAKNESS_CATALOG.md`, `OPPONENT_PROFILES.md`, `SCENARIO_DATABASE.md`, `.../Opponent Analyzer/Implementation/incoming/2026-07-24-stop-arm-gate-scenario/` |
| 2026-07-09 (Mon) | Tools&DevEnv agent (W3) | Repo had advanced past the backlog (CARLA harness + Colab burst shipped live by the loop), so pivoted to the two live gaps. (1) **Root-caused the pod2 CARLA camera-rendering blocker** (was "not fixable in-container"), independently confirming the loop's "3 RunPod pods inject compute-only driver stacks" observation: two stacked host-level causes — RunPod exposes GPUs compute-only (`NVIDIA_DRIVER_CAPABILITIES=compute,utility` → no Vulkan ICD/EGL → `vulkaninfo` GIPA-NULL while `nvidia-smi` works) + UE4.24 can't render Vulkan offscreen. **Turnkey graphics-pod recipe** gated on a single `vulkaninfo` probe (template `=all`/graphics → probe → Xvfb :99 + `-RenderOffScreen`); NOT urgent (milestone 1 needs no pixels). (2) **G-H measured experiment (P1.5): test-suite I/O cost** — cold 40.6 s vs warm 10.7 s (reported-test 9.2 s; stack src only 0.44 MB) → G-E cost is Google-Drive **hydration latency**, not compute; falsifier refuted. Fix: **pin `stack/` to Drive "Available offline"** → ~30 s/run saved across all 6 agents, 0 code (Sayed, 1 click). (3) **Increment (G-E): `profile_testsuite.py`** intake (`profile`/`check` regression guard, stdlib-only) — 9 pkg tests (0.30 s) + end-to-end `check` OK (181 passed, exit 0). (4) Tooling: **AlpaSim now public** (`NVlabs/alpasim`) — Phase-1 watch, verdict unchanged (40–60 GB VRAM, P5). No hypothesis status change (G-D N/A). | `.../Tools&DevEnv/Research/2026-07-09-carla-render-blocker-and-testsuite-io-cost.md`, `.../Tools&DevEnv/Implementation/incoming/2026-07-09-testsuite-io-profiling/`, KB + STATE + BACKLOG |
| 2026-07-09 (Thu 06:43) | Benchmarks & Eval agent | **Independent-test audit of the first live CARLA SC-01 run** (`2d87acb`) + metric fix (measured, local CPU, numpy-only, <3 s, $0 — G-H). The live run flagged LAL broken (both policies −0.7). (1) **LAL-v2 shipped** (intake, 7 analytic-GT tests, standalone-green): LAL-v1's −1.5 m/s³ hard-jerk trigger is **blind to smooth comfort-bounded anticipation** — reproduced the −0.7/−0.7 null and located the collapse cliff **exactly at −1.5 m/s³** (an ease-off smoother than ~4 s never fires it); LAL-v2 (deceleration-onset by speed drop, the pre-LoS generalization of TTB) gives **+0.3…+3.1 s anticipation lead vs −0.3 s reactive** across the whole sweep. (2) **SC-01 LOPS 0.834 recomputed** = analytic E 0.8325 of the injected σ=0.3 noise (inside 95% CI, N=5000) → reproducible, but reactive 0.0 is **structural** → presence not quality (P8). (3) **OKRI ≥3-seed CI-separation rule made numeric** (gap 19.5; ~2 seeds at SD≈5). LEADERBOARD: NAVSIM-v2 navhard refresh (DriveFuture 55.5 / DrivoR 56.3) + first **Live SC-01 block** + **competitor efficiency block** (W-05). Ledger H15 instrument-hardening (no upgrade, P8). | `.../Benchmarks & Eval/Implementation/incoming/2026-07-09-lal-v2-anticipation/`, `.../Benchmarks & Eval/Research/2026-07-09-sc01-live-metric-audit-and-lal-v2.md`, `Benchmarks & Eval/LEADERBOARD.md`, `REGULATION_TRACE.md` |
| 2026-07-09 (overnight + 06:15 report) | Loop (evening) + incident (night) | Evening: **K-step rollout lever landed** (config→trainer→contract→bake-off lever runnable; 181 tests) `c4375f8`; **Colab burst compute LIVE** (Sayed OAuth + T4 validated 33 s cold-to-done) `a604b21`; **SC-01 first live CARLA measurement** on pod2 real physics `2d87acb`; checkpoint relay via private HF verified; CARLA **Windows** build installed locally (rendering pivot: 3 RunPod pods — Community AND Secure — inject compute-only driver stacks). Night incidents (honest): (1) **trainer I/O-starved** after the 88 GB cache-tar job polluted the cgroup page cache — ~50 steps in 5 h; restart escalated (§4); (2) HF cache upload died on free-tier commit quota; (3) loop wakeup chain broke ~01:00 (turn closed without re-arm) — no iterations 01:00–06:00; rule: EVERY loop turn re-arms before closing; (4) local CARLA wedged in first-boot shader compile; relaunched -dx11. | §4 escalation, `test_kstep_rollout.py`, `colab_burst/README.md` |
| 2026-07-08 (Wed) | Architecture & Inference agent | Two deliverables. (1) **Bake-off harness** (intake, backlog #2, WP3): OFAT one-lever-per-run driver — each variant is the base config with EXACTLY one field flipped (verified by recursive `lever_diff`; a lying lever raises), scored through the D1–D3 gate runner so a **BLOCKED** gate = NO claim (D-004/G-AI1); multi-seed mean±CI; measured-params only (G-AI2). 8 config-native levers + 4 `planned` levers (AdaLN/RoPE/K-step-K≈4/tactical-MoE-on-σ) carrying gate+hyp+WP pointer. **16 pkg tests; MVP loop triaged `integrate` → `stack/tanitad/eval/bakeoff.py` (suite 178 green).** Decision-grade lever sweep needs matched-compute trained arms = pod2 Phase C. (2) **G-H measured experiment — spectral-sizing on the step-6500 trained ckpt** (BACKLOG P0.1, awaiting-a-trained-ckpt for 2 runs): 24 val eps / 7,176 pairs / 4060 → fit R²=0.99, operator rank ≈43, knee 31, k*=21 → **OVER-PROVISIONED** 2048 readout (task-relevant transition rank is ~tens). Rank still climbing (35→43 over 3k→6.5k) → decision-grade evidence for **D-021**, but default holds (keep 2048, re-measure at final Stage-0). No change executed (D-004/D-018). Lit: Delta-JEPA (2606.31232) = our A4/A5 independently; AdaLN/RoPE triangulated (2512.24497/OmniDreams); K≈4 rollout Pareto. Ledger H3/H4/H5/H1 evidence rows (no status change, P8). | `.../Architecture & Inference/Implementation/incoming/2026-07-08-bakeoff-harness/`, `.../Research/2026-07-08-bakeoff-harness-and-conditioning-levers.md`, `.../Research/2026-07-08-spectral_step6500.json` |
| 2026-07-08 (Sat) | Production & Optimization agent (run #1) | First run of the Saturday prod-opt stream (D-020 §3). (1) **ONNX export of the operative path DONE** (backlog P0.2): encoder+readout `[1,9,256,256]→[1,2048]` and predictor `(states[1,8,2048],actions[1,8,2])→(z1,z2,z4)` export **clean at opset 17 (legacy) AND opset 18 (dynamo, torch-2.11 default)** on `ckpt_full.pt`; PyTorch parity max\|Δz\| **8.8e-6 / 1.2e-5** (tol 1e-4); **zero unexportable ops** (MHA/FiLM/causal-triu supported) → TensorRT-on-Orin path unblocked. G-P2 honest note: ORT-CPU 1.4–4.4× SLOWER than Torch-CPU — ONNX value = portable TRT IR, not CPU speed. (2) **Compliance review #1 `stack/tanitad/data/`** (backlog P0.3): found+proved a **cache-key collision** in `epcache` (same silent-wrong-data class as the cosmos chunk-pairing bug: chunk_0/chunk_1 same-basename sets hash identically) + `save_episode` fail-fast guard; intake pkg (12 tests) → **orchestrator integrated (integrate-with-changes)** into stack/ with a legacy-cache read fallback; **suite 178 green**. PRODUCTION_READINESS + KB + STATE + BACKLOG updated. | `.../Production & Optimization/Research/2026-07-08-onnx-export-and-data-compliance.md`, `.../Implementation/onnx_export/`, `.../Implementation/incoming/2026-07-08-data-cluster-compliance/`, `stack/tanitad/data/{epcache,mixing}.py`, `stack/tests/test_epcache_key.py` |
| 2026-07-08 (loop, afternoon) | Autonomous loop iterations (MVP) | Four measured results on top of D-020: (1) **cosmos chunk-pairing bug found+fixed** — 28/60 extracted clips are chunk-1 and were getting chunk-0 actions; temporal semantics settled (121-frame chunks @30 Hz, 10 s/300-pose clips; T=39 exact); verified on real bytes; cosmos cleared for D-010 mix. (2) **SC-05/D8 preview** (step-6500 ckpt, 4060): naive relative imag-error inverted (AUROC 0.34) — confounds identified; **matched-pairs redesign measured: 16/23 same-scene clear→degraded pairs show higher imagination error (p≈0.047)** — first directional positive for H11; pre-registered re-runs at 15k/30k. (3) **I8 batch-1 latency baseline**: decision tick **15.07 ms p50 / 1.08 GB VRAM** fp32 un-optimized (~66 Hz vs 10–20 Hz req.); first real CNCE (median 2.02×10⁵) + TMS expert band → LEADERBOARD efficiency block + paper §7. (4) **Scenario→telemetry→metric wiring PASSED** end-to-end on the work-zone oracle (all 5 discriminative checks). Stack: **149 tests green**. Training healthy all day (single trainer, ~300 steps/h). | `stack/scripts/{d8_preview,cosmos_pairs,latency_cnce_baseline,scenario_suite_dryrun}.py`, `stack/experiments/{p0-d8-preview,p0-latency-baseline,p0-scenario-wiring}/`, LEADERBOARD, paper §7 |
| 2026-07-08 | D-020 program extensions (MVP orchestrator) | All five Sayed proposals executed. (1) Hub **deep-screen digest**: all 6 intake packages verified LIVE; findings routed (K-step/RoPE bake-offs → Architecture backlog, ZOD/synthetic-scenarios → DataEng backlog, D-021 proposed). (2) **`Paper/TANITAD_PAPER.md` v0.1** — living postdoc-level paper (4B architecture + SigReg/Epps–Pulley math, imagine-and-select + calibrated decode, H15 NLL, spectral theory, instrument doctrine, step-5000 results honestly bounded). (3) **Production & Optimization stream**: Master Plan §3 row, Saturday agent (06:44) + `PRODUCTION_READINESS.md` + backlog (latency baseline I8, ONNX parity, compliance review #1 = data/). (4) **Agent depth upgrade**: `_common-protocol.md` — mandatory measured experiment per run (gate G-H), burst-compute section (Colab CLI/idle-pod etiquette/4060), budgets 3→4 iters/2→4 h; per-discipline `BACKLOG.md` seeded ×6. (5) **`SCENARIO_DATABASE.md`**: SC-01…SC-12 from documented opponent failures (FACT/CLAIM/INFER), lifecycle → excellence-proven, joint Opponent↔DataEng↔Benchmarks duty wired into agent files. D-020 recorded; D-021 proposed (§4). | `Paper/TANITAD_PAPER.md`, `TanitAD Research Hub/2026-07-08-screening-digest.md`, `.../agents/production-optimization-agent.md`, `.../Production & Optimization/{BACKLOG,PRODUCTION_READINESS}.md`, `.../Opponent Analyzer/SCENARIO_DATABASE.md`, 6× `BACKLOG.md`, `DECISIONS.md` D-020/D-021 |
| 2026-07-14 | Data Engineering agent | **Cosmos-Drive-Dreams loader** (intake, D-014 sim arm): the license review (D-002) excluded real PhysicalAI-AV from public claims, so shipped a loader for the **CC-BY-4.0** synthetic corpus — the one *publicly-claimable* rich AV asset. No CAN → `poses_to_signals` derives steer (bicycle `κ=yaw_rate/v`, low-speed-clipped)/accel/yaw/v from per-frame 4×4 `vehicle_pose`; D-015 9-ch, D-016 focal (120°=PhysicalAI), **`CORPUS_META`≡comma2k19 (D-017 I7)** → admissible in the D-010 real+sim mix (proven by test); CLIP-level split (I3), per-weather episode_id. **9 pkg tests + stack 73✓/1s**, 0 new deps. Created **`DATASET_LANDSCAPE.md`** (D-012 standing duty, was missing): 3 tiers, license/size/actions/urban-richness/cost-to-batch. Lit: LAWM/Drive-JEPA/HiLAM/CLAW/DeFI H7 surge (external support, no upgrade, P8). P8: pose axis-order pod-verified via `verify_real_clip` before any trained claim. | `.../Data Engineering/Implementation/incoming/2026-07-14-cosmos-drive-dreams-loader/`, `.../Data Engineering/Research/2026-07-14-cosmos-drive-dreams-loader-and-landscape.md`, `.../Data Engineering/Research/DATASET_LANDSCAPE.md` |
| 2026-07-17 | Opponent Analyzer agent (run #1) | First live sweep. Built `WEAKNESS_CATALOG.md` v1 (W-01…W-07) + `OPPONENT_PROFILES.md` v1 (Wayve/Waymo/Pony/Momenta/Autobrains/Alpamayo/Tesla), all FACT/CLAIM/INFER-labeled (G-O1). Headline deltas: Waymo **3,871-vehicle construction-zone recall** (freeway autonomy suspended, 20+-city expansion frozen) → new **Work-Zone Phantom** scenario (W-01→H15/H9/H1, intake pkg, **9/9 offline tests**); NTSB school-zone VRU + school-bus stop-arm probes (W-02/W-03); Tesla open NHTSA **degraded-visibility** case (W-04→H11/H15/H2); Wayve Series D corrected to **$1.2 B/$8.6 B**, GAIA-3=15 B offline WM; NVIDIA **Alpamayo-2=32 B** on-car VLA (W-05 foil, still our supply chain); arXiv **latent-WM/JEPA surge** → "world model" no longer differentiating, moat=hierarchy+efficiency+imagination+self-monitoring. Recs to Thu agent: wire Work-Zone Phantom, add degraded-visibility D8 stressor, competitor param counts→CNCE leaderboard. Ledger H0/H6 evidence row (no upgrade, P8). Loop: 12/25 searches. | `.../Opponent Analyzer/Research/2026-07-17-opponent-sweep-w2.md`, `WEAKNESS_CATALOG.md`, `OPPONENT_PROFILES.md`, `.../Opponent Analyzer/Implementation/incoming/2026-07-17-work-zone-phantom-scenario/` |
| 2026-07-16 | Benchmarks & Eval agent | Custom metric suite shipped (intake, D-011): **LAL/TMS/OKRI/CNCE/LOPS** (Deep Think 14) + trajectory `extra_metrics` seam that plugs into Wednesday's D1–D3 gate runner — **verified live** against `run_d1`; 22 tests all on **analytic ground truth** (G-B2). Research: **open-loop ADE/FDE ⊥ closed-loop DS** (arXiv 2605.00066, ranking inversions) → validates decode-gates-necessary-not-sufficient + the custom-metric thesis; NAVSIM-v2 EPDMS 51.3 navhard (thin on occlusion = our OKRI/LOPS niche); Bench2Drive SOTA ctx (TF++ 86.97/71.97); CARLA ~5 DS seed variance → **≥3-seed mean±CI** rule adopted; WP.29 June-2026 ADS GTR (ISMR/DSSAD sub-asks). LEADERBOARD gains a separate closed-loop block + weak-claim footnote; REGULATION_TRACE enriched. Consumed **D-014** mid-run (MetaDrive retired) → suite is sim-agnostic, scenarios re-target CARLA-on-pod. Note: auto-commit swept my files into `5940129`/`47a89c4`; `hub(bench-eval)` reconcile = `51b432b`. | `.../Benchmarks & Eval/Implementation/incoming/2026-07-16-eval-metric-suite/`, `.../Benchmarks & Eval/Research/2026-07-16-*.md`, `Benchmarks & Eval/LEADERBOARD.md`, `Benchmarks & Eval/REGULATION_TRACE.md` |
| 2026-07-14 | Architecture & Inference agent | Two intake pkgs (D-011): **#0 `p0-spectral-sizing`** — latent-dim sizing from the action-conditioned transition spectrum (L2/2606.27014; knee vs 2048 readout; 8 tests) and **#1 D1–D3 gate runner** — instrument-doctrine gating (BLOCKED≠FAIL; I1–I4 first; vs-pool & probe A3 ablations; extra_metrics seam for Thu; 13 tests). Research: decode ≠ planning (2512.24497 → D1–D3 are necessary-not-sufficient, D4–D6 arbitrate); V-JEPA-2-AC 300 M ≈ our envelope; LeWM supports SIGReg-only (H3); DriveMoE/GEMINUS → route MoE on H15 σ (H2); native-TRT ViT INT8 trap → OwLite/ModelOpt (H5). Consumed mid-session D-012/D-013 + JEPA-theory note. Stack suite 65✓/1s | `.../Architecture & Inference/Implementation/incoming/2026-07-14-{spectral-sizing-p0,gate-runner-d1-d3}/`, `.../Architecture & Inference/Research/2026-07-14-*.md` |
| 2026-07-13 | Tools&DevEnv agent | D-010 sim arm unblocked: MetaDrive front-camera RGB path (6ch/256 2-frame stacks, comma2k19-identical geometry/alignment) + scripted perturbation policy (off-expert coverage) + occluder(H15/D9)/blocked-route(D5/D6) scenario configs; proved the old 1ch BEV adapter is rejected by `MixedWindowDataset`. Intake pkg (17 tests, 0 new deps, import 1.38 s); live rollout still gated on supervised MetaDrive source-install. KB: Alpamayo 2 Super (32B) + OmniDreams = Phase-1 watch, reinforce P5/C2 | `.../Tools&DevEnv/Implementation/incoming/2026-07-13-metadrive-frontcam-perturbation/`, `.../Tools&DevEnv/Research/2026-07-13-*.md` |
| 2026-07-07 | Data Engineering agent | comma2k19 (D-009) real-data-validated: `av` decodes real HEVC @~105 fps, A8 real≈0.053 → change-weighting justified; forked contract reconciled into shared `_contract` (channels param) + `test_comma2k19_contract.py`; comma2k19 exported; Windows `\|` confirmed (handled by `extract_comma2k19.py`); H7 deltas (LAOF, Sensorimotor-WM); data card + note | `.../Data Engineering/Research/2026-07-07-*.md`, `stack/tanitad/data/_contract.py`, `stack/tests/test_comma2k19_contract.py` |
| 2026-07-06 | D-008 scale-up | Model scaled to 261 M (measured per-component budget in Phase 0 Plan §2.1); H15 imagination in Phase 0 (module + losses + D9); exact data spec; RunPod runbook + ledger row; local 261 M pipe-check run | `stack/tanitad/models/imagination.py`, `stack/RUNPOD_RUNBOOK.md`, DECISIONS D-008 |
| 2026-07-06 | Tools&DevEnv agent | WP2 MetaDrive→toy-contract wrapper (17✓/1skip); MetaDrive install verdict (PyPI no-go py3.13, source is GO); AlpaSim/AlpaGym = Phase-1 cloud (40–60 GB VRAM); Rerun.io picked for viz | `stack/tanitad/data/metadrive_env.py`, `TanitAD Research Hub/Tools&DevEnv/Research/2026-07-06-*.md` |
| 2026-07-05 | Kickoff | Repo analysis, initial research, plans, stack scaffold, hub setup, first push | see §2 table |
