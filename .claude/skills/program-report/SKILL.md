---
name: program-report
description: Compile the detailed TanitAD program report (progress, agents, next steps, decisions) — chat + committed file + push
---

Compile the full D-025 program report NOW, exactly as the 07:57/12:57/17:57 cadence does:

1. Fresh measurements (parallel): pod1 step + 3-min pace; pod2 arm steps + any interim numbers;
   running workflows (TaskList); latest gates row; local machine state.
2. Agent updates & knowledge transfer table: check `git log --since` of the last report +
   the hub's newest `Research/*.md` and `Implementation/incoming/*` — what each agent produced
   and whether it's integrated (✅) / queued for triage (🔶) / rejected (❌), with the reason.
3. Program position vs the Master Plan phases and the four edges (planning, efficiency,
   safety/self-knowledge, data efficiency) — evidence grade per edge.
4. Ordered next steps; decisions required from Sayed WITH defaults; incidents honestly.
5. Write to `Project Steering/Reports/YYYY-MM-DD-HHMM-program-report.md`, commit+push,
   present the same content in chat, and send a PushNotification with the headline.
