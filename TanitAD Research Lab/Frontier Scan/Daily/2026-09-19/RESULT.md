<title>Frontier Scan 2026-09-19 — a posted-limit supplier exists, the target beats the pixels, and the action head must not bypass the world model</title>

# Frontier Scan · 2026-09-19 (LAB-RUN-016)

`22/22 tracks scanned · Band D ×7 claims (NVIDIA/Jim Fan ×3, Waabi ×2, Mobileye ×2), all new to the register · FULL TEXT ×3 local pypdf (DeepSight, DriveZero re-read, LGS re-read) + 3 HTML primaries via summariser (World Tokens, DriveVLA-M0, OneWM-VLA) · 11 empties named · Library 489 → 521 (7 banked today + 26 orphans from 09-17/09-18 recovered) · 1 correction to 09-18`

⚠️ **Scope of this run:** the PI asked for **the frontier scan**. **Today's four domain packages were NOT produced**, so gate half (a) is still open for 2026-09-19. That is stated here rather than hidden.

---

## 0 · Findings first — the seven that change something

| # | finding | class | moves |
|---|---|---|---|
| **1** | ⭐⭐⭐ **THE MM STANDING QUESTION HAS ITS FIRST POSITIVE ANSWER: two open corpora carry a MAP-SOURCED posted speed limit per lane, with zero ego provenance.** nuPlan: `speed_limit_mps` is a per-lane property (`nuplan/common/maps/nuplan_map/lane.py`, `Optional[float]`). WOMD: `LaneCenter.speed_limit_mph` (`optional double`, field 1, *"The speed limit for this lane"*). ⚠️ **Coverage is UNMEASURED and partial by construction**: nuPlan's own getter guards NaN (`speed_limit == speed_limit`). NAVSIM is built on nuPlan logs, so **the NAVSIM map files are the cheapest route to it** | PUBLISHED (source code, read 2026-09-19); coverage **HYPOTHESIS** | MM standing question (09-10) · the max-speed input channel · **PI queue**: a non-parity corpus for one head is a parity/admissibility decision |
| **2** | ⭐⭐⭐ **Two independent CONTROLLED ablations say the world-model TARGET matters more than its pixel fidelity. Semantic-feature targets beat pixel/texture targets for ACTION.** DeepSight `2605.10564` (full text), Table 3, Dev-10 routes: VAE-codebook target **27.75 DS** vs DINOv3-feature target **74.79** (1 frame); **14.66 vs 86.57** (5 frames). The VAE *degrades* with more frames (−13.09) while the latent target *gains* (+11.78). World Tokens `2608.09730`, Table 2, LIBERO-Long: an **RGB** anchor scores **91.5, below the no-world-model baseline (92.8)**; the full design scores **97.0** | PUBLISHED (DeepSight full text; World Tokens HTML via summariser) | ⭐ **combines directly with the PI's SAM3-map-as-BEV-GT direction (2026-09-13)**: DeepSight's GT is `DINOv3(BEV-rendered image OR segmentation map)` at t+0.5…2 s. **FS19-3**. Refutes as stated Jim Fan's pixel-coupling claim (N-19-2) |
| **3** | ⭐⭐⭐ **A world-model loss helps ACTION only if the action head cannot BYPASS the representation it shapes.** World Tokens: a world model **without** exclusive routing (**94.1**) scores **below** exclusive routing **without** a world model (**95.0**). Both together: **97.0**, superadditive. Real robot (R1 Pro, 96 trials/arm): **59.4 → 76.0 %** vs a **matched action-only baseline**. The world-model branch is **removed at deployment**, and latency is **61.85 ms vs π0.5 56.32 ms** | PUBLISHED (HTML via summariser); ⚠️ **one training run per arm, no CIs**: the `H-ESTIM-SEED-1` class. LIBERO binomial SE ≈ 0.9 pp on 500 episodes; training variance unknown | ⭐ **our bypass/echo defect family** (nav-echo 369/369, the 97.9 %→0.0 % action echo): a structural remedy published with a controlled ablation. **FS19-1**, S-8, T-10 |
| **4** | ⭐⭐ **B12: the planning literature's "long horizon" is 2 s.** DeepSight, titled *Long-Horizon World Modeling*, predicts **5 frames × 0.5 s = 2 s**. Minute-scale rollouts exist only in **video generation** (HorizonDrive ~1 min, ReWorld, Matrix-Game 3.0), never as a **planning** world model. ⇒ Our **6 s** tactical horizon is already **3×** the published planning "long", and **no published planning WM at 30 s was found** (strategic tier) | PUBLISHED + analysis | our horizon claims are a **differentiator, not a catch-up**; stamp this in the paper. The strategic-tier WM is **unprecedented at planning grade** (as of this scan) |
| **5** | ⭐⭐ **Injected I-3: published test-time adaptation in driving is LABEL-SUPERVISED, not label-free.** DriveVLA-M0 `2608.10413`: its memory holds **training** failures found by **oracle PDM scoring** (`Q < 0.5`). At test time, LoRA is updated from the retrieved cases' **stored expert labels**. Nothing is computed from the test scene's own future. **+1.3 PDMS** (91.0 → 92.3), LoRA backward **26.44 ms**, navhard **47.0 EPDMS two-stage**, ego at inference | PUBLISHED (HTML via summariser) | **I-3 served (partial)**: a route that **meets the auditability constraint** (every update traces to a stored, labelled case, which suits ISMR/DSSAD). It **drops I-3's "without labels" premise**, and **the label-free half stays unserved** in the driving literature |
| **6** | ⛔ **CORRECTION to 09-18 finding 1 (LGS `2602.11229`), from the banked PDF read locally.** *"LGS wins 0/16 systems at 1 step but 16/16 at 20"* **does not appear in the primary**; it was a summariser artifact. The primary says LGS **matches** the strongest deterministic baseline at 1 step, **wins 15/16 systems at both 5- and 10-step rollout**, and cuts 20-step L2RE **56.1 % → 30.2 %** at **13–77× less** dynamics-step compute. **Verified as stated:** +145/+155 % (flow removed), +26/+29 % (noising `k` removed), ≤ 16 % 1-step cost, and the `L_T(1−k) < 1` bound (eq. at pdf-text l.280–288). **All DriveZero numbers verified** (57.1 Scale / 51.5 base, both navhard two-stage; +0.53 vs DINOv3 ViT-S; 93.61 < 93.92 → 94.41; 5.70 M teacher; ego kinematics + nav command at inference) | MEASURED (local pypdf re-read) | `LEDGER_B11` correction entry appended; **the FS18-5 experiment is unaffected** (its committed read used the ≤ 16 % figure, which stands) |
| **7** | ⭐⭐ **Gradient planning through a FROZEN JEPA-class world model beats sampling: LEAP `2609.03294`**, LeWM + quasi-Newton over the whole action horizon, **77.5 → 94.8 %** mean success vs LeWM + CEM (4 domains, matched protocol) | PUBLISHED lib `2609.03294` **abstract-only** | ⭐ **combines with the refav1 inference-seed floor ≈ 0.30 m** (`D-REFAV1-SEED-GOAL-MISMATCH`): a deterministic optimiser from a fixed init **removes the third variance source** (inference) rather than averaging over it. **FS19-4** |

---

## 1 · ⭐⭐ Band D — three opponents, seven claims, all new to the register (§7.1)

### 1a · NVIDIA (Jim Fan, GEAR) — *"VLAs are dead, long live World Action Models"*
**Document.** Talk at Sequoia **AI Ascent 2026**, read through a verbatim-quoting summary post (eventual.ai, 2026-05-08) ⇒ **`RELAYED`**. The talk video is linked (`youtube.com/watch?v=3Y8aq_ofEVs`) and was not transcribed. Companion: NVIDIA Technical Blog *"Pretrained to Imagine, Fine-Tuned to Act"* (2026-06-15, `PUBLISHED-BLOG`, read in full). **Context:** NVIDIA sells the compute that video-backbone WAMs need, and its robotics lab ships DreamZero/GR00T.

| id | claim | experiment? | confirming (independent) | contradicting (actively sought) | verdict | binds on us? | flips it |
|---|---|---|---|---|---|---|---|
| **N-19-1** | *"Rest in peace [VLAs]. Long live world action models."* Video-WM pretraining supersedes VLM-backbone VLAs | ⛔ **NO.** The NVIDIA blog **concedes** it: *"This is not proof that WAMs are the better default"*, and its only comparison (DreamZero **1750** vs π0.5 **1622** RoboArena Elo) is **not matched** on data or params | ⭐ **World Tokens `2608.09730`** (Tang et al., not NVIDIA): **matched** action-only baseline, 59.4 → 76.0 % real-world | ⭐ **NVIDIA's OWN AV line**: Alpamayo 2 Super (2026-05) is a **34 B reasoning VLA**. The company's driving product did not follow its robotics lab's doctrine. Also the blog's own latency concession (row N-19-3) | **CONTESTED.** *WM objectives help action* is **SUPPORTED** (one matched ablation). *"VLAs are dead"* is **UNSUPPORTED-AS-STATED** | ⭐⭐ **YES, favourably, with one boundary.** We are a world model, not a VLA. ⚠️ Their WAM is a **pixel video backbone (Wan-14B)**, and that half does **not** bind (see N-19-2) | a matched-data, matched-params VLA vs WAM comparison in the real world where the VLA wins |
| **N-19-2** | *"If the video prediction works, the action works. If the video hallucinates, the action fails."* and *"physics emerges by predicting the next blob of pixels at scale"* | ⛔ **NO** (anecdote) | NVIDIA blog: *"video-generation quality is a requirement for good policies"*. ⛔ Same org, **not independent** | ⭐⭐ **Two controlled ablations, both independent:** DeepSight T3 (pixel/VAE target 27.75 vs semantic 74.79 DS) and World Tokens T2 (RGB anchor 91.5 **<** no-WM 92.8). ⭐ **Physics-IQ `2501.09038`**: physical understanding *"severely limited, and unrelated to visual realism"* across Sora/Runway/Pika/Lumiere/SVD/VideoPoet | **UNSUPPORTED-AS-STATED** for pixels: the coupling runs through the **target**, not through pixel fidelity. One contradiction outweighs the confirmations (V-6), and here there are two | ⭐⭐ **YES — CONFIRMS-US** (latent/semantic targets; JEPA family) | a matched ablation where a pixel-reconstruction target beats a semantic-feature target on **closed-loop** action |
| **N-19-3** ⭐ | **CONCESSION** (NVIDIA blog): *"3–4x slowdown at inference time, which matters a lot for real-time control"*; Fast-WAM **590–800 ms** vs π0.5 **~190 ms** per chunk; *"Good luck trying to run 10B+ WAM models on your local GPU"* | admission | World Tokens removes the WM branch at deploy (**61.85 ms**, 1.1× π0.5) | — | **SUPPORTED** | ⭐⭐ **YES** — the deploy-loop cost of a pixel WAM is incompatible with a 100 ms budget on Thor. Our thesis is **train-time WM, sub-300 M deploy** | a pixel WAM inside 100 ms on an edge SoC at matched quality |

### 1b · Waabi — *"Simulator realism: the new safety standard for the AV industry"* (Urtasun, 2025-03-11, `PUBLISHED-BLOG`)
**Context:** Waabi argues its sim-based safety case to regulators and to trucking customers. The post is a **measurement claim**, which makes it rarer than most doctrine.

| id | claim | experiment? | confirming (independent) | contradicting (actively sought) | verdict | binds on us? | flips it |
|---|---|---|---|---|---|---|---|
| **WB-19-1** | Waabi World reaches **99.7 % realism**, defined as `(1 − average relative distance) × 100` between the full autonomy stack's closed-loop trajectory in sim and its real trajectory on the same scenario; therefore sim can *"dramatically reduce the volume of driving in the real world"* | ⭐ **PARTLY.** It is a **paired** sim-vs-real measurement, a real counterfactual and **not** attribution-from-exposure. ⛔ But it reports **no scenario count, no module breakdown, no CI, no third party** (5 per-scenario scores shown, 99.0–99.8) | pseudo-sim R² **0.8** vs closed loop (`2506.04218`, held); ladder `2607.07196` (L2 envelope) — trajectory-level sim/real agreement **is** achievable | ⭐ **Structural:** an **average** relative distance is dominated by nominal driving. A rare divergent event moves it by ~1/n, and that event is the one a safety case exists for (our *oracle-gap-needs-a-random-control* family: the mean hides the tail). Independent: the reality gap is **per-facet** (perception / prediction / planning, `2509.22379`) and one scalar does not certify all three | **UNSUPPORTED-AS-STATED** as a *safety standard*. The number may be true; it does not license what it is used for | ⭐ **YES, as INSTRUMENT DESIGN** — **D-12 (AlpaSim fidelity)** must not adopt an average-trajectory realism score | Waabi publishing realism on the **safety-critical subset**, per event, with n and CI |
| **WB-19-1c** ⭐ | **CONCESSION** (same post): *"variations in actor behavior, differences in simulated sensor data, differences in the latency of the system"* compound trajectory differences | admission | — | — | **SUPPORTED** | ⭐ YES — the three error sources are exactly the ones our T1 tier cannot see (no reactive actors, no re-rendering, no latency). This is `EVAL_DOCTRINE` T1 ≠ T2, in an opponent's own words | — |

### 1c · Mobileye — *"Autonomous decisions: the bias-variance tradeoff in self-driving technology"* (Shashua & Shalev-Shwartz, 2024-05-15, `PUBLISHED-BLOG`)
**Note:** this post extends the already-registered **M-1** (Compound AI) and cites the unread **D-7** (`1604.06915`). It is adjudicated here because the register had **zero** rows for it.

| id | claim | experiment? | confirming (independent) | contradicting (actively sought) | verdict | binds on us? | flips it |
|---|---|---|---|---|---|---|---|
| **M-19-1** | "Zero-bias" E2E needs data **exponentially** larger than an engineered system whose abstractions add bias but cut variance; *"the amount of data required for each subsystem is exponentially smaller"* | ⛔ **NO** — learning-theory argument and analogy (calculator vs learned arithmetic); its formal source is their own `1604.06915` (**not independent**, D-7) | DriveZero `2609.06055` **decomposes** into separately pretrained perception + action and reaches navhard SOTA. **Ours, MEASURED:** a structural prior (speed as a 3rd action channel) took REF-A **3.73 → 0.83 m** — structure bought sample efficiency | Tesla/Wayve scale deployments (T-18-3, RELAYED); **DeepSight** is a monolithic E2E VLA that tops Bench2Drive — but only with **structure-shaped auxiliary losses**, which is Mobileye's own recommendation (M-19-1c) | **SUPPORTED** as a sample-complexity argument; **CONTESTED** as a system claim (no matched experiment on any side) | ⭐ **YES, favourably** — our hierarchy is "E2E trained, structure-shaped heads" | a matched-data comparison where a pure E2E net with **no** structured heads matches the structured one at small data |
| **M-19-1c** ⭐ | **CONCESSION ×2:** *"Waymo is clear evidence that the bias of an engineered system is sufficiently small"*, and E2E nets *"can include sensing-state branches … without sacrificing the E2E label"* | admission | — | — | **SUPPORTED** | ⭐⭐ YES — Mobileye itself concedes the E2E-vs-modular dichotomy is **not the real axis**; the axis is **what the intermediate supervision is**. That is our design question, and N-19-2 / finding 2 answer part of it | — |
| **M-19-2** | Tesla v12.3.6: *"~300 miles per critical intervention … MTBF of roughly 10 hours"*, *"6 orders of magnitude"* from a 10⁷ h target | ⛔ NO — crowd-sourced public tracker (RELAYED), **2024 vintage** | — | FSD is now v14 (T-18-1); the figure is stale by ≥ 2 major versions | **UNSUPPORTED-AS-STATED** today (historical only) | **NO** | a current, audited intervention rate |

### Guidelines derived
* **S-8 (strategic)** — *"World model at TRAINING time, not in the deploy loop."* Three independent lines: World Tokens (WM branch removed, 1.1× VLA latency), NVIDIA's own 3–4× latency concession, and our sub-300 M thesis. **Falsifier:** a v7-tiny arm where planning-by-rollout through the predictor beats the train-time-only WM auxiliary **at matched deploy latency**.
* **T-10 (tactical)** — *Route the action head ONLY through the WM-shaped representation (exclusive routing), and give every arm a BYPASS control.* A WM loss the action head can route around is decoration (94.1 < 95.0). **Falsifier:** FS19-1 reading bypass ≥ routed.
* **T-11 (tactical)** — *D-12 fidelity is stated per event on the safety-critical subset, with n and CI. Never as an average trajectory distance.* **Falsifier:** evidence that average and tail realism rank simulators identically (ρ ≥ 0.9 over ≥ 5 simulators).
* **T-12 (tactical)** — *WM prediction targets are semantic features (DINOv3-class, or DINOv3 of a BEV raster), never pixels.* **Falsifier:** FS19-3 reading pixel ≥ semantic.

### ⭐ Opponent STRENGTHS recorded (required)
12. **Waabi has paired sim/real replay infrastructure** (a digital twin of its own truck driven on real sensor data) **and publishes a quantitative sim-real number.** We have neither: AlpaSim is unprovisioned, and D-12 has no measurement yet.
13. **NVIDIA runs both paradigms at scale** (DreamZero/GR00T WAMs **and** the Alpamayo 34 B VLA), so it can hedge the VLA/WAM question with compute we do not have.
14. **Mobileye's decomposition argument has a decade of production behind it** (EyeQ fleet). Its concession on sensing-state branches shows it has already absorbed the E2E critique, which makes it a harder opponent to caricature than the headline suggests.

### Debts
**D-4** (UNECE primary): routes **9–13 all 403**, not bypassed ⇒ STANDS. **D-13** (Elluswamy transcript): 2nd probe EMPTY ⇒ STANDS. **D-14 (new):** Waymo *Reference Driver* (2026-06, blog + Nature Comms) has never been adjudicated. It is a claim about **how to benchmark an AV against humans**, which is squarely A5.

---

## 2 · DEEP items — five dimensions

### B12 · DeepSight `2605.10564` (FULL TEXT, local pypdf) ⭐⭐ — pre-committed rotation #1, discharged
| dim | |
|---|---|
| RELEVANCE | B12 horizon question; the WM-target choice (A2/A3); SAM3 BEV-GT direction; tactical/strategic horizon claims |
| CONSEQUENCE | (i) "long horizon" = **2 s** in planning WMs (finding 4). (ii) The biggest measured lever in the paper is the **target** (VAE → DINOv3: **+47.04 DS**, 1 frame), bigger than adding frames (+11.78) or CoT (+11.71 without WM, **+1.71 on top of WM**: 84.52 → 86.23). (iii) The WM adds **+3.57 %** latency (parallel world queries, one forward pass) |
| COMBINATION | ⭐ **GT = `DINOv3(BEV render or segmentation map)`.** We are building exactly such a raster from the SAM3 map (PI 2026-09-13, production on Thor since 09-15). ⇒ the SAM3 corpus gives us, at no labelling cost, a **DeepSight-style semantic BEV target** for the tactical predictor. ⚠️ Admissibility: the target is **label-side** (allowed to use anything). The predictor's **input** must stay vision-only; DeepSight feeds **ego state + target point** at inference, which is **inadmissible** for us |
| CHANCES / RISKS | Upside: the single largest published effect for a cheap change of target. ⛔ Risks: all ablations are on **Dev-10 routes, one seed**, and the λ_world sweep alone moves DS by **7.6** (78.93 / 86.57 / 79.82) — i.e. its own hyperparameter noise is comparable to the +8.8 BEV-vs-front-view effect. Bench2Drive is CARLA; the 3 B Qwen2.5-VL base is 10× our budget |
| EXPERIMENT | **FS19-3 (v7-tiny, 3 arms + replicate):** tactical predictor target ∈ {pixel/VAE recon · frozen-DINOv3 of the front camera · frozen-DINOv3 of the SAM3 BEV raster}, horizon 2 s **and** 6 s, matched params and steps. **Committed:** semantic beats pixel on T1 h=6 s ADE **and** on the lateral + longitudinal families, by more than the A0/A0b replicate floor. Pixel ≥ semantic ⇒ T-12 falls. BEV-raster target beats front-camera target ⇒ the SAM3 corpus earns a second product (WM target, not only a head GT) |

### A4 / A1 · World Tokens `2608.09730` ⭐⭐⭐
| dim | |
|---|---|
| RELEVANCE | the echo/bypass defect class; S-8 (train-time WM); v7 head wiring |
| CONSEQUENCE | A **mechanism** result, not a leaderboard one: the **routing** (95.0 without WM) carries more than the WM loss without routing (94.1), and the two are superadditive (97.0) |
| COMBINATION | ⭐ This is **our nav-echo lesson from the other side.** We measured a head that echoed its input (route head a bijection of nav, 369/369) and an open-loop action echo (97.9 % → 0.0 %). Both are *bypass*: the output found a shorter path than the representation we meant it to use. World Tokens makes the absence of a shorter path an **architectural invariant**, and shows it is worth +2.0–2.9 pp on top of the loss |
| CHANCES / RISKS | Upside: cheap to wire; deploy cost ≈ 0 (the branch is dropped). ⛔ Risks: **no seeds**, and 2.0–2.9 pp sits inside plausible training variance (we measured a **14.3 %** false-separated rate on our own rig from a same-seed replicate). Manipulation, not driving. The Canny "anchor" is hand-designed |
| EXPERIMENT | **FS19-1 (v7-tiny, 3 arms + A0b replicate):** {WM aux + **exclusive** routing (action head reads only the WM-shaped tokens) · WM aux + **bypass** (head also reads raw trunk tokens) · no WM aux + exclusive}. **Committed:** exclusive-with-WM must beat bypass-with-WM on T1 h=6 s ADE **and** on the hold-action echo test (hold-action ADE must degrade vs true-action) by more than the replicate floor. Otherwise T-10 is not a lever for us |

### B10 · DriveVLA-M0 `2608.10413` ⭐⭐ — pre-committed rotation #2, discharged
| dim | |
|---|---|
| RELEVANCE | injected **I-3**; B10 retrieval; the Lab's memory question |
| CONSEQUENCE | See finding 5. Retrieval split into a **map** (static) and an **agent** (dynamic) LoRA branch: map-only **+0.7**, map + agent **+1.3** PDMS. Full-TTT **92.4** ≈ LoRA-TTT **92.3** ⇒ the decoupled LoRA costs nothing |
| COMBINATION | ⭐ For I-3's **auditability** framing (ISMR + DSSAD, 09-01), a *label-supervised, retrieval-keyed* update is the auditable form: the store is finite (4 k / 10 k cases) and every update names its cases. Our analogue already exists **offline**: T1 failure windows scored by the four families are exactly such a store. ⛔ But DriveVLA-M0's failures are found by an **oracle scorer on training data**. PhysicalAI has **no map**, so the DAC/DDC terms cannot be computed. We would score failures by **collision/TTC (obstacle.offline) + longitudinal/lateral families only**, the same partial-reward limit found for FlowR2A on 09-18 |
| CHANCES / RISKS | Upside: +1.3 PDMS at 26 ms and no backbone change. Risks: no seeds; ego at inference; navtest v1 PDMS (not our stamp table); the gain is on the *same distribution* the memory was mined from |
| EXPERIMENT | **FS19-5 (0 GPU on banked T1 dumps):** mine the worst-5 % T1 windows by the four families, embed them with the frozen trunk, retrieve the k=3 nearest **train** failures for each **held-out** failure window. **Committed:** retrieved neighbours share the held-out window's failure family at ≥ 2× the random-neighbour rate ⇒ a retrieval memory has signal on our corpus and I-3's supervised route is worth a v7-tiny arm; ≤ 1.2× ⇒ closed |

### B8 · OneWM-VLA `2605.07931` ⭐⭐ (Band-B rotation — B8 has never had a DEEP)
| dim | |
|---|---|
| RELEVANCE | our **v6 readout geometry ceiling** (4×4 pooling = 30°/bin, 2.1–7.8× too coarse for BEV localisation, MEASURED); the predictor's token budget |
| CONSEQUENCE | On MetaWorld MT50 at H=30, success **falls monotonically** with per-view tokens: **1 → 53.13 %, 3 → 41.86, 6 → 33.85, 12 → 20.54** (256 = OOM). Static average pooling **72.8 %** vs adaptive **93.3 %** (LIBERO) |
| COMBINATION | ⭐ **This does NOT contradict our ceiling; it bounds it.** Their task is predicting *what happens next* for a manipulation policy; ours was *localising* in BEV. ⇒ **Split the two roles:** a heavily pooled "world token" as the **predictor's** state, and a full-resolution trunk for the **localisation/geometry readout**. The pooling-method ablation (72.8 vs 93.3) also warns that *how* we pool matters as much as *how much* |
| CHANCES / RISKS | Upside: predictor compute drops by the token ratio. ⛔ Risks: **no error bars**, single backbone, and the monotone curve is **confounded with throughput** (12 tokens runs at **0.13 FPS vs 4.81**, so a fixed wall-clock or step budget under-trains the wide arms). Fisher ratio: **77 %** of discriminative signal survives pooling, i.e. 23 % is lost |
| EXPERIMENT | **FS19-6 (v7-tiny, 3 arms + replicate, matched STEPS not wall-clock):** predictor state tokens/frame ∈ {1, 4, 16}, adaptive pooling, localisation head always on the full trunk. **Committed:** fewer tokens is admissible iff T1 h=6 s ADE is within the replicate floor of the 16-token arm **and** the lateral family (curvature/yaw) does not degrade. Otherwise the manipulation result does not transfer to driving |

### B11 · LGS `2602.11229` — local full-text re-read ⇒ correction (finding 6)
The re-read keeps both 09-18 levers and **strengthens the noising lever's standing**: removing `k` costs **+26/+29 %** at 5/10 steps, a real stability term, while flow matching remains the load-bearing one. ⚠️ The honest form of the headline is **"15/16 at 5 and 10 steps"**, and that is what may be quoted.

### A2 · LEAP `2609.03294` (abstract-only) → finding 7; **FS19-4 (0 GPU harness change, then v7-tiny eval):** replace refav1's iCEM with a quasi-Newton refinement from the iCEM mean, 5 inference seeds each. **Committed:** the inference-seed spread of T1 ADE must shrink ≥ 3× (from ≈ 0.30 m) at equal-or-better mean ADE; otherwise a gradient planner buys determinism only by losing quality, and the route is closed.

### A1 (security) · `2609.15781` (abstract-only) ⭐
A backdoored **latent WM checkpoint** hijacks the victim's own MPC/actor optimisation and still passes clean diagnostics (≥ ~75 % clean success). **Consequence for us:** the programme runs third-party checkpoints (DINOv3, SAM3, Cosmos, Alpamayo) as trunks and targets. **Recommendation:** pin the **sha256 of every upstream checkpoint** in the run record, the way the Library already pins papers. A clean-task eval is not evidence of an un-tampered model.

---

## 3 · The MM standing question — posted speed limit (finding 1, detail)

| supplier | field | provenance | coverage | cost to use | admissibility |
|---|---|---|---|---|---|
| **nuPlan** (and NAVSIM, which is built on nuPlan logs) | `lane.speed_limit_mps` (`Optional[float]`) | **map** — zero ego | ⚠️ **partial**: the getter guards NaN; the fraction is unmeasured | map DB per city (small); cameras are the large part and are **not needed** to measure coverage | ✅ a map attribute is a legal **label** source. As an **input** it is a map channel, like AlpaSim's `map.xodr` |
| **WOMD** | `LaneCenter.speed_limit_mph` (`optional double`) | **map** — zero ego | ⚠️ `optional` ⇒ unmeasured | TFRecords; Motion has no full camera stream | ✅ label source |
| AlpaSim / NuRec | `map.xodr` (09-10 note) | map | full on its scenes | provisioning | ✅ |
| PhysicalAI CoT signs | VLM readings of real signs | vision | **43 valued / 4,572** (E-DE-SIGN-3, 09-18) | none | ✅ but sparse |

⭐ **What this changes:** the max-speed channel is no longer blocked on **existence**. It is blocked on (a) a **coverage measurement** (0 GPU once the map DBs are local) and (b) a **PI decision**: a head trained or calibrated on a **non-parity corpus** does not re-select parity episodes, but it is a new data source for one head and needs the PI's ruling. ⛔ Still refused: any ceiling derived from ego dynamics (the refuted path).
**FS19-2 (0 GPU after a download the PI must approve):** load the NAVSIM/nuPlan map DBs and compute the fraction of lane polygons, and of ego-occupied lane-frames on navtrain, with a finite `speed_limit_mps`. **Committed:** ≥ 80 % of ego lane-frames ⇒ nuPlan is a full-coverage posted-limit label source for a max-speed head; < 50 % ⇒ WOMD is probed next; both < 50 % ⇒ AlpaSim `map.xodr` remains the only full-coverage supplier.

---

## 4 · Band C sweep (short)
**C1:** Tesla **Cybercab** entered paid service in Austin (2026-09-03). Waymo is live in **14 US cities** (Denver/San Diego/Tampa added 09-01), plus Singapore (09-17) and Allianz in Europe (09-15). Uber × Wayve London (09-03). **C2:** no JetPack after 7.2.1 (TRT 10.16.2 ⇒ the D-B1-GATE hazard noted 09-18 stands). **C3:** ⭐ **NHTSA Audit Query AQ26002** into Cybercab's FMVSS self-certification (2026-09-03, RELAYED); UN ADS Regulation + GTR adopted by GRVA in Jan 2026, WP.29 vote expected June 2026 (RELAYED); the primary is unreachable (D-4). **C4:** no navhard entry above DriveZero-Scale 57.1.

## 5 · ⛔ Coverage honesty
**22/22 tracks carry a named query.** DEEP: **D** (7 claims, 3 opponents), **A1/A4** (World Tokens), **A2** (LEAP, abstract-only ⇒ weak DEEP), **A5** (DeepSight B2D numbers into the stamp table), **B8** (OneWM-VLA, first-ever B8 DEEP), **B10** (DriveVLA-M0), **B11** (LGS local re-read + correction), **B12** (DeepSight full text). ⇒ **Band-B DEEP: 4 dedicated (B8, B10, B11, B12)**, which clears the ≥ 3 bar.
⚠️ **Where depth is thinner than it looks:** World Tokens, DriveVLA-M0 and OneWM-VLA were read through the fetch **summariser** from HTML (PDFs banked, not locally re-read; **today's correction #6 is exactly what that costs**). ⇒ **pre-commit their local re-read before any number enters the registry.** LEAP, RT-VLA and the backdoor paper are **abstract-only**. Jim Fan is **RELAYED**. **A3 has no dedicated item for the 4th day.** B1–B7, B9, B13 and C are scan-only. **Domain packages: not produced (see top).**

## 6 · Next rotation — pre-committed
1. **Local pypdf re-read** of World Tokens, DriveVLA-M0 and OneWM-VLA (the correction-#6 lesson).
2. **D-14** — Waymo *Reference Driver* (A5 + Band D).
3. **D-7** — read `1604.06915` (it now carries M-1 **and** M-19-1).
4. **A3 dedicated** — change the term, not the day: "DINOv3 BEV feature target world model" (DeepSight's GT route).
5. **B2/B3** — three and two consecutive transfer empties; try "gated delta net video predictor robotics" and "consistency distillation world model rollout".

---

## ⛔ CORRECTION (same day, appended — rows above are not rewritten)

**§0 row 1 and §3 overstated novelty.** *"The MM standing question has its first positive answer"* and the search log's *"first POSITIVE result in 9 days"* are **wrong as to novelty**. `Data Engineering/Research/2026-09-13-posted-speed-limit-supplier/RESULT.md` **F6 had already found both fields** (WOMD `LaneCenter.speed_limit_mph`, nuPlan per-lane limits) as PUBLISHED-CODE. Today's code read was a **re-find**. V-1 was applied to the Library and the register, **not to the Lab's own domain packages**.
**What IS new today:** the **MEASURED coverage** (`TanitAD Research Lab/Data Engineering/Research/2026-09-19-nuplan-posted-limit-coverage/RESULT.md`): Las Vegas and Pittsburgh **100 %**, Boston **7.1 %**, Singapore **0 %** (by lane length); ≈ **93 %** expected ego-frame coverage (ESTIMATED); and the **PI ruling** (2026-09-19) that a non-parity corpus may supply max-speed labels, which was 09-13's open risk (b).
Also closed after this report: **FS19-7** (all 49 missing Library PDFs re-downloaded from arXiv, **49/49 sha256-identical to the original bank**; `--verify` 521 entries, 0 orphans, **0 problems**; no G: path used). **D-4** remains blocked: unece.org holds a real browser on a Cloudflare bot check, which the Lab may not pass.
