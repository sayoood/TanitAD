<title>PLAN — E-LAB-ARCH-0831</title>

# PLAN — E-LAB-ARCH-0831

`Executed 2026-08-31. Compute budget: ZERO. No GPU, no Thor (a 24 h k60p30k arm is live there`
`and was not touched), no metered HF, no spend. Cost was wall-clock and network only.`

## Steps as executed

| # | step | status |
|---|---|---|
| 1 | Read `TANITAD_PROGRAMME.md` §2/§3, `AGENT_OPERATING_STANDARD.md`, `V7_LAUNCH_GATE.md` | done |
| 2 | ⛔ Anti-duplication preflight — grep `GOALS_AND_CLAIMS.md` for the neighbouring answers (MM-E10/11/12/14/E-DEC-48b) and list the prior Lab packages that already own these questions | done — SPEC §1; it removed two lines of enquiry before any search |
| 3 | Write SPEC with hypotheses and **both outcomes** committed before searching | done |
| 4 | Literature search, primary sources only; open every `abs` page myself | done |
| 5 | Verify the load-bearing mechanism claim at FULL TEXT, not abstract | done — `2605.25313` §4.2/§4.3 |
| 6 | Verify our side in OUR source rather than assuming it | done — `train_v6_staged.py` target construction |
| 7 | Bank every cited primary (`tools/kb_add.py`) | done — 7 banked, library 272 → 279 |
| 8 | Write `raw/QUOTES.md` (retrieval method per entry), then RESULT | done |
| 9 | Append one line to the field `KNOWLEDGE_BASE.md`; stage; do NOT commit | done |

## Priority order of the follow-on work this package identifies

Ranked by *(decision value) / (cost)*. ⛔ None of these is authorised by this package; each
needs its own SPEC with a pre-registered deliberate-regression arm.

| # | item | cost | why this rank |
|---|---|---|---|
| 1 | **Pre-register the (d) target-construction arm** — negative-action contrastive term, one variable against a matched baseline | one tiny-rig arm | the only P2 candidate with a published mechanism AND a published fix; simulator-free variant needs no new corpus and no PI geometry decision |
| 2 | **ATM/AITS as a screening instrument** — read the full text, decide whether it replaces our hand-rolled MM-E10/E12 probes | reading only | >100× cheaper checkpoint ranking, if its stated precondition holds for us |
| 3 | **Branched-vs-modulated conditioning** on the discrete tactical vocabulary | one arm | the 2018 fix for exactly the mechanism MM-E18 measured; ⚠️ needs a discrete channel, so it is a design change not a flag |
| 4 | **Cross-timescale consistency** added to the MM-E16 ladder before any strategic arm | design | without it a 3-level ladder is 3 predictors; `2602.19634` supplies the missing term |
| 5 | Full-text read of `2607.27017` to audit the certificate-gated protocol against our L1/L2/L3 | reading only | our ladder's external validation currently rests on an abstract |

## What was deliberately NOT done

- ⛔ No arm launched, no config written, no trainer edited. This package **re-orders a
  candidate list**; it does not spend compute.
- ⛔ No gate item marked closed — the closure rule in `V7_LAUNCH_GATE.md` reserves that to the PI.
- ⛔ `Mission Plan.md` untouched; `main` untouched; nothing committed or pushed.
