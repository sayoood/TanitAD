# PROPOSED REGISTER ROWS — for `Project Steering/GOALS_AND_CLAIMS.md`

⛔ **PROPOSED text from the Architecture & Inference FlyWheel, 2026-09-03
(BACKLOG R26 + R27). Statuses stay PROPOSED until the Master Mind / PI reads them.**
Raw: `TanitAD Research Lab/Architecture & Inference/Research/2026-09-03-steer-conversion-completion/`
(`SPEC.md`, `RESULT.md`, `raw/evidence.{py,json}`). Code:
`stack/tanitad/refs/refa_v1.py`; pin: `stack/tests/test_steer_conversion_complete.py` (14 tests).
**Tier T0** on every number. ⛔ **No driving claim is made and none may be derived from these
rows** — `D-REFAV1-BOUNDARY-NULL` already measured the effect of this repair on both banked
checkpoints as **exactly zero**.

---

| id | claim / hypothesis | status | evidence |
|---|---|---|---|
| `D-STEER-CONVERSION-COMPLETE` | ⭐ **THE HALF-CONVERSION IS CLOSED: `model_action_units` now applies to the GOAL AND the CANDIDATE, through ONE named boundary, and the default is byte-identical.** Confirmed at source before editing: `as_command` had **exactly one functional call site** in the whole file (`refa_v1.py:1832`; the other two hits are the import at `:61` and a comment at `:1831`), while HEAD's four `self.augment_actions(` sites were `:1264` (`forward`, TRAINING), `:1615` (`_cf_term`, TRAINING), **`:1683` (`_imagine_tactical_goal` — the unconverted goal)** and `:1838` (candidate, converted). ⇒ **exactly one of the 14 crossing points in `plan()`'s graph was wrong** (`SPEC.md` §1). The repair moves the conversion **one level up** into `RefAV1._model_actions(controls, v0, units)`, which both planner-side crossings now call; the two TRAINING sites still call `augment_actions` directly because the loader's `actions` are the v2ep COMMAND channel already — **which is why the conversion cannot live inside `augment_actions`**. ⭐ **The trap reproduced in miniature** (tiny random model, tactical decode pinned to `TURN_R`, goal `\|κ\| = 0.0800`): the turn's advantage over the straight line is **0.0** with κ on both sides, **−2.3842e-07** under the shipped half-conversion, and **+5.3644e-07** with steer on both sides — the same sign pattern the register measured on 25 real `TURN_R` windows (0 → −5.96e-08). Under the repair the turn sits at the goal term's floor (**1.00 ULP**) instead of 6.00 ULPs. ⚠️ **THIS IS A CORRECTNESS REPAIR AND BUYS NOTHING MEASURABLE** — `D-REFAV1-BOUNDARY-NULL`'s `share = +0.0000 [0.0000, 0.0000]` stands unchanged, and every number in this row is a float32 ULP multiple on a random model. Default `"kappa"` is byte-identical on a real rollout (`torch.equal` on controls, exact float equality on cost and on every baseline cost), and the new default goal equals the **pre-repair source expression** re-implemented in the test, not merely another spelling of the new code. | **PROPOSED — SUPPORTED (T0); resolves the "incomplete implementation" flag in `D-REFAV1-BOUNDARY-NULL`** | `RESULT.md` §1–3; `raw/evidence.json` → `P1`; `stack/tests/test_steer_conversion_complete.py::test_A1…A6`. Suite (`-k "refa or refav1 or kinematic or cost_surface or steer"`): **391→405 items, 389→404 passed, exactly +14 = the new file, 1 pre-existing failure unchanged** (`test_decision_check.py::test_refa_surfaces_the_measured_arm`, a mirror artifact — `Project Steering/` is not mirrored; the skip→pass flip is the GPU-busy self-skip in `test_refa_v1_precision.py`). ⛔ **ONE PRE-EXISTING TEST WAS AMENDED and it is outside the agent's declared ownership**: `test_steer_curvature_interface.py::test_D2` asserted that `cv`/`hold_v0` do NOT move between spellings *"because arctan(0) == 0"* — true **only** while the conversion was half applied, since the goal field itself now moves. The invariant was RELOCATED (not deleted) to a SUPPLIED `goal_field`, where nothing converts the reference; both halves pass. 107 further tests pass across the 7 `refa_v1`-importing files outside the filter |
| `C-REFAV1-COST-TIME-UNITS` | ⭐ **RESOLVED AS IMPLEMENTED-AND-GATED, NOT AS FIXED.** The defect is confirmed at source (`H = plan_steps = 10` operative steps = 2.0 s, `refa_v1.py:247, 548`; `tac_dt = 0.6`, `:191`; `_cost_chunk` rolls `self.tactical` on all 10 entries, `:1791, :1840` ⇒ 6.0 s) **and sharpened**: the dense feed is not only off-clock, it is **out of the training distribution** — `forward` builds `tac_a = acts_in[:, ::stride][:, :tac_steps]` (`:1270-1272`), so **every tactical action the predictor has ever seen is operative action `3j`**, exactly as the goal is built (`:1683`). Repair shipped as a call-site flag, `plan(cost_time_grid=...)`, **default `"dense"` = today's behaviour byte-for-byte**; `"tactical"` maps tactical step *j* to operative action `min(3j, H−1)` = `[0,3,6,9,9,9,9,9,9,9]` against today's `[0..9]` and the goal's `[0,3,…,27]`, keeping the candidate's terminal field at the goal's own **6.0 s**. Truncating to 2.4 s was rejected: `1 − cos` between fields at different horizons measures *how far ahead*, not *where*. ⭐ **AND THE EFFECT ON THE BANKED PANEL IS DECIDABLE MODEL-FREE, EXACTLY: NONE, FOR EVERY BANKED WINNER.** The banked config carries `speed_channel = false`, so the regrid is the identity on any candidate constant over the plan horizon — and over all 20 episodes / 140 windows / 3 arms of `t1_dump/decisions/`, **the winning controls are constant on 140/140 in EVERY arm** (`cl` all-zero on 140/140; `cl_navshuf` 122; `cl_oraclegoal` 96). ⇒ **every banked winner's own cost is bit-identical under the corrected grid**, and on the `D-REFAV1-GOAL-DEGENERATE` windows the zero-control rollout is literally the same tensor, so the argmin cannot move there either. ⛔ **UNVERIFIED:** the argmin on the remaining windows and the panel's surface statistics (goal-term ULP span, `turn_frac`, `\|κ*\|`) — those need the model, `cost_surface_probe.py` is read-only for this agent and has no such flag, and the dumps hold trajectories/controls/costs/labels only, **no features and no fields**. One dev-box GPU job on the two local checkpoints once the probe may take the flag. ⛔ **RESIDUAL, NOT REMOVED BY EITHER REPAIR:** the plan spans 2.0 s of a 6.0 s goal, so the injected seed can never reproduce its own goal (`cand_fixed[4:] ≠ goal_map[4:]`) — a `plan_horizon_s` design question, **escalated**. | **PROPOSED — SUPPORTED at source; repair IMPLEMENTED, DEFAULT-OFF, and its banked no-op PROVEN model-free; panel re-derivation UNVERIFIED** | `RESULT.md` §4; `raw/evidence.json` → `P2`, `P3`; `stack/tests/test_steer_conversion_complete.py::test_B1…B5`; banked config `C:\Users\Admin\refav1_eval_slice\ckpt_ep2\config.json` |
| `C-REFAV1-COST-T4-DEAD` | ⚠️ **THE SHIPPED COST HAS THREE LIVE TERMS, AND T4 IS NOT BEING DELETED.** Confirmed by an **ast walk** (not a grep — `cost_surface_probe.py` contains the identifier in its own re-implementation and a grep false-positives): neither production caller passes `target_speed` (`taniteval/tools/refav1_arm.py:735`, `taniteval/tools/cost_surface_probe.py:515`). ⛔ **But "dead" ≠ "unreachable":** `stack/tests/test_refa_v1.py:413` and `stack/tests/test_refa_v1_speed_channel.py:213` both exercise it, so removal is not a pure deletion, and **wiring it is a change to what the cost IS** — the tactical `lon` token already implies a target speed (`GOAL_LON_DV_MPS`, `refa_v1.py:126-128`), so making T4 live would give the tactical brain's LONGITUDINAL decision a second channel into the cost besides T1, which is precisely the lever `D-REFAV1-GOAL-DEGENERATE` says is missing — and would change every banked number. ⇒ **Recommendation: KEEP, and decide deliberately.** Shipped instead: the deadness claim is pinned by a test that also asserts a `.plan(...)` call was FOUND, so a rename cannot make it vacuously true (the `36-features` rot, in miniature), plus a hand-computable behavioural pin (`target_speed = v0` changes the `cv` cost by exactly 0.0; `v0 − 1` adds exactly `0.10·1.0²`). | **PROPOSED — SUPPORTED; ⛔ AWAITING A PI / MASTER MIND DECISION: wire T4 to the decoded `lon` token, or delete it together with its two tests** | `RESULT.md` §5; `stack/tests/test_steer_conversion_complete.py::test_C1_no_production_plan_call_site_passes_target_speed`, `::test_C2_T4_is_exactly_zero_when_absent_and_hand_computable_when_supplied` |
| `C-FLOAT32-SELF-SIMILARITY-FLOOR` | ⚠️ **`1 − cos(x, x)` IS NOT 0.0 IN FLOAT32, AND AN ASSERTION THAT IT IS WILL FAIL ON CORRECT CODE.** The self-similarity of a real terminal field read **2.00 ULPs** (1.1921e-07) on a tiny model here, and the cost-surface probe's control C3 failed its pre-registered *exactly-0.0* form at **2.00 / 4.00 ULPs** on the two banked checkpoints — the same number from an independent instrument. ⇒ **any test or probe comparing a field to itself must carry a 4-ULP budget**, and a "the goal term is exactly zero" claim about a self-comparison is a statement about float32, not about the model. *(Cost here: one test iteration. Cost elsewhere: `PANEL_VOID = True` on both cost-surface runs.)* Same family as the register's own finding that the shipped planner's winning cost takes six distinct ULP values over 140 windows, **some negative**, because float32 `cosine_similarity` can return > 1 — reproduced in these dumps at `plan_cost_cl` **min −2.384e-07**. | **PROPOSED — SUPPORTED, two independent instruments** | `RESULT.md` §3; `raw/evidence.json` → `P1` (`c_goal_turn_ULPs`), `P3` (`winning_cost_min`); `FLOOR_ULPS` in `stack/tests/test_steer_conversion_complete.py` |

---

## Consequences for rows already in the register

* **`D-REFAV1-BOUNDARY-NULL`'s last clause — *"as shipped the repair is INCOMPLETE … the flag
  is a trap for whoever turns it on"* — is DISCHARGED.** The mechanism finding and the null
  result both stand unchanged; only the implementation defect is closed. ⚠️ The row's
  `share = +0.0000 [0.0000, 0.0000]` was measured against the **half-converted** arm and has
  **not** been re-measured against the completed one. It is expected to stay 0.0 (the goal term
  is at the arithmetic's floor either way) but that is a **prediction, not a measurement**.
* **`H-REFAV1-COST-FLAT` is unaffected** — nothing here changes a weight or a term.
* **`C-STEER-CURVATURE-INTERFACE`'s repair is now applied at every crossing it names**, and its
  wheelbase constant (`STEER_WHEELBASE_M = 2.9`) is still read from one place
  (`stack/tanitad/models/kinematic.py`), never re-derived.
* **`D-REFAV1-GOAL-DEGENERATE` gains a corroborating model-free read:** the banked winner is
  **constant over the plan horizon on 140/140 windows in all three arms**, and all-zero on
  140/140 in the deployed `cl` arm.
