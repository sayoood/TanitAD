# SPEC — DiffusionDriveV2's RL stage, paper-exact, with the released code beside every item

**Package:** `TanitAD Research Lab/Architecture & Inference/Research/2026-09-15-ddv2-rl-prep/` · **Date:** 2026-09-15 · **Author:** Arch+Inference implementation/validation agent · **GPU for this file:** 0.
**PI question:** *"Prepare and validate the RL approach as described in the paper"* (DiffusionDriveV2).
**Scope of this file:** Task 1 only — what the method IS. Our implementation is diffed in `DIFF_OURS_VS_SPEC.md`; nothing here is a claim about our model.

## 0. Sources, and how to read a citation

| tag | what it is | identity (re-verified 2026-09-15 on this box) |
|---|---|---|
| **P** | arXiv **2512.07745v1** (DiffusionDriveV2), 17 pp. incl. supplement. `P§4.3` = section, `P Eq.7` = equation, `P Tab.7` = table | PDF `TanitAD Research Lab/Library/papers/2512.07745_DiffusionDriveV2-Reinforcement-Learning-Constrained-Truncate.pdf`, sha256 `076ce47e0b0a323c9bb0072432e7f1219ccdcf645b8ff6db172f1bb9efb85023` (equal to the 2026-09-05 bank); text `…/2026-09-05-diffusiondrive-v2-analysis/raw/2512.07745v1_text.txt` |
| **C** | released code `hustvl/DiffusionDriveV2@1cd12a1e155c34dcc471261835444c8d5587580b`, banked Apache-2.0 copy in `…/2026-09-05-diffusiondrive-v2-analysis/raw/ddv2_src/`. `rl.py:891` = `diffusiondrivev2_model_rl.py` line 891; `agent.py` = `diffusiondrivev2_rl_agent.py`; `cfg.py` = `diffusiondrivev2_rl_config.py`; `agent.yaml` = `diffusiondrivev2_rl_agent.yaml`; `scoring.yaml` = `default_scoring_parameters.yaml`; `training.yaml` = `default_training.yaml`; `sel.py` = `diffusiondrivev2_model_sel.py` | sha256 re-computed today for all nine files read and **equal** to `raw/ddv2_fetched_sha256.txt` (`rl.py` `2789081a…`, `agent.py` `0d121418…`, `cfg.py` `04b35a42…`, `agent.yaml` `6ffa45df…`, `scheduler.py` `5a602966…`, `scoring.yaml` `66563a99…`, `train_eval.md` `7c007113…`, `training.yaml` `7c9a077b…`, `sel.py` `2218e4ee…`) |
| **F** | behaviour of a framework the banked files CALL but do not contain (NAVSIM `pdm_score_para`/`PDMScorer`/enums, PyTorch Lightning loop, diffusers defaults) | ⚠️ **not in the bank ⇒ every F item is UNVERIFIED** unless stated otherwise |
| **E** | arithmetic done here (`raw/ddv2_step_sensitivity.txt` reproduces it) | ESTIMATED |
| **I** | a sibling programme deliverable, `…/2026-09-05-diffusiondrive-v2-analysis/RESULT.md` (§1.2–1.3) | INHERITED — **every I item used below was re-read against C today**; §11 lists the three places where C says something the sibling did not |

Evidence classes on claims: `PUBLISHED-PRIMARY` (P), `PUBLISHED-CODE` (C), `UNVERIFIED` (F or ambiguous P), `ESTIMATED` (E).

---

## 1. The policy — what is stochastic

| # | item | paper | code | asymmetry |
|---|---|---|---|---|
| 1.1 | object being optimised | the **truncated diffusion decoder** of DiffusionDrive, cold-started from its IL weights (P§4.1: *"we directly employ DiffusionDrive as our trajectory generator, leveraging its pre-trained weights"*) | `V2TransfuserModel._trajectory_head` (`rl.py:209-215`), loaded from `ckpts/diffusiondrive_navsim_88p1_PDMS` (`train_eval.md:33`) with `strict=False` (`agent.py:80`) | **A1** — `CustomTransformerDecoder(diff_decoder_layer, 1)` (`rl.py:733`): **one** cascade layer, v1 has two; the `strict=False` load silently drops v1's second layer (I, re-verified) |
| 1.2 | the MDP | each denoising step is a Gaussian policy `π_θ(τ_{t−1} | τ_t, z, a_k) = N(μ_θ(τ_t,t,z,a_k), η(1−α_t) I)` (P Eq.5), DPPO-style (P§4.2) | the step is `DDIMScheduler_with_logprob.step` (`rl.py:518-676`): DDIM mean `prev_sample_mean = √ᾱ_{t−1}·x̂0 + √(1−ᾱ_{t−1}−σ_t²)·ε̂` (`rl.py:627-630`), `prediction_type="sample"` so `x̂0` is the decoder output (`rl.py:596-598`, `:703-708`) | — |
| 1.3 | where the randomness comes from | η = 1 (DDPM) in training, η = 0 at eval (P§4.3) | **three sources are live in training**, and only one of them is in the log-probability: **(i)** the anchored-Gaussian start — fresh `randn` added at `t = 8` to every tiled anchor copy (`rl.py:818-820`); **(ii)** the per-step multiplicative scale noise (§2), the ONLY term the likelihood models (`rl.py:646-654`, `:666`, `:668-675`); **(iii)** `nn.Dropout(0.1)` at two residual sites of the decoder layer (`rl.py:409-410`, used `:456`, `:462`) plus one in the BEV grid-sample attention (`blocks.py:52`, `:109` of the V2 copy — the RL model imports v1's `blocks` (`rl.py:14`), whose file is **not banked** ⇒ that third site is UNVERIFIED) — **live because the head is put in `.train()`** (`agent.py:68`), in both the no-grad rollout and the grad pass. *Inventory completed 2026-09-15 (resume pass):* the two `nn.MultiheadAttention` modules of the layer also carry `dropout=config.tf_dropout` (`rl.py:418-429`), but `tf_dropout = 0.0` (`cfg.py:77`) ⇒ **inert**; the backbone GPT's `embd/resid/attn_pdrop = 0.1` (`cfg.py:54-56`) sit in modules put in `.eval()` (`agent.py:65-67`), and even if the framework re-enabled them (A17) the trunk runs **once per batch** and both passes consume the same outputs (`rl.py:230-260`) — so they cannot create a rollout-vs-grad-pass mismatch | **A2 (NEW here)** — the sibling analysis says *"the only stochasticity in the RL chain is the 2-scalar multiplicative scale"*; C also has (i) (which it noted elsewhere) and **(iii), which it did not**. The mean the log-probability is evaluated under in the grad pass (`rl.py:1072-1091`) uses **fresh dropout masks** from those under which the stored sample was drawn |
| 1.4 | η at train / eval | 1 / 0 (P§4.3) | agent forward passes `eta=1.0` (`agent.py:118`); model default `eta=0.0` (`rl.py:221`); `forward_test_rl` hard-codes `eta=0.0` (`rl.py:997`) | consistent |
| 1.5 | what "η = 1" actually does | *"equivalent to applying DDPM"* — additive Gaussian of variance η(1−α_t) (P Eq.5, §4.3) | `std_dev_t_add = 0.0` in both branches (`rl.py:640`, `:643`); `prev_sample = prev_sample_mean * variance_noise_mul + std_dev_t_add * variance_noise_add` (`rl.py:666`) — the additive noise is drawn (`:657-664`) and **multiplied by zero** | **A3** (I, re-verified) — η = 1 only switches the multiplicative floor on (`rl.py:638-639`); no DDPM noise exists |
| 1.6 | the likelihood actually used | the density of Eq.5 | isotropic Gaussian around the DDIM mean with `σ = clip(σ_t, min=0.1)` (`rl.py:668`), **summed over all 8 waypoints × 2 coordinates** (`rl.py:675`), evaluated at `prev_sample.detach()` (`rl.py:670`) | **A4** (I, re-verified) — not the sampler's density: the sampler has rank 2 per trajectory (two shared scalars) and scale `0.04·|μ|`; the likelihood has rank 16 and scale 0.1. ⇒ the gradient `∂logp/∂μ = (x−μ)/0.01 = 4·μ·ε` per coordinate is a **directional finite difference along μ**, not Eq.5's REINFORCE |
| 1.7 | **A18 (NEW, resume pass 2026-09-15) — x̂0 is CLAMPED to [−1, 1] inside every step** | not stated | both schedulers are built with **no `clip_sample=` argument** (`rl.py:697-708`; `sel.py:809-814`; v1 `transfuser_model_v2.py:400-404`), so diffusers' `DDIMScheduler` default **`clip_sample=True`, `clip_sample_range=1.0`** applies and `step` runs `pred_original_sample.clamp(−1, 1)` (`rl.py:609-614`) — **after** `pred_epsilon` was derived from the unclamped x̂0 (`rl.py:596-598`). MEASURED 2026-09-15 on diffusers **0.40.0** by executing the vendored class (`stack/tests/fixtures/ddv2_released_scheduler.py`): a port without the clamp is **not** bitwise the release; with it, bitwise equal at all 10 labels, η ∈ {0, 1} (`stack/tests/test_ddv2_rl.py`). The release's diffusers pin is **not banked** ⇒ the default in the authors' environment is UNVERIFIED (it is the default in 0.40.0). | ⇒ with x/50, y/20 the box is **x ∈ ±50 m, y ∈ ±20 m**. E (derived from the code, not measured on NAVSIM): (a) the **final** step's mean IS the clamped x̂0 (ᾱ_prev = 1), so every trajectory the RL model emits (`forward_test_rl` returns that mean, `rl.py:1002`) and every raw candidate the stage-II selector ranks before augmentation (`sel.py:1437-1445`) is **capped at 50 m forward / 20 m lateral** — a 4 s endpoint beyond 50 m needs an average speed > 12.5 m/s; v1's normalisation capped at 55.7 m (A5); (b) a clamped coordinate gets **zero** policy gradient at the final step (61 % of the weight, §3.4) and, at t > 0, only the ε̂ path's gradient, whose coefficient is **negative** (`−√(1−ᾱ_p−σ²)·√ᾱ_t/√(1−ᾱ_t)`; pinned analytically in the test file). How often NAVSIM trajectories reach the box is UNVERIFIED (no NAVSIM data here) |

## 2. Exploration — scale-adaptive multiplicative noise

| # | item | paper | code | asymmetry |
|---|---|---|---|---|
| 2.1 | form | `τ' = (1 + ε_mul) τ`, `ε_mul = (ε_long, ε_lat)` — **two** multiplicative Gaussian scalars, *"one longitudinal and one lateral"*, applied to a **normalized** trajectory (P§4.3) | `variance_noise_horizon = randn([B, N, 1, 1])·σ_mul + 1` and `variance_noise_vert` likewise (`rl.py:646-651`), concatenated on the last axis and repeated over the 8 waypoints (`rl.py:653-654`) ⇒ **x (column 0) and y (column 1) each scaled by one scalar per trajectory**, identical for all waypoints | ⚠️ "longitudinal/lateral" = the **ego-frame x/y axes** of the normalized state, not a path-tangent decomposition (C) |
| 2.2 | the state it acts on | the normalized trajectory (P§4.3) | the normalized DDIM state `x/50, y/20` (`rl.py:755-763`; applied at `:666` to `prev_sample_mean`, which lives in that space) | **A5** (I) — v1's normalization was `2(x+1.2)/56.9−1, 2(y+20)/46−1`; per-axis multiplicative noise is invariant to a pure per-axis scale (C), but the cold start saw a different noise geometry (I: σ_x 1.58 m / σ_y 0.63 m at t = 8, vs 0.90 / 0.73 m in v1) |
| 2.3 | **minimum std of the exploration noise** | **0.04** (P suppl. §7 *"minimum standard deviation of the multiplicative exploration noise to 0.04"*; P Tab.7 *"Minimum denoising std 0.04"*) | `std_dev_t_mul = torch.clip(std_dev_t, min=0.04)` with `std_dev_t = (eta·variance^0.5).clamp_(min=1e-10)` (`rl.py:618-619`, `:639`) | consistent |
| 2.4 | **minimum std of the likelihood** | **0.10** (P §7 *"at least 0.1 when evaluating the Gaussian likelihood"*; P Tab.7 *"Minimum log-variance std 0.10"*) | `std_dev_t_mul = torch.clip(std_dev_t, min=0.1)` then `log_prob = −(x−μ)²/(2σ²) − log σ − log√(2π)` (`rl.py:668-675`) | ⚠️ the paper's table says "log-variance std"; the code floors the **std** (C) |
| 2.5 | do the floors bind? | not stated | the rollout calls `set_timesteps(1000)` (`rl.py:804`) so `prev_timestep = t − 1` (`rl.py:580-582`); E: with `scaled_linear` 1e-4…0.02, σ_t(η=1) = **0.0120, 0.0117, 0.0114, 0.0111, 0.0107, 0.0104, 0.0099, 0.0094, 0.0083, 0.0000** at t = 18…0 ⇒ **both floors bind at every step** (reproduces the banked `raw/ddv2_noise_magnitudes.json` to 4 s.f., independently recomputed) | ⇒ exploration std is a constant **4 %** of each normalized coordinate; likelihood std a constant **0.1 normalized = 5.0 m in x, 2.0 m in y** (E) |
| 2.6 | compounding along the chain | not discussed | E: at the early labels the DDIM mean is dominated by the **input state**, `∂μ/∂x_t` = 0.936 at t = 18 → 0.342 at t = 2 → 0 at t = 0 (table in §3.4); so each step's scale noise is largely carried into the next input, and the decoder's residual output (`poses_reg[..., :2] = offset + noisy_traj_points`, `rl.py:477`) keeps tracking it | ⚠️ HYPOTHESIS (E, not measured by the authors or by us): the effective scale spread of the final sample is larger than one step's 4 % |

## 3. Rollout — chain, truncation, groups, samples per scene

| # | item | paper | code |
|---|---|---|---|
| 3.1 | truncation / start | noisy anchors `τ_t^k = √ᾱ_t a_k + √(1−ᾱ_t) ε` (P Eq.3), `T_trunc` not given for RL | `add_noise(norm(anchor), randn, t = 8)` (`rl.py:816-820`); the **decoder input** is clamped to `[−1, 1]` (x ∈ ±50 m, y ∈ ±20 m) at every step (`rl.py:826-827`) |
| 3.2 | chain length in RL training | `T_trunc` steps in Eq.6/7, value not given | **`step_num = 10`**, labels `round(arange(10)·2)[::-1]` = **[18, 16, …, 2, 0]** (`rl.py:801-807`), each a **one-unit** transition t → t−1 (`rl.py:580-582` with `num_inference_steps = 1000`); the last step's `ᾱ_prev` = `final_alpha_cumprod` (diffusers `set_alpha_to_one=True` default ⇒ 1.0 ⇒ μ = clamp(x̂0) exactly — **MEASURED on diffusers 0.40.0** by executing the vendored class, 2026-09-15; the release's own diffusers pin is not banked, so "the authors ran 1.0" stays UNVERIFIED) |
| 3.3 | chain at inference | 2 steps (P§5.2) | `forward_test_rl`: `step_num = 2`, labels [10, 0] (`rl.py:943-949`), η = 0 (`:997`), output = the **DDIM mean** of the last step (`rl.py:1002`) |
| 3.4 | **A6** label/noise mismatch | — | noise is added at t = 8 but the chain's first label is 18 (`rl.py:819` vs `:806`) — the same asymmetry v1 has with [10, 0] from t = 8 (I). E — per-step weight of the policy gradient on the network output, `∂μ/∂x̂0 × γ`: 0.0086, 0.0118, 0.0164, 0.0232, 0.0335, 0.0501, 0.0788, 0.1346, 0.2736, **1.0000** ⇒ **61.3 %** of the total sits on the final step (`raw/ddv2_step_sensitivity.txt`) |
| 3.5 | **groups per anchor G** | *"a group of G trajectory variations by diffusing the anchor with random Gaussian noises and exploration noise"* (P§4.4); G not given in P | **G = 4** (`cfg.py:37`, `rl.py:709`); the 20-anchor bank is tiled `[B, 4, 20, 8, 2] → [B, 80, 8, 2]` (`rl.py:813-815`); the decoder folds groups into the batch so all 80 share the 20-way head (`rl.py:471-476`) |
| 3.6 | anchors | K-means intents (P§3) | `kmeans_navsim_traj_20.npy`, 20 × 8 × 2, `requires_grad=False` (`rl.py:710-715`) |
| 3.7 | **samples per scene** | not given | **80 candidate chains** (4 × 20) + the **human GT trajectory appended as candidate 81** (`rl.py:867-868`) — all 81 scored by the simulator; decoder evaluations per scene = 80 × 10 (no-grad rollout) + 80 × 10 (grad pass) |
| 3.8 | yaw of a candidate | — | heading from the **Bézier derivative** of the sampled xy (`rl.py:865`, `:1123-1170`); the decoder's own heading output (`rl.py:478`) is discarded; the appended GT keeps its recorded heading (`rl.py:867`) |

## 4. Advantage

| # | item | paper | code | asymmetry |
|---|---|---|---|---|
| 4.1 | **intra-anchor GRPO** | `A^{k,i} = (r^{k,i} − mean_G r^k) / std_G r^k` (**P Eq.8**), `r^{k,i} = R(τ_0^{k,i})` applied to every denoising step (P§4.4) | `(reward_group − mean(dim=1)) / (std(dim=1) + 1e-4)` over the **G axis per anchor** (`rl.py:886-889`); `torch.std` is the unbiased (n−1) estimator (F, PyTorch default) | +1e-4 in the divisor (C) |
| 4.2 | **inter-anchor truncation** | `A_trunc^{k,i} = −1 if collision, max(0, A^{k,i}) otherwise` (**P Eq.10**), and it *"replaces A^{k,i} in the RL loss"* — i.e. it acts **per sample on the intra-anchor advantage**; there is **no across-anchor centring** in P or C | `advantages.clamp(min=0) * mask_positive` (`rl.py:893`) | — |
| 4.3 | **≥GT positive mask** | **absent from P** | `mask_positive = reward_group > (reward_gt − 1e-6)` where `reward_gt` is the simulator score of the appended human trajectory (`rl.py:871-873`, `:892`) — **per sample**, broadcast over the 80 candidates | **A7** (I, re-verified) — positive credit only for samples that at least tie the human's own PDM score |
| 4.4 | the −1 branch | "collision" (P Eq.10) | −1 wherever sub-score `no_collision != 1` **or** `drivable_area != 1` (`rl.py:896-904`); applied **after** the mask, so it overrides it; `ttc`, `comfort`, `progress`, `dir_weighted`, `final` are read and skipped (`rl.py:903-904`) | **A8** (I) — half of the hard penalty is map-dependent (DAC). ⚠️ F: NAVSIM's NC takes {0, 0.5, 1} (0.5 = non-at-fault/static cases in NAVSIM's definition) — `!= 1` sends the 0.5 case to −1 too; the enum values are **not banked** ⇒ UNVERIFIED |
| 4.5 | discount over denoising steps | `γ^{t−1}` inside Eq.7, *"mitigating instability in early denoising steps"*; **γ = 0.8** (P §7, P Tab.7) | `advantages.repeat(step_num) * 0.8 ** (step_num − i − 1)` (`rl.py:924-932`): weight **1.0 on the final (cleanest) step**, 0.8⁹ = 0.134 on the first | consistent |
| 4.6 | value model / baseline | none (GRPO) | none | — |

## 5. Reward — the paper never defines it; the code does

| # | item | paper | code / framework |
|---|---|---|---|
| 5.1 | **R(τ_0)** | undefined in all 17 pages (I: MEASURED-ABSENCE; re-read today, still absent). The eval metric is PDMS (P Eq.14, suppl. §7) | NAVSIM **`PDMSimulator` + `PDMScorer`** instantiated from `navsim/planning/script/config/pdm_scoring/default_scoring_parameters.yaml` (`rl.py:741-753`), run in a **16-process spawn pool** on each scene's lzma-pickled metric cache (`rl.py:119-140`, `:745-750`, `:785-798`) |
| 5.2 | simulator | P calls the benchmark *"closed-loop"* (P Tab.1 caption) | `proposal_sampling: num_poses 40, interval_length 0.1` = a **4 s rollout at 10 Hz** (`scoring.yaml:1-5`); F: NAVSIM's PDM simulator LQR-tracks each proposal with a kinematic bicycle model against **non-reactive replayed** agents. ⛔ Under the PI's 2026-09-02 ruling this is **self-action OPEN loop = our T1**, never closed loop |
| 5.3 | score formula | `PDMS = NC × DAC × (5·EP + 5·TTC + 2·C)/12` (**P Eq.14**) | `_pairwise_scores`: `final = prod(multi_metrics) × Σ_j w_j·wm_j / Σ_j w_j` (`rl.py:91`, `:111-115`) with weights **progress 5.0, ttc 5.0, comfortable 2.0, driving_direction 0.0** (`scoring.yaml:20-23`) ⇒ denominator 12 |
| 5.4 | **A9** multiplicative rows | NC, DAC | `mm = scorer._multi_metrics` is documented as `(3, N)` (`rl.py:44`) and **all three rows are multiplied** (`rl.py:46`, `:91`); rows 0 and 1 are read as `NO_COLLISION`, `DRIVABLE_AREA` (`rl.py:67-68`). ⚠️ The third row's identity (driving-direction compliance in NAVSIM v1's enum) is **F — not banked — UNVERIFIED**; if it is DDC, the reward multiplies a third gate the paper's Eq.14 does not have |
| 5.5 | **EP (ego progress)** normalisation | "Ego Progress" (P §7) | re-normalised **pairwise**: `raw_i = progress_raw_i × multi_prod_i`, `EP_i = raw_i / (max(raw_ref, raw_i) + 1e-6)` if `max > progress_distance_threshold = 5.0 m`, else `0` if `multi_prod_i == 0` else `1` (`rl.py:94-108`, `scoring.yaml:30`). ⇒ **EP saturates at 1.0** for any candidate at least as far along as the reference, and is 0 for any colliding/off-road candidate |
| 5.6 | **A10 (CORRECTION to the sibling)** which trajectory is the EP reference | — | the reference is **proposal index 0 of the scorer** (`rl.py:95`, `:54`), and the function returns `N−1` scores (`rl.py:115`). The model hands the pool **81** trajectories (80 + GT, `rl.py:868`) and reads back 81 scores whose **last** is the GT (`rl.py:871-873`) ⇒ index 0 is **prepended inside `pdm_score_para`**, which is **not banked**. F: NAVSIM's own `pdm_score` prepends `metric_cache.trajectory`, the **PDM-Closed planner's** trajectory. The code comments call index 0 *"GT"* (`rl.py:78`, `:40`). ⇒ the sibling's *"EP re-normalised against the GT as proposal 0"* is **UNVERIFIED**; the structure makes PDM-Closed more likely. **Not decidable from the bank.** |
| 5.7 | what each term NEEDS | — | F (NAVSIM, not banked; stated so the diff can be honest): **NC** — the ego rollout + every agent's box over 4 s (metric cache observation, replayed); at-fault logic. **DAC** — the **map's drivable-area polygons** (lanes, intersections, car parks) and the ego footprint corners. **EP** — the **route centerline** to measure progress along the route, and the reference proposal. **TTC** — ego + agents projected forward at constant velocity; a time-to-collision threshold (`stopped_speed_threshold 5e-3 m/s`, `scoring.yaml:29`; the TTC horizon is a `PDMScorerConfig` default not in the YAML ⇒ UNVERIFIED). **C (comfort)** — the ego rollout only: accel/jerk/yaw-rate thresholds (nuPlan's; values not banked ⇒ UNVERIFIED). **DDC** — lanes + route (weight 0 in the sum; possibly a gate — §5.4) |
| 5.8 | the GT's own score | — | the human trajectory is scored by the same simulator in the same call (`rl.py:868-873`) and becomes the per-scene **bar** of §4.3 |

## 6. Loss

| # | item | paper | code | asymmetry |
|---|---|---|---|---|
| 6.1 | **policy loss** | `L_RL = −(1/N_anchor)(1/G)(1/T_trunc) Σ_k Σ_i Σ_t γ^{t−1} log π_θ(τ_{t−1}^{k,i} | τ_t^{k,i}) A^{k,i}` (**P Eq.7**) | `per_token_loss = −exp(logp − logp.detach()) · A` (`rl.py:1096`) over `[B, 80, 10]`; the "ratio" is between a tensor and its own detached copy ⇒ **identically 1 ⇒ plain REINFORCE, one update per rollout** (I, re-verified) | — |
| 6.2 | **A11 (NEW here)** normalisation | `1/(N_anchor·G)` over **all** samples | `RL_loss_b = Σ_i (loss·[loss≠0]) / max(1, Σ_i [loss≠0])` per (row, step), then `mean` over the 10 steps and over rows (`rl.py:1099-1102`, `:1119`) ⇒ the average is over **samples with non-zero advantage only** | a batch in which 3 of 80 samples carry advantage gets the same per-sample weight as one in which 80 do; rows with no non-zero sample contribute 0 |
| 6.3 | the chain the gradient flows through | Eq.6/7: the per-step Gaussian log-likelihood | `get_rlloss` **re-runs the decoder with gradient on the stored chain** (`chains = all_diffusion_output[..., :-1]`, `chains_prev = [..., 1:]`, `rl.py:1033-1034`) and evaluates `log π` at the **stored** next state via `prev_sample=chains_prev[..., i]` (`rl.py:1085-1091`); gradient enters only through μ(x̂0) | dropout masks differ between the two passes (A2) |
| 6.4 | **IL term** | `L = L_RL + λ L_IL`, λ ∈ (0, 1) (**P Eq.9**), *"analogous to how the original GRPO provides regularization by adding a KL divergence"* (P§4.4); **BC loss weight 0.1** (P Tab.7) | L1 of **every one of the 80 chains, at all 10 steps, all decoder layers** (1 here) to the single GT, x/y only, in metres (`rl.py:1104-1112`) | **A12** (I) — every mode is pulled to the one GT, not v1's nearest-anchor L1 (`LossComputer` is built, `rl.py:735`, and never called in stage I) |
| 6.5 | **A13** IL weight per row | λ = 0.1 | `il_weight = 1.0` on rows where **no** advantage is positive, `0.1` otherwise (`rl.py:1113-1117`) | (I) |
| 6.6 | **A14 (NEW here)** the IL value is batch-global | — | `IL_loss_b += traj_l1.mean()` adds the **whole-batch scalar mean** to every row (`rl.py:1109-1110`) ⇒ `loss = mean_b(RL_b) + mean_b(il_weight_b) · IL_scalar` (`rl.py:1118-1119`); the per-row weights only set the **batch-average λ** | the code's comment `# (B,)` on the L1 is wrong about its shape |
| 6.7 | trust region | *"analogous to … KL"* — the IL loss IS the regulariser (P§4.4) | **no KL, no clip, no reference policy, no importance ratio** in `rl.py` (I: grep; re-verified by reading `get_rlloss` in full, `rl.py:1028-1120`) | — |

## 7. What trains, with what, for how long

| # | item | paper | code |
|---|---|---|---|
| 7.1 | **trainable parameters** | not stated in P | **`_trajectory_head.*` only**: every other parameter `requires_grad=False` and every other named module `.eval()`, the head `.train()` (`agent.py:62-68`). ⚠️ F: PyTorch Lightning calls `.train()` on the whole LightningModule during `fit`; whether the per-module `.eval()` survives depends on the Lightning version (not pinned in the bank) ⇒ **UNVERIFIED** whether backbone BatchNorm/dropout ran in train mode during stage I |
| 7.2 | head contents | — | the anchor encoder, time MLP, the 1-layer decoder (BEV grid-sample attention, agent MHA, ego MHA, FFN, AdaLN time modulation, reg+cls MLPs) (`rl.py:716-733`); the cls branch is in the head but **receives no gradient** in stage I (not in any loss) |
| 7.3 | optimiser | AdamW, **lr 2e-4, weight decay 1e-4** (P §7, Tab.7) | `optimizer_type = "AdamW"`, `weight_decay = 1e-4` (`cfg.py:120-122`); `lr: 2e-4` (`agent.yaml:17`); built over all parameters with an `image_encoder` group at `lr_mult 0.5` (`agent.py:144-183`, `cfg.py:124-131`) — frozen parameters have no `.grad`, so only the head moves (F: AdamW skips `grad is None`) |
| 7.4 | **A15 (NEW here)** schedule | *"cosine learning rate schedule with a 10 % linear warmup"* (P §7) | `WarmupCosLR(lr=2e-4, min_lr=1e-6, epochs=10, warmup_epochs=1)` (`agent.py:186-192`, `scheduler.py:39-50`); no `interval` key is set (`agent.py:194`) ⇒ F: Lightning's default steps it **once per epoch**. E: then epoch 0's LR is `2e-4·(0+1)/1 = 2e-4` — **the warmup is a no-op**; per-epoch LR 2.0e-4, 2.0e-4, 1.94e-4, 1.77e-4, 1.50e-4, 1.18e-4, 8.3e-5, 5.1e-5, 2.4e-5, 7.0e-6 (UNVERIFIED through F) |
| 7.5 | epochs | **10** (P §5.2, §7, Tab.7) | `trainer.params.max_epochs=10` (`train_eval.md:35`) |
| 7.6 | batch | **512 total**, 8 × L20 (P §5.2, Tab.7) | `batch_size: 64` per process (`training.yaml:25`) × 8 GPUs (P) ; `strategy: ddp_find_unused_parameters_true` (`training.yaml:40`) |
| 7.7 | precision, clipping | not stated | `precision: 16-mixed` (`training.yaml:41`); `gradient_clip_val: 0.0` — **no clipping** (`training.yaml:49`) |
| 7.8 | optimiser steps | not stated | 10 epochs × |navtrain| / 512. P gives *"navtrain (1,192 training scenes)"* (P §5.1) but not a sample count ⇒ the step count is **UNVERIFIED** |
| 7.9 | cold start | DiffusionDrive IL weights (P§4.1, §5.2) | `agent.checkpoint_path=ckpts/diffusiondrive_navsim_88p1_PDMS` (`train_eval.md:33`) |

## 8. The selector stage (stage II) — not the RL stage, recorded for completeness

Everything below is `PUBLISHED-CODE` at the sibling's cites, **spot-re-verified today at 13 lines** (`sel.py:844, 904, 1032-1037, 1270, 1298, 1305, 1346, 1352-1357, 1372, 1386, 1391, 1406, 1444, 1461, 1469, 1491`; `sel_agent.py:61-75`).

* Generator frozen; trainable = scorer encoder + coarse decoder (1 layer) + fine decoder (3 layers) + 10 sub-score heads (`sel_agent.py:61-78`). 20 epochs, same optimiser/batch as stage I (P §5.2, Tab.7).
* Training fan per scene: **2 groups × 20 anchors** at η = 0 (`sel.py:1298`), **+2 multiplicative augmentations with std ~ U(0.1, 0.2)** (`sel.py:1346`), **+~1 % of the GTRS vocabulary** (`sel.py:1352-1357`), all simulator-scored.
* Score = `σ(NC)·σ(DAC)·(5σ(TTC) + 5σ(EP) + 2σ(C))/12` (`sel.py:1032-1037`); BCE per sub-score + `MarginRankingLoss(margin=0.05)` (`sel.py:904`); coarse top-**32** → fine (`sel.py:1372`).
* Inference: 10 groups × 20 anchors, **+3 augmentations U(0.1, 0.3)** ⇒ 800 candidates (`sel.py:1444`, `:1270`); the foreign vocabulary is **train-only** (INHERITED from `…/2026-09-10-ddv2-usable-now/RESULT.md` §1.1).
* Attribution the authors publish: DiffusionDrive 88.1 → + V2's selector 89.1 → full V2 91.2 PDMS (P Tab.9) ⇒ **selector +1.0, RL ≈ +2.1**.

## 9. Evaluation

| # | item | paper | code |
|---|---|---|---|
| 9.1 | benchmark | **NAVSIM v1 navtest, PDMS** 91.2 (P Tab.1); **NAVSIM v2 navtest, EPDMS** 85.5 (P Tab.2; EPDMS = P Eq.15) | — |
| 9.2 | tier | *"closed-loop metrics"* (P Tab.1/2 captions) | ⛔ 4 s non-reactive kinematic replay ⇒ **our T1 (self-action open loop)**, per the PI's 2026-09-02 ruling |
| 9.3 | inference path | 2 denoising steps, η = 0 (P §5.2) | the deployable pick is the **stage-II selector's** (`sel.py:1390-1491`). Two eval scripts: `run_pdm_score_fast.py` (`train_eval.md:56-64`, *"several times faster"*) and the official `run_pdm_score.py`, which needs `sel.py:1491` uncommented (`train_eval.md:67-74`). ⚠️ **which path produced Tab.1's 91.2 is UNVERIFIED** (I, still true) |
| 9.4 | **A16 (NEW here)** the RL model's own test metric is an ORACLE | — | `forward_test_rl` scores 80 candidates (hard-coded `num_groups = 4`, `rl.py:951`) and reports `reward_group.max(dim=-1)` — **best-of-80 PDMS** (`rl.py:1010-1012`), not a selection any deployed model makes. Its `loss` is the L1 of all 80 candidates to the GT (`rl.py:1014-1015`). ⇒ any stage-I "validation reward" logged by this code is an oracle-selection number |
| 9.5 | raw-fan readout | P Tab.3: diversity / PDMS@1 / @5 / @10 on 20 raw trajectories: DD 42.3 / 93.5 / 84.3 / 75.3 → V2 **30.3 / 94.9 / 91.1 / 84.4** | ⚠️ P does not say what the "Top-K ranking" ranks by (true PDMS or a model score) ⇒ **UNVERIFIED** |
| 9.6 | intervals | none — every number is single-run, no seed count, no CI (I; re-read today) | — |
| 9.7 | ablations, all on a reduced-epoch base of 90.1 (P§5.4) | multiplicative vs additive noise 89.7 → 90.1 (Tab.4); intra-anchor grouping 89.2 → 90.1 (Tab.5); inter-anchor truncation 89.5 → 90.1 (Tab.6). **Not ablated anywhere: the reward, the ≥GT mask, the all-modes IL pull, G, the chain length, γ, the likelihood floor** | — |

## 10. The asymmetry register (paper ↔ code), numbered for the diff

| id | one line | class | new today? |
|---|---|---|---|
| A1 | decoder is 1 layer, v1's 2nd dropped by `strict=False` | PUBLISHED-CODE | inherited |
| A2 | dropout p = 0.1 live in rollout and grad pass (head `.train()`); a third, unmodelled stochastic source | PUBLISHED-CODE (+ one UNVERIFIED site) | **NEW** |
| A3 | "η = 1 DDPM" is multiplied by zero; only multiplicative scale noise | PUBLISHED-CODE | inherited |
| A4 | likelihood (rank 16, σ 0.1) ≠ sampler (rank 2, σ 0.04·|μ|) | PUBLISHED-CODE | inherited |
| A5 | normalization x/50, y/20 vs v1's | PUBLISHED-CODE | inherited |
| A6 | noise at t = 8, chain labels start at 18, one-unit transitions; 61 % of the gradient weight on the last step | PUBLISHED-CODE + ESTIMATED | label mismatch inherited; **weight share NEW** |
| A7 | ≥GT positive mask, per sample, absent from the paper | PUBLISHED-CODE | inherited |
| A8 | −1 on NC≠1 **or** DAC≠1 (paper: collision) | PUBLISHED-CODE | inherited |
| A9 | ~~three multiplicative rows multiplied; third row unidentified~~ **RESOLVED §13: `MultiMetricIndex` has TWO members (NC, DAC)** — the reward's gate is exactly Eq.14's NC × DAC; `rl.py:44`'s "(3, N)" comment is stale | PUBLISHED-CODE-UNBANKED (§13) | **NEW → resolved** |
| A10 | EP reference = scorer proposal 0 = **`metric_cache.trajectory` = the PDM-Closed planner's trajectory** (§13), NOT the human; the fork's own comment calls it "GT" | PUBLISHED-CODE-UNBANKED (§13) | **NEW (corrects the sibling) → resolved** |
| A11 | RL loss averaged over non-zero-advantage samples, not over N·G | PUBLISHED-CODE | **NEW** |
| A12 | IL pulls all 80 chains × 10 steps to the one GT | PUBLISHED-CODE | inherited |
| A13 | λ_IL = 1.0 on rows with no positive advantage, 0.1 otherwise | PUBLISHED-CODE | inherited |
| A14 | IL loss is a batch-global scalar; per-row λ only sets its batch-mean weight | PUBLISHED-CODE | **NEW** |
| A15 | "10 % linear warmup" is a no-op under per-epoch stepping | PUBLISHED-CODE + UNVERIFIED (framework default) | **NEW** |
| A16 | the RL model's own test reward is best-of-80 (oracle) | PUBLISHED-CODE | **NEW** |
| A17 | per-module `.eval()` on the frozen trunk may be undone by the framework's global `.train()` | UNVERIFIED | **NEW** |
| A18 | x̂0 clamped to [−1, 1] inside every step (diffusers `clip_sample=True` default; the constructors pass none) ⇒ emitted and ranked trajectories capped at 50 m / 20 m; zero final-step gradient on a clamped coordinate | PUBLISHED-CODE + MEASURED (diffusers 0.40.0 executed) + UNVERIFIED (authors' pin) | **NEW (resume pass)** — missed by the first pass of this file and by the WIP port; caught by the bitwise test |

## 11. Corrections this file makes to earlier programme text

1. **`…/2026-09-05-diffusiondrive-v2-analysis/RESULT.md` §1.2(b)** says *"the only stochasticity in the RL chain is the 2-scalar multiplicative scale"*. C also has the t = 8 start noise (which that document mentions under (c)) **and dropout p = 0.1 in train mode (A2), which it does not**. Mechanism-level consequence: the "stretch/shrink" reading of the policy stands for the likelihood; it does not hold for the sampler.
2. The same document §1.2(f) and §1.3 #9 say EP is re-normalised *"against the GT as proposal 0"*. The bank cannot establish that; the returned-shape arithmetic (81 in, 81 out, GT last) shows index 0 is prepended by an unbanked function whose NAVSIM namesake prepends PDM-Closed (A10). **Downgraded to UNVERIFIED**, not reversed.
3. §1.2(e)'s *"IL_loss = mean L1 … and il_weight is 1.0 for any batch row with no positive-advantage sample"* is correct about the weights and incomplete about the value: the L1 is a batch-global scalar (A14).

Neither correction changes a conclusion that document drew; they change what a port must copy.

## 12. The UNVERIFIED list for this file

1. ~~The third multiplicative metric row (A9) and NAVSIM's NC value set {0, 0.5, 1} (§4.4)~~ — **resolved in §13** (class PUBLISHED-CODE-UNBANKED: read at the pinned commit through a model-transcribing fetch; the bytes are not hashed or banked).
2. ~~The EP reference trajectory (A10)~~ — **resolved in §13** (same class).
3. ~~TTC horizon, comfort thresholds, DAC footprint rule (§5.7)~~ — **resolved in §13** except the Savitzky-Golay window/order used for the comfort derivatives and the at-fault `get_collision_type` / `EgoAreaIndex` rules, which live in files not read (UNVERIFIED).
4. The BEV-attention dropout site in v1's `blocks.py` (A2 third site) — v1 `blocks.py` not banked.
5. Per-epoch LR stepping (A15) and whether `.eval()` on the trunk survives Lightning (A17) — framework behaviour.
6. `final_alpha_cumprod = 1.0` at the last step (§3.2) and `clip_sample = True` (A18) — both **MEASURED as the defaults of diffusers 0.40.0** (the vendored class executed on this box); the diffusers version the authors ran is not banked, so what THEIR environment used stays UNVERIFIED.
7. Which evaluation script produced P Tab.1's 91.2 (§9.3); what "Top-K" ranks by in P Tab.3 (§9.5).
8. navtrain's sample count, hence the number of RL optimiser steps (§7.8).

## 13. Framework items resolved at the pinned commit (resume pass, 2026-09-15)

**Evidence class `PUBLISHED-CODE-UNBANKED`:** read from `raw.githubusercontent.com/hustvl/DiffusionDriveV2/1cd12a1e155c34dcc471261835444c8d5587580b/…` through a fetch tool that transcribes the page with a small model. The quotes below are what that transcription returned; **the bytes were not hashed and are not in the bank**. Re-banking the four files with sha256 upgrades this to PUBLISHED-CODE. Nothing here is a claim about NAVSIM upstream, only about the fork the release runs.

| # | question (was UNVERIFIED) | answer | file (fork, pinned commit) |
|---|---|---|---|
| 13.1 | **A10: what is scorer proposal 0?** | `pdm_score_para` builds `trajectory_states = concatenate([pdm_states[None], pred_states_batch])` with `pdm_states` from **`metric_cache.trajectory`** (its comment says *"第 0 条是 GT"*, i.e. "index 0 is GT"), and `metric_cache_processor.py` fills that field with **`pdm_closed_trajectory = self._pdm_closed.compute_planner_trajectory(planner_input)`**. ⇒ **EP is re-normalised pairwise against the PDM-Closed planner, not the human.** The appended human trajectory (candidate 81) is normalised against PDM-Closed too, so the ≥GT bar (A7) compares every candidate with the human's PDMS **under a PDM-Closed progress reference** | `navsim/evaluate/pdm_score.py`; `navsim/planning/metric_caching/metric_cache_processor.py` |
| 13.2 | **A9: how many multiplicative rows?** | `class MultiMetricIndex(IntEnum): NO_COLLISION = 0; DRIVABLE_AREA = 1` and `_multi_metrics = np.zeros((len(MultiMetricIndex), N))` ⇒ **two rows**; `WeightedMetricIndex` = PROGRESS 0, TTC 1, COMFORTABLE 2, DRIVING_DIRECTION 3 (weight 0.0). ⇒ `prod(mm)` in `_pairwise_scores` is exactly NC × DAC = P Eq.14; **no third gate** | `…/pdm_planner/utils/pdm_enums.py`; `…/scoring/pdm_scorer.py` |
| 13.3 | **NC value set** | `no_at_fault_collision_score = 0.0 if tracked_object.tracked_object_type in AGENT_TYPES else 0.5`, applied only to **at-fault** collisions (`ACTIVE_FRONT_COLLISION` or `STOPPED_TRACK_COLLISION`, or `ACTIVE_LATERAL_COLLISION` while the ego is in multiple lanes / non-drivable area); non-at-fault contacts are added to an ignore list; red-light tokens are skipped. ⇒ values **{0, 0.5, 1}**, and DDv2's veto `no_collision != 1` (`rl.py:900-902`) sends a **static-object (0.5)** at-fault contact to −1 as well | `…/scoring/pdm_scorer.py::_calculate_no_at_fault_collision` |
| 13.4 | **TTC** | binary; ego polygon (with its CENTER coordinate replaced by FRONT_LEFT) projected at constant velocity along its heading to `future_time_idcs = np.arange(0, 10, 3)` × 0.1 s = **{0, 0.3, 0.6, 0.9} s**; 0 if it intersects a track that is **ahead** of the rear axle, or (ego in multiple lanes / non-drivable / intersection) and not behind; skipped when ego speed < `stopped_speed_threshold` 5e-3 m/s | `…/scoring/pdm_scorer.py::_calculate_ttc` |
| 13.5 | **Comfort** | binary `np.all` of six checks from `ego_is_comfortable` over the 4 s simulated states: `max_abs_mag_jerk = 8.37` m/s³, `max_abs_lat_accel = 4.89` m/s², `max_lon_accel = 2.40` m/s², `min_lon_accel = -4.05` m/s², `max_abs_yaw_accel = 1.93` rad/s², `max_abs_lon_jerk = 4.13` m/s³, `max_abs_yaw_rate = 0.95` rad/s; derivatives by Savitzky-Golay (window/order **not read**, UNVERIFIED) | `…/scoring/pdm_comfort_metrics.py` |
| 13.6 | **DAC** | `off_road_mask = self._ego_areas[:, :, EgoAreaIndex.NON_DRIVABLE_AREA].any(axis=-1)` ⇒ 0 if the ego is in non-drivable area at **any** of the 41 simulated ticks (how `_ego_areas` is filled — corners vs centre — not read, UNVERIFIED) | `…/scoring/pdm_scorer.py::_calculate_drivable_area_compliance` |
| 13.7 | **EP (raw)** | `progress = centerline.project([start_center, end_center]); raw = progress[1] - progress[0]`, clipped at 0 — metres along the **route centerline**, first to last simulated tick | `…/scoring/pdm_scorer.py::_calculate_progress` |

**Consequence for a port (carried into `DIFF_OURS_VS_SPEC.md`):** the release's reward needs, per scene, (a) agent boxes with types over 4 s (NC, TTC), (b) a drivable-area map (DAC, and the at-fault / TTC area tests), (c) a route centerline (EP), (d) **a PDM-Closed reference trajectory** (EP normalisation) and (e) the human trajectory (the bar). A dataset without (d) can only normalise against the human, which is a DIFFERENT reward, not a port.

## 14. Verification record of this file (resume pass, 2026-09-15)

* Paper re-read on this box for every P cite in §1–§7 and §9: §4.1–4.5 (Eq. 3–10), §5.1–5.2, suppl. §7 and Tab. 7 (`raw/2512.07745v1_text.txt` lines 360–699, 840–855, 1495–1607). **No P cite was found wrong.**
* Code re-read in full: `rl.py:1-140, 143-270, 272-516, 677-1171`; `agent.py` (all); `cfg.py` (all); `agent.yaml`, `scheduler.py`, `scoring.yaml`, `training.yaml`, `train_eval.md` (all); `sel.py:1400-1445`; v1 `transfuser_model_v2.py:396-447`. **Two omissions found and fixed:** A18 (the x̂0 clamp) and the MHA dropout sites in A2 (inert). No C cite was found wrong.
* Executed: the release's `DDIMScheduler_with_logprob` (vendored verbatim, excerpt sha256 `ae98ea76…`, source sha256 `2789081a…` equal to the bank; bank blob `35555bbc` identical in the snapshot and HEAD) against the port — bitwise at all 10 labels, η ∈ {0, 1}, with the clamp live on ~50 % of coordinates.
