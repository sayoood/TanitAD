# Each line: a token from the LANDED version that this rewrite does not carry, and WHY.
# ⛔ A waiver is a claim that the information survives in a BETTER form, never that it
#    stopped mattering. Anything that cannot be written that way is a real drop and the
#    document is wrong, not the guard.
3.11   # superseded by the PRECISE value 3.107 read from heldout_L1-RL-s0_summary.json
       # ("selection_gap.sel_ade_over_fan_minade"). The rounded form was mine; the banked
       # artifact carries 3 dp, and the control's 2.497 is now printed beside it.
