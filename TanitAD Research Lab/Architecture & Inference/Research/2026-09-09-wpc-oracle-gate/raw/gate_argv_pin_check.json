[v3] --sel-score-emitted on a ddim arm: sel_score_emitted_t -1 -> 0 (the emitted fan IS the denoised state; -1 would index the refinement loop's embedding table instead)
[v3] --sel-score-emitted on a ddim arm: sel_score_emitted_t -1 -> 0 (the emitted fan IS the denoised state; -1 would index the refinement loop's embedding table instead)
[v3] WP-B waypoint index ON: mode=geom detach=False radius_m=0 hidden=32 scale_m=10 -> 4 decoder layers
[v3] --sel-score-emitted on a ddim arm: sel_score_emitted_t -1 -> 0 (the emitted fan IS the denoised state; -1 would index the refinement loop's embedding table instead)
[v3] --sel-score-emitted on a ddim arm: sel_score_emitted_t -1 -> 0 (the emitted fan IS the denoised state; -1 would index the refinement loop's embedding table instead)
[v3] WP-B waypoint index ON: mode=geom detach=False radius_m=0 hidden=32 scale_m=10 -> 4 decoder layers
[v3] --sel-score-emitted on a ddim arm: sel_score_emitted_t -1 -> 0 (the emitted fan IS the denoised state; -1 would index the refinement loop's embedding table instead)
[v3] WP-B waypoint index ON: mode=const detach=False radius_m=0 hidden=32 scale_m=10 -> 4 decoder layers
[v3] --sel-score-emitted on a ddim arm: sel_score_emitted_t -1 -> 0 (the emitted fan IS the denoised state; -1 would index the refinement loop's embedding table instead)
[v3] WP-B waypoint index ON: mode=shuffle detach=False radius_m=0 hidden=32 scale_m=10 -> 4 decoder layers
{
  "B0": {
    "pin": "OK",
    "agents_enable": true,
    "agents_oracle": true,
    "cross_agent": true,
    "wp_index_built": false,
    "stamp_agents_oracle": true
  },
  "B1": {
    "pin": "OK",
    "agents_enable": true,
    "agents_oracle": true,
    "cross_agent": true,
    "wp_index_built": true,
    "stamp_agents_oracle": true
  },
  "B0r": {
    "pin": "OK",
    "agents_enable": true,
    "agents_oracle": true,
    "cross_agent": true,
    "wp_index_built": false,
    "stamp_agents_oracle": true
  },
  "B1r": {
    "pin": "OK",
    "agents_enable": true,
    "agents_oracle": true,
    "cross_agent": true,
    "wp_index_built": true,
    "stamp_agents_oracle": true
  },
  "B1const": {
    "pin": "OK",
    "agents_enable": true,
    "agents_oracle": true,
    "cross_agent": true,
    "wp_index_built": true,
    "stamp_agents_oracle": true
  },
  "B1shuf": {
    "pin": "OK",
    "agents_enable": true,
    "agents_oracle": true,
    "cross_agent": true,
    "wp_index_built": true,
    "stamp_agents_oracle": true
  }
}
