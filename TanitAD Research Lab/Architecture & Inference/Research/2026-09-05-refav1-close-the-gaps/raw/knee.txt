================================================================================================
ANALYTIC UPPER BOUND ON A2's KNEE: W_flip = (basecost_cv - plan_cost) / mean(kappa^2)
  valid because W_JERK = 0 and W_VEND is a DEAD TERM (target_speed is never passed)
================================================================================================
  goal token       n    mean k^2     cv-plan      median  W_flip range
                       (nonzero)   advantage      W_flip     p25 - p75
  ------------------------------------------------------------------------------
  LANE_KEEP        8    0.006543     1.01222     184.330  108.57 - 367.85
               (+10 window(s) already straight at W_KAPPA=0, excluded)
  TURN_L           9    0.006698     0.99780     153.414  144.82 - 161.86
  TURN_R          13    0.006400     0.98537     153.070  148.87 - 157.39

  ⭐ COMMITTED PREDICTION FOR A2, from this arithmetic:
     W_KAPPA = 1         -> 100.0 % of TURN_L windows still prefer the turn (UPPER bound)
     W_KAPPA = 3         -> 100.0 % of TURN_L windows still prefer the turn (UPPER bound)
     W_KAPPA = 7         -> 100.0 % of TURN_L windows still prefer the turn (UPPER bound)
     W_KAPPA = 15.112    -> 100.0 % of TURN_L windows still prefer the turn (UPPER bound)
     n_true(turn_left) on this panel = 11, recall at W=0 is 0.3636 (4/11).
  ⛔ UPPER BOUND: iCEM reduces MAGNITUDE before it flips to straight, so real
     turn recall dies EARLIER than these numbers say. If A2 measures turn_left
     recall 0.0000 at a W where this predicts most windows still 'prefer' the
     turn, that CONFIRMS the two-candidate model is an upper bound -- it does
     not refute the arithmetic.

================================================================================================
MAGNITUDE MODEL -- because the collapse is by MAGNITUDE, not by a flip
================================================================================================
  A quadratic penalty against an ~linear goal gradient gives k*(W) = k0 / (1 + W/W0).
  Fitted on the W=15.11245 point: W0 = 5.2616
  W_KAPPA      predicted k*     MEASURED
  0                 0.08000      0.08000
  15.112            0.02066      0.02066
  151.12            0.00269      0.00840
  1                 0.06722      -- (A2)
  3                 0.05095      -- (A2)
  7                 0.03433      -- (A2)

  ⚠️ The model is fitted on ONE point and checked on another: at W=151.1245 it
     predicts 0.00269 against a measured 0.00840, i.e. it OVER-predicts the collapse
     at large W. So its {1,3,7} numbers are, if anything, PESSIMISTIC.

  ⭐ COMMITTED PREDICTION FOR A2 (both outcomes, before the arms run):
     W=1 and W=3 keep TURN_L magnitude at 0.051-0.067, comfortably above the
     0.02066 at which recall was measured 0.0000 => turn_left recall SURVIVES.
     W=7 lands at ~0.034, between the surviving 0.08 and the dead 0.02066 =>
     THE KNEE IS PREDICTED IN (3, 15.11], most likely NEAR W=7.
  ⛔ IF INSTEAD turn_left recall is 0.0000 already at W=1, the magnitude model is
     REFUTED and the collapse is not a smooth cost balance -- it is the goal SEED
     losing the argmin outright, which the W_flip ~153 bound says should NOT
     happen until W ~ 150. Either result is informative and both are written here.
