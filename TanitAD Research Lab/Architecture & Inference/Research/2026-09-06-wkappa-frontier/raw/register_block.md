
### The `W_KAPPA` frontier — an interior optimum with a mechanism, and a census correction (2026-09-06, W_KAPPA frontier stream)

⭐ **The question put to this stream:** *does an arm exist that tracks the road better than a
straight line and still turns left, or is the trade-off fundamental to this cost geometry?*
Pre-registration `…/2026-09-06-wkappa-frontier/SPEC.md` (commit `4a9ec31`, written before any arm
of the package produced output), amended in `SPEC_AMENDMENT_A1.md` **without changing a criterion**.
All rows **T1** (self-action open loop) except `ol` (**T0**); `kamm_over_rate` is a **COUNT over
27 windows** and never carries a CI. ⛔ Nothing here is closed loop, and nothing here is a
training-variance claim (`I17`/V2 unpriced for refav1).

| id | claim | status | evidence |
|---|---|---|---|
| ⭐⭐ **D-WKAPPA-FRONTIER-1** | ⭐⭐ **`W_KAPPA = 7` IS AN INTERIOR OPTIMUM FOR A MECHANISTIC REASON, NOT BECAUSE A CURVE BENT — THREE SATURATIONS, ALL MEASURED, ALL AT DIFFERENT PLACES.** (a) **SAFETY saturates at `W_KAPPA = 1`**: `kamm_over` is already **0.0000** non-vacuously (`max|kappa|` 0.0800 = 4.0x the `M58` gate) at 1, and the speed above which large curvature is never emitted is **6.378 m/s at `W_KAPPA` 1, 7, 15 AND `best` — identical** — against the recorded human's own 7.436. (b) **LANE-KEEP straightening saturates at ~7**: `LANE_KEEP` mean `k^2` **5.6e-5 -> 1.8e-5 -> 2.0e-6 -> 0.0** across 1/3/7/15.11, i.e. by 7 there is nothing left to straighten. (c) **TURN EXECUTION survives to 7 and collapses by 15.11**: `TURN_L` med\|k\|max holds at the commanded **0.08000** through 1/3/7 and falls to **0.02066** at 15.11, while `TURN_R` holds at 0.08000 throughout. ⇒ **7 is the unique weight at which all three hold**, and every unit spent above it buys no safety, no straightening, and costs the left turn | **SUPPORTED (MEASURED 2026-09-06, T1, 0 GPU — all four instruments re-read BANKED p4 dumps, nothing re-rolled).** `wk7` at seed 0 satisfies all three PASS clauses: curvature MAE **0.033522** = **0.006561 below the `ha0` straight floor 0.040083 = 3.3x the 0.00200 inference-seed floor**; `turn_left` recall **0.2727 = 3 of 11** against an exact 0.00000 recall floor; `kamm_over` **0.0000** non-vacuously. ⚠️ **Seed replication IN FLIGHT** (`wk7` seeds 1 and 2 on the dev-box 4060 as this row lands) — until it returns this is a **single-inference-seed** result on a **sampling** planner and is not a replicated claim | `…/2026-09-06-wkappa-frontier/raw/feas_census_all.txt`, `raw/kappa_by_goal_rungs.txt`, `raw/vocab_ceiling.txt`, `raw/ff_all_rungs.txt`; instruments `feas_audit.py` / `kappa_by_goal.py` / `four_family_table.py` of `…/2026-09-05-refav1-cost-geometry/` run **unmodified** |
| ⛔⛔ **D-WKAPPA-VARIANCE-SPLIT-1** | ⛔⛔ **THE TWO VARIANCES DISAGREE ABOUT `wk7`, AND NAMING WHICH ONE AN INTERVAL ANSWERS IS WHAT DECIDES THE VERDICT.** Under **INFERENCE-seed** variance — the floor this SPEC committed to — `wk7`'s curvature margin over the straight line is **3.3x the 0.00200 floor** and CLEARS. Under **EPISODE** variance, a paired episode-cluster bootstrap on the same windows reads **−0.0089 [−0.0242, +0.0016] NOT separated**; only `wk15` — the arm that does **not** turn — separates (**−0.0105 [−0.0259, −0.0009]**). ⇒ **the frontier point is established on the inference axis and is NOT established on the episode axis at n = 40 windows / 8 clusters.** ⭐ **This names the next lever exactly, and it is the OPPOSITE of the tiny-rig guidance:** the binding interval here is the **episode** one, so the cheapest next experiment is **MORE EPISODES, not more seeds** | **SUPPORTED as a statement about the two estimators (MEASURED 2026-09-06, T1, 0 GPU).** ⛔ **The interval itself is reported INDICATIVE, NOT DECISION-GRADE, because a control FAILED:** the recomputed `cl` mean reproduces the banked `curvature_mae_1pm` at `min_ds` 0.05 (**5/5 within 5e-4**) but the `ha0` side reads **0.042584 against the banked 0.040083** — a 0.0025 gap, **above the 0.00200 floor** — and no gate tried reproduces both arms. ⭐ It is quoted at all only because a **sensitivity sweep over `min_ds` 0.02 / 0.05 / 0.10 gives the IDENTICAL separated/not-separated verdict on every arm** | `…/raw/paired_curvature_indicative.txt`, `raw/paired_curv.py`, `raw/calib_ha0.py`; estimator `taniteval.ci.paired_episode_cluster_bootstrap` **imported, never edited** |
| ⛔ **D-WKAPPA-ZEROCENSUS-2** | ⛔ **THE ZERO-VIOLATION CENSUS IS 16 CONFIGURATIONS, NOT SIX — AND THE THREE CHEAPEST WERE THE ONES MISSING.** Auditing **25** p4 arms in **ONE generation** (the `D-REFAV1-CG-ZEROVIOL-SCOPE` fix: the vacuity comparison and the rate a claim rests on may not come from different files) finds **16** distinct configurations at `kamm_over` **0.0000**, `v0 >= 2`, `n = 27`: **12 non-vacuous** (`wk1`, `wk3`, `wk7`, `wk15`, `best`, `best_seed1`, `combined` s0 only, `seambase`, `seamon`, `lonseam`, `loncomb3`, `lonshift`) and **4 vacuous** (`cos_wk` 0.0000, `wk15_ladder` 0.0020, `bestlad` 0.0049, `wk151` 0.0166). ⛔ **`wk1`, `wk3` and `wk7` had never had their feasibility measured at all** — the published "six" census contains none of them — so the earlier count was again **scoped to the arms one artifact happened to hold**, which is the very error its own root-cause note was written about. ⇒ **the friction-circle zero is bought at `W_KAPPA = 1`**, and `best`'s standing as the arm that earns it does not survive the wider census | **SUPPORTED (MEASURED 2026-09-06, T1, 0 GPU, `assert_feasible`, `FEAS_VMIN=2`).** ⛔ Bracketed on both sides in the same generation: the recorded human `g` reads **0.0000** (it must — a real vehicle's motion is feasible by construction) and `ha` **0.1481** / `ha0_ext` **0.1852** / `ol` **0.1111** read non-zero (they must) | `…/raw/feas_census_all.txt`, `raw/feas_census_rest.txt` |
| ⛔ **H-WKAPPA-VOCAB-STRUCTURAL** | ⛔ **REFUTED — MY OWN HYPOTHESIS, KILLED BY ITS OWN CONTROL-BEARING PROBE.** `max|kappa|` reads **exactly 0.0800** — `GOAL_KAPPA_TURN`, the shipped `L1-0.08` vocabulary's single turn level — on **seven** arms (`wk3`, `wk7`, `wk15`, `best`, `best_seed1`, `seambase`, `lonseam`). I predicted the zero was therefore **structural**: `a_lat = v^2*kappa`, so `kappa <= 0.08` is inside the `mu*g` circle below **9.265 m/s**. ⛔ **MEASURED: this panel reaches 15.853 m/s**, well above that break-even — at which the vocabulary's own 0.08 would read **2.05 g**. **The cap alone does NOT guarantee the zero.** ⭐ **What the refutation exposed is the real mechanism** (`D-WKAPPA-FRONTIER-1`a): the penalty's safety function is to **confine large curvature to low speed**. With it OFF, `|kappa| >= 0.05` is emitted at up to **15.087 m/s** (`ccos_argmax`) and **10.579** (`gkappa`), for **3.258 g** and **2.169 g** — the circle exceeded **4.7x** | **REFUTED (MEASURED 2026-09-06, T1, 0 GPU).** Both controls passed in the same generation: the human `g` reads `max|kappa|` **0.1797**, which it **must** exceed the vocabulary cap being a real vehicle and not a token; `ha0` reads `max|kappa|` **exactly 0.0000**, it being a perfectly straight line | `…/raw/vocab_ceiling.txt`, `raw/vocab_ceiling.py` |
| ⭐ **D-WKAPPA-UNDEREXEC-1** | ⭐ **`I22` SHARPENED: `wk15`'s `turn_left` recall 0.0000 IS NOT A REFUSAL TO MOVE — IT IS UNDER-EXECUTION OF A COMMANDED MANOEUVRE.** `frac k != 0` stays **1.0000 on 9/9 `TURN_L`-goal windows** at `W_KAPPA = 15.11245`; the plan turns on every one of them, at **26 %** of the 0.08000 its own tactical brain commanded, which is below the labeller's threshold. ⇒ three distinct failure modes must now be distinguished, and only the first two have instruments: a **VACUOUS** arm that never moves (`cos_wk`, caught by the `M58` magnitude gate); an arm that **DECLINES the manoeuvre class**; and an arm that **ACCEPTS the class and under-executes it** — invisible to both the vacuity gate and to recall alone, and visible only in realised curvature **conditioned on the decoded goal token** | **SUPPORTED (MEASURED 2026-09-06, T1, 0 GPU).** ⚠️ The asymmetry is unexplained and is a named work item: `TURN_R` holds at 0.08000 at the same weight where `TURN_L` collapses, so a **symmetric** `|kappa|` penalty is producing a **signed** outcome — the cause must lie in the goal term it trades against, not in the penalty | `…/raw/kappa_by_goal_rungs.txt`; the `--w-kappa-by-goal` flag's own help text names the same 0.02066 |

⛔ **What this stream does NOT establish.** (1) **40 windows / 8 episodes, one checkpoint (21,109)** — no corpus-scale or parity claim. (2) **Inference-seed replicates are not training replicates**; `I17`/V2 stays unpriced. (3) `kamm_over_rate` is a **feasibility statistic, not a collision or off-road rate** — this programme has no reactive-agent simulator. (4) **STRATEGIC** (`n = 0`, the refav1 arm tool emits no route head) and **distance-keeping** (`n = 0`, no lead-agent track supplied) are **UNAVAILABLE with their reasons stated**, per clause 5 of the four-family rule, and are not silently dropped. (5) The stack used is the `tanitad-ctg` clone — the same snapshot that produced every banked p4 arm, chosen for comparability; it differs from repo HEAD by an **additive, default-off** distance-keeping cost term.

<!-- WKAPPA-FRONTIER-2026-09-06 -->
