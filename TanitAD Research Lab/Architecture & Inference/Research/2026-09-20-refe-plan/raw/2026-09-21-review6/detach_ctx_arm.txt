  detach_scorer_context=True  (shipped): backbone LoRA grad 0.000000  scene_proj grad 0.000000
  detach_scorer_context=False (mutated): backbone LoRA grad 401.570572  scene_proj grad 172142.449707
  validate_model's assertion `backbone_clean` would read: clean=True  mutated=False
  -> ARM IS LIVE: it goes RED under its own declared departure being removed
  -> but validate_model.py never performs this flip; it is the one asserting arm in the
     file with NO deliberate-regression control, and the control is these four lines.
