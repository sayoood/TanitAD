# REFe training readiness — the closing checklist

**Opened 2026-09-22 23:5x on PI instruction:** *"Loop autonomously over night to achieve training
readiness of refe and review the whole setup to finish, iterate until readiness achieved and no
issues are existing."*

⛔ **"Ready" is defined here as a TESTABLE state, not a feeling.** Every row below is either green
with a named artifact, or it is open with the reason. A row may not be closed on an argument — only
on a measurement or a run. `PI` rows cannot be closed by me at all.

**Arm chosen by the PI 2026-09-22: A + augmentation** (navtrain, no SimScale, K=2 with a per-frame
diversity search).

---

## 1 · Data

| # | item | state | evidence |
|---|---|---|---|
| D1 | navtrain scenario source from DB tokens | ✅ | `refe/navtrain_scenarios.py`; devkit equivalence 1,875 tokens, **0 disagreements**; yield **18,179/18,179** |
| D2 | rank-0 target bank, **18,122** frames (local 17.6 % of navtrain) | ✅ DONE 16:58 | **18,122/18,122 rows**, 33,743 s wall at 4 shards, **0 restarts**; the detached chain started its scorer (stage B) at 16:59 and the gate (C) follows -- `data/refe_navtrain/run_BC_r0.out` |
| D3 | augmentation ladder designed + cost measured | ✅ MEASURED | stratified 56 frames: tau 0.5 -> 52 % / 3.25 rollouts/frame; tau 0.3 -> 71 % / 2.70; **tau is a PI decision** (P3) |
| D4 | augmented bank (K=2, guaranteed diverse) | ⬜ blocked on D2+D3 | |
| D5 | scorer bank, both arms | ✅ path verified | rank 0: S1 31/31 on the corrected bank; **augmented: per-row route (R18), S1 29/29, mutation control lane-rank-1 0/10 vs goal-horizon 11/18** |
| D6 | camera pixels reachable **by the trainer** | ✅ **join MEASURED** (R21) | OpenScene `navtrain_current`, 32 tgz / 304.5 GB, layout `navtrain_current_N/<log>/<CAM>/<hash>.jpg`. On the one log held both as a DB and in shard 25: the producer's image choice == OpenScene's association, **61/61 frames x 4 cameras, both directions**; the REAL producer's rows resolve **61/61** in the trainer and decode. ⛔ The old *"basename-keyed so depth is irrelevant"* was true of `build_targets.index_images`, NOT of `train.FrameStore` -- which globbed the whole tree **per image** (808-1,040 ms / 41 K files MEASURED -> ~32-42 s per sample at scale, `raw/2026-09-23-train-readiness/glob_cost.txt`). Now one index, 40 ns lookups |
| D8 | trainer host RAM at full scale | ✅ MEASURED basis | scorer **36.9 KB/frame** + targets **10.4 KB/row** (tracemalloc, pilot) -> **~7.8 GiB** for 176 K tuples (ESTIMATED, linear); `pod_train.sh` refuses a container under 24 GiB |
| D9 | **rank-0 bank at 10 Hz** (supersedes D2's 20 Hz rows, see C17) | 🔄 RUNNING 2026-09-24 | REBALANCED on MEASURED rates: pod **978 logs / 85,109 frames**, 7 CPU-teacher shards, **~155 rows/min**; dev box **its 214 local logs / 18,179 frames**, 8 CPU-teacher shards (it is SHARED with other agents' refcv6 jobs: 39 rows/min on 8 CUDA shards, which is why it lost 249 logs back to the pod); dev rows reach the pod every 30 min (`code/devbox_upload.py`, dirs `r0_s*_DEV10`, inside the pod's resume glob); rows carry `sim_hz: 10` + `teacher_device`; the 20 Hz rows are KEPT aside (`r0_20hz_*`) |
| D7 | nuPlan DBs for ALL 1,192 navtrain logs (a pod holds none) | 🔄 tool ready, **pod-side** | `code/fetch_navtrain_dbs.py`; **88.80 GiB transit / 148.83 GiB disk MEASURED** (978/978 found); 68.5 GiB estimate was 2.2x low. **Pod-complete: 1,192/1,192, 97.57 GiB transit / 163.82 GiB disk** (train + val zips) |

## 2 · Code

| # | item | state | evidence |
|---|---|---|---|
| C1 | one shared row builder for both scenario sources | ✅ | val14 output **byte-identical** after the refactor |
| C2 | incremental write + resume | ✅ | resumed bank **== one-shot bank**; partial file parses mid-run, 0 torn rows |
| C3 | log-sharding, both builders | ✅ | partition exact: 3 shards, **overlap 0**, union == unsharded |
| C4 | thread pinning | ✅ | unpinned N=4 = 1.62x; pinned N=4 = **3.86x** |
| C5 | trainer consumes both banks | ✅ | `train.py` l.407/426/563: `ScorerBank` joined on `(log, token, step, rank)`, `loss = (l_traj + l_score)/accum` |
| C7 | **pod turnkey -- REHEARSED END TO END** | ✅ | `code/pod_teacher_env.sh` + `code/pod_dataprep.sh` (dbs calib pix rank0 aug score **assemble** gate) + `code/pod_train.sh` (weights preflight resume batch smoke train); both scripts run for real on a FAKE `/workspace` holding the one log we have as DB + OpenScene pixels (I6); runbook `POD_HANDOFF` §0/§0b; `.gitattributes` pins LF |
| C6 | ship list current | ✅ | re-derived from the import graph 2026-09-23 (`raw/2026-09-23-shiplist/`): **17 runtime** (+ `ckpt_io.py`, `calib_table.py`) · **10 gates** (+ `diag_train_resume.py`, `diag_calib.py`) · plus the shell-invoked stages (`pod_*.sh`, `fetch_navtrain_dbs.py`, the lock, the split) |
| C8 | **trainer saves and resumes** | ✅ FIXED (R20) | was: NO checkpoint, NO final model, NO resume. Now `--out` + `--resume`: partial ckpt (model + AdamW + schedule + RNG + exact data position), epoch snapshots, full `model_final.pt`, `summary.json` done-marker LAST; identity-checked resume |
| C9 | **every frame resolved at load** | ✅ FIXED (R21) | a missing frame RAISED mid-run -> crash loop under a supervisor. Now dropped + counted + named at load; > 0.1 % refused as a broken join |
| C10 | **pod training launcher** | ✅ written | `code/pod_train.sh`: pinned weights (sha256 == HF's own LFS hash) -> preflight -> resume test -> resident batch -> real-pixel smoke -> self-healing supervisor (stops on done=true, never restarts a deterministic refusal) |
| C11 | per-rank training bank (R19) | ✅ FIXED | trainer refuses a stray `targets_*.jsonl`; loads `{0: 56, 1: 29}` on the pilot; 29/29 rank-1 rows join their own scorer targets |
| C12 | **each sample's OWN camera rig** (PETR, the paper's cited embedding) | ✅ FIXED (R22) | was ONE baked vehicle: exact on **3.2 %** of 2,774 local logs (26-30 rigs/camera), rotation off by up to **2.67 deg**. Now `refe/calib_table.py` -> `train.py --calib` -> per-unique-rig frustum -> planner reads the scenario's DB. Default path bit-identical to before (12/12) |
| C13 | conformance gate reads what the trainer reads | ✅ FIXED (R23) | it read a SCORER row in the train dir and cried DIVERGED on a correct bank; now the trainer's glob, every rank file; a one-camera rank-1 file goes RED |
| C15 | **data prep speed, exact levers** (PI 2026-09-24: "not acceptable that the data prep takes so long") | ✅ MEASURED | profile: `_build_map_features` ~90 % of a rollout -> `refe/map_feature_cache.py` (exact memoisation of static map geometry): **1.81x**, pod 35 -> 68 rows/min; then sqlite 66 % of a cached rollout on the dev box -> `refe/query_cache.py` (exact, every hit AND miss re-verified against nuPlan's own query path: **2,976/2,976**, rows byte-identical on BOTH machines): pod 1.06x, dev 1.28x; the teacher's per-step visualisation dict skipped (`REFE_SKIP_DEBUG`, rows identical, `diag_lever_ab.py`): pod 1.10x. Pod 20 Hz: **35 -> ~80 rows/min** |
| C16 | a kill mid-flush can no longer fuse a good row onto a torn one | ✅ FIXED + mutation | `build_teacher_rollouts.open_append` (+ aug stats, scorer): a torn tail is terminated before appending; the OLD writer loses the next row, the fixed one keeps it (`ZZOPEN_APPEND_MUTATION_OK`) |
| C17 | **SIMULATION RATE: 10 Hz, nuPlan's own** (was 20 Hz) | ✅ SWITCHED 2026-09-24 -- **PI to confirm (P6)** | `navtrain_scenarios` built every scenario at `subsample_ratio=1.0` -> the teacher replanned at **20 Hz**; nuPlan's sim (`subsample_ratio_override: 0.5`), the teacher's published val14 numbers, our val14 logs and the controller default (`frame_time_interval: 0.1`) are all **10 Hz**. MEASURED on 73 frames / 10 logs (`diag_sim_rate.py`, `diag_sim_rate_pdm.py`): 10 Hz is **1.64x cheaper**; targets move **ADE 0.23 m / FDE 0.60 m** (not an exact lever); on our PDM calculators 10 Hz is no worse -- 0 collision / DAC / DDC at both, TTC 0.935 -> 0.943, progress 18.2 -> 18.7 m, PDM-like **0.9727 -> 0.9752**; curb clearance 0.647 -> 0.613 (3 frames). Decision rule written BEFORE the result. Rows carry `sim_hz`; the builders REFUSE to extend a bank of another rate (exit 5) |
| C18 | **training runs WHILE data prep runs** (`--grow`) | ✅ 9/9 | `train.py --grow`: the bank is re-read at every epoch boundary up to a BYTE SNAPSHOT stored in the checkpoint; `code/grow_assemble.py` builds `train_grow/` append-only (whole scorer frames only); `pod_train.sh GROW=1` (assemble stage + loop, rank-0-only preflight, `diag_grow.py` as the resume test). `refe/diag_grow.py` **8/9 first run**: epoch 0 after a MID-epoch bank append == the static run (every tensor), growth seen at the NEXT epoch (8 -> 12 tuples, 4 -> 7 scorer frames), a torn tail is not a row, the snapshot-ignoring mutation goes RED, `--grow-scenes` change REFUSED; the 9th arm first tested JSON parsing instead of the identity refusal -- fixed and re-run: a NON-grow resume on a grown bank is REFUSED (exit 4, `n_tuples 8 -> 12`). **9/9**; the pod re-runs the whole diag as its resume stage |
| C19 | a partial bank is an UNBIASED sample | ✅ | logs were processed in sorted = DATE order, so a growing bank was the early drives only; all three builders now shuffle their shard's logs (seed 20260924) -- order changes, rows do not |
| C20 | **the teacher runs on the CPU in data prep** | ✅ MEASURED | same 6 frames, both arms concurrent under production load: pod **28.4 s CPU vs 36.4 s CUDA** (7 processes time-slicing one GPU), dev box **29.6 vs 49.0 s**; CPU and CUDA rollouts differ by float noise only (0 - 7 cm/frame, vs 23 cm for the rate); the A40 is left WHOLLY to `train.py --grow` (0 GPU compute apps from data prep). `pod_dataprep.sh TEACHER_DEVICE=cpu` (default), rows record `teacher_device` |
| C21 | **scorer EGO VIEW: the two per-agent calculators run on agent 0 only** (data-prep lever for the scorer stage, 28 % of the remaining CPU) | ✅ EXACT on BOTH machines, ON by default -- **pod 1.77x, dev box 2.04x** (pod: 8 frames / 88 rows, REF == REF2 == EGO sha `ca65f041…`, 880/880 prefixes on the view, mutation differs; `raw/2026-09-24-levers-v2/pod_diag_scorer_ego_view.json`) | MEASURED per scored prefix: CenterLine 42.7 ms + OffRoad 17.8 ms of ~71 ms, because both build dense [N, 128 agents, ~1,024 segments] tensors while the scorer reads agent 0 only (28 slots valid, ONE controlled). `score_proposals._ego_view`: agent-axis fields sliced by an ENUMERATED list (`lanes_centers_lines` is [N, 8, 128, 2] -- a shape rule would slice a map), agent-index fields carried EMPTY, anything unclassified -> full object; exact from source (per-agent reductions only; collision/TTC keep the full object). `diag_scorer_ego_view.py`, 18 frames / 3 logs / 198 rows: REF == REF2 == EGO **byte-identical** (sha `b64d299a…`, PYTHONHASHSEED=0), EGO on **1,980/1,980** prefixes, 0 fallbacks; wrong-agent mutation -> different rows; **266 / 256 s -> 130 s = ~2.0x**. Found on the way: `inject_proposal` fills `agent_nearest_indices`, so the first A/B fell back on every prefix and tested nothing; and row BYTES follow the per-process string-hash seed (`targets` keys come from a set), so content equality is the criterion and bytes are compared under a fixed seed. `REFE_SCORER_EGO_VIEW=0` restores |
| C22 | **map-step fast path for the teacher rollouts** (the AUGMENTATION stage: 49 % of the remaining CPU) | ✅ EXACT on BOTH machines and BOTH paths, ON by default since 03:37 -- **pod rank 0 1.21x (16.5 -> 13.6 s / 6 rollouts), pod augmentation 1.31x (34.8 -> 26.5 s)**; the pod's 7 rank-0 shards were restarted onto it at 03:36:45 (supervisor relaunch, resume; `ZZRESTART A_r0 n=1` x7). ⚠ The pod's FIRST rank-0 A/B was VACUOUS -- a `head -c 400 | grep log_name` returned nothing, 0 logs, 0 rows -- and only the diag's `ref_rows_nonempty` check kept it from reading as identical (kept as `pod_map_step_rank0_ab_VACUOUS_0logs.json`; the runner now parses the row) | `refe/map_step_speedups.py` (`REFE_MAP_STEP_FAST`): the proximal-object query without a filtered GeoDataFrame per call, the drivable-area `unary_union` cached on the ordered row selection (**92-96 % hits**), the anchor rotation of route points and segments vectorised in the same float64 op order, a per-step edge-distance memo. MEASURED concurrently on the same frames: augmentation **166.3 -> 137.2 s for 26 rollouts = 1.21x**, rank 0 **36.7 -> 35.1 s = 1.05x**; rows AND search stats byte-identical; both mutations (segments +1 mm, route +1 mm) change rows. ⛔ cProfile had promised ~1.4x: it inflates call-heavy Python, so levers are now ranked by plain timers (`raw/2026-09-24-levers-v2/prof_roll_timers.py`). Found on the way: `feature_builder` imports `_sample_route_points_from_map` BY NAME, so a module patch never reached it (`route 0` calls) and the first mutation arm tested nothing |
| C23 | scorer: what is left after C21, MEASURED with plain timers (ego view ON, dev box, 6 frames, 4.41 s/frame) | 🔎 ranked, one cut in flight | per frame: `inject_proposal` (deepcopy + NPC splice + kinematics + k-NN) **18.1 %** · TTC 14.6 % · CenterLine 14.0 % (view) · **`route_arc_length` 13.4 %** · OffRoad 13.1 % (view) · collision 7.3 % · scenario build 7.4 % · comfort 5.0 %. **In flight:** `route_arc_length` computed all 128 slots and read `[0, 0]` -> ego-only under the same `REFE_SCORER_EGO_VIEW` flag (A/B re-run). **Parked:** a deepcopy that SHARES the map tensors (~5-9 %) -- the six calculators never write a scenario tensor in place (audit 2026-09-24: every item assignment / `*_` op targets a tensor the function creates), but the shared geometry helpers are not audited yet, so no exactness claim. TTC / collision need every NPC and stay whole |
| C14 | **an epoch is one pass over SCENES** (the paper's unit) | ✅ PI 2026-09-23 (R24) | was: an epoch over ROWS = 1.71x the paper's budget (4.40 M vs 2.58 M sample-passes). `train.py --epoch-unit scenes` (default): each scene once per epoch, its augmented twin taking turns; pilot 85 rows = **56 scenes**, `--epochs 2` = 2.00 scene-epochs; `pod_train.sh` passes it explicitly. ViT-L FP32 **~37-41 days** (was ~64-70) |

## 3 · Gates

| # | gate | state | evidence |
|---|---|---|---|
| G1 | consumer conformance (**load-bearing**) | ✅ | **CONSUMERS_CONFORM**, 7/7 arms, against the REAL navtrain bank |
| G2 | signal consistency | ✅ on pilot | `SIGNALS_CONSISTENT_PARTIAL_2_ARM_NA`; P1 max 0.160 m, P2 max 0.133 m, S1 18/18 |
| G3 | signal consistency on the FULL bank | ⬜ blocked on D2/D5 | |
| G4 | augmentation distinctness | ✅ by construction | every accepted row diverges >= TAU from its rank-0 target (recorded per row in `aug.divergence_m`) |
| G5 | architecture / rope / schedule / guard-audit / planner-holds | ✅ | all re-run green 2026-09-23 incl. `GUARDS_CAN_FAIL` 6/6 |
| G6 | **trainer loads the pretrained trunk** | ✅ FIXED (R15) | was NEVER loaded: 186/186 backbone tensors move under the mapper; trainer now REFUSES a zero/incomplete mapping |
| G7 | **target geometry = 4.0 s at 5 Hz** | ✅ FIXED (R16) | navtrain was 2.0 s at 10 Hz; paired check on 862/862 frames: corrected sample 0 == invalid sample 1 (both t=0.2 s); FIDELITY_OK 0.000 m on val14 |
| G8 | **resume is bit-exact, and the test can fail** | ✅ **18/18** (scene epochs, 2026-09-23 21:02; was 11/11) | `refe/diag_train_resume.py`: halt MID-epoch + resume == uninterrupted (every tensor equal); data position zeroed -> 228 tensors differ; optimiser dropped -> 228 differ; changed arg / no `--out` REFUSED; finished run untouched; planner strict load OK; wrong-trunk snapshot REFUSED. **+ scene epochs (R24):** a twin-scene bank (6 rows / 4 scenes) halts MID-epoch inside the twin-picking sampler and still resumes bit-exact; `--epoch-unit` change REFUSED; one row per scene per epoch; twins 12/13 over 25 epochs; every epoch mixed (0.48-0.52 augmented); parity sampler and row epoch both go RED (`raw/2026-09-23-train-readiness/resume_test_scene_epochs.log`) |
| G9 | real pixels through the trainer, with mutations | ✅ | REAL: 61/61 resolve + decode + train; one frame missing -> REFUSED (0.1 %) / DROPPED 1 named (5 %); a camera missing -> REFUSED 0/61 |
| G10 | **per-sample calibration against an independent reference** | ✅ 8/8 | `refe/diag_calib.py`: lifted points == OpenCV `undistortPointsIter` + textbook numpy quaternion to **1.3-1.7e-5 m** (baked rig AND the farthest real vehicle, 2.665 deg); another rig moves the encoding (2.88 m); mixed batch exact; cache names the rig; gradient reaches `pos3d_mlp`. `pod_train.sh` refuses to train without its PASS |
| G11 | the full gate suite on the post-R22 model | ✅ | architecture · rope · schedule · guard audit **6/6** · planner-holds · conformance (both ranks) · self-test -- all green 2026-09-23 |

## 4 · Open, needs the PI

| # | question | why it blocks |
|---|---|---|
| ~~P1~~ ✅ **ViT-L** (PI 2026-09-23) | ~~arm A = ViT-B or ViT-L?~~ | The handoff's arm A is **ViT-B**; the only published DINOv3 anchor is **ViT-L @ 94.55 PDMS** (Table A13). There is no DINOv3 ViT-B row. At K=2 this is 618-640 vs 1,746-1,892 A40-h. |
| P2 | forward the advisory to the Master Mind | live session messaging refused while unattended |
| ~~P3~~ ✅ **tau = 0.3 m** (PI 2026-09-23) | ~~augmentation threshold tau~~ | defines what counts as a second target; 0.5 m -> 52 % yield at 6.28 rollouts/useful target, 0.3 m -> 71 % at 3.77. The paper states none. |
| ~~P4~~ ✅ done: sha256 IDENTICAL to local copy | ~~one ~5 MB public nuPlan DB fetch~~ | validates `fetch_navtrain_dbs.fetch_one` (inflate + CRC) end to end |
| **P6** | **confirm the 10 Hz simulation rate (C17)** | taken under the 2026-09-24 mandate to cut data-prep time, on a pre-written rule; reversible (`REFE_SIM_HZ=20`, the 20 Hz rows are kept) |
| **P5** | **which A40 host -- by vCPUs** | data prep is CPU-bound: ~805 shard-h of rollouts (7.6 s/rollout/shard MEASURED over 50 min) + ~303 of scorer (6.2 s/frame MEASURED in the rehearsal) = **~1,110 shard-h**; at `SHARDS = vCPU/6`: **16 vCPU ~23 d · 32 ~9 d · 48 ~5.8 d · 64 ~4.6 d** before training's ~64-70 A40-days. A provisioning/spend choice, so the PI's |

## 5 · Open, mine (none blocks starting the pod)

| # | item | when it must close |
|---|---|---|
| ~~E1~~ ✅ | ~~`planner.FrameResolver` globbed the image tree per camera per step~~ -- now a basename index requiring the full relative path: **identical paths 61/61**, and 60/61 identical under a one-frame-missing mutation | closed 2026-09-23 |
| E2 | `flock` (util-linux) was SHIMMED in the dev-box rehearsal -- supervisor lock semantics are unexercised here (the missing-flock refusal DID fire, with its own message, when the shim was off PATH) | the pod's first `pod_train.sh` start |
| E3 | the 71 % augmentation yield is a 56-frame pilot; the pod's `aug_stats.jsonl` measures it at scale | after the pod's `aug` stage -- the training-time estimate scales with it |

---

## Iteration log

*(each loop iteration appends one line: what changed, what closed, what opened)*

- **I0 · 2026-09-22 23:5x** — checklist opened. G1 closed against the real bank (7/7). D2 at 2,023/18,179.
- **I1 · 2026-09-23 00:xx–01:29** — R15 (trainer never loaded the trunk) and R16 (navtrain horizon 2 s @ 10 Hz; also killed 3/4 shards) found and fixed; 2,117 invalid rows quarantined; all gates re-run green.
- **⚠️ 01:29–06:58 LOST** — the corrected run was killed when the previous session exited (all four shard logs stop at the same second, 0 tracebacks). 836 rows survived.
- **I2 · 2026-09-23 06:58** — run relaunched DETACHED via WMI (parent outside the session tree); resume verified, 0 duplicates; horizon fix proven on 862 paired frames; overnight fixes STAGED and blob-verified (they had been left unstaged); ship list recomputed; the "9 % GPU / wants CPU cores" claim RETRACTED (it was sampled mid-thrash; pinned load is bursty, mean ~34 %).
- **I3 · 2026-09-23 07:35-08:09** — R17 (off-by-one, shard 0 had died) fixed + self-healing shards; DB pull measured exactly (97.57 GiB transit / 163.82 GiB disk, train + val); teacher env recipe built; pod portability fixed; stratified augmentation pilot MEASURED (52 % at tau 0.5, city-skewed).
- **I4 · 2026-09-23 08:10-08:40** — R18: augmented half could not be scored (file + per-row route); fixed, mutation-verified. Pod orchestrator written and functionally tested (a first draft had every shard appending to ONE file); split file moved out of a gitignored `data/`; LF pinned for shipped scripts. Local rank-0 bank 2,445 rows, 0 restarts.
- **I5 · 2026-09-23 09:50-10:45** — writing the pod TRAINING runbook found four more defects, all fixed and mutation-tested: **R20** the trainer saved nothing (now bit-exact resume, 11 arms); **R21** a per-image recursive glob on OpenScene's layout (~32-42 s/sample) + crash-looping missing frames; **R22** one baked camera rig vs PETR's per-sample rigs (26-30 rigs/camera, up to 2.67 deg); **R23** the conformance gate read a scorer row. Pixel join MEASURED on the pod layout (61/61 x 4, both directions). `code/pod_train.sh` written; full gate suite green on the post-R22 model; dev-box rate re-measured (31.7 rows/min). End-to-end fake-pod rehearsal of both scripts on the one log held as DB + pixels: see I6.
- **I6 · 2026-09-23 10:16-11:20 — END-TO-END REHEARSAL of both pod scripts** on a fake `/workspace` (the one navtrain log held as a DB and inside OpenScene shard 25, its 4 cameras extracted with the tarball's own prefix). `pod_dataprep.sh`: rank0 61 -> aug **58/61** (1.84 rollouts/frame, 11.4 s/rollout on one contended shard) -> scorer 671 + 638 rows (**6.7 / 5.6 s per frame**) -> assemble (5 files incl. the rigs) -> gate CONSUMERS_CONFORM + SIGNALS_CONSISTENT on **both ranks**; gate mutation (one-camera rank 1) -> exit 1, no ZZDONE. `pod_train.sh`: weights (sha pins) -> preflight (diag_calib 9/9) -> resume **11/11** -> batch probe (budget from free memory; batch 4 PREDICTED over budget and not tried) -> smoke (**119/119** tuples resolve all cameras + carry their own rig) -> supervised train to `summary.json done=true` (254 samples = 2 x 119 + 16, exact) -> relaunch on the finished run: 1 s, model byte-identical. Found on the way and fixed: **R23b** (the gate stage printed and never asserted), assembly split out of `score`, a batch probe that hung on a spilling driver, block-buffered logs (`PYTHONUNBUFFERED`), and my own harness's `C:` PATH entry (not a script defect). Evidence `raw/2026-09-23-train-readiness/fakepod_rehearsal/`.
- **I7 · 2026-09-23 20:55-21:06 — SCENE EPOCHS (PI decision, R24).** An epoch was one pass over ROWS (~176 K for 103 K scenes = 1.71x the paper's budget, and twin scenes weighted double). `train.py --epoch-unit scenes` (default): every scene once per epoch, its augmented twin taking turns, mixed within each epoch; `epoch_unit` + `n_scenes` in the resume identity; duplicated rows refused. `diag_train_resume.py` **18/18** on a twin-scene bank (bit-exact resume from MID-epoch inside the twin picker; unit change refused; twins 12/13 over 25 epochs; 0.48-0.52 augmented per epoch; parity sampler + row epoch both RED). Pod rehearsal through `pod_train.sh`: **119 tuples in 61 scenes (3 single, 58 twins)**, smoke + supervised train to done=true. Training estimate **~37-41 A40-days** at FP32 (was ~64-70). Local bank D2 finished 16:58 (18,122/18,122); its scorer + gate run via the detached chain.
- **I8 · 2026-09-23 20:40 -> 2026-09-24 00:30 — DATA PREP CUT + PARALLEL TRAINING** (PI: "not acceptable that the data prep takes so long ... parallelize training and data prep ... 7 days"). Exact levers C15 (pod 35 -> ~80 rows/min at 20 Hz), torn-row fix C16, then the rate finding C17: switched to nuPlan's 10 Hz (1.64x, PDM no worse) -> pod **~130 rows/min**. Rank 0 restarted at 10 Hz on BOTH machines with a balanced log split (pod 729 / dev 463 logs, ~8 h), dev box fetching its 249 extra DBs. `--grow` training (C18) + shuffled log order (C19) so the A40 trains while augmentation and scoring continue. Scorer profiled: 5 s/frame on the pod (centre-line 41 %, ScenarioData deepcopy ~19 %). Remaining CPU work at 10 Hz ~3.1 M shard-s (aug 49 % / scorer 28 % / rank 0 18 %) -- hidden behind training.
- **I9 · 2026-09-24 01:17 local — TRAINING RUNNING (`--grow`, ViT-L).** Pixels 32/32 -> the pod-side watcher launched `pod_train.sh GROW=1`: preflight (diag_calib 9/9) -> batch probe **4** (22.74 GiB; 8 predicted 45.47 GiB, not tried) x accum 64 = 256 -> smoke (7,536/7,536 tuples resolve all 4 cameras from 413,152 indexed frames, 12 rigs) -> train. **MEASURED 57.2 s / optimiser step = 0.2236 s/sample WITH the 7 CPU-teacher rank-0 shards running beside it** (idle benchmark 0.2197: -1.8 %) -> 10,075 steps = **6.67 A40-days, ETA 2026-09-30 ~17:30 local** (7-day budget = 0.234 s/sample). Loss 19.5 -> 8.75 over steps 0-3. rank 0 meanwhile: pod 193 rows/min, dev box 112 rows/min (was 35 combined at 20:40).
- **I10 · 2026-09-24 02:10-02:36 Berlin — the NAVTEST EVAL PATH works end to end, and a live handover.** `eval/SPEC_NAVTEST.md` pre-registered (blob `04e55009…`, AMENDMENT 1 before any score); **E-0** W3's harness reproduced through our driver (200 tokens, max abs diff 0.0), **E-1** converter exact on analytic targets, **E-2** `snap_epoch001` -> 25/25 tokens scored, guards PASS (`eval/RESULT_E0_E2.md`); found + fixed: Hydra vs the package path, `planner.py`'s hidden `code/` dependency, my extractor's unreadable zips, a conflated frame control. Rank-0 HANDOVER by `code/handover_logs.py` (the logs the pod reaches LAST, with a local DB on the dev box): dev box 214 -> **409 logs** (+15,615 frames), pod 978 -> **783 logs**; both ~3.5 h to rank-0 complete.
- **I11 · 2026-09-24 02:45-03:40 Berlin — TWO MORE EXACT DATA-PREP LEVERS, both verified on BOTH machines and switched on.** Profiled the two stages still ahead (augmentation 49 % / scorer 28 % of the remaining CPU): the scorer spends 85 % of a prefix in two calculators that compute all 128 agent slots for an ego-only readout -> **C21 ego view, pod 1.77x / dev 2.04x**; the rollouts spend their map time on per-step container overhead and a union that is a pure function of the nearby polygons -> **C22 map-step fast path, pod 1.21x rank 0 / 1.31x augmentation**. Every A/B carries a determinism replicate or concurrent arms plus a MUTATION arm that must change rows, and three of them first measured nothing: the ego view fell back on 1,980/1,980 prefixes (`agent_nearest_indices`), the route mutation hit a function the builder imports by name (0 calls), and the pod's rank-0 A/B ran on 0 logs -- each caught by a check that had to read non-zero. cProfile's promise (~1.4x) did not survive plain timers. Pod rank 0 restarted onto C22 at 03:36:45. Eval: `eval/eval_checkpoint.py` (seam -> PDMS -> floors -> families, paired with earlier points) validated end to end on the E-2 log; the seam report now carries PROPOSAL diagnostics -- and at step ~90 the 64 proposals sit within **0.04 m** of each other (oracle-best 3.115 m vs random 3.160 m ADE, 7 of 64 ever best, 25 tokens / 1 log, `raw/2026-09-24-levers-v2/eval_chain_check_e2_ep002.json`): expected so early in winner-takes-all training, tracked across epochs by `eval/learning_curve.py` on W3's 200 tokens, which runs itself once the navtest frames are complete.
- **I12 · 2026-09-26 08:52-09:30 Berlin — back after a 2-day unsupervised gap: the pipeline had finished, three silent failures had not.** Data prep COMPLETE on both machines (pod rank 0 103,037 rows 06:43 Sep 24 · augmentation 01:07 Sep 25 · scoring 16:16 Sep 25; dev chain done 19:47 Sep 24) and training healthy (step 3,497, steady 57.0 s/step, ETA ~Sep 30 17:40). Found by counting rows on both sides, not by any alarm: (1) the dev box's uploaders died with the session (~19:02 Sep 24) and **124,619** augmented-scorer rows never reached the pod (dev 309,287 / pod 184,668) -- re-uploaded 852 MB, pod now 309,287 exactly; (2) the pod's bank ASSEMBLER loop died 10:06 Sep 25 with no traceback and `oom_kill 0` (killer unidentified), so training read **120,264 of ~189,888** scorer frames from epoch 8 on -- one catch-up pass (09:17) added **69,594**, total 189,858 (the ~30 left are each shard file's final frame, skipped by design); the trainer reads them from epoch 12 (~12:38); (3) the eval venv under %TEMP% had been partly deleted (~17:46 Sep 24; `import torch` failed), so the learning curve stopped at epoch 3 -- rebuilt OFFLINE from the uv cache into `C:/Users/Admin/venvs/driverl-eval` and verified by reproducing a banked seam BIT-FOR-BIT (`eval/EVAL_VENV.md`); `eval_checkpoint.py` now defaults to it. Learning curve epochs 1-3 on 200 tokens: PDMS 31.33 / 27.51 / 29.60 (STOP 62.58, CV 21.82, HUMAN 94.12), proposal endpoint spread 0.007 / 0.041 / 0.122 m; epochs 5, 8, 11 running. ⚠ Open: 10 older scripts still hard-code the dead venv path (listed in the agent report, none in the live pipeline). Live report: https://claude.ai/artifact/PfymZvPmm6Vu3ePiCdcufi (`report/`).
- **PI decisions 2026-09-23:** ViT-L · TAU 0.3 m · ONE pod (data prep, then training) · 5 MB fetch approved -> `fetch_one` validated against an independent reference (sha256 identical) · **an epoch = one pass over SCENES** (R24).

