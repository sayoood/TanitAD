# BANKED — `best` / `best_seed1` feasibility, and what the "zero" actually buys

`Banked 2026-09-06 by the register-repair agent. The `best_seed1` rows previously existed ONLY as`
`prose in `Project Steering/Decisions/2026-09-06-mm-decisions.md` (M77).`

⚠️ **HALF of the escalation was wrong, and the correction is recorded here rather than inherited.**
The brief said *"the `best` / `best_seed1` feasibility rows exist only in prose"*. **`best`'s
seed-0 row was ALREADY BANKED** — twice — in
`…/2026-09-05-refav1-cost-geometry/raw/panel_best.txt` and `raw/cell7_best_panel.txt`. What was
genuinely unbanked is **`best_seed1`**, i.e. the *replication* — which is precisely the half the
claim rested on. The earlier reading (*"`best` is not among the ten arms of `feas_audit_all.txt`"*)
is true and was quoted about **one** artifact as though it were about the package. ⭐ **Class: an
absence established on one artifact, generalised to the package** — the sibling of
`CLAUDE.md`'s *"absence found at ONE location is not absence"*.

| | |
|---|---|
| **evidence class** | **MEASURED (ours)** — re-run this turn, not copied from the decisions file |
| **tier** | **T1** (self-action open loop) for `cl` / `ha` / `ha0_ext`; **T0** for `ol`; `g` is the recorded human path |
| **arm** | refav1 @ **21,109**, `ccos`, `W_JERK 0`, **`W_KAPPA 15.11245`**, `W_VEND 64.2972`, **`--kamm-mu 0.7`**, **no ladder** (read from each record's own `cost` block) |
| **n** | **27 windows** at `v0 >= 2 m/s` (of 40; the near-stationary cut the ground-truth control forces — at `v0 >= 0` the control itself fails because `kappa = a_lat/v^2` explodes) |
| **estimator** | none — `kamm_over_rate` is a **COUNT over windows**, not a bootstrapped mean. It is quoted with its `n` and never with a CI it does not have. |
| **instrument** | `assert_feasible` (`tanitad.refs.feasible_decode`), which **re-derives** the scorer's envelope and Kamm flags from the rolled-out path rather than trusting bookkeeping |
| **provenance** | records `raw/rec_best.json`, `raw/rec_best_seed1.json` (copied into this package from `C:/Users/Admin/refav1_margin/p4out/`); dumps `dump_best` / `dump_best_seed1` at that same path; output `raw/feas_audit_best_seeds.txt`; four families `raw/ff_a2_rungs_and_best_seeds.txt` |

---

## 1. The feasibility rows, with both halves of the control in the same table

`FEAS_VMIN=2`, constants `A_MAX 4.000  KAPPA_MAX 0.200  MU_KAMM 0.700  dt 0.20 s`:

| arm | row | `kamm_over` | max abs a | **max abs kappa** | `peak_g` mean | **`peak_g` max** | vacuity gate (max abs kappa ≥ 0.02) |
|---|---|---|---|---|---|---|---|
| **`best`** (seed 0) | `cl` | **0.0000** | 1.500 | **0.0800** | 0.082 | **0.332** | ✅ **PASS, 4.0×** |
| ⭐ **`best_seed1`** | `cl` | ⭐ **0.0000** | 1.500 | **0.0800** | 0.081 | **0.332** | ✅ **PASS, 4.0×** |
| `wk15` | `cl` | 0.0000 | 1.500 | 0.0800 | 0.084 | 0.332 | ✅ PASS, 4.0× |
| `combined` (seed 0) | `cl` | 0.0000 | 1.165 | 0.1505 | 0.224 | 0.618 | ✅ PASS |
| ⛔ `combined_seed1` | `cl` | ⛔ **0.0741** | 1.072 | 0.1672 | 0.271 | **0.702 — outside the µ = 0.7 circle** | (n/a — not a zero) |
| ⛔ `bestlad` | `cl` | 0.0000 | 1.500 | **0.0049** | 0.036 | 0.153 | ⛔ **FAIL — VACUOUS (0.25×)** |
| ⛔ `wk151` * | `cl` | 0.0000 | 1.500 | **0.0166** | 0.053 | 0.158 | ⛔ **FAIL — VACUOUS (0.83×)** |
| ⛔ `cos_wk` | `cl` | 0.0000 | **0.000** | **0.0000** | 0.000 | 0.000 | ⛔ **FAIL — VACUOUS (an all-zero path)** |
| **`g` — the recorded human** | control | **0.0000** | 3.016 | 0.1701 | 0.178 | **0.373** | — |
| `ha0_ext` | control | **0.1852** | 2.310 | 0.7672 | 0.316 | 1.436 | — |
| `ha` | control | **0.1481** | 2.310 | 0.6211 | 0.316 | 1.595 | — |
| `ol` | control | **0.1111** | 2.973 | 0.4400 | 0.343 | 1.016 | — |

*`wk151`'s row is from the already-banked `…/2026-09-05-refav1-cost-geometry/raw/feas_audit_all.txt`
(same instrument, same `n = 27`, same constants); every other row was generated by this package's
`raw/feas_audit_best_seeds.txt` in ONE run.*

⛔ **The zero is bracketed on both sides in one table, which is what makes it a measurement.**
`g` **must** read 0.0000 (a recorded vehicle's motion is feasible by construction) and does;
three trivial arms **must** read non-zero and do. ⭐ **And the instrument reproduces the banked
`best` block of `panel_best.txt` digit-for-digit**, so the re-run is the same measurement, not a
new one.

⭐ **`bestlad` and `cos_wk` are in this table on purpose, in the SAME GENERATION as the zero they
qualify.** `D-REFAV1-CG-ZEROVIOL-SCOPE`'s root cause was a claim assembled from **two generations
of a regenerated artifact**; the durable fix is that the vacuity comparison and the rate a claim
rests on cannot come from different files. `bestlad`'s **0.0049** is now MEASURED here rather than
inherited from `M58`'s prose.

⛔ **The census is therefore SIX distinct configurations reading `kamm_over` 0.0000 at
`v0 >= 2`, not four**: `cos_wk`, `wk15`, `wk151` (`feas_audit_all.txt`), `combined`, `best` and
`bestlad`. **THREE of the six FAIL the vacuity gate** (`cos_wk` 0.0000, `bestlad` 0.0049,
`wk151` 0.0166 — all below `max|kappa| >= 0.02`), and **one of the remaining three is
seed-dependent** (`combined`). ⇒ the earlier "four arms" count was itself scoped to one artifact,
and the vacuous fraction is larger than the published form said.

## 2. ⭐ What replicates — and it is BOTH halves

* ⭐ **`best`'s zero replicates at a second inference seed**: 0.0000 → 0.0000, `peak_g` max
  0.332 → 0.332, max|kappa| 0.0800 → 0.0800. On a **stochastic** planner that is not free —
  `D-REFAV1-CG-ZERO-SEEDDEP` measured `combined`'s zero *breaking* at seed 1 (0.0000 → 0.0741).
* ⛔ **AND THE TURN REFUSAL REPLICATES TOO.** `turn_left` recall is **EXACTLY 0.0000 of
  n_true = 11 at BOTH seeds** (`raw/ff_a2_rungs_and_best_seeds.txt`), with `lane_keep` 1.0 and
  `turn_right` 0.5 at both. ⇒ **the same seed replicate that rescues the safety number also
  confirms the price**, and neither may be quoted without the other.

| | `best` (seed 0) | `best_seed1` |
|---|---|---|
| ADE (m) | 0.8838 [0.6639, 1.1063] | 0.8853 [0.6676, 1.1018] |
| `cl − ha0_ext` ADE | +0.0066 [−0.1494, +0.1758] **not sep.** | +0.0081 [−0.1543, +0.1872] **not sep.** |
| LON speed MAE | 0.7751 (`ha0_ext` 0.3058) | 0.7829 |
| `cl − ha0_ext` speed MAE | +0.4693 [+0.2969, +0.6822] ⛔ **sep. WORSE** | +0.4771 [+0.2961, +0.6751] ⛔ **sep. WORSE** |
| LAT curv MAE | 0.031281 | 0.030614 |
| LAT heading MAE (deg) | 15.2975 | 15.4045 |
| TAC lat κ | 0.2727 (`ha0_ext` 0.6277) | 0.2611 |
| **`turn_left` recall (n_true 11)** | ⛔ **0.0000** | ⛔ **0.0000** |
| `turn_right` recall (n_true 8) | 0.5000 | 0.5000 |
| `lane_keep` recall (n_true 21) | 1.0000 | 1.0000 |
| **STRATEGIC** | ⚠️ **UNAVAILABLE, n = 0** — the refav1 arm tool emits no route head (`missing ['route_pred','route_true']`) | same |
| **distance-keeping** | ⚠️ **UNAVAILABLE, n = 0** — *"no lead-agent track supplied"* | same |

## 3. ⛔ The superlative that does NOT survive, stated exactly

⛔ ***"`best` carries the programme's only replicated, non-vacuous zero friction-circle violation
rate"* is WITHDRAWN, and so is the "first" form.** What is true:

1. **SIX distinct configurations reach `kamm_over_rate` 0.0000** at `v0 >= 2` — `cos_wk`, `wk15`,
   `wk151` (`…/2026-09-05-refav1-cost-geometry/raw/feas_audit_all.txt`), `combined`, `best` and
   `bestlad` (this package's `raw/feas_audit_best_seeds.txt`). The zero is **not unique, and the
   previously published "four" was itself scoped to a single artifact**.
2. ⛔ **THREE of the six FAIL the `M58` magnitude (vacuity) gate, `max|kappa| >= 0.02`.** `cos_wk`
   emits an **all-zero path** — `max|a|` 0.000, `max|kappa|` 0.0000, `peak_g` 0.000 — and an
   all-zero path is inside every friction circle; its own trivial-profile probe reads
   `straight 1.0000 / const_speed 1.0000 / CONSTANT-VELOCITY 1.0000, identical to `ha0` on 40/40
   windows`. `bestlad` reads **max|kappa| 0.0049 = 0.25× the threshold** — MEASURED here, in the
   same table as the zeros it disqualifies. **That is the poisoned-floor class**, and it is why a
   safety zero needs a MOTION assertion beside it.
3. ⛔ **`combined`'s zero is SEED-DEPENDENT** — 0.0000 / **0.0741** — so it is not replicated.
   ⚠️ **`wk151` FAILS the magnitude gate too** — max|kappa| **0.0166 < 0.02** — and reads
   `turn_right` recall 0.0000 as well as `turn_left` 0.0000. ⇒ the three vacuity failures are
   `cos_wk` (0.0000), `bestlad` (0.0049) and `wk151` (0.0166).
4. ⭐ **`best` is the ONLY arm that is simultaneously zero, non-vacuous AND replicated** —
   `wk15` is zero and non-vacuous but single-seed, `combined` is zero and non-vacuous but breaks
   at seed 1, and the other three fail the magnitude gate. That much survives.
   ⭐ **`best`'s zero IS replicated and IS non-vacuous** — max|kappa| **0.0800 = 4x** the
   `M58` vacuity threshold of 0.02, `peak_g` max **0.332 = 89 % of the human's 0.373**, and
   `max|a|` **1.500**. Both surviving numbers are re-verified above against their artifacts.
5. ⛔ **AND THE ZERO IS BOUGHT BY NOT TURNING.** `turn_left` recall **exactly 0.0000 of 11** at
   both seeds, against `ccos_argmax`'s 0.3636 and a **0.00000 measured recall seed floor**. ⇒
   ⭐ **This is not a safety result. It is a refusal to execute a manoeuvre, measured on the
   safety axis.** The vacuity gate exists for exactly this failure and it **fires one level up**:
   `cos_wk` is vacuous because it never moves; `best` is not vacuous, but it never approaches the
   friction limit *in the manoeuvre class where the limit binds*, because it declines the class.
   ⇒ **A motion assertion is necessary and NOT sufficient. The sufficient form asserts motion IN
   THE REGIME THE SAFETY METRIC IS ABOUT.** Registered as `I22`.
6. ⇒ **The admissible sentence:** *"`best` is the only refav1 arm whose zero friction-circle
   violation rate replicates across inference seeds while remaining non-vacuous by the motion
   test — and it reaches that zero with `turn_left` recall 0.0000, so on this rig safety and turn
   execution are traded, not composed."*

## 4. What this does NOT establish

* ⛔ **`n = 27` windows / 8 episodes on one checkpoint.** Nothing here is a corpus-scale or
  parity claim, and `kamm_over_rate` has **no interval** — it is a count.
* ⛔ **Two inference seeds are not a training replicate.** `I17`/V2 is unpriced for refav1 and
  remains a named blocker.
* ⛔ **`kamm_over_rate` is a feasibility statistic, not a collision or off-road rate.** This
  programme has **no** reactive-agent simulator, so no safety-grade behavioural metric exists for
  this arm (`VOCABULARY.md`, CLOSED LOOP).
* ⚠️ **The `combined_seed1` `peak_g` max 0.702 sits outside the µ = 0.7 circle** — the cap is
  applied to candidate CONTROLS at plan time, while `assert_feasible` re-derives from the
  ROLLED-OUT path. Different objects; the residual is a stated scope difference, not a bug.

<!-- ARTIFACT-COMPLETE: BEST_FEASIBILITY_SEEDS -->
