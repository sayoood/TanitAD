"""Assemble a CONTIGUOUS, COMPLETE sorted PREFIX of the parity train corpus.

WHY A PREFIX, AND WHY IT IS NOT A RE-SELECTION. ``parity.check_uids(mode="subset")``
admits exactly one non-full episode set: the sorted PREFIX of the manifests uid
