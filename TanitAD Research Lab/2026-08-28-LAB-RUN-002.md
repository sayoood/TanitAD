# TanitAD Research Lab — LAB RUN 002 (daily summary)

`Run label 2026-08-28 (Master Mind trigger; respawn — the first instance died at
startup, no partial work). Literature-only pass: 0 GPU (dev-box embargoed before
12:00 Europe/Berlin, no pod/Thor, no spend). Four fields swept sequentially,
one work-package each, staged incrementally. Per-field detail:
<field>/Research/2026-08-28-<slug>/RESULT.md.`

## Per-field one-liners

| field | headline finding | banked primaries | key recommendation |
|---|---|---|---|
| **Data Engineering** (`2026-08-28-corridor-ref-untimed-cot`) | No published pipeline labels deliberate lane offset from an ego-excursion threshold — the reference is always lane-line semantics + a causal trigger object (Alpamayo-R1 CoC's in-lane vs out-of-lane nudge); untimed text supervises only via MIL / alignability gating / clip-level heads, and TAN measures just 30 % of untimed narrations as alignable at all (15 % well-aligned). | 2204.02968, 1912.06430, 2305.17861, 2408.10845, 1807.11546, 2305.08366 | Calibrate CORRIDOR_OFFSET against a lane-detector reference + `obstacle.offline` trigger (4th arm for the registered cross-agreement experiment); present the PI exactly two literature-backed options for untimed CoT tokens (clip-level MIL vs TAN-style gating) — blanket band-assignment has no published support. |
| **Architecture & Inference** (`2026-08-28-anticollapse-beyond-ema`) | The three beyond-EMA fix families, each measured: VICReg var/cov on BOTH branches (C-JEPA: EMA alone provably insufficient; +0.8/+1.0 at ViT-L); predictor CAPACITY is a threshold lever on what the encoder keeps (IWM: 12→18 layers flips equivariance 1/5→4/5 seeds) and runs OPPOSITE to the bottleneck intuition (NextLat: rank 160→53 with quality UP); pixels-as-main-target measurably lose to features (V-JEPA) — bracketing O14 as auxiliary-only with a frozen-teacher feature-target fallback. | 2404.08471, 2410.19560, 2403.00504, 2105.04906, 2210.02885, 2603.20111 | Register a VICReg-placement pair on the v7-tiny ladder → a controlled three-way vs the EMA bake-off and O14; run the predictor-capacity arm BEFORE any predictor-shrinking arm; pre-commit the O14 FLOODED/R1-EQUAL fallback (feature-target aux). |
| **Deployment & Optimization** (`2026-08-28-vit-int8-fp8-thor`) | ⛔ TensorRT on Thor sm_110 has a documented SILENT-FP32 fallback under FP8/FP4 flags (TRT issue #4590) — a false-positive generator for the B1 gate; recipe: W8A8 INT8 first (32 calib images suffice — PTQ4ViT; per-channel weights → per-tensor deploy via RepQ-ViT reparam), FP8 only where proven engaged; the dense-head cliff is at ≤6-bit (W4A4: box AP −23.2; SAM-L 36.4→5.4), invisible to scalar heads. | 2111.12293, 2212.08254, 2303.17951, 2405.03144 | B1 gate must verify precision by engine inspection + latency + size, never by builder flags; dense-head-first accuracy validation on the four families. |
| **Opponent Analysis / Benchmarks & Evals** (`2026-08-28-navsim-v2-camera-lane`) | Two "NavSim v2 EPDMS" protocols ~30 points apart (papers' navtest ~76–88 vs challenge navhard-two-stage ~35–56; camera-only bar: DriveFuture 55.5 learned, DrivoR+TOAD 56.3, privileged ceiling 56.6); Drive-JEPA (our reference) has NO navhard number; DrivoR leads camera-only at "roughly 40M parameters" — sub-300M is already community-demonstrated; every leader consumes ego status at inference. | 2606.07170, 2601.05083 | Pin the EvalFlyWheel target to navhard-two-stage EPDMS with split+harness-version stamps; build the NavSim perception interface as a DrivoR-style register-token scene estimator (0.6M / 250× / near-lossless — also closes the scene-token estimator gap); put ego-status to the PI as a two-arm design (benchmark arm + vision-pure arm), not a waiver. |

## ⭐ Cross-discipline transfer of the day

**Register-token compression as the universal scene-token interface.** Two
independent 2026 systems converge on the same mechanism: DrivoR's camera-aware
registers (0.6M params, 250× token reduction, near-lossless driving metrics) and
Alpamayo-R1's Flex video tokenizer (up to 20× compression, metrics maintained —
LAB-RUN-001). For us this is one mechanism serving three needs at once: the
NavSim rig adapter, the scene-token estimator gap, and a principled
information-bottleneck knob for the 4-brain seams (H-DEPLOY-1's seam story).
Cheapest first test: a register-compression cell on the v7-tiny ladder.

## Registered-claim touchpoints (no status changes made)

- D-DATA-GTAC-b stays OPEN — the Data Eng package adds the independent-reference
  design (lane detector + obstacle trigger) as a 4th arm; no in-house evidence
  produced today, so no register edit.
- D-LABEL-GT caveat (1) (untimed CoT = OPEN PI DECISION) — decision material
  prepared (two literature options with measured costs); escalated, not decided.
- E-DEC-67 / O14 — survey brackets it (aux-only; feature-target fallback);
  prereg untouched.

## Library state

116 entries after +18 today; `kb_add.py --verify` **clean (0 problems)**.
No hand-edits to LIBRARY.md (regenerated by the tool). No GPU used; no spend;
no commits (stage-only per operating standard).
