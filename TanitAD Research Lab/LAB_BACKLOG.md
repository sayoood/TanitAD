# LAB BACKLOG — the durable seed list for the daily Research Lab pass

## The update contract (Master Mind, 2026-08-29 — this is the workflow, binding)

**The same-turn rule, mirrored from GOALS_AND_CLAIMS:** any session that EXECUTES,
REFUTES, SUPERSEDES, or UNBLOCKS a row updates that row IN THE SAME TURN. A backlog
that is only read rots — that is how the last one accumulated void doctrine lines and
measurement-refuted items.

| motion | owner | when |
|---|---|---|
| **consume** — a row worked on gets `▶ TAKEN <date> <session>`; done gets `✅ DONE <date> → <artifact path>` | whoever executes (daily Lab run, FlyWheel, MM) | same turn |
| **propose** — new rows from Lab findings ("what this changes"), retraction follow-ups, FlyWheel escalations | the finder, appended under `## PROPOSED (unranked)`, never self-ranked | same turn |
| **fence** — a row refuted by measurement gets ⛔ REFUTED + the register row id, never silently deleted | whoever measures | same turn |
| ⭐⭐ **answer asks FIRST** — read `LAB_ASKS.md` and address every OPEN row **before** pulling seeds from this file (PI 2026-08-31: FlyWheels talk to the Lab directly). ⚠️ A question the Lab CANNOT answer is answered with WHY NOT, never left silent | Research Lab | every daily pass |
| ⭐ **verify** — run `stack/scripts/lab_backlog_drift.py` before ranking; it lists finding-commits registered since this file was last written, and exits non-zero if any are unproposed | mechanical | every daily run |
| **rank** — PROPOSED rows move into the ranked list; priorities re-sorted | **Master Mind only** — with each daily-run commit, full re-sort weekly | daily/weekly |
| **⛔PI rows** — approve/unpark/kill | **the PI** — one sentence suffices; MM records it | on decision |

**Ranking criteria, in order (the PI's stated priority, 2026-08-29):** (1) serves
driving-with-WM / hierarchical dominance / an excellent driving model; (2) unblocked
now; (3) evidence class of the expected payoff (MEASURED-lever > PUBLISHED > HYPOTHESIS);
(4) cost. Rows failing (1) rank below everything that passes it regardless of cost.

**The injection lane (PI addition, 2026-08-29):** the PI and the Master Mind may
INJECT research topics directly — explicitly INCLUDING topics that cannot be
considered inside the running production programme (frontier bets, out-of-scope
explorations). Injected rows live under `## INJECTED (PI / MM)` below, carry their
injector and date, and are EXEMPT from ranking criterion 1 — the production filter
does not demote them; that is the lane's purpose. ⭐ GUARANTEE: whenever the lane is
non-empty, the daily Lab run dedicates AT LEAST ONE of its four packages to the top
injected row — exploration cannot be crowded out by production-adjacent work. PI
injections outrank MM injections by default.

**Freshness tripwire:** the daily run's summary states the backlog delta (consumed /
proposed / none). A week with zero deltas while runs happened = the contract is being
violated — flag it in the program report.


`The daily Lab spawn reads this file; the Master Mind re-ranks weekly; rows are retired by
linking the answering package.`

`Seeded 2026-08-29 by the steering audit (LAB_STEERING_AUDIT_2026-08-29.md): harvested from the
five per-domain backlogs, the register's OPEN rows, and LAB-RUN-001/002/003's escalations and
named empty searches. Rows marked ⛔PI need a PI decision before compute is spent. "verify-first"
= the source predates 2026-08-22 and closure elsewhere is plausible — probe before working it.`

| # | prio | domain | question | why it matters now | source |
|---|---|---|---|---|---|
| 1 | P0 | Deploy | Implement `quant_gate.py` — the 4-stage fail-closed engine-inspection gate (kDETAILED precondition, precision census, size/latency cross-check, four-family dense-head stage); move the B1 recipe to strongly-typed + explicit Q/DQ | D-B1-GATE: TRT on Thor sm_110 silently falls back to FP32 under FP8/FP4 flags (#4590 re-verified OPEN) — every quantisation read before this gate is a false-positive generator | register D-B1-GATE + `Deployment & Optimization/Research/2026-08-29-quant-gate-spec/RESULT.md` |
| 2 | P0 ⛔PI | B&E | Replace the D1 gate's estimator: `stack/tanitad/eval/gates.py:249-251` adjudicates `passed` on a mean-of-split-means (4th clone of the forbidden `overlapping_holdout_se` family) | the gate that decides training continuations is running on the estimator the programme banned; PI decision row is OPEN | register D-GATE-D1-EST / H-EST-2 |
| 3 | P0 ⛔PI | B&E | Benchmark portfolio wholesale approval: GO = navhard-two-stage EPDMS (pinned) + navtest piggyback; GO-cond = Bench2Drive; 2nd wave = WOD-E2E; SKIP = nuScenes-OL claim-bearing, CARLA LB2.1, nuPlan — then start the 4 prep items (NAVSIM 450 GB pull; Bench2Drive+CARLA on the Vulkan-fixed pod; WOD-E2E val + RFS scorer; register-token rig adapter) | G3 (beat SOTA on community benchmarks) has no lane until this is approved; no single benchmark covers the four families, the GO set jointly does | `Benchmarks & Evals/Research/2026-08-29-benchmark-portfolio/RESULT.md` |
| 4 | P0 | Arch | MM-E1 stage-2 follow-through: if EMA joins the recipe → register a τ-ramp arm BEFORE adoption (our fixed τ=0.996 has no published operating point — every EMA-teacher line ramps τ); if EMA is out → SALT frozen-teacher fallback is pre-committed | the only measured lever that moves drift down (0.4531→0.36-0.40) is mid-decision tonight; adopting a mis-specified fixed-τ recipe would bake in a known defect | register MM-E1 + `Architecture & Inference/Research/2026-08-29-ema-warmup-rollout-composition/RESULT.md` |
| 5 | P0 | Arch | v7 head geometry: drop the h≥2 predictor heads for composed h=1 (+ short train-time unroll); they are vestigial under every objective we run | H-PROOF-6d (heads never trained, 1e-3 init), H-PROOF-7 (rolled h=1 predicts to 6 steps), and all four closest-relative WMs (DreamerV3, DINO-WM, TD-MPC2, V-JEPA2-AC) roll one-step models | register H-PROOF-6d/7 + LAB-RUN-003 |
| 6 | P0 | DataEng | Run the two waiting lane test sets with CLRerNet-EMA as-is (±25° corridor ≤3.1 % projection error; 640×256 upscales ×1.25 exactly) — this IS the transfer measurement; gate wider rollout on it, and pin the 4,719-vs-4,729 clip count at run time | the 4th arm of the registered g_tac cross-agreement experiment (D-DATA-GTAC-b) needs the lane-detector reference; transfer must be measured not assumed | `Data Engineering/Research/2026-08-29-lane-detector-deployment/RESULT.md` |
| 7 | ✅ DONE 2026-09-01 lab-run | DataEng | ⚠️ **RE-SCOPED, not simply answered** → `TanitAD Research Lab/Data Engineering/Research/2026-09-01-gtac-producer-cross-agreement/RESULT.md`. The producers are NEAR-DISJOINT (B refuses 3/4 LAT + 5/8 LON of A's tokens); LON agreement 1/7. There is no "2-vs-1 threshold contest" — B refutes the token at any threshold. Two admissibility defects found instead (A's `SPEED_BAND` = hindsight held-speed; A's `CORRIDOR_OFFSET` = 21.65 m on a plain arc). ⛔ Corpus-rate run still needs parity poses → see PROPOSED L-6. ORIGINAL ROW: D-DATA-GTAC-b: measure cross-agreement of the two blind-built g_tac producers (`tactical_goals.py` vs `g_tac_geom.py`) and settle the CORRIDOR_OFFSET 2-vs-1 contest with the lane-detector + `obstacle.offline` trigger as independent reference | two producers of the SAME label family are live with unmeasured agreement — a silent fork in the canonical labels | register D-DATA-GTAC-b |
| 8 | P0 ⛔PI | DataEng | D-LABEL-GT caveat 1 — untimed CoT tokens: put the two literature-backed options to the PI (clip-level MIL vs TAN-style alignability gating; TAN: only ~30 % of untimed narrations alignable) | blanket band-assignment has no published support; blocks the CoT channel of the canonical label set | register D-LABEL-GT + `Data Engineering/Research/2026-08-28-corridor-ref-untimed-cot/RESULT.md` |
| 9 | P0 | Arch | Register the VICReg-placement pair on the v7-tiny ladder → controlled three-way vs the EMA bake-off and O14; run the predictor-CAPACITY arm BEFORE any predictor-shrinking arm (IWM: capacity flips equivariance; NextLat: rank down, quality up) | the anti-collapse design space beyond EMA is mapped and cheap; capacity runs opposite to the bottleneck intuition, so shrinking first would misread | `Architecture & Inference/Research/2026-08-28-anticollapse-beyond-ema/RESULT.md` |
| 10 | P0 | Arch | H-RANK-16 residue: run frozen DINOv3 through `spectrum_report` on the SAME 12 val clips at n=1440 — until then ⛔ no arm may be failed on the 8.56 participation floor | the gate compares arms to a number no current instrument reproduces (20.52 vs 40.77 vs 8.56 on three rigs) | register H-RANK-16 (QUEUED) |
| 11 | P1 ⛔PI | B&E | Ego-status two-arm design portfolio-wide: benchmark arm (consumes ego status like every leader) + vision-pure arm (our binding rule), reported side-by-side | every NavSim camera-only leader consumes ego status at inference; without the two-arm design our rows are either non-comparable or rule-breaking | LAB-RUN-002/003 (PI decision, restated) |
| 12 | P1 | B&E | D-EPDMS-FAM: design the tactical/strategic decision-quality companions for any EPDMS row (EPDMS measures compliance+outcome, nothing maps to tac.manoeuvre_decision / strat.route_goal; comfort unmapped) | four-families rule binds every published number, including community-benchmark rows | register D-EPDMS-FAM |
| 13 | P1 | Arch | H-RANK-17: additive motion latent `z_t + m_t ≈ z_{t+1}` (TDV transfer) on the v7-tiny ladder + the n_stack=1 corollary (settles H-RANK-8 in the same session) | run-001's designed-but-never-executed arm; removes the static-latent shortcut by construction; both outcomes committed | LAB-RUN-001 + `…/2026-08-22-lab-run-001/RESULT.md` (Arch) |
| 14 | P1 | B&E | Pseudo-simulation pilot on our NuRec/gsplat assets (3DGS counterfactual observations + proximity weighting; published R²=0.8 vs closed-loop): does it hold R² ≥ 0.7 against our T1 at < 1/10 compute? (CPU prep: select the 10 val episodes with NuRec recon; script the stage-2 perturbation grid) | we hold every ingredient (NuRec msgpack open, gsplat 492 FPS on Thor, T1 harness); would give closed-loop-correlated eval without a renderer in the loop | LAB-RUN-001 (B&E) — ⛔ CONFIRMED id collision: the register now holds TWO H-EVAL-1 rows; re-register this one under a fresh id first |
| 15 | P1 | Deploy | H-DEPLOY-1: per-component INT8 PTQ sweep of the v7-tiny champion on the 4060 (W8A8 first, 32 calib images, RepQ-ViT reparam; dense-head-first accuracy validation) — is quantisation error seam-dominated, and does a full-rank trunk quantize better than a collapsed one? | connects the rank campaign to deployment; INT8-first ordering is doc-verified; feeds the B1 gate's expected deltas | LAB-RUN-001/002 (Deploy) |
| 16 | P1 ⛔PI | Opp | Opponent teardown #1: Alpamayo-R1 (open weights, 10B, Chain-of-Causation) — H-OPP-1: is the hard-case gain mostly decoder-and-data (reasoning-suppressed run retains most of it)? | first fully inspectable big-league opponent; measurable, not just readable; gated on metered compute or a validated INT4 path | LAB-RUN-001 + register H-OPP-1 |
| 17 | P1 | Arch | Orbis 2 (2607.15898) deep-read: planning-time hierarchy or representation-only? (+ CheckVLA 2607.26789 = published run-time verification ≈ our H11/A9) | top differentiation risk to H1 positioning — "hierarchy a planner USES, with a number" is the only claim left to us; never executed | Opponent STATE run #5 recommendation |
| 18 | P1 | Arch | H1b (hierarchy-edge) is still UNTESTED — D5/D6 have never run; re-frame the matched-params hierarchy-vs-flat test for the v7/B1 world (PREREG_E_EXP1 exists as a starting point) | the programme's thesis hypothesis has no measured datapoint; every architecture decision implicitly assumes it | HYPOTHESIS_LEDGER §1.1 H1b + PREREG_E_EXP1 |
| 19 | ✅ DONE 2026-09-01 lab-run | B&E | **ANSWERED: NO — fed on 0 of 71 readable artifacts** → `TanitAD Research Lab/Benchmarks & Evals/Research/2026-09-01-distance-keeping-fed-check/RESULT.md`. Instrument present + admitted, refusal retired 08-18; the gap is SUPPLY (`win["lead"]` never attached) plus a second gap the row missed (28 artifacts still carry the stale reason string). ORIGINAL ROW: verify-first: is the LONGITUDINAL distance-keeping family FED on the current eval path (`win["lead"]` for the val episodes)? The instrument (lead_metrics.py, D-LEAD-1-admitted) existed unfed as of 08-03 | a binding family reported UNAVAILABLE while its instrument sits idle is a criteria-completeness violation | AI STATE P0 L1 + criteria machinery |
| 20 | P1 | B&E | verify-first: apply the registry rows named in `MANEUVER_LABEL_MISMATCH.md` §6 (v2-arm maneuver_acc scored on v1 labels; corrected number needs a GPU re-run) and finish the DIR_YAW 0.15→0.10 re-read (driver has a path-depth bug; prereg + envelope banked) | the registry currently states a number the mismatch doc says is unrecoverable from banked data; scoped by D-LABEL-GT's new canonical labels | `Benchmarks & Evals/TACTICAL_LABEL_STATE_INDEX.md` §6 |
| 21 | P1 ⛔PI | MM/all | The intake stage: 26+ packages verdictless (oldest 2026-07-08), 9-11 unmerged agent branches — decide once: staff a triager, or declare intake advisory-behind-tests | measured cost: the CTRV floor sat 18 days while gates published CV-only verdicts; four STATE files independently flag the same unstaffed stage | Opp/AI/B&E/DataEng STATEs (08-02/03) |
| 22 | P1 | DataEng | H-DATA-1: does frozen-feature leverage predict a clip's value for WM training? (v7-tiny pair: leverage-K vs random-K, NON-parity) — the DataFlyWheel moat mechanism (MOSAIC: 80 % less data at matched performance) | B1 opens a 4,719-clip corpus; a working selection signal compounds on every future corpus | LAB-RUN-001 (DataEng) + register H-DATA-1 |
| 23 | P1 | DataEng | P3 seed: Y-pilot-50 — 50 YouTube dashcam videos through Y0-Y2 (self-calibration → canonicalize → filter → pseudo-actions); measure focal-recovery spread, action agreement r vs VO, A8 | TanitAD_DataReconstruction is product P3; the strategy + falsifiers have been written since 07-10 and never run; pairs with the IDM head (G2: r ≥ 0.6 vs real CAN) | `Data Engineering/Research/YOUTUBE_DASHCAM_STRATEGY.md` + DE GOALS G2 + H7 |
| 24 | P1 | Deploy | A2 (from the Thor plan): compliance review of `taniteval/` + `stack/tanitad/eval/` — the one unreviewed cluster that decides every deployment gate | its premise was vindicated: the D1-gate estimator clone (row 2) lived exactly there; finish the review so the next clone is caught structurally | `Deployment & Optimization/BACKLOG.md` A2 |
| 25 | P1 | Deploy | O2-pre: fix the encoder's shape-derived pooling so it exports at the DEPLOYED geometry (adaptive_avg_pool fails at 176×624; "exports clean" was only ever true at 256×256) — re-verify at the v7 geometry before any engine build | blocks every TRT encoder engine; the class (claims true only at one geometry) already cost one false claim | `Deployment & Optimization/BACKLOG.md` O2-pre |
| 26 | P2 ⛔PI | DataEng | ZOD access: sign the CC-BY-SA agreement (opendataset@zenseact.com) — the one blocker on the owned real-urban tier (loader shipped + geometry-passed 07-18); re-rank the ZOD/PandaSet tier under B1 | the owned-data moat and EU/night diversity remain unserved; cheap to unblock, but B1 outranks it this cycle | DE STATE/BACKLOG + OWN_DATASET_PLAN §7 |
| 27 | P2 | DataEng | Parity-val balance check (max abs d vs train on the four-family axes; falsifier: < 0.10 closes it) + `validate_pool()` over every selection parquet we hold | parity stays the comparison domain for the tiny ladder under B1's boundary — an unquantified val offset travels into every ladder read | DE BACKLOG P0 3/4 |
| 28 | P2 | DataEng | verify-first: v2-clean-val manifest (n=400, sha256 `abe041db72a045b3…`) — PI freeze-or-reject; and the OODVAL protocol may have consumed it already | an un-frozen val manifest invites silent re-selection (parity-class risk) | DE STATE 2026-08-02 escalation 1 |
| 29 | P2 | B&E | Midpoint-corrected CTRV as a stronger floor (measured half-step bias 0.3044 m on a 1-rad arc) + the high-speed LATERAL question (top-decile lateral flips to floor — does it survive on current arms?) | every published margin is a lower bound on the artifact; the lateral half of the high-speed weakness is unowned | B&E BACKLOG P0 4/5 + STATE F4 |
| 30 | P2 | B&E | WP.29 regulation trace: paragraph-level extraction of ECE-TRANS-WP.29-2026-139e into REGULATION_TRACE.md (ISMR / DSSAD / MRM → TanitAD artifact per row) | the Phase-1 safety case's traceability; untouched since 07-16, no successor mechanism exists | B&E GOALS G3 + BACKLOG P1.3 |
| 31 | P2 | Opp | Complete the Waabi profile (simulation-first, closest public analogue to our data-efficiency thesis; in-loop vs offline-factory question) + the deltas watch (Pony Q2, Mullin bill, recalls) | the most architecturally adjacent unprofiled player; the weekly-deltas discipline died with the rotation | Opp BACKLOG run-#6 items 4/6 |
| 32 | P2 | Opp | Sharpen the efficiency wedge: DrivoR leads NavSim camera-only at ~40 M params — "sub-300M" is community-demonstrated; restate our efficiency claim against DrivoR/AR1/GAIA-4 with named numbers | W-05 (no params-vs-benchmark table from NVIDIA) stays open, but the wedge must now beat 40 M, not 32 B | LAB-RUN-002 (Opp/B&E) + W-05 |
| 33 | P2 | Arch | LIT-3 remainder: the O1 margin-∝-action-difference + median-gate variant — the ONE untested O1 rescue (plain, stop-grad, stop-grad+detach all refuted; O1 measurably buys environment content) | closes the O1 thread with a measured yes/no instead of an assumption | register E-DEC-15 |
| 34 | P2 | Arch | Quality-per-FLOP ledger: auto-append FLOPs/latency (4060 + Thor-projected) to every ladder/bake-off arm | the efficiency moat needs a measured series, not per-paper spot reads; the old G3 idea survives the goal file's retirement | AI GOALS G3 (harvested) |
| 35 | P2 | Deploy | Thor doc refresh: RUNBOOK §6 O-table vs the quant-gate spec; ALPASIM_ON_THOR unblock line (tanitad-eval is TERMINATED; path = Vulkan-fixed pod / Bench2Drive prep); re-price O6/O14 for v7-class engines | stale ops docs are how resurrected-bug launches happen (pod-drift class) | audit §1.5 + LAB-RUN-003 |
| 36 | P2 | Lab-infra | `kb_add.py` hardening: atomic download (size + %%EOF check, temp+rename) and an index lock around load-modify-write | both failure modes MEASURED in run 001 (truncated 5 MiB PDF without %%EOF; concurrent add clobbered the repaired index) | LAB-RUN-001 process hazards 3/4 |
| 37 | P2 | Lab-infra | Resume the per-field KNOWLEDGE_BASE.md findings layer (1-line dated entries per run package), or formally re-declare it generated-from-packages; fix the top-level router's five-field paths | constitution §2 duty; zero entries appended since 2026-08-18 across all fields; Deploy KB empty by declaration while two quant surveys landed | audit S-1 / C-1 |
| 38 | P2 | DevEnv | Viz backbone triple: pin `rerun-sdk==0.34.1` in a requirements file; write the two-RecordingStream dual-sink tee (single-sink baseline 52,966 B/window; stub guard shipped); wire the Viewer-MCP so viz is verified not asserted | P7's replay/visualize UI rests on this; the pin exists in NO requirements file (measured) | _devenv BACKLOG P0 2/3/4 |
| 39 | P3 | Lab-infra | Standing empty-search register — do NOT re-run without a new trigger, watch for publications instead: (a) no published pipeline labels lane-offset from an ego-excursion threshold (08-28); (b) no official V-JEPA 2.1 on HF, 2 probes (08-27); (c) Drive-JEPA has no navhard number (08-28); (d) TRT calib-cache format is not officially documented (08-29); (e) PhysicalAI-AV has no session id / absolute clock, 4 probes — drive-level splits impossible (08-02) | absence findings rot into re-searches; each cost a session once already | LAB-RUN-002/003 + register + DE STATE |

## Retired-by-supersession (do not re-open without new evidence)

- Open-loop SC-13 anticipation probing (retired run #5: effect is static-frame + ego-kinematic).
- rollout-k extension / h≥2 horizon extension (E-DEC-66 inert; H-PROOF-6d/7 vestigial heads).
- NAVSIM/Bench2Drive entry-requirements audit (done: benchmark portfolio, LAB-RUN-002/003).
- NuRec feasibility probe (done: msgpack open, gsplat 492 FPS on Thor).
- AlpaSim "docker-host-only" blocker (refuted: runs bare on A40; renderer via layer-fetch).
- "NEVER to HF, even privately" doctrine (superseded by TANITAD_PROGRAMME §0: private HF is the rule).


## ⛔⛔ PROPOSED (unranked) — THE v7 GATE PROBLEMS, injected 2026-08-31 by the Master Mind

⚠️ **WHY THIS SECTION EXISTS, AND IT IS AN ADMISSION.** The update contract above says any
session that produces a finding proposes a row **in the same turn**. Between 2026-08-30 and
2026-08-31 I generated MM-E10 through MM-E19 plus five gate problems and **proposed nothing** —
this file has not been touched since 2026-08-29. MEASURED against its own text: **`hold-action`
0 hits · `action-deaf` 0 · `L3` 0 · `teacher-forced` 0 · `target construction` 0 · `O11` 0.**

⇒ **The Lab's own seed list contained NONE of the five problems now gating v7.** Today's run
produced good work only because the questions were hand-fed in the spawn brief. Left to its
standing instruction — *"pull the top unblocked rows from LAB_BACKLOG"* — it would have
researched a stale list. **A backlog that is only read rots; this one was not even written to.**

| # | pri | field | row |
|---|---|---|---|
| P-1 | P0 | Arch | ⭐⭐ **P1 — no v7 arm has ever beaten its own hold-action control.** All three arms `LOSES_TO_HOLDV0` on every distance metric, heading ~95° = chance. **This is the programme's actual problem**; the rows below are hypotheses about why. ⭐ The one positive: `copy_detector CLEAN`, echo 0.0000 — fake-skill-by-echo traded for honest absence of skill. → `V7_LAUNCH_GATE.md` P1 |
| P-2 | P0 | Arch | **P2 — the predictor does not use its actions.** ELIMINATED: the missing objective (MM-E11, ratio FELL 0.40× vs a 10× criterion), zero-init FiLM (it trained), a starved gain (MM-E18, converged). OPEN: (a) horizon *[under test]*, (b) ⭐ **our "action" is realised motion, not a command — a genuine command channel has NEVER been tested**, (c) representation, (d) ⭐⭐ **TARGET CONSTRUCTION — our target is teacher-forced, so it already contains the action's effect** (UWM-JEPA 2605.25313, source-verified: *"applies beyond the unitary parameterisation"*) |
| P-3 | P0 | Arch | ⭐⭐⭐ **O11 — the predictor HAS become action-sensitive once, and that arm was abandoned at 25 %.** `o11p30k` sat at the provable no-information floor for 5,200 steps, then `pick_acc 1.000` for 12 consecutive rows. ⛔ NOT one-variable (4 diffs incl. scratch init), and a scene-matching shortcut is available. **A 4-minute actdiv probe on its banked ckpt decides whether an 8.6 h arm is worth running** |
| P-4 | P0 | Arch | **P5/L3 — does the predictor ADD anything over `z_t`?** The ladder calls it *"the dissociation gate, and the actual v7 bar"*. ⭐ Independently corroborated — UWM-JEPA reports the same pattern and *"locat[es] the separation in the predictor rather than the encoder"* |
| P-5 | P0 | Arch | ⛔ **L2 IS NOT PASSED AS SPECIFIED — corrected 2026-08-31 after the PI pushed back.** L2 requires beating frozen DINOv3 on **five** targets (`n_agents`, `n_free_cols`, `occ_{l,c,r}`); **we beat it on ONE** (`n_agents` +0.1220 vs +0.0998). C156 retracted the *recommendation* to swap in DINOv3, **not** the underlying "behind on 4 of 5". ⚠️ Two panels also report different numbers for the same target (+0.1220/+0.0998 lead-matched vs +0.3881/+0.2754 E-DEC-29) — **reconcile before either is quoted** |
| P-6 | P1 | Arch | **P3 — drift's 3.3× effect is real and seed-stable but UNATTRIBUTED.** ⛔ `--init-from` REFUTED (E-DEC-60). Ablation now designed and one-variable: `postrain30k + --freeze-encoder`. ⭐ Dual-purpose — the same arm reads drift AND L3 |
| P-7 | P1 | Deploy | ⭐ **A flat strategic rollout is UNDEPLOYABLE** — latency is linear in K (measured), so K=300 costs ~350 ms / ~1,015 ms against a 100 ms budget, and CUDA graphs' 2.57× does not close it. **Temporal abstraction is a deployment requirement, not an efficiency preference** → `Research/2026-08-31-rollout-depth-latency` |
| P-8 | P1 | Arch | ▶ **ADDRESSED 2026-09-01 (literature)** → `TanitAD Research Lab/Architecture & Inference/Research/2026-09-01-longhorizon-bptt-stability/RESULT.md` — no primary in our reference class back-props a 60-step full chain, and the fix is a horizon CURRICULUM, not a smaller clip. ⛔ The mitigation arm has NOT run. ORIGINAL: ⛔ **k=60 with full-chain BPTT is GRADIENT-UNSTABLE at clip 1.0** (gnorm median 5.71 → 2.1e9; killed at step 9,000 on a pre-committed criterion). The 2.4× wall-clock was the priced half of the 6 s horizon; **this is the unpriced half** |

⚠️ **Unranked by design** — per the contract the finder proposes and never self-ranks; the
Master Mind ranks at the next daily-run commit, and ⛔PI rows are the PI's call.

## INJECTED (PI / MM)

*PI injections: just tell the Master Mind the topic in one sentence — it lands here
the same turn, top of the lane.*

| # | topic | injector | date | state |
|---|---|---|---|---|
| I-1 | Latent model-predictive planning with a LEARNED terminal value over composed h=1 rollouts — does a cheap value head beat fan-scoring at equal rollout budget? | MM | 2026-08-29 | ⚠️ **COST HALF served 2026-09-01** → `TanitAD Research Lab/Deployment & Optimization/Research/2026-09-01-terminal-value-vs-fan-scoring/RESULT.md` — MEASURED: breadth is **5.94× cheaper** than depth at equal step-budget (73.33 → 12.34 ms at B=960), value head **0.150 ms = 1.2 %**, breadth flat to N≈32. ⛔ **The QUALITY half — does it produce BETTER plans — is UNTOUCHED and is the real experiment. Row STAYS OPEN.** |
| I-2 | [SHARPENED 2026-09-02, priority should FALL: HALO full text read - the method is confirmed but its payoff regime is 128K-1M context, which v7 (K in 8/60/300) does not occupy, and the paper concedes distilled hybrids underperform from-scratch. RUN P-6 BENCHMARK FIRST. Stays open.] State-space / linear-attention predictors (Mamba-class) as the O5 predictor at matched params — long-horizon composition cost vs our transformer | MM | 2026-08-29 | ⚠️ **RE-SCOPED 2026-09-01 by primary read** → `Frontier Scan/Daily/2026-09-01/RESULT.md` F3 + `Frontier Scan/LEDGER_B2_post_transformer.md`. ⛔ **The "25 %-budget" claim is STRUCK — UNSUPPORTED-AS-STATED.** `2601.22156` is *Hybrid Linear Attention Done Right*; its claim is a **training-data** figure (2.3B tokens to CONVERT a trained transformer, <0.01 % of pretrain, vs >10B prior), not a compute/memory budget. ⭐ **The method changes with it: CONVERT the trained v7 predictor rather than TRAIN a Mamba at matched params** — categorically cheaper on a fixed fleet. ⚠️ Demonstrated on *language* at extreme context; transfer to short-horizon visual latents is a HYPOTHESIS, and the 2.3B figure does NOT translate into our units. **Stays open.** |
| I-3 | Self-supervised closed-loop adaptation: can T1 rollout disagreement (prediction-vs-observation at deployment) supervise online encoder updates without labels? | MM | 2026-08-29 | ⭐ **UNBLOCKED (re-scoped) 2026-09-01** → `Frontier Scan/Daily/2026-09-01/RESULT.md` F8 + `Frontier Scan/LEDGER_C3_regulatory.md`. **The "UNECE bans online learning" constraint is UNSUPPORTED AT TWO INDEPENDENT PROBES** — the second source states such provisions are *not mentioned*. ⚠️ **NOT "refuted": the GRVA primary returned HTTP 403 and is UNREAD (register debt D-4).** ⭐ The real constraint the relayed claim obscured is **ISMR + DSSAD** — in-service monitoring/reporting and a data-storage system — so the binding question is **auditability of an adapting model, not legality.** ⇒ The in-vehicle path is **not closed**; design against auditability, and read the GRVA primary before committing. |


## PROPOSED (unranked)

`Appended by the 2026-08-31 frontier scan (first pass under DAILY_RESEARCH_CHARTER.md).`
⛔ **Never self-ranked — the Master Mind ranks.** Each row names the finding that produced it.

| # | domain | proposed work item | source finding | cost |
|---|---|---|---|---|
| P-1 | Arch | **LatentAlign arm on the v7-tiny ladder**: sigmoid-anneal the prediction target from grounded (teacher-forced) to self-predicted, sweeping `e₀` over 3 values. Pre-committed read: h=1 action/scene ratio must rise ≥3× (0.004 → ≥0.012) at equal-or-better ADE. Controls: constant-predictor (must read the no-information value) + raw-pixel floor. | `Frontier Scan/Daily/2026-08-31` F1 (lib `2605.09701` full text) | 2 tiny arms |
| P-2 | Arch | **Action-source mixture arm**: draw the training action from `{p_gt, p_kin, p_∅}` (GT · constant-accel kinematic decoy · learned null token), sweep `p_∅ ∈ {0, 0.1, 0.3}`. Pre-committed read: hold-action ADE **must degrade** vs true-action ADE, else the echo is not removed and the arm is refuted. Report decoy/GT action distance or the null is uninterpretable. | same F2 | dataloader change + 3 tiny arms |
| P-3 | Opp/B&E | **Alpamayo same-corpus comparability check (0 GPU)**: determine whether the Alpamayo eval split intersects parity split `e438721ae894` / skip-hash `f09e44db`. Pre-committed read: disjoint ⇒ **no comparison admissible** and we say so; intersecting ⇒ our first same-corpus external baseline. | F3 | 0 GPU |
| P-4 | Arch/Lab | **EB-JEPA as an external anti-collapse CONTROL**: clone `facebookresearch/eb_jepa` on the dev-box 4060, run the action-conditioned example as-is, check the published 97 % Two-Rooms planning success reproduces. Pre-committed read: if it does not reproduce, the rig is unusable as a control and we stop — cheap gate before any port to our data. | F5 (lib `2602.03604`, abstract-only) | 4060, hours |
| P-5 | Arch | ⛔ **Resolve correction-watch CW-1**: DriveFuture's headline navhard EPDMS **55.5** vs its own navhard ablation rows **30.9–34.6**. Until resolved, **no DriveFuture navhard level may enter a comparability table**. | F1 | 0 GPU |
| P-6 | Deploy | **Read the banked-but-unread primary `2601.22156`** and re-check the ~25 %-of-from-scratch distillation-budget claim in its own table; then benchmark an SSM/linear-attention predictor block vs our transformer block at K ∈ {8, 60, 300} on the 4060 using the existing `rollout_depth_bench.py` harness. Pre-committed read: SSM wins only if ms/step is **flat in K AND lower in absolute terms at K=60**; flat-but-slower refutes. | F5; serves injected **I-2** | 0 GPU then 4060 |
| P-7 | Lab hygiene | **Second probe for empty E1**: a driving-specific query for DINOv3-class encoder benchmarks on dense/BEV tasks. One probe is not absence — this exists so a single-probe miss cannot harden into folklore. | `raw/search_log.md` E1 | 0 GPU |
| P-8 | Lab hygiene | **Scan the four tracks this pass never reached**: B10 semantic search, B12 memory/long-context, B13 3D-occupancy-rendering (nearest our measured readout-geometry ceiling), C3 regulatory. | `Frontier Scan/TRACKS.md` | 0 GPU |

`Second pass 2026-08-31 (PI-directed): Band D introduced, tracks B10/B12/B13/C3 closed. Rows P-9 … P-16 appended.`

| # | domain | proposed work item | source finding | cost |
|---|---|---|---|---|
| P-9 | B&E / paper | **Adopt strategic guideline S-1: make T1 the burden of proof.** Three independent lines now agree open-loop is uninformative (our action echo 97.9 % vs 0.0 % hold-action · Alpamayo open-loop cannot separate 3.3× params · Waymo L5 *"completely indifferent to your actions"*). State in the paper that any T0-only claim is **unevaluated**. | Waymo adjudication W-5 | 0 GPU |
| P-10 | Paper / strategy | **Adopt S-2: argue "small at deployment" as a position, not a constraint.** Waymo concedes *"teacher-student models to optimize onboard compute"*; Alpamayo's trajectory metric is flat across 3.3×. | W-3, N-1 | 0 GPU |
| P-11 | Arch | [DONE 2026-09-02 - SERVED AND CLOSED: the pre-registered outcome was MET. SparseOcc++ needs dense per-voxel semantic-occupancy GT AND LiDAR-projected depth; PhysicalAI-AV has neither, so the line is CLOSED. See Frontier Scan/Daily/2026-09-02/RESULT.md F2 and LEDGER_B13_3d_geometry.md. Guideline S-3 redirects to the self-supervised occupancy family.] ORIGINAL: **Adopt S-3: claim the geometry gap as a differentiator.** Waymo names *"lack sufficient spatial awareness"* as a frontier-VLM limitation; we have it MEASURED (4 azimuth bins / 120°). Read `2607.04732` SparseOcc++ and establish what supervision its sparse latent needs. **Pre-committed read: if it requires occupancy GT we cannot produce, the line is CLOSED and we say so.** | W-7, B13 ledger | 0 GPU first |
| P-12 | Steering | ⛔ **Adopt S-4: fix the programme's scope statement so Waymo L1 cannot land.** TanitAD claims a vision-only *latent world model on a camera corpus* under a *leak-avoidance* inference rule — **not** L4 sensor sufficiency. Zero-cost removal of the strongest opponent argument against us. | W-1 | 0 GPU |
| P-13 | B&E | **Adopt T-2: every critic/scorer ships with a constant-only control and a raw-input floor**; audit the v7 gate's learned components for policy-critic correlation. | W-6 + our ridge-probe post-mortem | 0 GPU |
| P-14 | Opp | **Read the three unread Waymo technical sources**, above all **"The Waymo World Model" (2026-02)** — the highest-value unread document currently known to the programme — plus *Demonstrably Safe AI* (2025-12) and the Foundation Model post (2025-12). Register debts D-1/D-2/D-3. | `OPPONENT_CLAIMS_REGISTER.md` | 0 GPU |
| P-15 | Arch | **Read `2606.21775` (variable-length latent world models) for the horizon ladder.** ⛔ **Sequence AFTER P-1/P-2**: adding horizons to a predictor whose action/scene ratio is 0.004 could deepen the defect, making the result uninterpretable. **Pre-committed read: variable-length wins only if it beats COMPOSED h=1 at matched compute.** | B12 ledger | 0 GPU then tiny arms |
| P-16 | DataEng | **Pull the Springer embedding-based scenario-retrieval chapter** — it resolves empty E4 (video/driving corpus curation, which the B9 query missed) and is an offline curation lever against our parity-locked 2376-episode corpus. | B10 ledger | 0 GPU |

`Appended by the 2026-09-01 daily Lab run (LAB-RUN-006). Rows L-1 … L-9.`
⛔ **Never self-ranked — the Master Mind ranks.** Each row names the finding that produced it.

| # | domain | proposed work item | source finding | cost |
|---|---|---|---|---|
| L-1 | Arch | ⛔⛔ **Replace the `k60clip05p30k` arm's mitigation before it is spent.** Register a **horizon curriculum** `K(step) = min(60, 8 + floor(step/Delta))` (Looped-WM form) starting at the already-stable k=8, INSTEAD of `--clip 0.5`. Pre-committed read: the arm survives past step 9,000 with gnorm median in single digits, and `o5_loss` does not degrade >20 % vs the k=8 incumbent. | `2026-09-01-longhorizon-bptt-stability` F1/F2 — three primaries use a curriculum; **none** uses a smaller scalar clip, and the prereg's own text says the problem is direction, not magnitude | 1 arm (same cost as the planned one) |
| L-2 | Arch | **Adopt AGC (per-tensor relative clipping) as the optimiser-side option**: clip per-tensor gradients at 30 % of the weight matrix's L2 norm (DreamerV3's shipped setting). ⚠️ **Do NOT combine with L-1 in one arm** without saying so — two levers in one arm is non-attributable. | same, F2.1 | small optimiser change |
| L-3 | Deploy/Arch | ⭐ **The QUALITY half of I-1** — train a terminal value head on our latent and compare against fan-scoring **at matched LATENCY** (not matched step-count; they differ ~6×). Controls per CLAUDE.md: a constant-only predictor that must read the no-information value, and a raw-input floor. | `2026-09-01-terminal-value-vs-fan-scoring` — the cost half is settled, the quality half is untouched | tiny-ladder arms |
| L-4 | Deploy | **Re-measure breadth-vs-depth on Thor.** The 5.94× and the free-breadth region (N≲32) are RTX-4060 properties; Thor saturates at batch 8 on a different workload, which would SHRINK the free region. Same script, different host. ⛔ Only when Thor is idle. | same, §5 limits | minutes, 0 spend |
| L-5 | DataEng ⛔PI | ⛔ **Admissibility decision on `tactical_goals.py`'s output**: (a) `SPEED_BAND` is derived from `geometry(held-speed)` = hindsight ego behaviour, the exact substitution `g_tac_geom`'s F-14 blocker names as inadmissible; (b) `CORRIDOR_OFFSET` reads **21.65 m** on a plain constant-curvature arc (21× its own 1.0 m threshold). **Is A admissible as canonical supervision?** The Lab measured; it does not fence a producer. | `2026-09-01-gtac-producer-cross-agreement` F3/F4 | 0 GPU, PI decision |
| L-6 | DataEng | **Corpus-rate `g_tac` cross-agreement on parity poses.** The 1/7 figure is a SCENARIO-SET rate and must never be quoted as a corpus rate. Needs `physicalai-train-e438721ae894` poses (2–6 s band ⇒ ~60 samples/window); the dev box has none (local dumps hold 4 waypoints). Belongs to a FlyWheel with pod access. | same, §5 + `raw/search_log.md` E1 | pod-side, cheap |
| L-7 | DataEng | **Add a curvature guard to `CORRIDOR_OFFSET`** as the cheap interim if the token keeps shipping: gate on band turn angle (B's own stratification found only `theta < 0.05 rad`, **21.7 % of corpus**, admissible). That single gate catches F4. | same, F4 | small code change |
| L-8 | B&E | ⭐ **Wire `win["lead"]` into the eval path** — `four_families` already computes the family; it only needs `lead_source.select_lead_causal` run over the val episodes' `obstacle.offline` join and attached per window. **No new instrument, no new science, no GPU.** ⛔ AND **pin the stale reason with a test**: assert no artifact in `taniteval/results/` contains `"no lead-agent state exists"` — a prose correction demonstrably did not reach 28 files in two weeks. | `2026-09-01-distance-keeping-fed-check` F1/F2/F3 | wiring + 1 test |
| L-9 | Lab-infra / B&E | ⚠️ **Four banked eval artifacts are UNREADABLE from the dev box** — `driving_flagship-30k` / `-nospeed` / `-speed` / `-v16-ab-ft`.json (32–33 KB each), failing via BOTH Python `open()` and .NET `File.ReadAllText`, across 3 runs and 8+ retries. **An unauditable artifact is not a quotable one, and the registry may cite them.** Try a different host; if they are corrupt, say so on the record. | same, F4 | 0 GPU |


`Appended by the 2026-09-01 FRONTIER SCAN (LAB-RUN-007). Rows FS-1 … FS-6.`
⛔ **Never self-ranked — the Master Mind ranks.** Each row names the finding that produced it.
⭐ This pass discharged six standing debts (D-1/D-2/D-3, B2/B12/B13) and **corrected three of our own
records**; rows below are the work those corrections created.

| # | domain | proposed work item | source finding | cost |
|---|---|---|---|---|
| FS-1 | B&E | [UNBLOCKED 2026-09-02, not executed - the definition is now in hand: N=10 windows, best-vs-worst, RELATIVE change. SCOPE CORRECTION: port the FORMULATION, not the metric - theirs is over image-quality scores, ours is a trajectory quantity. Row STAYS OPEN; 0 GPU and ready.] ORIGINAL: ⭐⭐ **Port WorldRoamBench's SEGMENT-BASED drift metric into `taniteval` and re-score the banked v7 rollout dumps.** Our drift is read **start-vs-end**, which by their construction **cannot see non-monotonic mid-sequence collapse**. **Pre-committed read: monotone drift ⇒ the current instrument stands; non-monotone collapse ⇒ every drift number we hold is a LOWER BOUND and the instrument must change.** | `Daily/2026-09-01` F7 (lib `2606.31672`, abstract-only) · `LEDGER_A5_benchmarks.md` | **0 GPU** — the dumps exist |
| FS-2 | Arch | ⚠️ **Do NOT delete the h≥2 head slots while executing P0 backlog row 5.** Keep them at 1e-3 init so the variable-length (VLWM) direction stays reachable. **Asymmetry is the argument: dropping is cheap, re-adding after the v7 geometry freezes is not.** H-PROOF-6d/7 show the heads are *vestigial under our current objectives* — which is not the claim that multi-step prediction is wrong. | F4 (lib `2606.21775`) · `LEDGER_B12_memory_horizon.md` | **0 GPU** — a config decision |
| FS-3 | Arch | **Three-way interface arm on the v7-tiny ladder**, matched params: learned-embedding interface (Waymo-shaped) vs our current interface vs a pure-E2E control. **Pre-committed read: if the embedding-interface arm does not beat the pure-E2E control on rank AND decodability, the much-publicised architectural convergence is cosmetic and we say so.** ⛔ Exists because Waymo's hybrid claim has **no ablation** — we should not inherit it. | F2 / register W-13 | tiny-ladder arms |
| FS-4 | Deploy | **Re-run the FP8/FP4 precision census on JetPack 7.2.1** (released 2026-08-12) once `quant_gate.py` exists. The TensorRT matrix lists Thor at CC 11.0 with FP8/FP4 supported; our MEASURED finding is a **silent FP32 fallback** (#4590). ⚠️ **The measurement wins** — but our measurement predates this release, and a fail-closed gate must not inherit a stale platform assumption either. | F9 · `LEDGER_C1_releases.md` C2 entry | Thor time, **after** row 1 |
| FS-5 | Lab hygiene | ⛔ **`TRACKS.md` should carry a BANKED-BUT-UNREAD count per track.** MEASURED this pass: **5 of 6 primaries needed were already banked** (2 of 10 on 2026-08-31), and **all three Band-B debts were banked-but-unread rows**. The Library is at 310 entries / 2,115.6 MB and growing faster than it is read. ⭐ *A banked primary with no five-dimension analysis is not an asset, it is a debt with a hash.* | F6 (MEASURED, `kb_add.py` output) | 0 GPU |
| FS-6 | Lab hygiene | [DONE 2026-09-02 - LEDGER_A2_jepa.md and LEDGER_A4_vla.md CREATED with their entries; the TRACKS.md links now resolve.] ORIGINAL: ⛔ **Fix two broken links in the growing artifact**: `TRACKS.md` has linked `LEDGER_A2_jepa.md` and `LEDGER_A4_vla.md` since 2026-08-31 and **neither file exists** (B2's was created this pass). Create them with their existing entries, or drop the links. *(Root-cause class: a link written in the same turn as the intention to write the file, never verified.)* | `Frontier Scan/TRACKS.md` third-pass block | 0 GPU |


`Appended by the MASTER MIND, 2026-09-02, for findings produced overnight. Rows MM-1 … MM-4.`
⛔ **The finder proposes in the same turn** — this block exists because `lab_backlog_drift.py` reports on COMMIT ORDER and therefore read `OK` here, while the content gap was real: the MM-E19 k=8 attribution landed in `105c7f8`/`8d9dddc` *before* the backlog's own commit (`16aedf4`) only because I committed the Lab's staged work later that night. ⚠️ **That is a live blind spot in the drift instrument** — an ordering check cannot see a finding that predates a backlog write it did not cause. MM-4 proposes the fix.

| # | domain | proposed work item | source finding | cost |
|---|---|---|---|---|
| MM-1 | Arch | ⭐⭐ **Re-judge L-1's horizon curriculum on a SCENE-SPREAD-AWARE criterion, not the raw h1 ratio.** MEASURED 2026-09-02: against the k=8 clip-0.5 control, `o5_k` 8→60 moves the **action** spread **0.83×** but the **scene** spread **1.71×** (0.83/1.71 = 0.483 vs the observed 0.484 — the decomposition closes exactly). ⇒ the k=60 arm's halved ratio is **mostly a denominator effect**, not action attenuation, so a curriculum tuned to raise the raw ratio could "succeed" purely by holding scene drift down while leaving action sensitivity where it was. Needs a pre-committed criterion that reads the **numerator and denominator separately**, plus a decision on whether scene-spread growth over a 60-step rollout is itself the defect. | MM-E19-K8 attribution, `…/Research/2026-09-01-mm-e19-k8-attribution/RESULT.md` | 0 GPU to design; the arm itself is one v7-tiny run |
| MM-2 | Lab hygiene / Arch | ⚠️ **Pre-registration outcome tables must carry a WORSE branch.** `PREREG_MM_E19_K60_HORIZON.md` committed exactly three outcomes — `HORIZON-WORKS` (≥10× rise), `HORIZON-PARTIAL` (rise <10×), `HORIZON-INERT` (unchanged within noise) — and the arm **fell 0.48×**, which is off the table entirely. A result with no pre-committed row invites being force-fitted onto the nearest one (here, "INERT"), which would have understated a real effect. Audit the other preregs for the same omission and add the branch to the template. | same; logged as a prereg defect rather than a verdict | 0 GPU |
| MM-3 | Lab infra | ⛔ **Audit every banked instrument for "raw" that is only a POINTER.** `mm_e19_read.py`'s output JSON stores `{cmd, rc, seconds, log}` per stage and **no numbers** — the values lived only in log files outside the repo, which a later run of the same tool overwrote. The k=60 arm's ratio survived solely because a register row happened to quote it. **Banking a JSON that references external logs by path is not banking the raw data — it fails silently, because the JSON still parses and still looks complete.** Sweep `taniteval/` and `mm-e19-probes/` for the same shape; make each writer inline its numbers. | MM-E19-K8 §6; two runner defects fixed in `105c7f8` | 0 GPU |
| MM-4 | Lab infra | ⚠️ **`lab_backlog_drift.py` compares COMMIT ORDER and can read `OK` while a real content gap exists.** MEASURED 2026-09-02 (this block is the counter-example). A finding committed *before* a backlog write it did not inform is invisible to it. Proposed: keep the commit-order check (it is exact and cannot rot) and **add a content probe** — score the backlog's own text for the identifiers of recently registered findings (`MM-E19`, `k8`, gate problem ids) the way the 2026-08-31 audit did by hand, and report any that score zero hits. ⚠️ Scope it honestly: a keyword probe **is** a heuristic and heuristics rot, which is why it must ADD to the git check rather than replace it. | the drift tool reading `OK` against a real gap, this tick | 0 GPU |
`Appended by the 2026-09-02 daily Lab run (LAB-RUN-008). Rows L-10 ... L-18.`
⛔ **Never self-ranked — the Master Mind ranks.** Each row names the finding that produced it.

| # | domain | proposed work item | source finding | cost |
|---|---|---|---|---|
| L-10 | Arch/DataEng | ⭐ **Control or scope the `max_horizon` axis in the MM-E19 attribution.** `o5_k` 8→60 also moved the DERIVED `max_horizon` 20→60, cutting training windows **415,002 → 319,002 (−23.1 %)** and truncating window STARTS to the first ~77 % of every episode. It is `o5_k`'s mechanical consequence, so it cannot be held fixed without an explicit `--max-horizon` — but it is a **candidate contributor to the 1.71× scene-spread rise** that reframed MM-E19, and the running k=8/clip-0.5 control (`max_horizon` 20) does **not** cover it. | `2026-09-02-future-action-horizon-and-window-budget` F3 | 0 GPU to scope; 1 arm to control |
| L-11 | DataEng/Training | **Hoist the episode drop-out census out of the `w_s1_multi` branch** (`train_v6_staged.py:5120-5151`). The guard that stops a longer horizon silently re-selecting the corpus — the reason PI decision D4's parity reasoning holds — is **stage-conditional**, and never ran for any v7-tiny arm. Outside it, only the all-or-nothing `if not len(ds_train)` check at `:5102` fires. | same, F5 | small code change |
| L-12 | DataEng | **Measure the TRAIN corpus's SHORTEST episode.** `reachable_strategic_ticks` refuses on the minimum, not the mean; the mean is MEASURED at 198.92 but the minimum is bounded only at `T ≥ 67`. Any K above ~5 needs it. Also: re-derive the docstring's reachability table at T ≈ 199, or reduce it to a formula. | same, F1/F2 | pod-side one-liner |
| L-13 | Arch/B&E | **Add a paired episode-cluster bootstrap to the actdiv probe.** The 0.83× action factor and 1.71× scene factor carry **no interval**; per the estimator rule none is quoted, but a decision statistic without one is a gap. Same for the drift probe (`latentmotion.py`). | `2026-09-02-scene-matched-action-criterion` §Limits | instrument work |
| L-14 | Deploy | **Delete the dead `nn.AdaptiveAvgPool2d` still constructed at `readout.py:89`** on the non-exact path. Inert today (`forward` never calls it there; MEASURED — it does not reach the exported graph), but it sits on exactly the path where it used to be the export blocker. | `2026-09-02-o2pre-deployed-geometry-export` F4 | trivial |
| L-15 | Opp | ⛔ **Read `2601.05083` and verify DrivoR's parameter count before any efficiency-wedge claim quotes ~40 M.** Backlog row 32's premise is already wrong on leadership (bare DrivoR is 4th at 54.6, behind DriveFuture 55.5); the parameter half is **unverified**. The primary is banked and readable. | `2026-09-02-cw1-resolution-and-evidence-integrity` F4 | 0 GPU |
| L-16 | Lab-infra | ⛔ **Add a COMPLETENESS check to `kb_add` and run `library_integrity.py` alongside `--verify`.** MEASURED: `--verify` reported "310 entries, 0 problems" for a Library containing a file truncated to 14.5 % — before *and* after the repair — because it re-hashes against the hash recorded at bank time. Sharpens backlog row 36 from "atomic download" to "verify the bytes are a whole document". | same, F5 | small; the checker exists in that package's `code/` |
| L-17 | B&E | **A rollout-trajectory drift metric as a NEW instrument** (per-step divergence along an imagined rollout — what WorldRoamBench actually offers). ⚠️ Contingent on a confirming probe: stated as *"not found at the searched locations"*, not as absence. ⛔ Propose as an ADDITION, never as a correction to the latent-drift metric, which measures something else. | `2026-09-02-drift-metric-axis-audit` F1/§gap | instrument work |
| L-18 | B&E | **Disambiguate "drift" across the register, the paper and the criteria registry.** It names ≥3 quantities with different units and nulls: latent drift (`r` of `Δz` on `z_t`), `lat_drift` (`control.py:959`, m per m), `max_drift` (`goal_provenance.py:310`, a determinism check). | same, F4 | 0 GPU |

### ⛔ FS-1 — PREMISE REFUTED BY MEASUREMENT 2026-09-02; RE-SCOPE, DO NOT RETIRE

FS-1 reads *"Our drift is read **start-vs-end**, which by their construction cannot see
non-monotonic mid-sequence collapse."* **MEASURED from the instrument's own source**
(`mm-e19-probes/latentmotion.py`, E-DEC-59): drift is the `r` of a k-fold-fit linear probe
predicting `Δz = z_{t+k} − z_t` from `z_t` — a **per-window predictability statistic at one
fixed `k`**. There is no sequence being reduced to endpoints, so porting WorldRoamBench's
segment-based metric would **not** correct the numbers carrying P3 and P5/L3.

⭐ **The concern survives on the `k` axis**, and the banked data sits exactly where it hides:
drift is **flat across a 15× horizon change** (k 4→60, six arm/band pairs, **−2.39 % to
+0.38 %**, constant control exactly 0.0 on all 16 reads). ⇒ FS-1's cheap correct form is a
**k-sweep of the probe we already have** — k ∈ {1, 2, 4, 8, 15, 30, 45, 60}, `n_rows` reported
per k. Pre-committed read: monotone/flat ⇒ the single-k summary stands; a dip or peak at
intermediate k ⇒ every single-k drift number we hold is INCOMPLETE.
→ `TanitAD Research Lab/Benchmarks & Evals/Research/2026-09-02-drift-metric-axis-audit/RESULT.md`

`Appended by the MASTER MIND, 2026-09-02 (second block of the day), for the four findings `lab_backlog_drift.py` flagged as unproposed. Rows P-17 … P-20.`
⛔ **Never self-ranked.** Each row names the finding that produced it. ⚠️ These are distinct from the MM-1…MM-4 block earlier the same day — MM-2 (prereg WORSE branch) and MM-3 (pointer-only "raw") already cover two of `6daea6ef3`'s three fixes, and are not duplicated here.

| # | domain | proposed work item | source finding | cost |
|---|---|---|---|---|
| P-17 | Arch | ⭐⭐ **A strategic rung at K=8 (16 s) is now LAUNCHABLE and was not yesterday.** The 8–30 s band was believed corpus-unreachable past 12 s; that rested on reading `w120` as a frame count. MEASURED: episodes ~199 frames (min 190), `max_k = 9 (18 s)`, and K=8 needs only `T > 166` — which the observed short tail clears by 24 frames. ⇒ price a K=8 rung: rollout compute at 8 ticks, and whether the one-tick collapse (P4's *other* blocker, which stands) makes it moot before the data does. ⚠️ **Prefer K=8 over K=9**: K=9 leaves only 4 windows on the shortest observed episode and would refuse outright if any un-censused episode is ≤ 186 frames. | `8a576c50e` w120 correction + `c2eee1dda` episode-length distribution | 0 GPU to price; the arm itself is one v7-tiny run |
| P-18 | DataEng | **Census episode lengths over all 2,376 parity episodes** (and the 4,713 B1 set). The `max_k = 9` verdict currently rests on a **208-episode prefix (8.8 %)**, and a sample minimum is an **upper bound** on the population minimum — one episode ≤ 186 frames drops to zero windows at K=9 and makes the corpus-side F-11 guard refuse. ⭐ **Method is already proven and costs no IO**: frame count = `file_size / (9·256·256)` since `frames_u8` is `[T,9,256,256]` uint8 and the tensor is the file; verified 5/5 against real tensor shapes including both extremes. Needs pod/Thor access only. Closes the open half of **L-12**. | `c2eee1dda` — the 208-sample distribution and its stated scope limit | 0 GPU, minutes |
| ~~P-19~~ | Lab hygiene / Arch | ✅ **DONE 2026-09-02 — `Project Steering/CORPUS_TOKEN_GLOSSARY.md`**, 11 tokens adjudicated against source with the defining line cited for each. ⭐ Two shapes named: **a suffixed number is rarely a count** (`w120` = degrees · `-b14` = patch size · `256x640` = pixels · `e4m3` = exponent/mantissa bits — none is "how many"), and **a hash names a SELECTION, not a property** (`e438721ae894` names the clips that are IN, `f09e44db` names the 24 that are OUT — they look alike and mean opposite things). ⚠️ One row (`EVAL6`) is marked INFERRED, not defined: no source definition exists in `stack/` or `taniteval/`, so it is flagged rather than asserted. Original ask: ⚠️ **Sweep for other IDENTIFIER-TOKEN misreads of the `w120` class.** `w120` was read as "120 frames" when it is the 120° FOV, and the wrong reading reached a **live launch guard**, a test that defended it, a docstring, a help string and two comments. The class is *a token in a filename or config key interpreted as a different quantity than it encodes*. Candidate tokens to adjudicate against source: `256x640cyl`, `-b14`, `e438721ae894`, `f09e44db`, `v2ep`, `w120` elsewhere, and any `-w<N>-` in a cache name. ⇒ produce a one-line glossary mapping each corpus/cache token to what it actually encodes, with the source file that defines it. | `8a576c50e` — six sites from one misread token | 0 GPU |
| P-20 | Lab infra ⛔PI | ⛔ **PI DECISION REQUIRED — a test pinning REGISTRY numbers to their raw JSON.** The drift pair `0.669/0.679` was prose; it reached `GOALS_AND_CLAIMS`, `V7_LAUNCH_GATE` P3 and **`MODEL_REGISTRY.md` itself**, where a derived programme constant (*"first run-to-run variance estimate", 1.49 %*) was computed from it — the true value is **1.00 %**. Precedent exists: `test_physicalai_feature_readset.py` pins the feature counts against source *because prose rotted four times*. ⚠️ **Flagged to the PI and deliberately NOT built**: it would bind future registry edits, which is a policy change rather than a fix, and that is the PI's call. | `a6a963950` + the registry correction; the sweep that found all three sites | 0 GPU to build; the cost is the constraint it imposes |


`Appended by the 2026-09-02 FRONTIER SCAN (LAB-RUN-008). Rows GS-1 … GS-7.`
⛔ **Never self-ranked — the Master Mind ranks.** Each row names the finding that produced it.
⭐ This pass discharged **D-5, D-6 and FS-6**, unblocked **FS-1**, **CLOSED one line on its pre-registered
outcome**, and struck one unattributed number.

### Backlog motions taken this turn

| row | motion |
|---|---|
| **P-11** (SparseOcc++ supervision) | ✅ **DONE 2026-09-02 — SERVED AND CLOSED** → `Frontier Scan/Daily/2026-09-02/RESULT.md` F2 + `LEDGER_B13_3d_geometry.md`. The pre-committed read (*"if it requires occupancy GT we cannot produce, the line is CLOSED"*) was **met**: it needs dense per-voxel semantic occupancy GT **and** LiDAR-projected depth. **Guideline S-3 redirects to the self-supervised occupancy family.** |
| **FS-6** (broken ledger links) | ✅ **DONE 2026-09-02** — `LEDGER_A2_jepa.md` and `LEDGER_A4_vla.md` **created** with their entries; `TRACKS.md` links now resolve. |
| **FS-1** (segment drift metric) | ⭐ **UNBLOCKED 2026-09-02** (not executed) → the formulation is now fully specified (N=10 windows, best-vs-worst, **relative** change). ⚠️ **Scope correction attached: port the FORMULATION, not the metric** — theirs is over image-quality scores, ours is a trajectory quantity. **Row stays open; it is 0 GPU and ready.** |
| **I-2** (Mamba-class predictor) | ⚠️ **SHARPENED, priority should FALL** → `RESULT.md` F5 + `LEDGER_B2_post_transformer.md`. HALO's method is confirmed but **its payoff regime is 128K–1M context, which we do not occupy** (v7 runs K in {8,60,300}), and the paper concedes distilled hybrids underperform from-scratch training. ⛔ **Run P-6's benchmark BEFORE any conversion work.** Stays open. |
| **I-3** (online adaptation) | ⭐ **ADVANCED** → `LEDGER_C3_regulatory.md`. Ban now **unsupported at THREE probes** (still not refuted — all three were secondaries). ⭐ **The SMS spans *post-deployment*, so an adapting model is a SAFETY-CASE obligation, not an illegality.** ⛔ **D-4 escalated to the PI queue — the Lab has exhausted its routes (3× HTTP 403).** |
| **P-6** (read `2601.22156`) | ✅ **the read half is DONE 2026-09-02**; the benchmark half is untouched and is now the gating step for I-2. |
| **row 32** (efficiency wedge vs DrivoR) | ⚠️ **STRENGTHENED, needs restating** → DrivoR **56.3 EPDMS** is within **0.3** of privileged PDM-Closed (**56.6**). The wedge must beat **~40 M**, not 32 B. ⛔ Numbers barred from any comparability table until row 3 + D-EPDMS-FAM land (V-5: split-mixing). |

### New proposed rows

| # | domain | proposed work item | source finding | cost |
|---|---|---|---|---|
| **GS-1** | Arch / B&E | ⛔⛔ **AUDIT EVERY ACTION-DECODABILITY PROBE FOR THE ENDPOINT-CONCATENATION LEAK.** Delta-JEPA: an inverse decoder reading `[z_t, z_t+1]` can succeed on *"action-correlated cues"* absorbed into `z_t+1` **"without requiring the model to represent the actual transition"*. **Any TanitAD probe of that shape is confounded by construction, and every P-2 branch eliminated on its evidence must be re-opened.** Same family as the nav-echo bijection (scored 1.0000) and the `situations.py` label-provenance leak. | `Daily/2026-09-02` F1 · `LEDGER_A2_jepa.md` | **0 GPU** — an instrument audit |
| **GS-2** | Arch | ⭐⭐ **The FREE half of the LDAD test, before any arm is designed.** Fit a displacement decoder (`Dz_t = z_t+1 - z_t` → action) on **banked v7 latents**, with **two mandatory controls: a constant-only predictor that must read the no-information value, and a shuffled-action control.** ⛔ **Committed in advance: if LDAD loss is already near the floor at initialisation, the objective is TAUTOLOGICAL on our action channel (which is realised motion, r 0.9988) — the line is REFUTED for TanitAD and no arm is spent.** Only a non-trivial gap earns an LDAD arm on the v7-tiny ladder. | same | **0 GPU**, then 1 tiny arm if it passes |
| **GS-3** | Arch | ⭐⭐ **Conditioning-interface two-way at matched params on the v7-tiny ladder: current FiLM vs action-as-token.** VLWM names layer-internal conditioning as the reason action contributions *"[are] difficult to disentangle"* — and our elimination of FiLM rested on *"it trained"*, which was never evidence of innocence. **Committed read: if action-as-token does not raise the h=1 action/scene ratio >=3x (0.004 → >=0.012), the conditioning interface is EXONERATED and P-2 branch (c) closes — leaving (b), our action channel, as the sole survivor.** | F4 · `LEDGER_B12_memory_horizon.md` | tiny-ladder arms |
| **GS-4** | Arch | **Explicit geometry loss on the v7 readout.** SparseOcc++ Table V: the linear head wins geometry IoU (**36.8**) *"because [it] is supervised by an explicit geometry loss"*, while the mask formulation imposes *"no explicit supervision on occupied voxels"*. Our readout has **no explicit geometry term at all**. **Committed read: if it does not move azimuth-bin decodability, the ceiling is RESOLUTION-bound and the readout must be re-architected** — the expensive branch, but then a known-necessary one. ⭐ Materially cheaper than re-architecting first. | F2 · `LEDGER_B13_3d_geometry.md` | 1 tiny arm |
| **GS-5** | Arch / paper | ⭐⭐ **Adopt S-5: position the modular-vs-E2E question as UNRESOLVED ON BOTH SIDES, and TanitAD as the programme that settles it** (backlog row 18 / H1b). Mobileye's proof is an **existence result** with a *given* decomposition; the E2E-superiority literature is equally unablated. ⛔ **And T-3: never cite `1604.06915` for our hierarchy without the given-vs-learned caveat** — our decomposition must be *learned*, so the separation does not transfer for free. ⭐ **Converts our biggest positioning weakness into the differentiator.** | F7 · register M-1/M-2 | 0 GPU |
| **GS-6** | Lab-infra | ⛔ **Standing rule + check: any number entering a TanitAD document from a web search must be traced to its primary IN THE SAME TURN, or carried as `UNATTRIBUTED` and barred from decisions.** MEASURED: the *"sub-50 ms driving-VLA latency requirement"* does not exist in its cited primary. **Third consecutive day on which the failing load-bearing number was one nobody had opened the primary for** (D-1's 85.1/92.1, B2's 25 %, now this). | F6 · `LEDGER_A4_vla.md` | 0 GPU |
| **GS-7** | Lab-infra ⛔ escalation | ⛔ **CHARTER TENSION NEEDING A MASTER-MIND RULING, not another apology.** Amendment §8.1 ranks Band D and full-text depth **above** breadth; the result is that **track breadth has now failed two consecutive days** (12 of 22 unscanned). Both passes were correct under §8.1 and both violated §6. ⇒ **Either the breadth mandate is re-scoped (e.g. a scan-only rotation of N tracks/day, explicitly budgeted), or depth is capped.** ⭐ Pair with **FS-5**: a banked-but-unread count per track (first instalment written into `TRACKS.md` this pass) — **6 of 8 primaries today were already banked, including the pass's single most consequential finding.** | `TRACKS.md` 4th-pass block · F-process | 0 GPU, PI/MM decision |


`Appended by the 2026-09-02 frontier scan, SECOND turn — escalated to the Master Mind for the v7f design freeze.`
⛔ **GS-2 REVISED and rows GS-8 … GS-10 added.** Reason: the first pass named the *tautology* risk as the
decisive transfer blocker for Delta-JEPA. **It is not.** LDAD is an **encoder-shaping** loss, and our trunk
is frozen — a structural mismatch that decides whether the objective has a mechanism at all.
Source: `Frontier Scan/LEDGER_A2_jepa.md` entry 2026-09-02-02.

| # | domain | proposed work item | source finding | cost |
|---|---|---|---|---|
| **GS-2 (REVISED)** | Arch | ⛔ **TWO blockers must be tested, not one, and in this order.** **(1) FROZEN-ENCODER MISMATCH — the decisive one:** LDAD's `Dz` is built from **two ENCODER outputs**, so it shapes the **latent geometry**; on a frozen trunk it has nothing to shape, and the paper explicitly criticises frozen encoders (DINO-WM: *"limits task-specific adaptation"*). ⇒ **State whether v7f freezes the trunk. If it does, the LDAD line is INAPPLICABLE AS PUBLISHED and we record that** — a predictor-side `Dz-hat` variant would be **our** objective and would NOT inherit their ablations. **(2) TAUTOLOGY (as originally written):** fit a displacement decoder on banked v7 latents with a **constant-only control** and a **shuffled-action control**; near-floor loss at init ⇒ tautological on our realised-motion channel (r 0.9988) ⇒ REFUTED for us, no arm spent. | `LEDGER_A2_jepa.md` 2026-09-02-01 + -02 | **0 GPU** for both |
| **GS-8** | Arch / B&E | ⭐ **ANCHORED actdiv — a diagnostic upgrade with no training change, and it survives a frozen trunk.** Re-express the hold-action control as displacement against the **zero-action** prediction, `z_t+1-hat(a) - z_t+1-hat(0)`, and read **MONOTONICITY in action magnitude**, not merely separation. Delta-JEPA's Figure 6 is exactly this measurement, and **LeWM fails it** (*"means remain concentrated near the origin and substantially overlap"*) — so it is a published, discriminating instrument. **Committed read: if our arms show separation but NOT monotonicity, "the predictor uses its actions" is false in a way our current binary control cannot see.** | same | **0 GPU** |
| **GS-9** | Arch / B&E | ⭐⭐ **Add a TRANSITION-LEVEL rung to the probe ladder: predict `Dx` from `Dz`.** Our ladder probes **states** only (`z_t` → attributes); Delta-JEPA probes **transitions** (`Dz` → state-deltas, their Table 5), which is where **P-4 / L3** — *does the predictor add anything over `z_t`?* — actually lives. Protocol from the paper: freeze the encoder, linear **and** MLP probes, ~20k consecutive pairs, **split train/test BY TRAJECTORY**, 3 seeds. ⛔ Carry our own controls: constant-only + raw-pixel floor. **A missing rung, not a refinement.** | same | **0 GPU**, banked latents |
| **GS-10** | Arch | ⚠️ **`o11p30k` actdiv probe, now carrying a MECHANISM hypothesis.** `HYPOTHESIS`: o11 is the only arm that ever became action-sensitive and its four diffs **included scratch init** = a **free encoder**; if action-sensitivity is encoder-shaped, that is a coherent mechanism, and it is the same statement as REF-A's frozen-encoder ceiling seen from the other side. ⛔ **Does NOT license a design change** — o11 is confounded four ways and a scene-matching shortcut was available. What changes is the **value of the probe**: 4 minutes on a banked checkpoint now tests a named mechanism rather than settling a curiosity. **Run it before the v7f freeze.** | same; gate problem P-3 | **~4 min**, banked ckpt |

### ⭐ Design constraints escalated to v7f (not backlog rows — decisions owned by the Master Mind)

1. ⛔ **Run the endpoint-leak probe audit (GS-1) BEFORE the freeze.** An inverse decoder reading
   `[z_t, z_t+1]` can succeed on endpoint cues *"without requiring the model to represent the actual
   transition"*. **If any P-2 branch was eliminated on such a probe, v7f may be resting on it.**
2. ⚠️ **Record the trunk-freeze decision explicitly**, with its consequence for this line. If a partial or
   late unfreeze is reachable at ~zero cost, **keep it reachable** — the FS-2 asymmetry applies: cheap now,
   expensive once the geometry freezes.
3. ⭐ **Reserve the additive motion-latent slot `m_t`** (H-RANK-17 / backlog row 13). Row 13 makes the
   displacement explicit by construction; LDAD supervises it. **They compose rather than compete**, and the
   slot keeps the line reachable even under a frozen trunk.
4. ⛔ **Exclude `v` from any action-decode target; use `(a, kappa)`.** `v` is our realised-motion channel
   and the leak-prone one. Their Table 3: raw action best, and **adding extra state-change signals made it
   worse** — *"redundant or less action-aligned information"*. Alignment beats breadth.


---

`Appended by the 2026-09-05 daily Lab run (LAB-RUN-009) — frontier scan + the missing Data Engineering package.`
⛔ **Never self-ranked — the Master Mind ranks.** Each row names the finding that produced it.
⭐ This pass produced the **first Wayve adjudication** the register has ever held, **resolved a four-day-old
empty search one pass before it was retired as folklore**, and **corrected two of our own records**.

### Backlog motions taken this turn

| row | motion |
|---|---|
| **L-11** (hoist the episode drop-out census) | ⭐ **MEASUREMENT HALF DONE 2026-09-05** → `TanitAD Research Lab/Data Engineering/Research/2026-09-05-f11-census-guard-scope/RESULT.md`. **CONFIRMED AT SOURCE and upgraded from assertion to measurement: `w_s1_multi = 0.0` in 3 of 3 banked arm configs (9 readings), so the F-11 census and both refusals have NEVER EXECUTED on any arm we can inspect.** ⛔ **The patch is written (`code/hoist_f11_census.patch`) and deliberately NOT APPLIED** — `train_v6_staged.py` is a live-run file and the Lab does not edit a producer (precedent L-5). ⇒ **new row DE5-1 carries the application.** ⚠️ **L-11's own line numbers had rotted**: it cites `:5120-5151`; the code is at `:6297-6329`. |
| **L-12** (shortest TRAIN episode) + **P-18** (episode-length census over all 2,376) | ⭐⭐ **ROUTE CHANGED — the pod requirement may be removable.** The census input `ep_lens = [e.frames.shape[0] for e in train_eps]` is a comprehension over **already-loaded** episodes: no IO, no GPU. **Hoisting L-11 prints `shortest_episode_frames` and `n_episodes_dropped` on EVERY run over the FULL episode set** — not P-18's 8.8 % prefix. ⇒ **both rows become "read the next run's F-11 line" once DE5-1 lands.** ⛔ They do NOT close until that line is actually read: the 190-frame minimum is still a prefix **upper bound**. |
| **row 32** (efficiency wedge vs DrivoR) | ⛔⛔ **BARRED — DO NOT QUOTE, pending reconciliation.** Three independent 2026 navhard tables cap SOTA at **≤ 45.0 EPDMS** (GuideFlow `2511.18729` full text: 23.1–45.0 · IDOL `2605.31476` **38.0** claiming best-of-comparable · RAP-DINO `2510.04333` **36.9** claiming SOTA) against our recorded **DrivoR 56.3 / PDM-Closed 56.6 / DriveFuture 55.5**. **Three independent papers do not miss a 56.3 that outranks them by 18 points.** ⇒ different split, version or scoring-basis era — **V-5's named corruption path**. New register debt **D-10**; work item **FS5-3**. ⚠️ **Does NOT retract CW-1's resolution**, which settled internal consistency; this is a new question about the LEVEL. |
| **I-1** (terminal value head vs fan-scoring — QUALITY half) | ⭐ **ADVANCED by external evidence, still OPEN** → `Frontier Scan/Daily/2026-09-05/RESULT.md` F3. GuideFlow measures the **scorer** at **+15.9 EPDMS** against **+4.0** for all three generative constraint mechanisms combined. ⇒ **the "does scoring quality matter more than generation quality" question that I-1/L-3 pose has a published answer pointing hard at YES** in an adjacent architecture. ⛔ **Not a substitute for the experiment** — their scorer is GTRS-Dense on a v2-99 backbone, so **+15.9 is not attributable to the scorer concept alone** and may never be quoted as our expected delta. |
| **P-5** (L2 not passed: we beat frozen DINOv3 on 1 of 5 targets) | ⚠️ **RE-FRAMED, not resolved** → F5. `2501.08118`: a **frozen** DINOv2 trunk with a proper BEV lift head beats its baseline by **+7.4 IoU on half the data and half the iterations**. ⇒ **"behind on 4 of 5" may be a READOUT result rather than a REPRESENTATION result.** ⛔ It does not settle P-5; it says the settling experiment is a readout experiment. ⇒ **GS-4's priority should rise**; work item **FS5-4**. |
| **empty E1** (DINOv3 × BEV driving) | ⭐⭐ **RESOLVED at the FOURTH probe and REMOVED from the standing empty-search register's candidate list.** It was one pass from retirement as a real absence (`TRACKS.md` next-rotation item 6). It was never absence — all three earlier probes led on the token **`DINOv3`**; the evidence is under **`DINOv2` + `Metric3Dv2`**. ⇒ hygiene row **FS5-6**. |
| **row 39** (standing empty-search register) | ⚠️ **AMENDMENT PROPOSED, not applied** — see **FS5-6**. Every entry in row 39 was promoted on repeated probes of the **same terms**. E1 shows that is insufficient. |
| **debt D-9** (register) | ⛔ **HALF-CORRECTED.** It states LeWM `2603.19312` and Sub-JEPA `2605.09241` are *"UNBANKED and unread"*. **Both ARE in `library.json`** — verified before any web search (V-1). **The unbanked half is FALSE; the unread half stands**, and is now worse: we cite LeWM through **two** critics (Delta-JEPA and now ATM) without having read it. |

### New proposed rows

| # | domain | proposed work item | source finding | cost |
|---|---|---|---|---|
| **FS5-1** | Arch / B&E | ⭐⭐⭐ **Reconnect REF-C's ranking to the REFINED fan and re-score the banked 201 windows.** Today's audit found *"the ranking never sees the refined fan (201/201)"*. GuideFlow measures the scorer at **+15.9 EPDMS** vs **+4.0** for all generative constraint mechanisms combined ⇒ **the blinded component is, on published evidence, this family's largest lever.** **Pre-committed read: if the four families do not move, the blindness is cosmetic and we say so; if they move, the wiring defect has been suppressing the family's largest published lever and it is priced P0.** ⭐ *A solve, not a refutation — the shape the PI asked for on 2026-09-05.* | `Daily/2026-09-05` F3 · `LEDGER_B7_diffusion_flow.md` | **0 GPU** |
| **FS5-2** | Arch / B&E | ⭐⭐⭐ **Compute the FULL 2×2 ATM on banked v7 latents — the OFF-DIAGONAL `G_{T→P}` above all.** Our probes read only the diagonal, and **the diagonal carries the GS-1 endpoint-concatenation leak by construction** (`ξ = [z_t, z_{t+1}, Δz]`); the off-diagonal does not, because an endpoint cue in the real `z_{t+1}` need not survive into `ẑ_{t+1}`. Published: `D_{T,T}` correlates **ρ=0.813** with planning success vs **ρ=0.498** for prediction loss ⇒ **our `o5_loss` ranking is the weaker signal.** ⛔ **Mandatory controls (they report no chance floor): constant-only predictor must read the no-information value; shuffled-action must read chance. Report `n`.** **Pre-committed read: `G_{T→P}` separating arms `o5_loss` ranks equally ⇒ adopt it in the ladder; `D_{T,T}` at the floor at init ⇒ the diagonal is tautological on our realised-motion channel and only the off-diagonal is admissible for us.** | `Daily/2026-09-05` F2 · `LEDGER_A2_jepa.md` 2026-09-05-01 | **0 GPU**, banked latents |
| **FS5-3** | B&E / Opp ⛔ blocking | ⛔ **Reconcile the navhard EPDMS SCORING BASIS before any external comparability claim ships.** Three independent tables cap navhard at **≤ 45.0**; we hold **55.5 / 56.3**. Determine split, NAVSIM version and scoring-basis era for each. **Until then row 32 is BARRED.** Register debt **D-10**. | `Daily/2026-09-05` F4 · `LEDGER_A5_benchmarks.md` | **0 GPU** |
| **FS5-4** | Arch | **Read `2501.08118` §method for the BEV lift-head specification and diff it against our `readout.py` pooling.** **Pre-committed read: if their head carries explicit geometry or depth supervision ours lacks, GS-4 is the right next arm and P-5's "behind on 4 of 5" is re-scoped as a HEAD finding; if the heads are comparable, the representation reading of P-5 stands and GS-4 gains nothing.** ⚠️ Their +3 IoU Metric3Dv2 result says depth carries real weight, so "frozen trunk" is not the whole story. | `Daily/2026-09-05` F5 · `LEDGER_A3_vision_encoders.md` | **0 GPU** |
| **FS5-5** | Arch / B&E | ⭐ **Re-score the EXISTING RL and veto-only arms with the reward decomposed per family and per event band**, instead of aggregated. `2607.08072`: a scalar rollout reward **hides cross-dimension degradation**; the fix is event-localised credit. ⇒ today's *"the RL stage makes refcv3's fan LESS safe... it is the REWARD that does it"* is a **known field failure mode**, so the response is *change the reward's granularity*, not *debug the arm*. **Pre-committed read: regression visible in one family while another improves ⇒ mechanism CONFIRMED on our data, fix is granularity; every family moving the same way ⇒ REFUTED for us, and the reward's sign or shape is the defect.** | `Daily/2026-09-05` F6 · `LEDGER_B5_post_training.md` | **0 GPU to re-score** |
| **FS5-6** | Lab hygiene | ⛔ **An empty search may not be promoted to standing absence until one probe has varied the TERM, not the phrasing.** MEASURED: E1 was empty at three probes and queued for retirement as folklore; the fourth probe found `2501.08118` immediately because it dropped `DINOv3` for `DINOv2`+`Metric3Dv2`. **Same family as the `w120` identifier misread — a token standing in for a quantity it does not encode.** ⇒ amend row 39's promotion rule, and re-probe its five standing entries under varied terms before any is cited as absence again. ⭐ *A term-specific empty is evidence about the TERM, not about the world.* | `Daily/2026-09-05` F5 · `raw/search_log.md` E1 | **0 GPU** |
| **DE5-1** | DataEng / Training | ⛔ **APPLY the F-11 census hoist** — `Data Engineering/Research/2026-09-05-f11-census-guard-scope/code/hoist_f11_census.patch`, ⚠️ **UNTESTED, written by reading**. Hoist the census, its print and the `n_drop` **corpus-re-selection** refusal out of `if w_stage.w_s1_multi:`; **keep the `s1_multi_k` reachability refusal inside** (it is genuinely multi-tick-specific). Add `max_horizon_in_force` + `w_s1_multi_in_force` to the printed dict so the census is re-derivable from the log (backlog **MM-3**'s "raw that is only a pointer"). Add the two-regime test to `stack/tests/test_v6_s1_multitick.py`. ⚠️ **A first hoisted run may legitimately REFUSE — that is the guard working and must not be "fixed" by lowering it.** **Owner: TrainingFlyWheel / MM — the Lab measured, it does not edit a live trainer.** | `2026-09-05-f11-census-guard-scope` F1/F2/F3 | small code change + 1 test |
| **DE5-2** | Lab hygiene | ⛔ **Two mechanical rules this pass paid for.** **(a) Backlog rows pin defects by SYMBOL or a quoted line, never by line number** — L-11's `:5120-5151` rotted in three days (the file moved ~1,180 lines). **(b) On the G: mount a Python `os.walk` is the MANDATORY second probe for any absence claim**: grep returned **2** files, the walk returned **20**, and **the 3 files grep missed were the ones that turned the finding from inference into measurement.** ⚠️ Add a third: **a key-shaped probe that misses a NESTED key reads exactly like absence** — two structured JSON probes returned "not found" for a value present nine times; raw-text search settled it. | `2026-09-05-f11-census-guard-scope` F5/F6 + `raw/search_log.md` P3 | **0 GPU** |


---

`Appended by the 2026-09-09 daily Lab run (LAB-RUN-010) - full pass: frontier scan + all four domain packages.`
⛔ **Never self-ranked - the Master Mind ranks.** Each row names the finding that produced it.
⭐ This pass **restored track breadth after a four-pass failure (22/22)**, **resolved debt D-10 in
mechanism**, produced the **first Alpamayo 1.5 adjudication**, and **corrected two of our own records**.

### Backlog motions taken this turn

| row | motion |
|---|---|
| **I-1** (top INJECTED row - terminal value vs fan-scoring) | ⭐ **SERVED 2026-09-09** -> `TanitAD Research Lab/Deployment & Optimization/Research/2026-09-09-i1-quality-half-prescreen/RESULT.md`. **The QUALITY half is still OPEN and this does not answer it** - what changed is the cheapest next step. `2608.12939` **proves** action-conditioned latent divergence bounds *"multi-step prediction error **and planner cost**"* (CEM selection regret **-15.2 +/- 2.0 %**), so a **0-GPU Stage-0 pre-screen** can refute the line before any value-head arm is spent. ⛔ Three constraints now bind the comparison: **matched LATENCY** (not step-count), **INFERENCE-seed replicates** (iCEM samples; 0.30 m floor), and **FS5-1 must land first** or the value head is compared against a fan-scorer wired to the unrefined fan. **Row STAYS OPEN.** |
| **row 4 / MM-E1** (EMA tau ramp) | ⛔⛔ **PREMISE HALF-RETRACTED 2026-09-09.** The row states *"our fixed tau=0.996 has no published operating point - every EMA-teacher line ramps tau."* **`2606.28383` uses a FIXED alpha = 0.996, does not ramp, and measures a 44-fold collapse in discriminative power without EMA.** ⚠️ **Scope: 1,289,130 params on structured agent-state vectors, nuPlan mini, 1,322 scenarios - NOT pixels, NOT our scale.** Only the sentence *"no published operating point exists"* dies; **the ramp-or-not design question at v7 scale is untouched and the arm still needs to run.** |
| **row 22 / H-DATA-1** (frozen-feature leverage predicts clip value) | ⭐⭐ **RE-SCOPED, and the scoring half is now FREE** -> `.../Data Engineering/Research/2026-09-09-control-relevant-clip-value/RESULT.md`. The signal is `s = ||z_hat - z||_2`, the JEPA's own prediction error, which we already compute every step and discard. ⛔ **But its CONSTANT-VELOCITY baseline reads rho = 0.314** - a large share of "complexity" is kinematic. ⇒ **score-and-floor BEFORE the leverage-K vs random-K training arm**; a clip-value number without a kinematic floor is uninterpretable. New row **FS9-3**. |
| **row 32** (efficiency wedge vs DrivoR) | ⛔ **STAYS BARRED, and now for a SECOND independent reason.** Reason 1 (D-10) is unchanged. Reason 2 is new: **`2603.24581` states 104M AT INFERENCE and 191M in training.** Both the unverified ~40M DrivoR figure (L-15) and this 104M are inference-time counts. **A TanitAD count quoted training-inclusive against either is a V-5 unit error that would understate us.** Guideline **T-5**; new row **FS9-7**. |
| **debt D-10** (navhard scoring basis) | ⭐⭐⭐ **MECHANISM RESOLVED 2026-09-09; PROVENANCE OPEN.** Two disjoint EPDMS populations - navhard **23.1-45.0** (5,912 observations) and "NAVSIM v2" **84.8-89.3** on *"12k evaluation scenarios"* - and the maintainers *"discourage the use of self-reported and unofficial 'NAVSIM v2' benchmark splits"*. ⛔ **Our own 55.5 / 56.3 match NEITHER cluster**, so their provenance is unknown rather than disputed. Guideline **T-4**; new row **FS9-2**. |
| **debt D-9** (LeWM `2603.19312` unread) | ⛔⛔ **HARDENS. `2608.12939` is the THIRD independent critic of LeWM we have now read without reading LeWM.** Three restatements through channels we did not choose is not a substitute for the primary. **Promoted to rotation item 1 and it stays there.** |
| **debt D-8** (Kairos / B9 primary) | ⛔ **WORSENS. The curation section failed at THREE routes today** (pdf `maxContentLength exceeded`, abs landing-only, html truncated mid-section 4.2). Its two-level-curation claim stays **`RELAYED` and decided nothing** in today's package. New row **FS9-8**. |
| **debt D-4** (UNECE GRVA primary) | ⚠️ **CHARACTER CHANGED, still unread at FIVE routes.** Route 4 was a **direct primary PDF URL on unece.org** returning **HTTP 403**; route 5 (jasic.org) returned a CIDFont stream with no text layer. ⇒ **This is a RETRIEVAL-CHANNEL block on a document we have located, not a discovery failure.** ⛔ **It is no longer a Lab item** - a human with a browser settles it in two minutes. Stays with the PI. |
| **GS-1** (endpoint-concatenation leak audit) | ⭐ **GAINS A NEW DEPENDENT, which raises its value.** RLIR `2509.23958` (5-10 % action-following gain) has a reward that **is** an inverse decoder scored on the model's own output - exactly the shape GS-1 names. On our realised-motion channel (`r = 0.9988`) that reward is plausibly satisfiable by the leak. ⇒ **RLIR is BLOCKED FOR TANITAD PENDING GS-1.** New row **FS9-6**. |
| **FS5-1** (reconnect REF-C ranking to the refined fan) | ⭐⭐ **PRIORITY SHOULD RISE - it now GATES I-1/L-3.** Comparing a learned value head against a fan-scorer that never sees the refined fan (201/201) measures our wiring defect, not the design question. It is 0 GPU and ready. |
| **GS-7** (charter breadth-vs-depth tension) | ⭐⭐ **EVIDENCE SUPPLIED, RULING PROPOSED - not another apology.** Breadth and depth coexisted in one budget this pass **because two of the three full-text reads came from SCAN hits on stale tracks (B10, A5), not from the debt list.** ⇒ **8.1's depth-first ordering starves breadth only when depth is spent on standing debts.** **Proposed clause: keep 8.1's order, and spend the day's full-text budget on the best hit AVAILABLE, debt or scan.** New row **FS9-10**. |
| **P-11 / guideline S-3** (self-supervised occupancy redirect) | ⭐ **THE REDIRECT HAS A TARGET.** The B13 sweep surfaced **OccFeat** (auxiliary BEV pretraining head predicting occupancy plus self-supervised image features) and **SelfOcc** - the self-supervised occupancy family S-3 pointed at after SparseOcc++ closed on its occupancy-GT requirement. New row **FS9-9**. |
| **FS-4** (JetPack FP8/FP4 census re-run) | ⚠️ **UNCHANGED for a second consecutive pass.** Newest JetPack remains 7.2.1 (2026-08-12); empty E-C2b. It is gated on `quant_gate.py`, not on a platform release. |
| **row 39** (standing empty-search register) | ⚠️ **E-B3 is a candidate for promotion and is NOT promoted yet.** It is empty at two probes and the second **varied the TERM** per FS5-6, but B3 is the one Band-B track that has never produced a transfer path, and E1's four-probe history says one more term-set is cheap insurance. |

### New proposed rows

| # | domain | proposed work item | source finding | cost |
|---|---|---|---|---|
| **FS9-1** | Arch / B&E | ⭐⭐⭐ **Port the Separation Rate (SR) and Invariance Radius (IR) onto the v7-tiny ladder, WITH OUR OWN CONTROLS.** ACPC is a **post-hoc diagnostic on a FROZEN model** - the first item in the action-identifiability family our frozen trunk does not block, after LDAD (encoder-shaping) and AITS (encoder-side) both did. SR is a **rate in [0,1] against a self-calibrated threshold** (`IR_q + delta`), i.e. **rig-relative by construction** - the property H-RANK-16's 8.56 participation floor lacks (20.52 / 40.77 / 8.56 on three rigs). Published operating band: **SR 0.967-0.984 healthy, 0.066 collapsed.** ⛔ **Mandatory controls they do NOT ship: a constant-predictor arm that must read the no-information value, and a shuffled-action arm that must read chance. Report n per cell.** **Pre-committed read: if SR separates the three v7 arms in an order `o5_loss` does NOT reproduce, adopt SR as a ladder rung; if all arms and the constant control read within noise, SR is uninformative on our rig and we say so and stop.** ⭐ Also reads the **DENOMINATOR** MM-1 showed our h=1 ratio was moving through. | `Daily/2026-09-09` F1 - `LEDGER_A2_jepa.md` 2026-09-09-01 | **0 GPU**, banked latents + checkpoints |
| **FS9-2** | B&E ⛔ blocking | ⛔⛔ **Split-stamp all six external EPDMS numbers before any of them is quoted.** For 55.5, 56.3, 89.3, 86.1, 85.1 and 84.8 extract from the primary: split name, scenario count, NAVSIM version/commit, leaderboard-submitted yes/no. **Pre-committed read: if counts partition cleanly into ~5.9k navhard and ~12k navtest, D-10 CLOSES and every number gets a stamp; if any paper reports ~12k AND calls it navhard, the split names themselves are unreliable and we stop using published EPDMS for external comparison entirely.** ⚠️ Note our own assignment of "12k" to navtest is an **inference from the count**, not a quotation. | `Daily/2026-09-09` F2 - `LEDGER_A5_benchmarks.md` 2026-09-09-01 | **0 GPU** |
| **FS9-3** | DataEng | ⭐⭐ **Clip-value score-and-floor on parity-train, four arms.** Score every parity-train episode with `s = ||z_hat - z||_2` from a banked v7 checkpoint, reported against **(a) a constant-velocity floor, (b) a shuffled-score control that must read the no-information value, (c) a random-encoder floor that must read ~zero.** ⛔ **Pre-committed read: if the constant-velocity floor recovers more than ~0.5 Spearman of the learned ranking, the signal is predominantly kinematic and H-DATA-1 is RE-AIMED at what the latent adds over kinematics - not abandoned.** Only a learned ranking clearly above the kinematic floor earns row 22's non-parity leverage-K vs random-K arm. ⚠️ The source paper **concedes it never tested downstream training value** - that gap is ours to fill. | `Daily/2026-09-09` F4 - `LEDGER_B10_retrieval.md` 2026-09-09-01 | **0 GPU** to score |
| **FS9-4** | Deploy ⭐ serves I-1 | ⭐⭐ **Stage-0 pre-screen for injected row I-1.** Compute the ACPC-style rollout-divergence statistic on banked v7 latents, with a constant-predictor control and a raw-input floor. ⛔ **Committed in advance: if the divergence bound for our best arm already admits a planner-cost gap larger than the entire fan-vs-value effect we could hope for, the latent is the binding constraint, NO value-head arm runs this cycle, and the finding is that I-1 is blocked on REPRESENTATION not on scoring.** ⚠️ Watch the self-check hazard: if the value head trains on the same latent the diagnostic reads, the screen shares the defect it screens for. | `Daily/2026-09-09` F1 + `2026-09-01-terminal-value-vs-fan-scoring` | **0 GPU** |
| **FS9-5** | Arch / B&E | ⭐ **Build the planner-vs-predictor disagreement monitor (guideline T-6).** Band-D claim A15-1's contradiction is that a third party had to build a **runtime** reasoning-trajectory consistency monitor (`2608.29583`) for the alignment NVIDIA claims RL fixed. Our analogue: **log planner-selected vs predictor-implied trajectory divergence per window** on banked dumps. **Pre-committed read: if planner and predictor never measurably disagree on banked windows, the monitor has no signal and the line closes.** | Register A15-1 | **0 GPU**, banked dumps |
| **FS9-6** | Arch / B&E ⛔ ordering | ⛔ **RLIR `2509.23958` is BLOCKED pending the GS-1 endpoint-leak audit, and this row records the dependency so it is not rediscovered.** Its inverse-dynamics reward is precisely the shape GS-1 names; on our realised-motion channel (`r = 0.9988`) it is plausibly satisfiable by the leak. **Adopting it first would spend RL compute optimising our best-documented confound.** ⭐ Contrast to carry: ACPC compares two ROLLOUTS under identical actions and never reads the real `z_{t+1}` endpoint, so it is leak-resistant where RLIR is leak-prone. | `Daily/2026-09-09` F5 - `LEDGER_B5_post_training.md` 2026-09-09-01 | **0 GPU** (an ordering rule) |
| **FS9-7** | Lab hygiene | ⭐ **Adopt T-5: every parameter count in a comparability table carries INFERENCE-TIME or TRAINING-TIME**, exactly as every metric carries its eval tier. MEASURED trigger: Latent-WAM is **104M at inference / 191M in training**, and DrivoR's ~40M is unverified (L-15). **Apply it to our own sub-300M claim FIRST**, so we are not the ones caught. ⚠️ A notation rule, not a falsifiable claim - marked as such. | `Daily/2026-09-09` F3 | **0 GPU** |
| **FS9-8** | Lab hygiene / B9 | **Kairos `2606.16533` curation section by a FOURTH route** (ar5iv mirror, or per-section HTML). Three routes failed today. Until then its two-level-curation claim is `RELAYED` and bars nothing and decides nothing. Closes debt **D-8**. | `Daily/2026-09-09` search log E-K | **0 GPU** |
| **FS9-9** | Arch / B13 | **Read OccFeat (and SelfOcc) for their supervision requirements**, the self-supervised occupancy family guideline S-3 redirected to after SparseOcc++ closed on needing dense per-voxel occupancy GT plus LiDAR depth. **Pre-committed read: if OccFeat's occupancy-reconstruction plus feature-distillation heads need only camera + self-supervised features, GS-4's explicit-geometry-loss arm is buildable on our corpus; if it needs voxel GT we cannot produce, this branch closes too and we say so.** | `Daily/2026-09-09` Q10 - `LEDGER_B13_3d_geometry.md` | **0 GPU** first |
| **FS9-10** | Lab governance ⛔ MM ruling | ⭐⭐ **Close GS-7 with a one-clause amendment rather than a fifth escalation.** MEASURED this pass: breadth (22/22) and depth (3 full texts) coexisted in one budget **because two of the three full-text reads came from SCAN hits on stale tracks, not from the debt list.** ⇒ **Proposed clause to amendment 8.1: keep the priority order, and spend the day's full-text budget on the best hit AVAILABLE, whether it surfaced from a standing debt or from that day's scan.** Debt-clearing then competes on value instead of outranking breadth by construction. | `TRACKS.md` sixth-pass block | **0 GPU**, MM decision |
| **FS9-11** | Lab infra ⛔ escalation | ⛔⛔ **THE DAILY SCAN HAS MISSED FIVE OF THE LAST SEVEN DAYS** (2026-09-03, 09-04, 09-06, 09-07, 09-08). 09-07 produced eleven domain packages, so the Lab was working - **only this artifact was skipped.** The 09-05 pass escalated two days; nothing changed and the count grew. ⇒ **This is a trigger/scheduling defect, not a budget one, and it needs a mechanical fix (a gate that fails loudly when yesterday's `Daily/<date>/` is absent), not another note.** | `Frontier Scan/Daily/` directory listing, MEASURED | **0 GPU** |
| **FS9-12** | Lab infra | ⚠️ **The G: mount cost this pass more than any research question, and the mitigation is known.** MEASURED today: three outage waves (7, 15 and 10+ consecutive Errno 22 failures), with **metadata resolving throughout** - `os.walk` returned 41 control directories while content reads failed. **The working pattern is: author on local disk, land with a retry loop that re-reads and asserts byte length, stage by explicit forward-slash path, verify by blob comparison with a 40-char shape check.** ⇒ propose promoting that to a small shared helper rather than each agent rediscovering it. ⚠️ Also measured: the Drive client had **already self-restarted 16 minutes earlier** and was still not serving, so a second restart would only have reset its own recovery. | this pass's process record | **0 GPU** |
