# LAB BACKLOG — the durable seed list for the daily Research Lab pass

## The update contract (Master Mind, 2026-08-29 — this is the workflow, binding)

**The same-turn rule, mirrored from GOALS_AND_CLAIMS:** any session that EXECUTES,
REFUTES, SUPERSEDES, or UNBLOCKS a row updates that row IN THE SAME TURN. A backlog
that is only read rots — that is how the last one accumulated void doctrine lines and
measurement-refuted items.

| motion | owner | when |
|---|---|---|
| **consume** — a row worked on gets `▶ TAKEN <date> <session>`; done gets `✅ DONE <date> → <artifact path>` | whoever executes (daily Lab run, FlyWheel, MM) | same turn |
| **propose** — new rows from Lab findings ("what this changes"), retraction follow-ups, FlyWheel escalations | the finder, appended under `## PROPOSED (unranked)`, never self-ranked | same turn |
| **fence** — a row refuted by measurement gets ⛔ REFUTED + the register row id, never silently deleted | whoever measures | same turn |
| ⭐⭐ **answer asks FIRST** — read `LAB_ASKS.md` and address every OPEN row **before** pulling seeds from this file (PI 2026-08-31: FlyWheels talk to the Lab directly). ⚠️ A question the Lab CANNOT answer is answered with WHY NOT, never left silent | Research Lab | every daily pass |
| ⭐ **verify** — run `stack/scripts/lab_backlog_drift.py` before ranking; it lists finding-commits registered since this file was last written, and exits non-zero if any are unproposed | mechanical | every daily run |
| **rank** — PROPOSED rows move into the ranked list; priorities re-sorted | **Master Mind only** — with each daily-run commit, full re-sort weekly | daily/weekly |
| **⛔PI rows** — approve/unpark/kill | **the PI** — one sentence suffices; MM records it | on decision |

**Ranking criteria, in order (the PI's stated priority, 2026-08-29):** (1) serves
driving-with-WM / hierarchical dominance / an excellent driving model; (2) unblocked
now; (3) evidence class of the expected payoff (MEASURED-lever > PUBLISHED > HYPOTHESIS);
(4) cost. Rows failing (1) rank below everything that passes it regardless of cost.

**The injection lane (PI addition, 2026-08-29):** the PI and the Master Mind may
INJECT research topics directly — explicitly INCLUDING topics that cannot be
considered inside the running production programme (frontier bets, out-of-scope
explorations). Injected rows live under `## INJECTED (PI / MM)` below, carry their
injector and date, and are EXEMPT from ranking criterion 1 — the production filter
does not demote them; that is the lane's purpose. ⭐ GUARANTEE: whenever the lane is
non-empty, the daily Lab run dedicates AT LEAST ONE of its four packages to the top
injected row — exploration cannot be crowded out by production-adjacent work. PI
injections outrank MM injections by default.

**Freshness tripwire:** the daily run's summary states the backlog delta (consumed /
proposed / none). A week with zero deltas while runs happened = the contract is being
violated — flag it in the program report.


`The daily Lab spawn reads this file; the Master Mind re-ranks weekly; rows are retired by
linking the answering package.`

`Seeded 2026-08-29 by the steering audit (LAB_STEERING_AUDIT_2026-08-29.md): harvested from the
five per-domain backlogs, the register's OPEN rows, and LAB-RUN-001/002/003's escalations and
named empty searches. Rows marked ⛔PI need a PI decision before compute is spent. "verify-first"
= the source predates 2026-08-22 and closure elsewhere is plausible — probe before working it.`

| # | prio | domain | question | why it matters now | source |
|---|---|---|---|---|---|
| 1 | P0 | Deploy | Implement `quant_gate.py` — the 4-stage fail-closed engine-inspection gate (kDETAILED precondition, precision census, size/latency cross-check, four-family dense-head stage); move the B1 recipe to strongly-typed + explicit Q/DQ | D-B1-GATE: TRT on Thor sm_110 silently falls back to FP32 under FP8/FP4 flags (#4590 re-verified OPEN) — every quantisation read before this gate is a false-positive generator | register D-B1-GATE + `Deployment & Optimization/Research/2026-08-29-quant-gate-spec/RESULT.md` |
| 2 | P0 ⛔PI | B&E | Replace the D1 gate's estimator: `stack/tanitad/eval/gates.py:249-251` adjudicates `passed` on a mean-of-split-means (4th clone of the forbidden `overlapping_holdout_se` family) | the gate that decides training continuations is running on the estimator the programme banned; PI decision row is OPEN | register D-GATE-D1-EST / H-EST-2 |
| 3 | P0 ⛔PI | B&E | Benchmark portfolio wholesale approval: GO = navhard-two-stage EPDMS (pinned) + navtest piggyback; GO-cond = Bench2Drive; 2nd wave = WOD-E2E; SKIP = nuScenes-OL claim-bearing, CARLA LB2.1, nuPlan — then start the 4 prep items (NAVSIM 450 GB pull; Bench2Drive+CARLA on the Vulkan-fixed pod; WOD-E2E val + RFS scorer; register-token rig adapter) | G3 (beat SOTA on community benchmarks) has no lane until this is approved; no single benchmark covers the four families, the GO set jointly does | `Benchmarks & Evals/Research/2026-08-29-benchmark-portfolio/RESULT.md` |
| 4 | P0 | Arch | MM-E1 stage-2 follow-through: if EMA joins the recipe → register a τ-ramp arm BEFORE adoption (our fixed τ=0.996 has no published operating point — every EMA-teacher line ramps τ); if EMA is out → SALT frozen-teacher fallback is pre-committed | the only measured lever that moves drift down (0.4531→0.36-0.40) is mid-decision tonight; adopting a mis-specified fixed-τ recipe would bake in a known defect | register MM-E1 + `Architecture & Inference/Research/2026-08-29-ema-warmup-rollout-composition/RESULT.md` |
| 5 | P0 | Arch | v7 head geometry: drop the h≥2 predictor heads for composed h=1 (+ short train-time unroll); they are vestigial under every objective we run | H-PROOF-6d (heads never trained, 1e-3 init), H-PROOF-7 (rolled h=1 predicts to 6 steps), and all four closest-relative WMs (DreamerV3, DINO-WM, TD-MPC2, V-JEPA2-AC) roll one-step models | register H-PROOF-6d/7 + LAB-RUN-003 |
| 6 | P0 | DataEng | Run the two waiting lane test sets with CLRerNet-EMA as-is (±25° corridor ≤3.1 % projection error; 640×256 upscales ×1.25 exactly) — this IS the transfer measurement; gate wider rollout on it, and pin the 4,719-vs-4,729 clip count at run time | the 4th arm of the registered g_tac cross-agreement experiment (D-DATA-GTAC-b) needs the lane-detector reference; transfer must be measured not assumed | `Data Engineering/Research/2026-08-29-lane-detector-deployment/RESULT.md` |
| 7 | ✅ DONE 2026-09-01 lab-run | DataEng | ⚠️ **RE-SCOPED, not simply answered** → `TanitAD Research Lab/Data Engineering/Research/2026-09-01-gtac-producer-cross-agreement/RESULT.md`. The producers are NEAR-DISJOINT (B refuses 3/4 LAT + 5/8 LON of A's tokens); LON agreement 1/7. There is no "2-vs-1 threshold contest" — B refutes the token at any threshold. Two admissibility defects found instead (A's `SPEED_BAND` = hindsight held-speed; A's `CORRIDOR_OFFSET` = 21.65 m on a plain arc). ⛔ Corpus-rate run still needs parity poses → see PROPOSED L-6. ORIGINAL ROW: D-DATA-GTAC-b: measure cross-agreement of the two blind-built g_tac producers (`tactical_goals.py` vs `g_tac_geom.py`) and settle the CORRIDOR_OFFSET 2-vs-1 contest with the lane-detector + `obstacle.offline` trigger as independent reference | two producers of the SAME label family are live with unmeasured agreement — a silent fork in the canonical labels | register D-DATA-GTAC-b |
| 8 | P0 ⛔PI | DataEng | D-LABEL-GT caveat 1 — untimed CoT tokens: put the two literature-backed options to the PI (clip-level MIL vs TAN-style alignability gating; TAN: only ~30 % of untimed narrations alignable) | blanket band-assignment has no published support; blocks the CoT channel of the canonical label set | register D-LABEL-GT + `Data Engineering/Research/2026-08-28-corridor-ref-untimed-cot/RESULT.md` |
| 9 | P0 | Arch | Register the VICReg-placement pair on the v7-tiny ladder → controlled three-way vs the EMA bake-off and O14; run the predictor-CAPACITY arm BEFORE any predictor-shrinking arm (IWM: capacity flips equivariance; NextLat: rank down, quality up) | the anti-collapse design space beyond EMA is mapped and cheap; capacity runs opposite to the bottleneck intuition, so shrinking first would misread | `Architecture & Inference/Research/2026-08-28-anticollapse-beyond-ema/RESULT.md` |
| 10 | P0 | Arch | H-RANK-16 residue: run frozen DINOv3 through `spectrum_report` on the SAME 12 val clips at n=1440 — until then ⛔ no arm may be failed on the 8.56 participation floor | the gate compares arms to a number no current instrument reproduces (20.52 vs 40.77 vs 8.56 on three rigs) | register H-RANK-16 (QUEUED) |
| 11 | P1 ⛔PI | B&E | Ego-status two-arm design portfolio-wide: benchmark arm (consumes ego status like every leader) + vision-pure arm (our binding rule), reported side-by-side | every NavSim camera-only leader consumes ego status at inference; without the two-arm design our rows are either non-comparable or rule-breaking | LAB-RUN-002/003 (PI decision, restated) |
| 12 | P1 | B&E | D-EPDMS-FAM: design the tactical/strategic decision-quality companions for any EPDMS row (EPDMS measures compliance+outcome, nothing maps to tac.manoeuvre_decision / strat.route_goal; comfort unmapped) | four-families rule binds every published number, including community-benchmark rows | register D-EPDMS-FAM |
| 13 | P1 | Arch | H-RANK-17: additive motion latent `z_t + m_t ≈ z_{t+1}` (TDV transfer) on the v7-tiny ladder + the n_stack=1 corollary (settles H-RANK-8 in the same session) | run-001's designed-but-never-executed arm; removes the static-latent shortcut by construction; both outcomes committed | LAB-RUN-001 + `…/2026-08-22-lab-run-001/RESULT.md` (Arch) |
| 14 | P1 | B&E | Pseudo-simulation pilot on our NuRec/gsplat assets (3DGS counterfactual observations + proximity weighting; published R²=0.8 vs closed-loop): does it hold R² ≥ 0.7 against our T1 at < 1/10 compute? (CPU prep: select the 10 val episodes with NuRec recon; script the stage-2 perturbation grid) | we hold every ingredient (NuRec msgpack open, gsplat 492 FPS on Thor, T1 harness); would give closed-loop-correlated eval without a renderer in the loop | LAB-RUN-001 (B&E) — ⛔ CONFIRMED id collision: the register now holds TWO H-EVAL-1 rows; re-register this one under a fresh id first |
| 15 | P1 | Deploy | H-DEPLOY-1: per-component INT8 PTQ sweep of the v7-tiny champion on the 4060 (W8A8 first, 32 calib images, RepQ-ViT reparam; dense-head-first accuracy validation) — is quantisation error seam-dominated, and does a full-rank trunk quantize better than a collapsed one? | connects the rank campaign to deployment; INT8-first ordering is doc-verified; feeds the B1 gate's expected deltas | LAB-RUN-001/002 (Deploy) |
| 16 | P1 ⛔PI | Opp | Opponent teardown #1: Alpamayo-R1 (open weights, 10B, Chain-of-Causation) — H-OPP-1: is the hard-case gain mostly decoder-and-data (reasoning-suppressed run retains most of it)? | first fully inspectable big-league opponent; measurable, not just readable; gated on metered compute or a validated INT4 path | LAB-RUN-001 + register H-OPP-1 |
| 17 | P1 | Arch | Orbis 2 (2607.15898) deep-read: planning-time hierarchy or representation-only? (+ CheckVLA 2607.26789 = published run-time verification ≈ our H11/A9) | top differentiation risk to H1 positioning — "hierarchy a planner USES, with a number" is the only claim left to us; never executed | Opponent STATE run #5 recommendation |
| 18 | P1 | Arch | H1b (hierarchy-edge) is still UNTESTED — D5/D6 have never run; re-frame the matched-params hierarchy-vs-flat test for the v7/B1 world (PREREG_E_EXP1 exists as a starting point) | the programme's thesis hypothesis has no measured datapoint; every architecture decision implicitly assumes it | HYPOTHESIS_LEDGER §1.1 H1b + PREREG_E_EXP1 |
| 19 | ✅ DONE 2026-09-01 lab-run | B&E | **ANSWERED: NO — fed on 0 of 71 readable artifacts** → `TanitAD Research Lab/Benchmarks & Evals/Research/2026-09-01-distance-keeping-fed-check/RESULT.md`. Instrument present + admitted, refusal retired 08-18; the gap is SUPPLY (`win["lead"]` never attached) plus a second gap the row missed (28 artifacts still carry the stale reason string). ORIGINAL ROW: verify-first: is the LONGITUDINAL distance-keeping family FED on the current eval path (`win["lead"]` for the val episodes)? The instrument (lead_metrics.py, D-LEAD-1-admitted) existed unfed as of 08-03 | a binding family reported UNAVAILABLE while its instrument sits idle is a criteria-completeness violation | AI STATE P0 L1 + criteria machinery |
| 20 | P1 | B&E | verify-first: apply the registry rows named in `MANEUVER_LABEL_MISMATCH.md` §6 (v2-arm maneuver_acc scored on v1 labels; corrected number needs a GPU re-run) and finish the DIR_YAW 0.15→0.10 re-read (driver has a path-depth bug; prereg + envelope banked) | the registry currently states a number the mismatch doc says is unrecoverable from banked data; scoped by D-LABEL-GT's new canonical labels | `Benchmarks & Evals/TACTICAL_LABEL_STATE_INDEX.md` §6 |
| 21 | P1 ⛔PI | MM/all | The intake stage: 26+ packages verdictless (oldest 2026-07-08), 9-11 unmerged agent branches — decide once: staff a triager, or declare intake advisory-behind-tests | measured cost: the CTRV floor sat 18 days while gates published CV-only verdicts; four STATE files independently flag the same unstaffed stage | Opp/AI/B&E/DataEng STATEs (08-02/03) |
| 22 | P1 | DataEng | H-DATA-1: does frozen-feature leverage predict a clip's value for WM training? (v7-tiny pair: leverage-K vs random-K, NON-parity) — the DataFlyWheel moat mechanism (MOSAIC: 80 % less data at matched performance) | B1 opens a 4,719-clip corpus; a working selection signal compounds on every future corpus | LAB-RUN-001 (DataEng) + register H-DATA-1 |
| 23 | P1 | DataEng | P3 seed: Y-pilot-50 — 50 YouTube dashcam videos through Y0-Y2 (self-calibration → canonicalize → filter → pseudo-actions); measure focal-recovery spread, action agreement r vs VO, A8 | TanitAD_DataReconstruction is product P3; the strategy + falsifiers have been written since 07-10 and never run; pairs with the IDM head (G2: r ≥ 0.6 vs real CAN) | `Data Engineering/Research/YOUTUBE_DASHCAM_STRATEGY.md` + DE GOALS G2 + H7 |
| 24 | P1 | Deploy | A2 (from the Thor plan): compliance review of `taniteval/` + `stack/tanitad/eval/` — the one unreviewed cluster that decides every deployment gate | its premise was vindicated: the D1-gate estimator clone (row 2) lived exactly there; finish the review so the next clone is caught structurally | `Deployment & Optimization/BACKLOG.md` A2 |
| 25 | P1 | Deploy | O2-pre: fix the encoder's shape-derived pooling so it exports at the DEPLOYED geometry (adaptive_avg_pool fails at 176×624; "exports clean" was only ever true at 256×256) — re-verify at the v7 geometry before any engine build | blocks every TRT encoder engine; the class (claims true only at one geometry) already cost one false claim | `Deployment & Optimization/BACKLOG.md` O2-pre |
| 26 | P2 ⛔PI | DataEng | ZOD access: sign the CC-BY-SA agreement (opendataset@zenseact.com) — the one blocker on the owned real-urban tier (loader shipped + geometry-passed 07-18); re-rank the ZOD/PandaSet tier under B1 | the owned-data moat and EU/night diversity remain unserved; cheap to unblock, but B1 outranks it this cycle | DE STATE/BACKLOG + OWN_DATASET_PLAN §7 |
| 27 | P2 | DataEng | Parity-val balance check (max abs d vs train on the four-family axes; falsifier: < 0.10 closes it) + `validate_pool()` over every selection parquet we hold | parity stays the comparison domain for the tiny ladder under B1's boundary — an unquantified val offset travels into every ladder read | DE BACKLOG P0 3/4 |
| 28 | P2 | DataEng | verify-first: v2-clean-val manifest (n=400, sha256 `abe041db72a045b3…`) — PI freeze-or-reject; and the OODVAL protocol may have consumed it already | an un-frozen val manifest invites silent re-selection (parity-class risk) | DE STATE 2026-08-02 escalation 1 |
| 29 | P2 | B&E | Midpoint-corrected CTRV as a stronger floor (measured half-step bias 0.3044 m on a 1-rad arc) + the high-speed LATERAL question (top-decile lateral flips to floor — does it survive on current arms?) | every published margin is a lower bound on the artifact; the lateral half of the high-speed weakness is unowned | B&E BACKLOG P0 4/5 + STATE F4 |
| 30 | P2 | B&E | WP.29 regulation trace: paragraph-level extraction of ECE-TRANS-WP.29-2026-139e into REGULATION_TRACE.md (ISMR / DSSAD / MRM → TanitAD artifact per row) | the Phase-1 safety case's traceability; untouched since 07-16, no successor mechanism exists | B&E GOALS G3 + BACKLOG P1.3 |
| 31 | P2 | Opp | Complete the Waabi profile (simulation-first, closest public analogue to our data-efficiency thesis; in-loop vs offline-factory question) + the deltas watch (Pony Q2, Mullin bill, recalls) | the most architecturally adjacent unprofiled player; the weekly-deltas discipline died with the rotation | Opp BACKLOG run-#6 items 4/6 |
| 32 | P2 | Opp | Sharpen the efficiency wedge: DrivoR leads NavSim camera-only at ~40 M params — "sub-300M" is community-demonstrated; restate our efficiency claim against DrivoR/AR1/GAIA-4 with named numbers | W-05 (no params-vs-benchmark table from NVIDIA) stays open, but the wedge must now beat 40 M, not 32 B | LAB-RUN-002 (Opp/B&E) + W-05 |
| 33 | P2 | Arch | LIT-3 remainder: the O1 margin-∝-action-difference + median-gate variant — the ONE untested O1 rescue (plain, stop-grad, stop-grad+detach all refuted; O1 measurably buys environment content) | closes the O1 thread with a measured yes/no instead of an assumption | register E-DEC-15 |
| 34 | P2 | Arch | Quality-per-FLOP ledger: auto-append FLOPs/latency (4060 + Thor-projected) to every ladder/bake-off arm | the efficiency moat needs a measured series, not per-paper spot reads; the old G3 idea survives the goal file's retirement | AI GOALS G3 (harvested) |
| 35 | P2 | Deploy | Thor doc refresh: RUNBOOK §6 O-table vs the quant-gate spec; ALPASIM_ON_THOR unblock line (tanitad-eval is TERMINATED; path = Vulkan-fixed pod / Bench2Drive prep); re-price O6/O14 for v7-class engines | stale ops docs are how resurrected-bug launches happen (pod-drift class) | audit §1.5 + LAB-RUN-003 |
| 36 | P2 | Lab-infra | `kb_add.py` hardening: atomic download (size + %%EOF check, temp+rename) and an index lock around load-modify-write | both failure modes MEASURED in run 001 (truncated 5 MiB PDF without %%EOF; concurrent add clobbered the repaired index) | LAB-RUN-001 process hazards 3/4 |
| 37 | P2 | Lab-infra | Resume the per-field KNOWLEDGE_BASE.md findings layer (1-line dated entries per run package), or formally re-declare it generated-from-packages; fix the top-level router's five-field paths | constitution §2 duty; zero entries appended since 2026-08-18 across all fields; Deploy KB empty by declaration while two quant surveys landed | audit S-1 / C-1 |
| 38 | P2 | DevEnv | Viz backbone triple: pin `rerun-sdk==0.34.1` in a requirements file; write the two-RecordingStream dual-sink tee (single-sink baseline 52,966 B/window; stub guard shipped); wire the Viewer-MCP so viz is verified not asserted | P7's replay/visualize UI rests on this; the pin exists in NO requirements file (measured) | _devenv BACKLOG P0 2/3/4 |
| 39 | P3 | Lab-infra | Standing empty-search register — do NOT re-run without a new trigger, watch for publications instead: (a) no published pipeline labels lane-offset from an ego-excursion threshold (08-28); (b) no official V-JEPA 2.1 on HF, 2 probes (08-27); (c) Drive-JEPA has no navhard number (08-28); (d) TRT calib-cache format is not officially documented (08-29); (e) PhysicalAI-AV has no session id / absolute clock, 4 probes — drive-level splits impossible (08-02) | absence findings rot into re-searches; each cost a session once already | LAB-RUN-002/003 + register + DE STATE |

## Retired-by-supersession (do not re-open without new evidence)

- Open-loop SC-13 anticipation probing (retired run #5: effect is static-frame + ego-kinematic).
- rollout-k extension / h≥2 horizon extension (E-DEC-66 inert; H-PROOF-6d/7 vestigial heads).
- NAVSIM/Bench2Drive entry-requirements audit (done: benchmark portfolio, LAB-RUN-002/003).
- NuRec feasibility probe (done: msgpack open, gsplat 492 FPS on Thor).
- AlpaSim "docker-host-only" blocker (refuted: runs bare on A40; renderer via layer-fetch).
- "NEVER to HF, even privately" doctrine (superseded by TANITAD_PROGRAMME §0: private HF is the rule).


## ⛔⛔ PROPOSED (unranked) — THE v7 GATE PROBLEMS, injected 2026-08-31 by the Master Mind

⚠️ **WHY THIS SECTION EXISTS, AND IT IS AN ADMISSION.** The update contract above says any
session that produces a finding proposes a row **in the same turn**. Between 2026-08-30 and
2026-08-31 I generated MM-E10 through MM-E19 plus five gate problems and **proposed nothing** —
this file has not been touched since 2026-08-29. MEASURED against its own text: **`hold-action`
0 hits · `action-deaf` 0 · `L3` 0 · `teacher-forced` 0 · `target construction` 0 · `O11` 0.**

⇒ **The Lab's own seed list contained NONE of the five problems now gating v7.** Today's run
produced good work only because the questions were hand-fed in the spawn brief. Left to its
standing instruction — *"pull the top unblocked rows from LAB_BACKLOG"* — it would have
researched a stale list. **A backlog that is only read rots; this one was not even written to.**

| # | pri | field | row |
|---|---|---|---|
| P-1 | P0 | Arch | ⭐⭐ **P1 — no v7 arm has ever beaten its own hold-action control.** All three arms `LOSES_TO_HOLDV0` on every distance metric, heading ~95° = chance. **This is the programme's actual problem**; the rows below are hypotheses about why. ⭐ The one positive: `copy_detector CLEAN`, echo 0.0000 — fake-skill-by-echo traded for honest absence of skill. → `V7_LAUNCH_GATE.md` P1 |
| P-2 | P0 | Arch | **P2 — the predictor does not use its actions.** ELIMINATED: the missing objective (MM-E11, ratio FELL 0.40× vs a 10× criterion), zero-init FiLM (it trained), a starved gain (MM-E18, converged). OPEN: (a) horizon *[under test]*, (b) ⭐ **our "action" is realised motion, not a command — a genuine command channel has NEVER been tested**, (c) representation, (d) ⭐⭐ **TARGET CONSTRUCTION — our target is teacher-forced, so it already contains the action's effect** (UWM-JEPA 2605.25313, source-verified: *"applies beyond the unitary parameterisation"*) |
| P-3 | P0 | Arch | ⭐⭐⭐ **O11 — the predictor HAS become action-sensitive once, and that arm was abandoned at 25 %.** `o11p30k` sat at the provable no-information floor for 5,200 steps, then `pick_acc 1.000` for 12 consecutive rows. ⛔ NOT one-variable (4 diffs incl. scratch init), and a scene-matching shortcut is available. **A 4-minute actdiv probe on its banked ckpt decides whether an 8.6 h arm is worth running** |
| P-4 | P0 | Arch | **P5/L3 — does the predictor ADD anything over `z_t`?** The ladder calls it *"the dissociation gate, and the actual v7 bar"*. ⭐ Independently corroborated — UWM-JEPA reports the same pattern and *"locat[es] the separation in the predictor rather than the encoder"* |
| P-5 | P0 | Arch | ⛔ **L2 IS NOT PASSED AS SPECIFIED — corrected 2026-08-31 after the PI pushed back.** L2 requires beating frozen DINOv3 on **five** targets (`n_agents`, `n_free_cols`, `occ_{l,c,r}`); **we beat it on ONE** (`n_agents` +0.1220 vs +0.0998). C156 retracted the *recommendation* to swap in DINOv3, **not** the underlying "behind on 4 of 5". ⚠️ Two panels also report different numbers for the same target (+0.1220/+0.0998 lead-matched vs +0.3881/+0.2754 E-DEC-29) — **reconcile before either is quoted** |
| P-6 | P1 | Arch | **P3 — drift's 3.3× effect is real and seed-stable but UNATTRIBUTED.** ⛔ `--init-from` REFUTED (E-DEC-60). Ablation now designed and one-variable: `postrain30k + --freeze-encoder`. ⭐ Dual-purpose — the same arm reads drift AND L3 |
| P-7 | P1 | Deploy | ⭐ **A flat strategic rollout is UNDEPLOYABLE** — latency is linear in K (measured), so K=300 costs ~350 ms / ~1,015 ms against a 100 ms budget, and CUDA graphs' 2.57× does not close it. **Temporal abstraction is a deployment requirement, not an efficiency preference** → `Research/2026-08-31-rollout-depth-latency` |
| P-8 | P1 | Arch | ▶ **ADDRESSED 2026-09-01 (literature)** → `TanitAD Research Lab/Architecture & Inference/Research/2026-09-01-longhorizon-bptt-stability/RESULT.md` — no primary in our reference class back-props a 60-step full chain, and the fix is a horizon CURRICULUM, not a smaller clip. ⛔ The mitigation arm has NOT run. ORIGINAL: ⛔ **k=60 with full-chain BPTT is GRADIENT-UNSTABLE at clip 1.0** (gnorm median 5.71 → 2.1e9; killed at step 9,000 on a pre-committed criterion). The 2.4× wall-clock was the priced half of the 6 s horizon; **this is the unpriced half** |

⚠️ **Unranked by design** — per the contract the finder proposes and never self-ranks; the
Master Mind ranks at the next daily-run commit, and ⛔PI rows are the PI's call.

## INJECTED (PI / MM)

*PI injections: just tell the Master Mind the topic in one sentence — it lands here
the same turn, top of the lane.*

| # | topic | injector | date | state |
|---|---|---|---|---|
| I-1 | Latent model-predictive planning with a LEARNED terminal value over composed h=1 rollouts — does a cheap value head beat fan-scoring at equal rollout budget? | MM | 2026-08-29 | ⚠️ **COST HALF served 2026-09-01** → `TanitAD Research Lab/Deployment & Optimization/Research/2026-09-01-terminal-value-vs-fan-scoring/RESULT.md` — MEASURED: breadth is **5.94× cheaper** than depth at equal step-budget (73.33 → 12.34 ms at B=960), value head **0.150 ms = 1.2 %**, breadth flat to N≈32. ⛔ **The QUALITY half — does it produce BETTER plans — is UNTOUCHED and is the real experiment. Row STAYS OPEN.** |
| I-2 | State-space / linear-attention predictors (Mamba-class) as the O5 predictor at matched params — long-horizon composition cost vs our transformer | MM | 2026-08-29 | ⚠️ **RE-SCOPED 2026-09-01 by primary read** → `Frontier Scan/Daily/2026-09-01/RESULT.md` F3 + `Frontier Scan/LEDGER_B2_post_transformer.md`. ⛔ **The "25 %-budget" claim is STRUCK — UNSUPPORTED-AS-STATED.** `2601.22156` is *Hybrid Linear Attention Done Right*; its claim is a **training-data** figure (2.3B tokens to CONVERT a trained transformer, <0.01 % of pretrain, vs >10B prior), not a compute/memory budget. ⭐ **The method changes with it: CONVERT the trained v7 predictor rather than TRAIN a Mamba at matched params** — categorically cheaper on a fixed fleet. ⚠️ Demonstrated on *language* at extreme context; transfer to short-horizon visual latents is a HYPOTHESIS, and the 2.3B figure does NOT translate into our units. **Stays open.** |
| I-3 | Self-supervised closed-loop adaptation: can T1 rollout disagreement (prediction-vs-observation at deployment) supervise online encoder updates without labels? | MM | 2026-08-29 | ⭐ **UNBLOCKED (re-scoped) 2026-09-01** → `Frontier Scan/Daily/2026-09-01/RESULT.md` F8 + `Frontier Scan/LEDGER_C3_regulatory.md`. **The "UNECE bans online learning" constraint is UNSUPPORTED AT TWO INDEPENDENT PROBES** — the second source states such provisions are *not mentioned*. ⚠️ **NOT "refuted": the GRVA primary returned HTTP 403 and is UNREAD (register debt D-4).** ⭐ The real constraint the relayed claim obscured is **ISMR + DSSAD** — in-service monitoring/reporting and a data-storage system — so the binding question is **auditability of an adapting model, not legality.** ⇒ The in-vehicle path is **not closed**; design against auditability, and read the GRVA primary before committing. |


## PROPOSED (unranked)

`Appended by the 2026-08-31 frontier scan (first pass under DAILY_RESEARCH_CHARTER.md).`
⛔ **Never self-ranked — the Master Mind ranks.** Each row names the finding that produced it.

| # | domain | proposed work item | source finding | cost |
|---|---|---|---|---|
| P-1 | Arch | **LatentAlign arm on the v7-tiny ladder**: sigmoid-anneal the prediction target from grounded (teacher-forced) to self-predicted, sweeping `e₀` over 3 values. Pre-committed read: h=1 action/scene ratio must rise ≥3× (0.004 → ≥0.012) at equal-or-better ADE. Controls: constant-predictor (must read the no-information value) + raw-pixel floor. | `Frontier Scan/Daily/2026-08-31` F1 (lib `2605.09701` full text) | 2 tiny arms |
| P-2 | Arch | **Action-source mixture arm**: draw the training action from `{p_gt, p_kin, p_∅}` (GT · constant-accel kinematic decoy · learned null token), sweep `p_∅ ∈ {0, 0.1, 0.3}`. Pre-committed read: hold-action ADE **must degrade** vs true-action ADE, else the echo is not removed and the arm is refuted. Report decoy/GT action distance or the null is uninterpretable. | same F2 | dataloader change + 3 tiny arms |
| P-3 | Opp/B&E | **Alpamayo same-corpus comparability check (0 GPU)**: determine whether the Alpamayo eval split intersects parity split `e438721ae894` / skip-hash `f09e44db`. Pre-committed read: disjoint ⇒ **no comparison admissible** and we say so; intersecting ⇒ our first same-corpus external baseline. | F3 | 0 GPU |
| P-4 | Arch/Lab | **EB-JEPA as an external anti-collapse CONTROL**: clone `facebookresearch/eb_jepa` on the dev-box 4060, run the action-conditioned example as-is, check the published 97 % Two-Rooms planning success reproduces. Pre-committed read: if it does not reproduce, the rig is unusable as a control and we stop — cheap gate before any port to our data. | F5 (lib `2602.03604`, abstract-only) | 4060, hours |
| P-5 | Arch | ⛔ **Resolve correction-watch CW-1**: DriveFuture's headline navhard EPDMS **55.5** vs its own navhard ablation rows **30.9–34.6**. Until resolved, **no DriveFuture navhard level may enter a comparability table**. | F1 | 0 GPU |
| P-6 | Deploy | **Read the banked-but-unread primary `2601.22156`** and re-check the ~25 %-of-from-scratch distillation-budget claim in its own table; then benchmark an SSM/linear-attention predictor block vs our transformer block at K ∈ {8, 60, 300} on the 4060 using the existing `rollout_depth_bench.py` harness. Pre-committed read: SSM wins only if ms/step is **flat in K AND lower in absolute terms at K=60**; flat-but-slower refutes. | F5; serves injected **I-2** | 0 GPU then 4060 |
| P-7 | Lab hygiene | **Second probe for empty E1**: a driving-specific query for DINOv3-class encoder benchmarks on dense/BEV tasks. One probe is not absence — this exists so a single-probe miss cannot harden into folklore. | `raw/search_log.md` E1 | 0 GPU |
| P-8 | Lab hygiene | **Scan the four tracks this pass never reached**: B10 semantic search, B12 memory/long-context, B13 3D-occupancy-rendering (nearest our measured readout-geometry ceiling), C3 regulatory. | `Frontier Scan/TRACKS.md` | 0 GPU |

`Second pass 2026-08-31 (PI-directed): Band D introduced, tracks B10/B12/B13/C3 closed. Rows P-9 … P-16 appended.`

| # | domain | proposed work item | source finding | cost |
|---|---|---|---|---|
| P-9 | B&E / paper | **Adopt strategic guideline S-1: make T1 the burden of proof.** Three independent lines now agree open-loop is uninformative (our action echo 97.9 % vs 0.0 % hold-action · Alpamayo open-loop cannot separate 3.3× params · Waymo L5 *"completely indifferent to your actions"*). State in the paper that any T0-only claim is **unevaluated**. | Waymo adjudication W-5 | 0 GPU |
| P-10 | Paper / strategy | **Adopt S-2: argue "small at deployment" as a position, not a constraint.** Waymo concedes *"teacher-student models to optimize onboard compute"*; Alpamayo's trajectory metric is flat across 3.3×. | W-3, N-1 | 0 GPU |
| P-11 | Arch | **Adopt S-3: claim the geometry gap as a differentiator.** Waymo names *"lack sufficient spatial awareness"* as a frontier-VLM limitation; we have it MEASURED (4 azimuth bins / 120°). Read `2607.04732` SparseOcc++ and establish what supervision its sparse latent needs. **Pre-committed read: if it requires occupancy GT we cannot produce, the line is CLOSED and we say so.** | W-7, B13 ledger | 0 GPU first |
| P-12 | Steering | ⛔ **Adopt S-4: fix the programme's scope statement so Waymo L1 cannot land.** TanitAD claims a vision-only *latent world model on a camera corpus* under a *leak-avoidance* inference rule — **not** L4 sensor sufficiency. Zero-cost removal of the strongest opponent argument against us. | W-1 | 0 GPU |
| P-13 | B&E | **Adopt T-2: every critic/scorer ships with a constant-only control and a raw-input floor**; audit the v7 gate's learned components for policy-critic correlation. | W-6 + our ridge-probe post-mortem | 0 GPU |
| P-14 | Opp | **Read the three unread Waymo technical sources**, above all **"The Waymo World Model" (2026-02)** — the highest-value unread document currently known to the programme — plus *Demonstrably Safe AI* (2025-12) and the Foundation Model post (2025-12). Register debts D-1/D-2/D-3. | `OPPONENT_CLAIMS_REGISTER.md` | 0 GPU |
| P-15 | Arch | **Read `2606.21775` (variable-length latent world models) for the horizon ladder.** ⛔ **Sequence AFTER P-1/P-2**: adding horizons to a predictor whose action/scene ratio is 0.004 could deepen the defect, making the result uninterpretable. **Pre-committed read: variable-length wins only if it beats COMPOSED h=1 at matched compute.** | B12 ledger | 0 GPU then tiny arms |
| P-16 | DataEng | **Pull the Springer embedding-based scenario-retrieval chapter** — it resolves empty E4 (video/driving corpus curation, which the B9 query missed) and is an offline curation lever against our parity-locked 2376-episode corpus. | B10 ledger | 0 GPU |

`Appended by the 2026-09-01 daily Lab run (LAB-RUN-006). Rows L-1 … L-9.`
⛔ **Never self-ranked — the Master Mind ranks.** Each row names the finding that produced it.

| # | domain | proposed work item | source finding | cost |
|---|---|---|---|---|
| L-1 | Arch | ⛔⛔ **Replace the `k60clip05p30k` arm's mitigation before it is spent.** Register a **horizon curriculum** `K(step) = min(60, 8 + floor(step/Delta))` (Looped-WM form) starting at the already-stable k=8, INSTEAD of `--clip 0.5`. Pre-committed read: the arm survives past step 9,000 with gnorm median in single digits, and `o5_loss` does not degrade >20 % vs the k=8 incumbent. | `2026-09-01-longhorizon-bptt-stability` F1/F2 — three primaries use a curriculum; **none** uses a smaller scalar clip, and the prereg's own text says the problem is direction, not magnitude | 1 arm (same cost as the planned one) |
| L-2 | Arch | **Adopt AGC (per-tensor relative clipping) as the optimiser-side option**: clip per-tensor gradients at 30 % of the weight matrix's L2 norm (DreamerV3's shipped setting). ⚠️ **Do NOT combine with L-1 in one arm** without saying so — two levers in one arm is non-attributable. | same, F2.1 | small optimiser change |
| L-3 | Deploy/Arch | ⭐ **The QUALITY half of I-1** — train a terminal value head on our latent and compare against fan-scoring **at matched LATENCY** (not matched step-count; they differ ~6×). Controls per CLAUDE.md: a constant-only predictor that must read the no-information value, and a raw-input floor. | `2026-09-01-terminal-value-vs-fan-scoring` — the cost half is settled, the quality half is untouched | tiny-ladder arms |
| L-4 | Deploy | **Re-measure breadth-vs-depth on Thor.** The 5.94× and the free-breadth region (N≲32) are RTX-4060 properties; Thor saturates at batch 8 on a different workload, which would SHRINK the free region. Same script, different host. ⛔ Only when Thor is idle. | same, §5 limits | minutes, 0 spend |
| L-5 | DataEng ⛔PI | ⛔ **Admissibility decision on `tactical_goals.py`'s output**: (a) `SPEED_BAND` is derived from `geometry(held-speed)` = hindsight ego behaviour, the exact substitution `g_tac_geom`'s F-14 blocker names as inadmissible; (b) `CORRIDOR_OFFSET` reads **21.65 m** on a plain constant-curvature arc (21× its own 1.0 m threshold). **Is A admissible as canonical supervision?** The Lab measured; it does not fence a producer. | `2026-09-01-gtac-producer-cross-agreement` F3/F4 | 0 GPU, PI decision |
| L-6 | DataEng | **Corpus-rate `g_tac` cross-agreement on parity poses.** The 1/7 figure is a SCENARIO-SET rate and must never be quoted as a corpus rate. Needs `physicalai-train-e438721ae894` poses (2–6 s band ⇒ ~60 samples/window); the dev box has none (local dumps hold 4 waypoints). Belongs to a FlyWheel with pod access. | same, §5 + `raw/search_log.md` E1 | pod-side, cheap |
| L-7 | DataEng | **Add a curvature guard to `CORRIDOR_OFFSET`** as the cheap interim if the token keeps shipping: gate on band turn angle (B's own stratification found only `theta < 0.05 rad`, **21.7 % of corpus**, admissible). That single gate catches F4. | same, F4 | small code change |
| L-8 | B&E | ⭐ **Wire `win["lead"]` into the eval path** — `four_families` already computes the family; it only needs `lead_source.select_lead_causal` run over the val episodes' `obstacle.offline` join and attached per window. **No new instrument, no new science, no GPU.** ⛔ AND **pin the stale reason with a test**: assert no artifact in `taniteval/results/` contains `"no lead-agent state exists"` — a prose correction demonstrably did not reach 28 files in two weeks. | `2026-09-01-distance-keeping-fed-check` F1/F2/F3 | wiring + 1 test |
| L-9 | Lab-infra / B&E | ⚠️ **Four banked eval artifacts are UNREADABLE from the dev box** — `driving_flagship-30k` / `-nospeed` / `-speed` / `-v16-ab-ft`.json (32–33 KB each), failing via BOTH Python `open()` and .NET `File.ReadAllText`, across 3 runs and 8+ retries. **An unauditable artifact is not a quotable one, and the registry may cite them.** Try a different host; if they are corrupt, say so on the record. | same, F4 | 0 GPU |


`Appended by the 2026-09-01 FRONTIER SCAN (LAB-RUN-007). Rows FS-1 … FS-6.`
⛔ **Never self-ranked — the Master Mind ranks.** Each row names the finding that produced it.
⭐ This pass discharged six standing debts (D-1/D-2/D-3, B2/B12/B13) and **corrected three of our own
records**; rows below are the work those corrections created.

| # | domain | proposed work item | source finding | cost |
|---|---|---|---|---|
| FS-1 | B&E | ⭐⭐ **Port WorldRoamBench's SEGMENT-BASED drift metric into `taniteval` and re-score the banked v7 rollout dumps.** Our drift is read **start-vs-end**, which by their construction **cannot see non-monotonic mid-sequence collapse**. **Pre-committed read: monotone drift ⇒ the current instrument stands; non-monotone collapse ⇒ every drift number we hold is a LOWER BOUND and the instrument must change.** | `Daily/2026-09-01` F7 (lib `2606.31672`, abstract-only) · `LEDGER_A5_benchmarks.md` | **0 GPU** — the dumps exist |
| FS-2 | Arch | ⚠️ **Do NOT delete the h≥2 head slots while executing P0 backlog row 5.** Keep them at 1e-3 init so the variable-length (VLWM) direction stays reachable. **Asymmetry is the argument: dropping is cheap, re-adding after the v7 geometry freezes is not.** H-PROOF-6d/7 show the heads are *vestigial under our current objectives* — which is not the claim that multi-step prediction is wrong. | F4 (lib `2606.21775`) · `LEDGER_B12_memory_horizon.md` | **0 GPU** — a config decision |
| FS-3 | Arch | **Three-way interface arm on the v7-tiny ladder**, matched params: learned-embedding interface (Waymo-shaped) vs our current interface vs a pure-E2E control. **Pre-committed read: if the embedding-interface arm does not beat the pure-E2E control on rank AND decodability, the much-publicised architectural convergence is cosmetic and we say so.** ⛔ Exists because Waymo's hybrid claim has **no ablation** — we should not inherit it. | F2 / register W-13 | tiny-ladder arms |
| FS-4 | Deploy | **Re-run the FP8/FP4 precision census on JetPack 7.2.1** (released 2026-08-12) once `quant_gate.py` exists. The TensorRT matrix lists Thor at CC 11.0 with FP8/FP4 supported; our MEASURED finding is a **silent FP32 fallback** (#4590). ⚠️ **The measurement wins** — but our measurement predates this release, and a fail-closed gate must not inherit a stale platform assumption either. | F9 · `LEDGER_C1_releases.md` C2 entry | Thor time, **after** row 1 |
| FS-5 | Lab hygiene | ⛔ **`TRACKS.md` should carry a BANKED-BUT-UNREAD count per track.** MEASURED this pass: **5 of 6 primaries needed were already banked** (2 of 10 on 2026-08-31), and **all three Band-B debts were banked-but-unread rows**. The Library is at 310 entries / 2,115.6 MB and growing faster than it is read. ⭐ *A banked primary with no five-dimension analysis is not an asset, it is a debt with a hash.* | F6 (MEASURED, `kb_add.py` output) | 0 GPU |
| FS-6 | Lab hygiene | ⛔ **Fix two broken links in the growing artifact**: `TRACKS.md` has linked `LEDGER_A2_jepa.md` and `LEDGER_A4_vla.md` since 2026-08-31 and **neither file exists** (B2's was created this pass). Create them with their existing entries, or drop the links. *(Root-cause class: a link written in the same turn as the intention to write the file, never verified.)* | `Frontier Scan/TRACKS.md` third-pass block | 0 GPU |
