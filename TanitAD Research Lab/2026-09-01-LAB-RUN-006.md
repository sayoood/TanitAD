<title>LAB-RUN-006 - 2026-09-01</title>

# LAB RUN 006 — 2026-09-01

**Agent** daily Research Lab pass (scheduled trigger) · **Branch** `agent/arch-inf-20260803` ·
**Compute** dev-box RTX 4060 + CPU. ⛔ **Thor and every pod untouched. No spend.**
**Staged, never committed** — the Master Mind commits.

⚠️ **Trigger-time note.** The task fired against "today"; between firing and execution the date
rolled to **2026-09-01**. Yesterday's four packages (2026-08-31) were found and correctly *not*
counted as today's. Two `2026-09-01-*` directories exist under Architecture & Inference
(`fp8-cache-gate`, `refcv3-training-readiness`) but are **Master Mind ops artifacts, not a Lab
pass** — one is empty but for a `raw/` dir, the other holds a `READINESS.md`. Neither carries
`RESULT.md` + `raw/search_log.md`, and no `LAB-RUN-006` existed. The pass therefore ran.

---

## The four packages

| # | domain | package | headline | class |
|---|---|---|---|---|
| 1 | **Architecture & Inference** | `2026-09-01-longhorizon-bptt-stability` | **ASK-1 ANSWERED** — no published recipe in our reference class back-props a 60-step full chain, and the clip is not the lever | PUBLISHED (PRIMARY, 5 banked) |
| 2 | **Deployment & Optimization** | `2026-09-01-terminal-value-vs-fan-scoring` | serves **injected I-1** — breadth is **5.94×** cheaper than depth at equal step-budget; the value head costs **1.2 %** | **MEASURED** (4060, 120 reps) |
| 3 | **Data Engineering** | `2026-09-01-gtac-producer-cross-agreement` | ranked row **7** — the two `g_tac` producers are **near-disjoint**, LON agreement **1/7**; two admissibility defects found | **MEASURED** (CPU) |
| 4 | **Benchmarks & Evals** | `2026-09-01-distance-keeping-fed-check` | ranked row **19** — the binding longitudinal family is fed on **0 of 71** artifacts | **MEASURED** (census) |

### ⭐ The convergence worth reading twice

Packages 1 and 2 were run independently, by different methods, and land on the same fact:

- **Package 1 (literature):** TD-MPC2 plans at **H=3**, DreamerV3 imagines at **H=15**; both
  refuse to deepen the chain and instead **bootstrap past the horizon with a learned value**.
- **Package 2 (our own GPU):** at equal budget, **K=3 with many candidates is 5.94× cheaper**
  than K=60 with few — and candidates are *free* up to N≈32.

⇒ **The literature caps K because deep chains do not train; our microbenchmark caps K because
deep chains do not fit the control loop. TD-MPC2's H=3 is simultaneously a training choice and
the cheapest point on our measured budget surface.** ASK-1 and injected row I-1 turn out to have
the *same answer*, which neither question anticipated.

---

## Backlog delta (required by the update contract)

| motion | detail |
|---|---|
| **asks answered** | **ASK-1** → ANSWERED via `stack/scripts/lab_ask.py --answer ASK-1 --date 2026-09-01` (the tool, never by hand). It was the only OPEN ask. |
| **injected served** | **I-1** — ⚠️ **COST half served, row STAYS OPEN.** The quality half is untouched and is re-proposed as **L-3**. The lane's guarantee (≥1 package to the top injected row) is met. |
| **consumed** | ranked row **7** ✅ DONE → package 3 (⚠️ **re-scoped**, see below) · ranked row **19** ✅ DONE → package 4 · PROPOSED **P-8** ▶ ADDRESSED (literature; the arm has **not** run) |
| **proposed** | **9 new rows, L-1 … L-9**, appended under `## PROPOSED (unranked)`. Never self-ranked. |
| **fenced** | none — nothing was refuted by measurement today. Row 7 was **re-scoped**, which is weaker than refutation and is recorded as such. |

### ⚠️ One consumed row came back with its question corrected

Row 7 asked to *"settle the CORRIDOR_OFFSET 2-vs-1 contest"*. **Measured, there is no contest:**
`g_tac_geom` refuses `CORRIDOR_OFFSET` at *any* threshold (and the constants are 1.0 vs 0.75, not
2 vs 1). The real finding is two **admissibility** defects — see escalation E-3. A row answered
by correcting its premise is still answered, but the Master Mind should re-read it rather than
trust the ✅.

---

## ⛔ ESCALATIONS FOR THE MASTER MIND

**E-1 — the `k60clip05p30k` arm should not be spent as specified.** `PREREG_MM_E19_K60_HORIZON.md`
§3c commits to `--clip 0.5` as *"the smallest change that addresses the measured"* instability.
No banked primary uses a smaller scalar clip for long-horizon stability, and the **prereg's own
text** says why it cannot work: `clip_grad_norm_` returns the **PRE-clip** norm, so updates were
already bounded to 1.0 — *"the step becomes 'move 1.0 along whatever exploded'"*. Halving it makes
it 0.5 along the same bad direction. **Recommend a horizon curriculum (L-1) at identical cost.**
⚠️ This is a HYPOTHESIS about an unrun arm, not a measurement; the clip-0.5 arm could survive by
luck, and if it does the outcome is uninformative about the mechanism.

**E-2 — `LAB_BACKLOG`'s update contract is being violated upstream, and the tool says so.**
`stack/scripts/lab_backlog_drift.py` reports **22 unproposed finding-commits**, 16 of them dated
2026-09-01 — all Master Mind commits (refav1 caches, MM-E19, B1 build, refcv3 relaunch, the
H-REFAV1-MOTION refutation). Per the contract *"the finder proposes"*, so **these 22 rows are the
Master Mind's to write, not mine** — I did not author them on its behalf, because a row
mis-stating another finder's result is worse than a missing row. The Lab's own 9 rows are filed.

**E-3 — a label-admissibility decision is needed (PI-class), filed as L-5.** `tactical_goals.py`
emits `SPEED_BAND` with provenance `geometry(held-speed)` — precisely the hindsight-ego
substitution `g_tac_geom`'s F-14 blocker names as inadmissible — and its `CORRIDOR_OFFSET` reads
**21.65 m** on an ordinary constant-curvature arc (21× its own 1.0 m threshold). If these labels
are feeding canonical supervision, that is a live defect. **The Lab measured it; the Lab does not
fence a producer.**

**E-4 — four banked eval artifacts cannot be read from the dev box** (`driving_flagship-30k`,
`-nospeed`, `-speed`, `-v16-ab-ft`.json). Confirmed by **two independent APIs** across three runs
and 8+ retries. **An unauditable artifact is not a quotable one**, and `MODEL_REGISTRY.md` may
cite them. Filed as L-9.

**E-5 — a one-line, high-value wiring task is available now (L-8).** `four_families` already
computes the distance-keeping family; it needs only `win["lead"]` attached. Zero GPU, no new
science, and it converts a binding family from UNAVAILABLE to measured. ⛔ Pair it with a **test**
that forbids the stale reason string — the 2026-08-18 prose correction reached **none** of the 28
artifacts in two weeks, which is the same class as the "2 of 36 features" rot that needed pinning.

## PI-queue items

1. **L-5** — is `tactical_goals.py`'s output admissible as canonical supervision? (E-3)
2. **I-1's quality half (L-3)** — worth tiny-ladder compute now that the cost half says the
   cheap corner is 5.94× cheaper?
3. Standing, untouched today: rows **2**, **3**, **8**, **11**, **16**, **21**, **26** remain
   `⛔PI` and unblocked only by a decision.

---

## Deliverable manifest — every artifact, and where it lives

⭐ All paths are `repo:` under `G:\…\TanitAD`, staged on `agent/arch-inf-20260803`.
**Nothing produced today lives in only one place outside the repo.**

| # | artifact | staged |
|---|---|---|
| 1 | `TanitAD Research Lab/Architecture & Inference/Research/2026-09-01-longhorizon-bptt-stability/{SPEC,RESULT}.md`, `raw/search_log.md` | ✅ |
| 2 | `TanitAD Research Lab/Deployment & Optimization/Research/2026-09-01-terminal-value-vs-fan-scoring/{SPEC,RESULT}.md`, `code/breadth_vs_depth_bench.py`, `raw/{breadth_vs_depth_4060.json, breadth_vs_depth_4060_reps120.json, search_log.md}` | ✅ |
| 3 | `TanitAD Research Lab/Data Engineering/Research/2026-09-01-gtac-producer-cross-agreement/{SPEC,RESULT}.md`, `code/gtac_cross_agreement.py`, `raw/{gtac_cross_agreement.json, search_log.md}` | ✅ |
| 4 | `TanitAD Research Lab/Benchmarks & Evals/Research/2026-09-01-distance-keeping-fed-check/{SPEC,RESULT}.md`, `code/dk_census.py`, `raw/{dk_census.json, search_log.md}` | ✅ |
| 5 | 4 × `Research/KNOWLEDGE_BASE.md` (one dated finding each) | ✅ |
| 6 | `TanitAD Research Lab/LAB_BACKLOG.md` (2 consumed, 1 injected-served, 1 annotated, 9 proposed) | ✅ |
| 7 | `TanitAD Research Lab/LAB_ASKS.md` (ASK-1 → ANSWERED, via the tool) | ✅ |
| 8 | `TanitAD Research Lab/Library/` — **1 new PDF banked** (`2412.01522`) + 2 new (`2606.18208`, `2608.25017`); `2310.16828` and `2301.04104` already held, tags/citations updated. Index 306 → **309 entries** | ✅ |
| 9 | This file | ✅ |

## Research banking

Five primaries underpin package 1; **all five are banked** in `TanitAD Research Lab/Library/`
with sha256, so every external number in this run is `PUBLISHED (PRIMARY)` and none is
`PUBLISHED-SECONDARY`. ⚠️ **One relay was caught in flight**: a search summary attributed the
progressive-horizon curriculum to `2512.09929`; fetching that paper returned *"no gradient-specific
safeguards or horizon curriculum schedules"*. The claim was re-traced to **`2606.18208` §3.5.4**
and quoted from the PDF. Recorded in package 1's `raw/search_log.md` §3 — this is exactly the
failure the banking rule exists to stop.

## Process notes worth keeping

- ⭐ **A 40-rep benchmark produced a confident, plausible, wrong-by-12 % headline** (6.65× vs the
  true 5.94×) *and* a silently failing control. It was caught **only** because C1 had a
  pre-committed external value to agree with — the 2026-08-31 package on an identical config.
  Both JSONs are banked so the correction is auditable. This is the CLAUDE.md ridge-probe lesson
  in a latency costume: *the only thing that catches a manufactured result is a control that must
  read a known value.*
- **The G: mount was in a bad wave for ~25 minutes.** A local-mirror attempt copied 2 of 75 files
  in 6.6 minutes and was abandoned. Package 4's counts are reported over **71 readable** files
  with 4 named UNREAD — never folded in, per the rule that a G: failure count is admissible only
  with zero Errno 22.
- **`git add` was verified with `git ls-files --cached` per package**, not trusted from exit code.
