<title>LAB-RUN-016 — 2026-09-19 frontier scan</title>

# LAB-RUN-016 · 2026-09-19 · Frontier scan (PI-requested)

`Research Lab · run by the Master Mind session on the PI's instruction "run the daily frontier scan". Repo D:\Projects\TanitAD, branch agent/arch-inf-20260803. Nothing committed or pushed.`

## Gate
* **(b) frontier scan:** was ABSENT at start ⇒ **run today** → `Frontier Scan/Daily/2026-09-19/RESULT.md`.
* **(a) four domain packages:** ⛔ **NOT produced.** No `*/Research/2026-09-19-*` package exists. The PI asked for the scan only, so **the domain half of today's pass stays owed**.

## What the scan produced (findings first; full detail in RESULT §0)
1. ⭐⭐⭐ **A posted speed limit exists as a map attribute in open corpora**: nuPlan `lane.speed_limit_mps`, WOMD `LaneCenter.speed_limit_mph`. Both have zero ego provenance, read in source code. Coverage is unmeasured (FS19-2).
2. ⭐⭐⭐ **The WM target matters more than pixel fidelity**, per two independent controlled ablations (DeepSight T3, World Tokens T2). DeepSight's GT is DINOv3 of a BEV render, which is the SAM3-raster route (FS19-3).
3. ⭐⭐⭐ **Exclusive routing**: a WM loss helps action only if the action head cannot bypass it (94.1 < 95.0 < 97.0). This is our echo/bypass class (FS19-1).
4. ⭐⭐ The planning-WM "long horizon" in the literature is **2 s**. Our 6 s tactical horizon is a differentiator.
5. ⭐⭐ Injected **I-3** served in part: published driving TTA is label-supervised and auditable, not label-free (FS19-5).
6. ⛔ **Correction** to 09-18 (LGS): "0/16 → 16/16" does not reproduce; the primary says 15/16 at 5 and 10 steps. DriveZero numbers verified.
7. ⭐⭐ LEAP gradient planning vs CEM targets refav1's inference-seed floor (FS19-4).
* **Band D:** 7 claims adjudicated (NVIDIA ×3, Waabi ×2, Mobileye ×2), all new; guidelines S-8, T-10…T-12; strengths 12–14; D-14 opened; D-4 and D-13 still stand.
* **Coverage:** 22/22 scanned; Band-B DEEP ×4 (B8 first ever, B10, B11, B12); Band C swept; 11 empties named; 11 ledgers appended (B8 ledger created).

## Library
**489 → 521 entries.** 7 banked today (1 re-find: `2606.14010`). ⛔ **26 orphan PDFs** from the 09-17/09-18 passes had lost their index rows (the concurrent-write race) and were recovered with `--reindex-orphans`. ⛔ `--verify`: **49 MISSING** (indexed, but absent from D: and from git) ⇒ escalation 2.

## Backlog delta
* **Consumed / served:** I-3 **served (partial)** (row motion + motions table); MM standing question **answered in part**; FS18-5 unaffected by the correction.
* **Proposed (unranked):** **FS19-1 … FS19-9** (9 rows) under `## 2026-09-19 frontier scan (LAB-RUN-016) — backlog motions`.
* **Ranked rows taken:** none (scan-only run).

## Integration escalations (Master Mind)
1. **Posted-limit supplier found** (finding 1). Using it needs a map download plus a ruling on a non-parity corpus feeding one head ⇒ PI item below.
2. ⛔ **49 Library PDFs are stranded off D:** (`kb_add.py --verify`). Spot check shows at least one still on the pre-move G: tree. Copy and stage them; the Lab may not touch G:. Also: the orphan race is still live, so every pass should end with `--verify`.
3. ⛔ **Registry hygiene:** the 09-18 LGS headline was a summariser artifact. No registry row cites it: `2602.11229` has 0 hits in `GOALS_AND_CLAIMS.md` and in `MODEL_REGISTRY.md`, against a same-breath control of `refav1` reading 272 / 11. The only `0/16` hit (`D-EVAL-SPLIT`) is unrelated. Rule proposal FS19-9: no summariser number enters the registry without a local re-read.
4. **Paper:** stamp the horizon finding (planning-WM "long" = 2 s in the literature; ours is 6 s tactical) and T-18-2/N-19-2 as external corroboration, labelled with their evidence classes.

## PI-queue items
* **FS19-2:** approve downloading the nuPlan/NAVSIM **map DBs** (maps only, no sensor blobs) to measure posted-limit coverage. Then rule whether a **non-parity corpus** may supply labels or calibration for the max-speed head. (Parity episodes are not re-selected.)
* **D-4** remains blocked at 13 routes (403 / bot challenge). The GRVA text on in-service learning needs a human download.

## Deliverable manifest
See the final message of this run for the per-file staging verification (`git ls-files --stage` blob vs `git hash-object`).

## Follow-up, same day — PI approved all three escalations
* **FS19-2 DONE** → `Data Engineering/Research/2026-09-19-nuplan-posted-limit-coverage/RESULT.md`: nuPlan posted-limit coverage **LV 100 %, PIT 100 %, BOS 7.1 %, SG 0 %** (MEASURED, by lane length); ≈ 93 % expected ego-frame coverage (ESTIMATED); PI ruling (non-parity labels allowed for the max-speed head) recorded in `GOALS_AND_CLAIMS.md`. New proposed row **FS19-2b** (vision-readability across cities; needs the PI's size OK for the sensor subset).
* **FS19-7 DONE:** 49/49 Library PDFs re-downloaded from arXiv, **all sha256-identical**; `--verify` 521 / 0 orphans / **0 problems**. Only D: used.
* **D-4 still blocked** on a human action: regulation identified as **UN R185** (`ECE/TRANS/WP.29/2026/137`); unece.org shows a bot check even to a real browser.
* ⛔ **Correction:** finding 1 of the morning report was a **re-find** of 09-13 F6, not a first. Logged as `RETR-2026-09-19-POSTED-LIMIT-REFIND`; new row LI19-1.
