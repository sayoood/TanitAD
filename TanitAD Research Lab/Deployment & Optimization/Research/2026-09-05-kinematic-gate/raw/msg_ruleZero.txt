RULE ZERO's next arms, one admissible and one killed by its own known-answer control — plus an ESCALATION: a feasibility-aware decode exists on ONE disk

⛔ **ESCALATION FIRST (integration, not a merge request in a README).** MEASURED 17:15Z by a
positive assertion with a same-breath control:
  git cat-file -e HEAD:stack/tanitad/refs/feasible_decode.py  -> does NOT exist in HEAD
  git cat-file -e HEAD:stack/tanitad/refs/refc.py             -> PRESENT   (the control)
while the frozen clone C:/Users/Admin/refcv4b_repo holds `stack/tanitad/refs/feasible_decode.py`
(13,672 B) and `stack/tests/test_feasible_decode.py` (7,791 B), both written minutes earlier by a
LIVE sibling stream. ⛔ They are NOT committed here — sweeping a live sibling's in-progress work
under this stream's message is the documented git-hygiene failure — but they are on ONE disk and
absent from git, which is the condition the operating standard exists to make impossible. The
owning stream must stage them.

⭐ And it changes this package's P5 from proposing the successor to SUPPLYING ITS EVIDENCE. The
module's own docstring opens on the same 8.56x measurement; §4b/§4c now hand it (1) the frontier
it must beat, (2) the operation NOT to use, (3) the guard its arms must carry.

⭐ **RULE ZERO next arm 1 — ADMISSIBLE and NEGATIVE.** Similarity scaling the bank by v0/v_ref
(`raw/v0_conditioned_bank.json`, C1 and C2-OBJECT both pass) buys `off_reach` 0.7822 -> 0.6770
and PAYS `envelope` 0.0156 -> 0.2421 (**15.5x worse**), `peak_g` 0.4808 -> 0.5498, `oracle_ade`
1.0985 -> 1.1571. The arithmetic says why before the data does: under a spatial scale s on a
fixed time grid, speed scales by s and curvature by 1/s, so **lat_acc = v^2*kappa scales by s** —
and this corpus needs scales up to 3.63x. A similarity scale is the WRONG OPERATION.

⛔ **RULE ZERO next arm 2 — INADMISSIBLE, AND THE CONTROL IS THE POINT.** A control-space-roll
stand-in produced a spectacular result — at lambda = 0, `off_reach` 0.7822 -> 0.0000, `envelope`
0.8879 -> 0.0009, `oracle_ade` 1.0985 -> 0.4715 — and it is **NOT QUOTED**, because `C3b`, which
rolls a SYNTHETIC constant-speed arc whose answer is known exactly, reads **3.17e-02 m** against a
1e-4 m bar. The integrator takes the heading of the segment containing the current arc position
and steps a full v*dt along it: first-order wrong on a curve. That column would have been this
stream's headline.

⚠️ Two GRID errors were caught by the same control on the way and fixed, both the `dt`/scope
family: `ARM_HORIZONS` is uniform at 0.5 s only over the FIRST FOUR slots (rl_refcv3_min.py:610),
and anchor slot 0 sits at t = 0.5 s, not the origin. ⚠️ And `C3` itself was MIS-SPECIFIED at
first — it demanded an identity a constant-speed roll cannot satisfy on a variable-speed anchor —
so it was re-specified STRICTER on the set where the identity is real (C3a) plus a known-answer
synthetic case (C3b). A control that cannot be satisfied is not a control.

⇒ What would settle it and what blocks it: the repo's own tested `refc_sampler.roll_controls`
(present in the repo tree, ABSENT from the frozen clone this stream ran in) applied to anchors
that carry a CONTROL sequence. refcv3's `core.decoder.anchors` are WAYPOINTS; refcv4b's anchors
declare `control_units` via `anchor_meta.py`. ⇒ **the conditioning experiment belongs on refcv4b
and needs the un-frozen tree.** Not a compute blocker.

Also re-runs P4 and the distance-keeping sub-family on the CORRECTED ranking (`sel_score_v3`):
`gate1 == model` on **400/400 windows, every metric exactly 0.0000 [0, 0]** — the
deliberate-regression control now passes by construction, and the as-published ranking had
inflated gate2's apparent share of the selection gap from **46.2 % to 50.3 %**.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
