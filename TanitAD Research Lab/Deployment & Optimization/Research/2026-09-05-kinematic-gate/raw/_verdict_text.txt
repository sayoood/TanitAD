**RESOLVED — SUCCESS on all four committed clauses (MEASURED 2026-09-05).** Two EPISODE-DISJOINT draws of 400 EVAL windows / 70 episodes each, paired episode-cluster bootstrap n_boot 4,000. (1) `sel_envelope` **0.1075 -> 0.0775 (A) / 0.1100 -> 0.0900 (B)**, delta **-0.0300 / -0.0200**, separated on BOTH, same sign, min |delta| 0.0200 against a replicate floor of 0.0100; (2) `sel_peak_g` **-0.0378 / -0.0330** against a floor of 0.0048 (`sel_infeasible` and `sel_flagged` likewise QUOTABLE); (3) `ade_m` **+0.0064 (A) / -0.0099 (B)**, **NOT separated on either draw** and |delta| < the committed 0.010 m, with the sign FLIPPING between draws -- it satisfies BOTH halves of clause 3; (4) every guard passes. **G-CKPT** md5 + step; **G-DET** two forwards bitwise identical (max|d| 0.000e+00) so V3 is a STRUCTURAL IDENTITY on this config; **G-OFF** gate1 == model on **400/400 and 400/400 windows, every metric exactly 0.0**; **G-REP** episode overlap 0; **G-CTRL** oracle moves ade -0.2423 / -0.2851 separated. ⭐ The one separated FOUR-FAMILY movement is an IMPROVEMENT and is quotable: LONGITUDINAL `accel_mae` **-0.1846 [-0.2840, -0.1014] (A)** and **-0.1402 [-0.2327, -0.0629] (B)**, both separated, floor 0.0444, i.e. **-19.5 % / -14.3 %** -- the `comfort` jerk term where the mechanism predicts it. ⛔ **The SPEC's own committed LATERAL hypothesis is NOT SUPPORTED**: curvature and yaw-rate SIGN-FLIP between draws (-0.0937/+0.1519 and -0.0457/+0.0761, none separated), so the gain is carried by the longitudinal half of `comfort`, not the curvature half -- reported, not dropped. ⚠️ **The cost is real and un-intervalled**: TACTICAL manoeuvre agreement falls ~2.5 pp consistently on both draws (5-way 0.9225 -> 0.8975 and 0.9350 -> 0.9075), and the gate changes the pick on **~49 %** of windows; `four_families.tactical_from_trajectory` returns an aggregate accuracy, so these carry no paired CI and closing them is a work item. STRATEGIC is `UNAVAILABLE` with `defect: false` (a corpus property, not `D-NAVCOMP-SHAPE-1`), and is a STRUCTURAL IDENTITY for this lever -- `core.route` is not re-run, so `route_logits` are the same tensor for every rule. ⛔ **T1 NOT RUN** (the 4060 was saturated all session); no T1 claim is made
