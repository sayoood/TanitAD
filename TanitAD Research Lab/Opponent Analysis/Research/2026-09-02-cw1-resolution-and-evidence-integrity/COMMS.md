# COMMS — E-OPP-CW1-1

`2026-09-02 · Research Lab · Opponent Analysis`

## ⛔ ESCALATED — five items, in priority order

### E1 — ⭐ CW-1 can be closed, and the Lab may not close it

`Frontier Scan/LEDGER_A1_world_models.md:60-63` fences every DriveFuture navhard
**level** out of every comparability table. The reconciling sentence is now in
hand, from the repaired primary `2605.09701`:

> *"All ablations are conducted **without GTRS-Dense scorer [49]**."*

⇒ the 30.9–34.6 rows and the 55.5 headline are different configurations, named as
such by the paper. Independently corroborated by `2606.07170` (TOAD), which
reproduces DriveFuture at 55.5 in its own six-planner table.

**Recommendation: NARROW rather than lift** — a DriveFuture navhard level becomes
admissible **only with its setting attached** (`NAVSIM-v2 navhard, with GTRS-Dense
scorer`), since a level travelling without its setting was the whole defect.
⛔ Per the record (*"closing a research direction is a programme call"*), the Lab
reports the criterion met; the PI / Master Mind close.

### E2 — ⛔ `Benchmarks & Eval/LEADERBOARD.md:1036` carries two errors of the CW-1 class

The line reads *"DrivoR 56.3 EPDMS navhard, arXiv 2606.07170"*. Against the
primary:
* **56.3 is DrivoR + TOAD** (test-time Cross-Entropy search on a frozen planner).
  **Bare DrivoR is 54.6** — confirmed by two independent primaries (TOAD Tables
  3/7, and DriveFuture's own Figure 1(c)).
* **`2606.07170` is TOAD's arXiv id.** DrivoR is **`2601.05083`**.

⚠️ It sits **one line below** CW-1's own subject in an orientation block the file
declares a merge target for the (currently empty) NavSim comparability table — so
the next mechanical row-drop propagates it into the table CW-1 exists to protect.
**Owner:** EvalFlyWheel (owns `LEADERBOARD.md`).

### E3 — ⛔ `LAB_BACKLOG.md` row 32's wedge names the wrong leader

The row says *"DrivoR leads NavSim camera-only at ~40 M params."* Bare DrivoR is
**fourth** on navhard (54.6), behind **DriveFuture 55.5**, which TOAD itself calls
*"the strongest learned method"*. DrivoR leads only **with** test-time search,
which is inference compute rather than parameters.
⚠️ The **~40 M figure is unverified** — `2601.05083` is banked and readable but
unread this pass, so it must not be quoted until someone reads it (row **L-15**).

### E4 — ⛔⛔ `kb_add.py --verify` gives the same verdict for a corrupt Library and a healthy one

MEASURED this pass, before and after repairing a provably truncated file:

```
verified 310 entries, 0 orphan(s), 0 problem(s)      <- corrupt
verified 310 entries, 0 orphan(s), 0 problem(s)      <- repaired
```

`--verify` re-hashes against the hash recorded at bank time; a file truncated
*before* it was hashed verifies forever. ⇒ **`LAB_BACKLOG.md` row 36's hardening
must add a COMPLETENESS check at bank time** (`%%EOF` + a real parse), not only the
atomic download. Hash-verification structurally cannot supply one.
⚠️ **Honest framing for whoever ranks this: 309 of 310 entries are fine.** This is a
0.3 % rate, not rot — but it is invisible to the guard, and it landed on the one
paper an open correction-watch needed.
**Owner:** Research Lab infra / Master Mind.

### E5 — ⭐ Backlog rows I-1 and L-3 should carry a SEARCH arm

TOAD improves six frozen planners with no retraining (+2.3 % to +43.6 % EPDMS) —
independent support for the latent test-time-planning direction. But it also
reports that *"simply re-ranking the base planner's proposals with the scorer can
even hurt performance, whereas TOAD improves it."*

**Re-ranking is not search, and fan-scoring is re-ranking.** L-3 as written pits a
terminal value head against fan-scoring; both sit in the family TOAD reports as
weaker. ⇒ add a CEM-style search arm warm-started from the proposals, or record why
not — otherwise a null result would be a property of the design, not of value heads.
⚠️ Also worth pre-committing: *"gains are largest for lower-performing planners"*.
Our arms are weak, so a large test-time gain would be the **expected** result and
must not be read as evidence of a strong world model.

## Actions taken directly (not merely proposed)

* ✅ **Repaired the truncated primary.** `2605.09701` refetched from arXiv
  (1,373,148 → 9,453,407 B; `%%EOF` present; 24 pages parse), written with a
  sha256 read-back, and its index `bytes`/`sha256` updated atomically with the old
  hash recorded in the entry note. `--verify` → 310/0/0 against the **correct**
  hash. Pre-repair index backed up to `raw/library.json.pre-repair.bak`.
  ⚠️ `LIBRARY.md` was regenerated by `kb_add.py --reindex`, **never hand-edited**.
* ✅ **Citation registered** — the entry's `cited_by` now names this package.

## Backlog motions taken this pass

* **Proposed L-15** (Opp) — read `2601.05083` and verify DrivoR's parameter count
  before any efficiency-wedge claim quotes ~40 M.
* **Proposed L-16** (Lab-infra) — make `library_integrity.py` (this package's
  `code/`) a standing check, run alongside `--verify` rather than instead of it.

⛔ Not self-ranked — the finder proposes; the Master Mind ranks.

## Asks lane

`LAB_ASKS.md` carried **no OPEN row** at this pass (verified twice — a Python read
of the file, and `python stack/scripts/lab_ask.py --list`, which reports
`ASK-1 ANSWERED`). No ask was answered or opened by this package.
