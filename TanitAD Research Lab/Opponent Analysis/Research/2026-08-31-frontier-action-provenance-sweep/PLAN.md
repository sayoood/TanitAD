<title>PLAN — E-LAB-OPP-0831</title>

# PLAN — E-LAB-OPP-0831

`Executed 2026-08-31. Compute budget ZERO: no GPU, no Thor, no metered HF, no download.`

## Steps as executed

| # | step | status |
|---|---|---|
| 1 | Write SPEC first, including the **three-tier admissibility rule**, because part of this sweep was delegated and a relayed quote is indistinguishable from a verified one once written down | done |
| 2 | Delegate the broad frontier sweep (Q1–Q4) with an explicit instruction to open every `abs` page and to say *"UNVERIFIED — could not open"* rather than relay a snippet | done |
| 3 | ⭐ **Independently re-verify the load-bearing claims myself**, not the whole return | done — 5 sources: `2606.12987` (abs **and** full text), `2607.22535`, `2608.04896`, the NAVSIM README, `2503.20523` |
| 4 | ⭐ Bank everything the sweep named, because **banking is itself a verification tier** — it confirms existence, title and date even for items I did not read | done — 9 banked, library 279 → 288 |
| 5 | Split the output into VERIFIED / BANKED / RELAYED and write the relayed items as a **ranked verification queue**, not as findings | done — `raw/QUOTES.md` |
| 6 | Append KB lines to **both** Opponent Analysis and Benchmarks & Evals; stage; do not commit | done |

## ⭐ What step 3 actually caught — the reason it is not optional

| relayed claim | what my own check found |
|---|---|
| "GAIA-2 conditions on speed and curvature" | ⛔ **The abstract does not state the action parameterisation at all.** The attribution was dropped. |
| "a blind probe scores 79.6 EPDMS vs human replay 74.0" | ⛔ **Not in the abstract.** The *direction* is verified (*"outrank human replay and PDM-Closed"*); the **numbers are not** and were removed. |
| "`2606.12987` takes actions from CAN-bus" | ⭐ **Confirmed verbatim in the full text** — and it turned out to be the most valuable single line in the sweep. |

⇒ two relayed claims failed verification and one was upgraded. **A pass-through of the
delegated report would have put two unsupported numbers into a claim register.**

## Follow-on work — the verification queue

⛔ None authorised here; all are reading, no compute.

| # | target | what it would settle |
|---|---|---|
| 1 | `2607.15898` **Orbis 2** | ⭐⭐⭐ both R1 (our hold-action-floor **novelty claim**, currently FLAGGED) and R2 (P4's first driving-domain hierarchy precedent). One read, two open questions. |
| 2 | the field-wide action-provenance table (R8) | whether *"our action channel is the field norm"* may be written at all |
| 3 | `2506.09981` **ReSim** | whether **action-space coverage** is a genuine third P2 lever |
| 4 | `2608.04896` full text | the actual score values, and whether the audit reaches **two-stage** scoring or stops at single-stage |
| 5 | `2511.23369` **SimScale** | how split-dependent an EPDMS delta is (R7) |

## What was deliberately NOT done

- ⛔ **No novelty claim resolved on relayed evidence** — SPEC §3, committed before the sweep returned.
- ⛔ **No number quoted that I did not read.** Two circulating figures were dropped for this reason.
- ⛔ No benchmark run, no leaderboard touched, no pinned target changed.
- ⛔ `Mission Plan.md` untouched; `main` untouched; nothing committed or pushed.
