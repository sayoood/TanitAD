# TanitAD Research Lab — LAB RUN 003 (daily summary)

`Run label 2026-08-29 (Master Mind trigger; third spawn — the first two died on CLI
restarts/API error with no partial work). Literature + docs-verification pass: 0 GPU, no
Thor, no spend. Four fields swept sequentially, one work-package each, staged incrementally
(each package staged the moment it finished). Per-field detail:
<field>/Research/2026-08-29-<slug>/RESULT.md.`

## Per-field one-liners

| field | headline finding | banked primaries | key recommendation |
|---|---|---|---|
| **Architecture & Inference** (`2026-08-29-ema-warmup-rollout-composition`) | Early-EMA damage is DOCUMENTED with the fix stated verbatim (Mean Teacher 2017: teacher must "forget the old, inaccurate, student weights quickly"; data2vec: τ ramped so the teacher moves fastest "when the model is random"; BYOL ablation: constant-τ choice swings top-1 by 2.7–4.1 pts) — and OUR arms run fixed τ=0.996 (no ramp, `train_v6_staged.py:6401`), putting the 2k arms' teacher horizon at 12.5 % of the run, 10–100× off every published operating point, while the 30k arm sits inside the band ⇒ the transient account predicts tonight's cos gap narrows. Symmetric caveat committed BEFORE the read: the 2k drift gain may share the transient origin. Rollout side: all four closest-relative WMs (DreamerV3 H=15, DINO-WM, TD-MPC2 H=3/λ=0.5, V-JEPA 2-AC T=2) roll ONE-step models — multi-step lives in the training loss, never in per-horizon heads; small-data theory (2603.23465) says composed-one-step is the right regime for us, matching F0's vestigial h≥2 heads. | 1703.01780, 2202.03555, 2006.07733, 2301.08243, 2509.24317, 2606.03532, 2310.16828, 1906.08253, 2603.23465 (+cited-by: 2411.04983, 2506.09985, 2301.04104) | If EMA-JOINS tonight: register a τ-ramp arm before recipe adoption (fixed-τ is a mis-specification nobody ships). If EMA-OUT: SALT (frozen teacher ≥ V-JEPA 2 at ⅓ FLOPs) licenses the pre-committed fallback. v7: drop h≥2 heads for composed h=1 + short train-time unroll. |
| **Benchmarks & Evals** (`2026-08-29-benchmark-portfolio`) | Ranked 7-row portfolio for PI wholesale approval: GO = navhard-two-stage EPDMS (pinned) + navtest piggyback (the only placement of reference Drive-JEPA); GO-conditional = Bench2Drive (true closed-loop T1 analog; 44 scenarios × 220 routes; Apache-2.0 2 M-frame expert set; ref eval cost 8×H800×2 d); GO-second-wave = WOD-E2E (4,021 long-tail segments, RFS vs human preference, val labels public); SKIP = nuScenes-OL claim-bearing (AD-MLP: perception-free MLP −20 % L2; BEV-Planner: ego-status dominance; PDM: open/closed misaligned — the field's version of our measured action-echo), CARLA LB 2.1 (~6.8 DS era + 2.0→2.1 scoring break), nuPlan. No single benchmark covers the four families; the GO set jointly does. | 2406.03877, 2510.26125 (+cited-by: 2406.15349, 2305.10430, 2312.03031, 2306.07962) | Approve wholesale; schedule the four prep items now (NAVSIM 450 GB pull, Bench2Drive+CARLA on the Vulkan-fixed pod, WOD-E2E val + RFS scorer, shared register-token rig adapter); ego-status two-arm design applies portfolio-wide (PI decision, restated). |
| **Data Engineering** (`2026-08-29-lane-detector-deployment`) | CULane SOTA is plateaued at ~81.5 — CLRerNet-EMA ckpt (81.55, Apache-2.0, mmdet-3.3 docker) is still the deploy choice; newer buys deployability only (Polar R-CNN 81.49, NMS-free, 20 anchors). Projection gap QUANTIFIED for our rig: straight ground lane = v ∝ sin(u/f) in cylindrical — deviation 2 % @±20°, 8 % @±40°, 17 % @±60°, so the ego corridor (±25°, where the 68 turn_suppression + 41 CORRIDOR_OFFSET records live) is ≤3.1 % ⇒ run as-is there; 640×256 upscales EXACTLY ×1.25 to the native 800×320 input. Transfer must be measured, not assumed (2507.18653: cross-dataset degradation up to near-zero under extreme shift). Cost on 4060: minutes for the waiting sets, 1.7–5.2 h full-corpus (ESTIMATED band; `speed_test.py` first). ⚠️ D016 rectifier is pinhole-Brown-Conrady, NOT cylindrical — a cylindrical→pinhole sibling (~30 lines) is needed before any frame-edge lane inventory. | 2411.01499, 2507.18653 (+cited-by: 2305.08366) | Run the two waiting test sets FIRST with CLRerNet-EMA as-is (doubles as the transfer measurement); gate wider rollout on that, not CULane F1; clip-count discrepancy (4,719 vs 4,729) flagged for run-time pinning. |
| **Deployment & Optimization** (`2026-08-29-quant-gate-spec`) | D-B1-GATE turned into an implementable 4-stage fail-closed spec, every API doc-verified today: IEngineInspector JSON (exact field names), trtexec `--exportLayerInfo/--exportProfile`, ⛔ kDETAILED-at-build precondition (default engines are UNINSPECTABLE ⇒ gate REFUSES, never passes blind), precision census with named-mandatory GEMMs + unattributed-fraction ⇒ INCONCLUSIVE, size/latency cross-checks (the same evidence #4590's reporter used), four-family dense-head stage. #4590 re-verified OPEN from the primary (Thor devkit SM 110, TRT 10.13.3.9/CUDA 13.0, silent FP32 under FP8/FP4 flags, no workaround). Structural fix: strongly-typed + explicit Q/DQ — weak-typing precision flags are REMOVED in TRT 11, so the legacy path is a dead end anyway. Calib-cache format captured (never officially documented — fragility named). | cited-by updates: 2111.12293, 2212.08254, 2303.17951, 2405.03144 (vendor docs not bankable as PDFs; URLs + retrieval date in the package) | DeployFlyWheel implements `quant_gate.py` from the stage list; B1 recipe moves to strongly-typed + Q/DQ; no FP8/FP4 on Thor via builder flags, ever. |

## ⭐ Cross-discipline transfer of the day

**EMA-teacher scheduling is domain-universal, and the newest domains are converging on
replacing the EMA outright.** The identical warmup rationale appears independently in
semi-supervised vision (Mean Teacher 2017), speech/NLP SSL (data2vec 2022), and now LLM
on-policy distillation (CGTR 2026: continuous EMA = "chronic teacher contamination", gated
HARD refresh beats it) — while video SSL (SALT 2025) shows a frozen teacher matches/beats
V-JEPA 2 at a third of the FLOPs. For us this brackets tonight's MM-E1 stage-2 read from
both sides: whichever outcome lands, the literature-backed next lever (τ-ramp vs frozen
teacher) is already named in the package.

## Registered-claim touchpoints

- **D-B1-GATE — UPDATED IN THE SAME TURN** (evidence column extended, status stays OPEN):
  spec package path + #4590 re-verification + strongly-typed recommendation appended to the
  row. No status change (implementation is DeployFlyWheel's).
- MM-E1 stage-2 — NO register change: the 30k read is tonight and belongs to the Master
  Mind; the package supplies the interpretation frame and the pre-committed symmetric caveat.
- D-DATA-GTAC-b stays OPEN — the lane package operationalizes the 4th-arm reference
  (detector chosen, cost estimated, waiting sets identified); no in-house evidence today.
- Benchmark portfolio — restates the pinned navhard target (2026-08-28); adds GO/SKIP rows
  as a PI decision request, decides nothing itself.

## Library state

**129 entries after +13 new banks today (116 → 129; 9 Arch&Inf + 2 Benchmarks + 2 DataEng;
13 further cited-by updates on already-banked entries); `kb_add.py --verify` clean: 129
entries, 0 orphans, 0 problems.** No hand-edits to LIBRARY.md. 0 GPU used; no spend; no commits (stage-only per
operating standard); suite untouched (no code changed).
