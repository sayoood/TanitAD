# DELIVERABLE MANIFEST — 2026-08-04 sitclf event-recall deployability

**Contract:** `Project Steering/AGENT_OPERATING_STANDARD.md`. **STAGED, NEVER PUSHED.** Every path
below was verified present in the index with `git ls-files --cached`, not with a `git add` exit code.

**Branch** `agent/arch-inf-20260803` · **0 pod GPU-h** — no pod was touched, nothing was pushed.

---

## 1. Pre-registration — content-pinned and re-verified

| | |
|---|---|
| file | `TanitAD Research Hub/Architecture & Inference/Implementation/incoming/2026-08-04-sitclf-event-recall/PRE_REGISTRATION.md` |
| SHA-1 at pin time (**before** any number was read) | `7a62ca201603c7732b0bae0d56d13840c3979171` |
| SHA-1 re-verified **after** the run | **`7a62ca201603c7732b0bae0d56d13840c3979171` — MATCH** |
| pin recorded in | `PRE_REGISTRATION.sha1` and inside `results_event_retune.json` (`_prereg_sha1`) |

---

## 2. Repo artifacts — all under `…/incoming/2026-08-04-sitclf-event-recall/`

| file | what it is | evidence class |
|---|---|---|
| `PRE_REGISTRATION.md` · `.sha1` | arms, controls, multiplicity, decision rule, 5 predictions — all fixed before measuring | — |
| **`EVENT_RECALL_DEPLOYABILITY.md`** | ⭐ **the report** — leads with deployable / not-deployable and the cost | MEASURED |
| `TABLES.md` | every table, generated by `render_tables.py` from the JSONs; nothing retyped | MEASURED |
| `run_event_retune.py` | the analysis: grid rebuild (real + clip-permuted null), event yardstick, chance floor, Holm, pooled oracle, `RETUNE_SEL`, per-fold | — |
| `results_event_retune.json` | its output — every arm, every control, both null twins | MEASURED |
| `grid_null.npz` (23.5 MB) | the 25 clip-permuted NULL score columns, banked for 0-GPU re-analysis | MEASURED |
| `four_families_retune.py` · `results_four_families.json` | the four binding families for both candidates, causal **and** legacy ego | MEASURED |
| `verify_chance_floor.py` · `verify_chance_floor.json` | ⭐ **independent verifier** — the analytic floor (promoted `stack` fn) vs this stream's 200-draw simulation, plus every headline re-derived | MEASURED |
| `render_tables.py` | the renderer | — |
| `run_log_retune.txt` · `run_log_families.txt` · `run_log_verify.txt` | run logs | — |

## 3. `stack/` changes — additive, promoted, tested

| file | change |
|---|---|
| `stack/tanitad/eval/sitclf_deploy.py` | `event_anticipation_report` now also returns **`chance_event_recall`** (exact hypergeometric, not a simulation), `chance_alarm_precision_h_max`/`_deploy`, `event_recall_vs_chance`, `alarm_precision_lift_h_max`/`_deploy`. **The floor now travels with the number it qualifies**, so a report cannot quote the headline without it. Nothing existing was modified |
| `stack/tests/test_sitclf_deploy.py` | **+4 tests** (43 → 47 in that file): the floor keys must exist; the analytic floor matches a 400-run simulation; ⭐ a *dispersed* score beats a *concentrated* one on event recall at the identical budget — the defect itself pinned as a test; a useless score lands at a precision **lift** of ≈ 1.0 |

## 4. Off-Drive mirror (the repo mount died for 2 h last night)

`C:/Users/Admin/wt-mirror-sitclf-event-recall/` — a full copy of the deliverable directory plus the
two `stack/` files. Refreshed after the last edit.

## 5. Test suite — exact counts, and the two foreign failures

`cd stack && OMP_NUM_THREADS=6 pytest -q` →
**2 failed, 2,066 passed, 12 skipped, 2 xfailed, 10 warnings in 1,020.82 s (0:17:00).**

⚠️ **The two failures are FOREIGN and TRANSIENT — verified at three probes, not assumed**
(`pytest_result.txt` has the full record):

1. re-run in isolation: `tests/test_nav_known_channel.py tests/test_refc_labels_v3_wiring.py` →
   **18 passed in 25.60 s. BOTH PASS.**
2. the files under test — `stack/scripts/refc_train.py` and `stack/tanitad/refs/refc.py` — are
   **another stream's, staged-modified while this suite was running** (`git status --short --
   stack/`). Both failing tests assert on `refc_train.py`'s argparse/config wiring; a file rewritten
   mid-suite is the mechanism.
3. neither failing test imports anything this stream touched.

**This stream's own tests:** `pytest -q stack/tests/test_sitclf_deploy.py` → **47 passed in 27.93 s**
(43 before, **+4** added).

⚠️ **Moving baseline, disclosed:** the brief quotes 2,043 passed. This run collected **2,068**
(2,066 + 2): **+25**, of which **+4 are mine** and **+21** arrived from concurrent streams during
the session — checked against `git log`/`git status` before treating the change as anything else.

---

## 6. ⚠️ ESCALATIONS — integration, not "please merge" in a doc

1. ⭐ **The `+0.3333 lane_change event recall` row in
   `…/2026-08-03-sitclf-horizon/PER_SITUATION_HORIZON.md` §1 and `TABLES.md` §4 must not be cited as
   skill.** MEASURED here: the identical cell on **clip-permuted features** earns the identical
   `+0.0877` (`CELL_w8_L1.0`) and `+0.1579` (`CELL_w32_L1.0`), and the uniform-random floor for that
   metric is **0.8088** against the deployed head's **0.1404**. The parent banked the row honestly
   and asked for a powered study; this is that study, and the answer is that the metric was the
   problem. **Someone owning those two files should add the caveat**; I did not edit another
   stream's banked report.
2. ⭐ **`stack/tanitad/eval/sitclf_deploy.py` and `stack/tests/test_sitclf_deploy.py` are modified in
   the working tree and need a review + commit by whoever owns `stack/` this session.** The change
   is additive and the suite is green, but `event_anticipation_report`'s return dict gained six keys
   — any consumer that asserts on its exact key set will need a look. Probed: the only callers at
   HEAD are `stack/tests/test_sitclf_deploy.py` and the two research-hub verifiers, both updated.
3. **`stack/scripts/sitclf_train.py` has `--win` but no `--lead`.** Acting on §0's recommendation
   needs a substrate/label rebuild at `lead_s = 1.0`, which is a **work item**, not a flag. It is not
   built here because the pre-registration's `NOT DEPLOYABLE` branch had to remain live until the
   number was read, and because the horizon question in §5 of the report is the PI's, not mine.
4. **A PI decision is required and this stream cannot substitute for it:** is a **1 s** warning
   horizon actionable for the tactical layer? The whole recommendation hangs on it and no metric
   here answers it.

## 7. Nothing stranded

No artifact of this stream exists only on a pod, only in a worktree, or only in my context. The
banked NULL grid (`grid_null.npz`) is in the repo so the null controls are re-checkable without
re-running any fit.
