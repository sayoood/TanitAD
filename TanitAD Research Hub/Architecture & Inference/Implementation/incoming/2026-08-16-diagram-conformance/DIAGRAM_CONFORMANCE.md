# DIAGRAM CONFORMANCE — the v6 design diagram, audited against the code, element by element

**Date:** 2026-08-16 · **Branch:** `agent/arch-inf-20260803` @ `a84a1a0` · **Auditor:** arch-inf subagent
**Mandate (PI, verbatim):** *"the most important thing [is] that v6 is following the design aligned in the diagram."* The diagram is BINDING; this document is the element-by-element conformance matrix.

**Method.** Full reads of `stack/tanitad/models/v6.py` (2,939 lines), `stack/scripts/train_v6_staged.py` (2,960), `stack/scripts/v6_chain.py` (1,453); targeted reads of `tanitad/models/tactical.py`, `tanitad/models/sigreg.py`, `tanitad/models/kinematic.py`, `scripts/train_stage_a.py`, `scripts/stage_a_probes.py`, the probe/instrument scripts, and the three spec documents (`HIERARCHY_VOCABULARY.md`, `V6_TRAINER_DESIGN.md`, `V6_TRAINING_MEASURES.md`, all under `…/incoming/2026-08-07-hierarchical-wm-redesign/`). Code sites are located **by content** (class/function names + quoted fragments), never by line number. Evidence class per row: **MEASURED** = read in source / exercised by the 144-test run below in this audit; **INHERITED** = quoted from a doc/registry, not re-verified here.

**Verdict legend:**
| verdict | meaning |
|---|---|
| ✅ **CONFORMS** | the code implements the diagram element; site quoted |
| 🟦 **DIVERGES — code right** | code deliberately departs, with a MEASURED reason; the **diagram** needs the update (list in §6) |
| 🟥 **DIVERGES — code wrong** | code fails to implement a binding element; fix needed (list in §5) |
| ⬜ **NOT BUILT** | element absent; what building it needs is stated |
| 🟨 **PARTIAL** | some sub-elements conform, others do not (split inside the row) |

**Summary:** 31 ✅ · 4 🟦 · 3 🟥 · 8 ⬜ · 6 🟨 across 52 audited elements. The three 🟥 are: the missing `g_str → P_T` conditioning port (§1.2 — the top finding; S-T trains next), the S-T gate's four-families under-coverage (§1.3 — surface half fixed in this audit, see §7), and the S-S gate's missing goal-provenance audit (§1.4 — surface half fixed in this audit, see §7).

**Two data-only code edits were made during this audit** (`STAGE_GATE_SPEC` reported-probe additions; behaviour-safe, tests pass — §7). Everything else is a finding, not an edit.

---

## 1. STAGED TRAINING (priority 1 — S-T/S-S run next; an error here costs GPU-days)

### 1.1 The ladder itself — "staged training, each stage gated by the frozen WM-physics battery"

| element | verdict | evidence |
|---|---|---|
| S-W → S-T → S-S → S-J stage order | ✅ CONFORMS | `v6.py` `STAGES = ("S-W", "S-T", "S-S", "S-J")`; `train_v6_staged.py` `STAGE_PRECONDITION = {"S-W": None, "S-T": "S-W", "S-S": "S-T", "S-J": "S-S"}`; pinned by `test_stage_ladder_is_the_spec_order`. MEASURED |
| each stage gated BEFORE the next begins; a failed stage never propagates upward | ✅ CONFORMS | `assert_stage_precondition` refuses on missing gate file, wrong-stage gate, `pass: false` (**no override for FAIL anywhere**), and `pass: null` (INCONCLUSIVE ≠ PASS; override only with `--allow-inconclusive-gate` **and** a recorded `--gate-off-reason`). A `--dry-run` gate is stamped `_dry_run` and refused for real launches. MEASURED (and exercised: 144/144 in `tests/test_v6_staged.py` + `test_v6_stage_revalidation.py` + `test_v6_chain.py`, this audit) |
| stage freeze = exactly the declared groups | ✅ CONFORMS | `STAGE_GROUPS` (S-W: encoder/readout/predictor_op/aux · S-T: layer_tac/planner · S-S: layer_str · S-J: all) + `apply_stage_freeze` which **raises on any parameter escaping the partition**; `group_of` refuses unmapped parameters. Pinned by `test_stage_groups_partition_the_model`, `test_stage_freeze_trains_exactly_the_declared_groups`. MEASURED |
| each stage starts from the stage below (one lineage) | ✅ CONFORMS | `preflight` refuses S-T/S-S/S-J without `--init-from`; `load_stage_init` strict-loads with the per-stage `STAGE_MAY_INTRODUCE` allowlist (S-T may introduce `cand_score.` only) and md5-stamps the loaded trunk; `RESUME_CONTRACT` + `assert_resume_lineage` refuse cross-stage resumes, unlabelled ckpts, and optimiser-less fp16 snapshots. MEASURED |
| **beyond the diagram** (code is stricter) | 🟦 code right | `STAGE_INVALIDATES` + `S_S_REVALIDATIONS`: S-S retrains the goal the frozen S-T selector consumes (`goal_head_str → e_g_str → goal_head_tac(cond) → e_g_tac`, the selector's ONLY input), so S-S's gate REQUIRES `sel_gap_revalidated` + `TACTICAL_revalidated`. The diagram has no backward-invalidation concept. Diagram-update item D-6. MEASURED |

### 1.2 S-W — "operative WM only, planner absent, λ_plan≡0; O1…O6"

| element | verdict | evidence |
|---|---|---|
| planner absent, λ_plan ≡ 0 | ✅ CONFORMS | `STAGE_LAMBDA_PLAN["S-W"] = 0.0`; `V6LossWeights.for_stage("S-W")` zeroes `t1/s1/lambda_plan/seam_op/w_select/w_anchor`; `preflight` **refuses** `--stage S-W` with a non-zero `--lambda-plan` or any `--selector`/`--anchor-goal` (it would change the state_dict and break the live run's strict resume). Pinned by `test_lambda_plan_is_zero_in_S_W_by_construction`, `test_S_W_touches_no_planner_parameter`. MEASURED |
| **O1** response-form L_ctrl **from step 0**, counterfactual action arms each batch | ✅ CONFORMS | imported, never re-implemented: `from train_stage_a import TRAIN_ARMS, sample_random_deltas, stage_a_losses`; `TRAIN_ARMS = ("left", "right", "brake", "throttle", "random")`; `v6_loss_step` calls `stage_a_losses(..., ctrl_form="response")` whenever `w.o1_* > 0` — in force from step 0 of S-W (default weights 1.0/1.0/0.3). One declared difference from stage A, said out loud in the docstring: the encoder TRAINS in S-W (states carry gradient). MEASURED |
| gain trained into [0.5, 2]× the unicycle analytic | ✅ CONFORMS | the response form IS the mechanism (L1 on `(rolled_cf − rolled_factual)` vs `(analytic_cf − analytic_factual)` — "the exact quantity the W3 gain gate measures", `train_stage_a.py`); the band is gated by `stage_a_probes.GATE_GAIN = (0.5, 2.0)` (P3: median gain vs unicycle-analytic, lateral gated, longitudinal reported) and appears in `STAGE_GATE_SPEC["S-W"]["criteria"]["P3_gain"]` = "median gain in [0.5, 2.0], WITHOUT post-training". MEASURED |
| **O2** time-scaled near-field (not metres) | ✅ CONFORMS | `time_to_reach` / `time_to_reach_weights` (`exp(-t_reach/τ)`, capped at the 6 s horizon, speed-floored, mean-1 normalised) + `o2_near_field_loss` on readout CELLS. `half_weight_distance_m` makes "higher speed ⇒ wider metre band, same time band" unit-pinned. ⚠️ the catalog's own O2 row still says "0–40 m band" — code follows the LATER `HIERARCHY_VOCABULARY` §2 PI correction, which the diagram also carries. Cell ranges are a **declared ESTIMATED monotone image-row prior** (`readout_grid_ranges` — PhysicalAI ships no depth/map). MEASURED |
| **O3** masked spatial latents | ✅ CONFORMS | `sample_cell_block_mask` (contiguous blocks — the load-bearing property, RC3) + `near_field_band_mask` + `MaskedCellPredictor` + `o3_masked_cell_loss` (masked cells ONLY; `--o3-mode action` predicts from the action-conditioned rolled latent). MEASURED |
| **O4** interaction sampling, label-free | ✅ CONFORMS | `kinematic_saliency` (|jerk|, |decel|, steering reversals — **from actions ONLY**), `saliency_weights` (floor keeps every window reachable — parity: O4 reweights, never removes), `InteractionSampler`; `alpha=0` = the uniform control arm. Pinned by `test_o4_never_removes_a_window_parity_is_sacred`. MEASURED |
| **O5** rollout consistency, error at every step | ✅ CONFORMS | `o5_rollout_consistency_loss` + `rollout_step_weights` (`endpoint` exists only as the ablation control). ⚠️ trainer default `--o5-k 20` (2 s); **the live config-E run uses `o5_k=60` — the §4b 6 s rollout contract** (INHERITED: `HANDOVER_TO_LOCAL_2026-08-15.md`, `…/2026-08-15-v6-thor-resume/RESUME_TOOLING.md`). Code-fix F-6 proposes aligning the default. MEASURED (mechanism) / INHERITED (live flag) |
| **O6** SIGReg + spectrum monitor | 🟨 PARTIAL | SIGReg via `o6_sigreg_loss → position_relaxed` (λ=0.1, 512 slices; `free_dims=0` default = the full form — "full_relaxed" maps to this call) ✅; spectrum monitor `spectrum_report`/`SpectrumAccumulator`/`o6_rank_verdict` with rank-ceiling stamping, jackknife CI, and pooled-admissibility ✅ — **but only on the operative latent** (`z_op_win`). "per layer" (diagram: *latent spectrum monitored per layer*; catalog X4: per-layer SIGReg + spectrum at T/S scale) is ⬜ NOT BUILT — no sigreg/spectrum on `z_tac`/`z_str`. Fix F-4. MEASURED |
| S-W gate: P1 · P3 · P6 · **P8** · rank retention ≥ 0.8× | 🟨 PARTIAL | `STAGE_GATE_SPEC["S-W"]`: required `("P1","P3","P6")` ✅ with owners (`probe_latent_state.py`, `stage_a_probes.py`); **P8 is `reported`, not required** — 🟥/🟦 split: the diagram puts P8 in the gate; P8 attempt-2 only just ported (`train_p8_occupancy.py` + `tests/test_p8_v6.py` exist, MEASURED) so requiring it today would gate S-W on a just-landed instrument. Decision row: fix F-5 (promote P8 when its artifact exists) or diagram-update D-3. The naked "rank retention ≥ 0.8×" is 🟦 **DIVERGES — code right**: MEASURED (`SIGREG_GATE_POWER.md`, INHERITED here) that at the live run's n=48 the naked criterion fires on nothing 9–38 % of the time with power 0.11; replaced by `o6_rank_verdict`'s three clauses (admissible ceiling ≥ 1024 · CI-based retention vs 0.8 · absolute floor 64) which can say INCONCLUSIVE. Diagram-update D-4. |

### 1.3 S-T — "tactical on FROZEN world stage; goal-conditioned prediction · manoeuvre contrastives · roll-cost distillation into the selector · momentum-aware temporal consistency; gate: four families at 0–2 s AND 0–6 s · T1 tier"

| element | verdict | evidence |
|---|---|---|
| tactical trains on the FROZEN world stage | ✅ CONFORMS | `STAGE_GROUPS["S-T"] = ("layer_tac", "planner")` — encoder/readout/predictor_op frozen; `for_stage("S-T")` zeroes every O-term; the g_tac seam trains through `predictor_op.intent_proj`/`intent_gate`, which are **grouped `planner`**, not `predictor_op` (the 2026-08-13 fix: with them under `predictor_op`, S-W trains them at zero gradient and S-T freezes them exactly when g_tac first flows). MEASURED |
| goal-conditioned prediction (T1) | 🟨 PARTIAL | `t1_latent` = tactical latent prediction against a stop-grad/EMA target **one tactical tick (stride_tac = 5 = 2 Hz) ahead** ✅; the predictor rolls under the tactical ACTION pair `e_a_tac` (not raw controls) ✅ — but the **`g_str` conditioning of P_T is missing** (next row), so "goal-conditioned" is weaker than spec'd. MEASURED |
| **`g_str` conditions the tactical predictor — `P_T(z_tac, a_tac \| g_str)`** | 🟥 **DIVERGES — CODE WRONG (top finding)** | The diagram, `HIERARCHY_VOCABULARY` §5 (*"z_tac_{t+k} = P_T(z_tac_t, a_tac_t \| g_str)"*), `V6_TRAINER_DESIGN` §1.2's ASCII (*`g_str ────┘ → P_T`*), **and `V6Stack`'s own class docstring** all spec the port. The code does not build it: `predictor_tac = FTac(cfg.d_tac, d_goal=2*cfg.d_goal_embed, …)` and the forward is `zh_tac = self.predictor_tac(z_tac, e_a_tac)` — `FTac.forward(z_tac, g_flat)` has ONE conditioning input (`in_proj = nn.Linear(d_tac + d_goal, hidden)`), and `2*d_goal_embed` is fully consumed by the concatenated LAT×LON action embeddings. `e_g_str` reaches only `goal_head_tac(z_tac_p, cond=e_g_str)`. **Consequence if S-T launches as-is:** the strategic→tactical dynamics downlink never trains in its own stage — exactly the `intent_proj` defect one level up (a conditioning path present in the diagram, absent from the optimisation), the class the 2026-08-13 seam fix repaired for g_tac→P_O. Fix F-1 (must land BEFORE S-T). MEASURED |
| manoeuvre contrastives (catalog T2: time-reversal + lane-mirror hard negatives) | ⬜ NOT BUILT | no contrastive term in `v6_loss_step`; no augmentation machinery in the trainer. Needs: a T2 loss (label-free augmentations of the window + a contrastive head on `z_tac`) + a weight in `V6LossWeights`. Fix F-7 |
| roll-cost distillation into the selector (catalog T4) | ⬜ NOT BUILT — **and refuted as designed**; see §3 | no distillation term exists. The selection loss that DOES exist (`w_select` → E-OBJ-1 `softade`: expected fan error under the scorer's own softmax) trains on the TRUE fan error, not on roll-cost — deliberately, because the roll-cost ranking signal was refuted (§3). Building T4 as drawn would distil a refuted signal. Diagram-update D-1 |
| momentum-aware temporal consistency (catalog T5) | ⬜ NOT BUILT | no cross-window plan-flip-flop penalty; no plan-switch-rate logging. Needs consecutive-window batches (the current sampler draws windows independently) — a sampler + loss change. Fix F-8 |
| interaction curriculum (catalog T3: multi-agent kinematic entropy from predicted occupancy) | ⬜ NOT BUILT | O4 is the ego-kinematic version only; T3's multi-agent extension needs the P8 occupancy readout in the loop. Fix F-9 (gated on P8 maturity) |
| S-T gate: **four families** at 0–2 s AND 0–6 s, T1 tier | 🟥 **DIVERGES — code wrong** (surface half fixed in this audit, §7) | Pre-audit `STAGE_GATE_SPEC["S-T"]` covered 2 of 4 families (TACTICAL required, LATERAL reported; LONGITUDINAL and STRATEGIC absent) and pinned neither horizon — against the binding four-families rule (Sayed 2026-08-02: "an eval that reports ADE alone is INCOMPLETE… per-family, never pooled") and §4b's eval consequence (*"four families + oracle/selected reported at BOTH 0–2 s and 0–6 s"*). LONGITUDINAL is the family S-T's own thesis moves (the tactical layer **owns target speed**; 88.7 % of the oracle gap is longitudinal). **This audit added `LONGITUDINAL_family` + `STRATEGIC_family` to `reported` and the both-horizons criterion** (behaviour-safe: reported probes are stubbed, never adjudicated — §7). The `required`-promotion decision + numeric criteria stay with the PI/integrator (fix F-2). ⚠️ UNVERIFIED sub-item: whether `taniteval/tools/eval_four_families.py` emits per-family metrics at BOTH bands (the instrument exists and drives `four_families.all_families`; its band coverage was not instrument-tested in this audit). The trainer itself logs `plan_ade_0_2s` / `plan_ade_2_6s` split by `cfg.split_bands` ✅. |
| T1 tier discipline | ✅ CONFORMS | `sel_gap` criterion says "at T1 tier"; the trainer stamps every artifact "TRAINING numbers… capability claims are T1 only" (`metrics.json`, `_run_config`); the gate dict carries a tier field. MEASURED |

### 1.4 S-S — "strategic on frozen tactical; g_str from the VLM+geometry+SAM pipeline into the GOAL HEAD only · domain-diverse mix; gate: STRATEGIC family + goal-provenance audit"

| element | verdict | evidence |
|---|---|---|
| strategic on frozen tactical | ✅ CONFORMS | `STAGE_GROUPS["S-S"] = ("layer_str",)`; `for_stage("S-S")` zeroes everything else incl. `w_select`/`w_anchor`; `preflight` refuses a non-zero `--w-select`/`--w-anchor` in S-S (a launch line must not advertise a loss not in force) while the selector GEOMETRY is carried forward (`assert_geometry_carry` — an S-T ckpt's `cand_score.*` keys otherwise die as unexpected). MEASURED |
| **g_str supervision from the VLM+geometry+SAM pipeline, into the GOAL HEAD only** (catalog S2) | ⬜ NOT BUILT — **by recorded decision** | `V6_TRAINER_DESIGN`: *"S2 (`g_str` supervision) is not wired here and must not be faked — it arrives from the [PH0→PH1→PH2] pipeline"* (stated twice). The trainer's only S-S loss is `s1_latent` (strategic latent prediction). The label pipeline is the DataEng **S2-gap** stream's deliverable (live agent). When it lands: the supervision must enter `goal_head_str` ONLY (a CE/metric loss on `g_str` logits/args), never a trunk loss — the goal-head-only constraint is structurally ready (goal heads are planner-side, X3-isolated from every encoder). Fix F-3 records the wiring point |
| domain-diverse mix (catalog S3) | ⬜ NOT BUILT | no domain-stratified sampling in `train()` (episode draw is uniform / O4-weighted only). Needs the VLM/scena strata as a SAMPLER input — which is admissible for the data MIX (it is not a model input) but must be declared. Fix F-10 |
| S1 horizon: strategic clock 2 s tick vs "horizon 8–30 s" | 🟨 PARTIAL | training target is **one strategic tick ahead** (`stride_str = 20` steps = 2.0 s at the 0.5 Hz clock) — a 1-tick loss; the 8–30 s capability appears only as gate-reported `S1_ade_8_30s` (owner `t1_eval.py`). Multi-tick strategic rollout training is not built. Fix F-11 |
| gate: STRATEGIC family | ✅ CONFORMS | `STAGE_GATE_SPEC["S-S"]["required"]` includes `STRATEGIC_family` (owner `eval_four_families.py`; criterion "computable at all — measured vs n/a today"). MEASURED |
| gate: **goal-provenance audit** | 🟥 **DIVERGES — code wrong** (surface half fixed in this audit, §7) | Pre-audit, the audit was absent from `STAGE_GATE_SPEC["S-S"]` entirely. **This audit added `goal_provenance` to `reported`** with owner + promotion trigger (§7). The audit instrument itself is ⬜ NOT BUILT: it must verify (a) every g_str label path terminates in `goal_head_str` (config + X3-probe evidence), (b) no situation-classifier output in any goal input (the 2026-08-03 binding rule's check: *"could this have been computed from the situation classifier's output?"*), (c) label provenance (VLM/geometry/SAM artifact hashes) recorded in the run row. Promote to `required` the moment S2 lands — pre-S2 it is vacuous (nothing to audit) and a required probe would manufacture INCONCLUSIVE gates. Fix F-3 |
| S-S beyond the diagram | 🟦 code right | S-S's gate also REQUIRES `sel_gap_revalidated` + `TACTICAL_revalidated` (see §1.1 last row) — the diagram does not know S-S invalidates S-T's selector certificate. Diagram-update D-6. MEASURED |

### 1.5 S-J — "optional joint polish; isolation stays ON; gate: no-harm on the full battery, else revert"

| element | verdict | evidence |
|---|---|---|
| optional; isolation stays ON | ✅ CONFORMS | chain note: "OPTIONAL. Run only if S-T/S-S plateau. Trains everything with isolation still ON"; isolation is a config invariant (`isolate_planner_from_encoder`/`isolate_uplink` default True) independent of stage; `assert_isolation` makes every parameter differentiable FOR THE PROBE so a frozen stage cannot produce a vacuous pass. MEASURED |
| gate: no-harm on the full battery, else revert | ✅ CONFORMS | `STAGE_GATE_SPEC["S-J"]`: required `("X3_isolation", "no_harm")`; criteria "zero live forbidden edges" + "battery FLAT across the joint phase (H-COTRAIN rule)"; reported P1/P3/P6/four_families. "Else revert" is enforced by the ladder semantics: a FAIL has no override anywhere (`assert_stage_precondition`), so a failed S-J cannot propagate; the S-S checkpoint remains the lineage. MEASURED |

---

## 2. THE HIERARCHY (priority 1, continued)

### 2.1 Header row

| element | verdict | evidence |
|---|---|---|
| four abstraction layers, each with **its own predictor and action space** | 🟨 PARTIAL | Three of four: strategic `predictor_str = FTac(d_str, d_goal=d_goal_embed)` conditioned on `e_a_str` (own vocabulary `STRATEGIC_ACTION_TOKENS`); tactical `predictor_tac = FTac(d_tac, 2*d_goal_embed)` on `e_a_tac` (own factored vocabularies `TACTICAL_LAT_ACTIONS` × `TACTICAL_LON_ACTIONS`); operative `OperativePredictor` on continuous (a, κ) (+ the speed-lifted third channel). The **context/fallback layer has no predictor and no action space** — it exists as instruments (P7/P2, §2.5), which matches the diagram's own context-row content (self-knowledge, monitors) but not the header's "four… each with its own predictor". Diagram-update D-7 (header wording) |
| goals condition **downward**; latents flow **upward through stop-grad** | ✅ CONFORMS | Downward ports in `V6Stack.forward`: (1) `e_g_str → goal_head_tac(cond=…)`; (2) `e_g_tac → predictor_op(intent=self._cut(e_g_tac, cut))` — detached under isolation so the WM loss cannot train the goal head backwards through the port; (3) `e_g_tac → emit()` (concatenated into the emission feature); (4) `e_g_tac → cand_score`/`anchor_head` (selection). The spec'd fifth port `g_str → P_T` is MISSING (🟥 §1.3). Upward: `uplink_tac`/`uplink_str` cut **unconditionally under `isolate_uplink`** (`_cut` = detach), targets from `stopgrad` (own detached output) or `ema` (`_EmaCopy`, requires_grad=False, excluded from optimisers); `layer_targets` runs under `no_grad`. MEASURED, and probed: `assert_isolation` edges 2–3 backprop `[z_tac, zh_tac]` / `[z_str, zh_str]` against everything below. |
| ONE 6 s trajectory spans operative+tactical bands | ✅ CONFORMS | §2.4. |
| "sub-300M parameters" | 🟦 **DIVERGES — code right (PI-approved)** | `PARAM_BUDGET = 300_000_000` is the code default and `assert_param_budget` enforces `cfg.param_budget` at build time, pre-GPU — but the budget is CLI-declarable (`--param-budget`), and the **LIVE run is config E: 336,542,025 params at a PI-raised budget of 350 M** (INHERITED: `HANDOVER_TO_LOCAL_2026-08-15.md` — "param_budget raised to 350 M"; `RESUME_TOOLING.md` — rebuild from banked `config.json["args"]` = 336,542,025; PI: "the bigger optimized variant, I think 330 Mio"). The diagram header and `v6.py`'s comment ("sub-300M is a programme INVARIANT") are both stale against that decision. Diagram-update D-2 + fix F-12 (comment). |
| no perception label, map or reward in any trunk loss | ✅ CONFORMS | Verified loss-by-loss for the trunk-training stage (S-W): O1 uses **ego odometry** (`gt_ego_waypoints` from poses — labels may use ego, PI 2026-08-03) through the `StepDisplacementReadout` metric readout; O2/O3/O5 are latent self-supervision; O6 is a regulariser; O4's sampler reads ACTIONS ONLY (its docstring: "the obstacle join and the VLM fields are frozen-probe/eval-strata material, never a training-time selector"). No obstacle join, no VLM field, no map, no reward anywhere in `v6_loss_step`. ⚠️ the encoder-cell wording "labels may supervise readouts and goal heads only — never a trunk loss" is stricter than the header and, read literally, collides with catalog-REQUIRED O1 (ego labels through the readout reach the encoder in S-W) — diagram-update D-8 aligns the wording to the header ("no PERCEPTION label…"). MEASURED |

### 2.2 Strategic brain

| element | verdict | evidence |
|---|---|---|
| clock ≈ 0.5 Hz | ✅ CONFORMS | `V6Config.hz_str = 0.5`; `stride_str` property = 20 operative ticks; the trainer computes the S1 target from `b["future_frames"][:, stride - 1]` with `stride = cfg.stride_str` — one strategic tick ahead. `__post_init__` refuses non-monotone clocks ("a higher layer ticking faster than a lower one is not a hierarchy"). Pinned by `test_clocks_and_strides`. MEASURED |
| horizon 8–30 s+ | 🟨 PARTIAL | trained horizon is one 2 s tick (§1.4 row S1); 8–30 s is gate-reported only. Fix F-11 |
| influences beyond 6 s **through goals only** | ✅ CONFORMS | structural: in `V6Stack.forward`, `z_str` reaches the plan ONLY via `goal_head_str → e_g_str → goal_head_tac → e_g_tac → emit`; no strategic tensor enters the emission, the selector, or `P_O` directly. MEASURED |
| P_S: `z_str(t+K) = P_S(z_str, a_str)` | ✅ CONFORMS | `zh_str = self.predictor_str(z_str, e_a_str)` — the strategic layer's OWN action tokens condition its OWN predictor. MEASURED |
| `a_str` = PREPARE_LANE_CHANGE · REDUCE_TO · PREPARE_EXIT · PREPARE_STOP | ✅ CONFORMS (superset) | `STRATEGIC_ACTION_TOKENS` carries all four plus `HOLD_CORRIDOR`, `RESUME_CRUISE` (the diagram abbreviates). MEASURED |
| `g_str` = FOLLOW_MAIN_ROAD (default) · ROUTE_TO · LANE_TARGET · TURN | ✅ CONFORMS (superset) | `STRATEGIC_GOAL_TOKENS` (11 tokens) carries all four — TURN split into `TURN_LEFT`/`TURN_RIGHT` — plus corridor/exit/stop tokens and `NONE_ABSTAIN`; `FOLLOW_MAIN_ROAD` documented in-code as "THE DEFAULT whenever no navigation route is set up (PI 2026-08-11)". ⚠️ the default is a documented convention, not yet an enforced inference-time prior (no deployment-side default injection exists — nothing at inference sets g_str when the head abstains). Noted in F-13. MEASURED |
| **g_str conditions the tactical predictor** | 🟥 CODE WRONG | §1.3 top finding. |

### 2.3 Tactical brain

| element | verdict | evidence |
|---|---|---|
| 2–6 s band of the SHARED trajectory | ✅ CONFORMS | `TAC_BAND_S = (2.0, 6.0)`; `V6Config.__post_init__` **refuses** a band gap/overlap ("a gap or an overlap here IS the stitched-trajectory defect") and a tactical band not ending at the horizon; `band_slice`/`split_bands` are the ONE place bands become indices (views, not copies). Pinned by `test_band_slicing_is_correct_and_seam_free`, `test_config_refuses_a_band_gap_or_overlap`. MEASURED |
| clock ≈ 2 Hz | ✅ CONFORMS | `hz_tac = 2.0`; `stride_tac = 5`; T1 targets one tactical tick ahead in the trainer. MEASURED |
| **owns target speed (signs + priors)** | 🟨 PARTIAL | vocabulary-level ✅: `SPEED_BAND` is a tactical goal token with the PI decision quoted in-code ("SPEED_BAND is a TACTICAL responsibility (PI 2026-08-11): target speed from sign/OCR + corridor speed priors, with the strategic REDUCE_TO acting only as an upper envelope"). Supervision/derivation ⬜ NOT BUILT: no sign/OCR prior enters any loss or label path in v6; the vision-derived speed-band work sits unintegrated in `…/incoming/2026-08-04-target-speed/code/vt_band_from_vision.py` (untracked incoming stream). Fix F-14 |
| P_T factored a_tac: **LAT** (LANE_KEEP/CHANGE · EVADE) × **LON** (FOLLOW · BRAKE_TO · WAIT) | ✅ CONFORMS (naming drift) | factoring is structural: `act_head_lat`/`act_head_lon` over `TACTICAL_LAT_ACTIONS` (LANE_KEEP, LANE_CHANGE_L/R, ABORT_LC, NUDGE_L/R) × `TACTICAL_LON_ACTIONS` (FOLLOW, CRUISE, YIELD_MERGE, BRAKE_TO, CREEP, HOLD) — "the 5-way-mixed softmax (the programme's single largest known defect) is retired BY DESIGN". Naming drift vs the diagram: EVADE lives in the GOAL vocabulary (`EVADE_IN_CORRIDOR`), not the LAT action list; WAIT maps to `HOLD` (action) / `WAIT_FOR_ONCOMING` (goal). Diagram-update D-9 (or a deliberate rename — the partition choice is defensible either way). MEASURED |
| `g_tac` = ANCHOR_GOAL · SPEED_BAND · GAP · YIELD_AT · STOP · LIGHT_REACT | ✅ CONFORMS (superset) | `TACTICAL_GOAL_TOKENS` (9) carries all six (GAP→`GAP_TARGET`, STOP→`STOP_POINT`, LIGHT_REACT→`TRAFFIC_LIGHT_REACT`) + `CORRIDOR_OFFSET`, `WAIT_FOR_ONCOMING`, `EVADE_IN_CORRIDOR`. Beyond the diagram (all default-OFF, byte-identity-proved): factored LAT×LON g_tac pair, multilabel gates, typed categorical arg channel, `AnchorGoalHead` regress-then-snap (E-AG1/E-AG2-measured). MEASURED |
| goal tokens carry optional constraints (within_m · by_time_s · at_arc_m · hold_for_s) | ✅ CONFORMS | `CONSTRAINT_SLOTS = ("within_m", "by_time_s", "at_arc_m", "hold_for_s")` — exact; wired end-to-end (`GoalHead.arg_head` emits, `GoalVocabulary.encode` consumes with `arg_mask` so an UNSET slot contributes exactly zero — "Unset = unconstrained"). Pinned by `test_unset_constraint_slots_contribute_nothing`. MEASURED |
| near-field scales by **time-to-reach, not metres** | ✅ CONFORMS | §1.2 O2 row. |
| **g_tac conditions the operative predictor** | ✅ CONFORMS | `OperativePredictor`'s `intent` port IS the seam (`P_O(z_op, (a,κ) \| g_tac)`); plus the dedicated S-T seam loss `seam_op` on `zhat_op_seam` (detached-trunk forward, so gradient reaches ONLY the goal-injection side — "admissible in planner_side under X3 … BY CONSTRUCTION"), added 2026-08-13 after the PI question exposed that no S-T loss flowed through the goal-conditioned operative prediction. MEASURED |
| one vocabulary, two views (§5) | ✅ CONFORMS | `GoalHead.vocab is GoalConditioner.vocab` — same `nn.Module` object per seam; pinned by `test_goal_token_embedding_is_the_SAME_tensor_emitting_and_consuming` + `test_shared_table_is_counted_once`; `apply_stage_freeze` reports the shared tables explicitly. MEASURED |

### 2.4 Operative brain + the ONE 6 s trajectory

| element | verdict | evidence |
|---|---|---|
| 0–2 s band, clock 10 Hz | ✅ CONFORMS | `OP_BAND_S = (0.0, 2.0)`; `hz_op = 10.0`; `__post_init__` refuses `hz_op` contradicting `dt` ("the operative clock IS the control tick"). MEASURED |
| continuous controls **never tokenised** | ✅ CONFORMS | the emission emits continuous (a, κ) through `UnicycleEmission`; no control quantisation anywhere in v6 (the anchor vocabulary quantises the GOAL, behind default-off flags, never the controls). MEASURED |
| P_O(z_op, (a,κ) \| g_tac) | ✅ CONFORMS | §2.3 last row. |
| unicycle emission — diagram shows `a = a_max·tanh, κ = κ_max·tanh` | 🟦 **DIVERGES — code right** | `V6Config.emission_squash = "squash"` (`tanitad.models.kinematic._squash`: identity inside the range, C1 rational tail, gradient alive at 100× the limit). MEASURED 2026-08-15 (cited in-code): fp32 `d/draw tanh(raw)` is **EXACTLY 0.0 from raw ≥ 10**, and the live S-W history logged a gnorm-354,076 spike — the regime that kills a tanh head. Pinned by `tests/test_emission_squash.py::test_tanh_gradient_is_EXACTLY_zero_from_raw_10_not_51`. `"tanh"` is kept ONLY for bit-exact reproduction of banked v5.8f rows. ⛔ do NOT "fix" code back to the diagram. Diagram-update D-5 |
| **diffusion proposals** — diffuse the 6 s CONTROL sequence, temporally correlated noise | ⬜ NOT BUILT | the v6 fan is `cand_queries` (N=8 learned query embeddings added to `plan_proj(z_op)`) → deterministic `UnicycleEmission` — no denoise loop, no noise schedule, no temporally correlated noise anywhere in `v6.py`/`train_v6_staged.py` (greps: "diffus", "correlated noise" — zero v6 hits). The only diffusion in the programme is REF-C's `AnchoredDiffusionDecoder` (`tanitad/refs/refc.py`, DiffusionDrive-style truncated denoise over TRAJECTORIES, not controls). §4b specs it verbatim ("the diffusion proposal generator diffuses the full 6 s control sequence"). Needs: a proposal-generator module (control-sequence denoiser with OU/temporally-correlated noise), a declared arm flag (state_dict change ⇒ post-S-W introduction via `STAGE_MAY_INTRODUCE`), and a pre-registered comparison vs the query fan. Fix F-15 |
| ONE 60-step (a,κ)@10 Hz rollout; seam discontinuity-free **by construction**; seam metrics **verify, never repair** | ✅ CONFORMS | `PLAN_STEPS=60`, `DT=0.1`; ONE `unicycle_rollout` 0→6 s; bands are SLICES (`split_bands` returns views — "materialising it as a copy is how a 'seam' gets invented"); no seam-repair term exists anywhere. Verification side: trainer logs `plan_ade_0_2s`/`plan_ade_2_6s` per §4b ("report the bands SEPARATELY") ✅; a dedicated X2 seam instrument is 🟨 **BUILT, WITH ZERO REAL-ARM NUMBERS — both halves stated because either alone misleads.** BUILT: `taniteval/taniteval/seam.py` + `taniteval/tools/seam_probe.py` (two nulls, three-valued verdict, episode-cluster bootstrap, `--self-test` against three synthetic arms incl. two planted stitch defects). **The producer side is now closed too** — `taniteval/taniteval/seam_dump.py` banks `emit()`'s 60-step plan and `train_v6_staged.py --dump-seam-plan` writes one dump per checkpoint save, DEFAULT-OFF and zero extra GPU (the plan is already computed for the step's loss); the round trip `real emit() → dump → the real probe CLI` is pinned by `test_v6_seam_dump.py::test_a_REAL_v6_emit_round_trips_into_a_dump_the_PROBE_accepts`. NOT YET MEASURED: **no arm has been probed.** ⚠️ The live v6F run is S-W, where the emission head is at its zero-init, every control is exactly (0,0) and the probe correctly returns DEGENERATE — `seam_dump_from_plan` refuses to bank it by default. ⇒ **this becomes a real number at S-T or later, and not before.** Fix F-16 (instrument closed; the reading is outstanding). Pinned by `test_the_two_bands_come_from_one_rollout`, `test_emission_returns_60_step_controls_and_integrated_waypoints`, `test_v6_seam_probe.py`, `test_v6_seam_dump.py`. MEASURED (presence + tests) / NOT MEASURED (any seam verdict) |
| L_ctrl response form from step 0 · counterfactual arms · gain [0.5, 2]× | ✅ CONFORMS | §1.2 O1 rows. |
| selection cell | — | §3, dedicated reading. |

### 2.5 Context & fallback brain

| element | verdict | evidence |
|---|---|---|
| self-knowledge: fan spread + roll-cost variance → calibrated uncertainty (P7 ρ = 0.716, measured) | ✅ CONFORMS (as instrument) | `scripts/w7_roll_rerank.py` owns P7 (`P7_GATE_RHO = 0.3`, "PRE-REGISTERED: rho >= 0.3, CI excluding 0", episode-cluster bootstrap CI on Spearman ρ). The ρ=0.716 figure is INHERITED (diagram/registry). This is exactly where the refuted roll-cost SURVIVES (§3): as the uncertainty signal, not the selector. Not a module inside `V6Stack` — instrument-side by design (matches the catalog C2 "measure" half). MEASURED (instrument) / INHERITED (ρ) |
| nuisance non-retention **monitored, never optimised** | ✅ CONFORMS | P2 in `scripts/probe_latent_state.py` ("P1+P2 — latent-state decodability & nuisance non-retention"); catalog C1: "not a loss — measure-only; if P2 fails, the fix is masking/augmentation in O3, never a reverse-gradient hack" — and indeed NO loss term in the trainer touches it; P2 sits in the S-W gate's `reported`. MEASURED |
| **fallback fires when imagined consequences disagree beyond the calibrated band** | ⬜ NOT BUILT | no fallback module, trigger, or calibrated-band comparator exists in the stack (greps across `tanitad/`: no v6 fallback machinery). Needs: (1) the calibrated band from P7 (per-stratum ρ + spread→error mapping), (2) a runtime comparator over the fan/roll disagreement, (3) a defined fallback action (e.g. the CV/hold-v0 baseline emission), (4) a T1 eval of the trigger's operating point. Depends on C2's trained-in half, whose T4 vehicle is refuted (§3) — the calibration source survives (P7), the distillation route does not. Fix F-17 |

---

## 3. THE SELECTION CELL — how the diagram must now be read (priority 2)

**The diagram says:** *"Selection = latent MPC (the W7 mechanism): roll every candidate through P_O under its own actions; cost = imagined-vs-candidate consistency + kinematics; distilled selector warm-starts, MPC refines the top-K."* `HIERARCHY_VOCABULARY` §5 says the same one level more generally: *"Each layer's selector is roll-cost-based over ITS OWN predictor (W7 pattern per level)."*

**The measurements that supersede that cell (all INHERITED here from their artifacts, quoted with stamps in the code that cites them):**

1. **The pure roll-consistency argmin is REFUTED — winner's curse.** W7-FULL selected **3.3348 m over a 0.1273 m-oracle fan**; replicated independently on the banked REF-C-XL fan (`GoalDistanceScorer` docstring, `sel_winners_curse_law.py`, 881 windows / 40 episodes / 256 candidates, paired episode-cluster bootstrap): the roll-consistency score selects at **6.4501 m against a 0.1639 m oracle**, is **+5.9787 [+5.3217, +6.7625] WORSE than the shipped supervised selector**, and E-WC measured its normalised error-rank **RISING with N** (0.241 at N=4 → 0.286 at N=256) while its lower-tail hit rate collapses (0.57 → 0.28). A candidate-DEPENDENT cost has a degenerate minimiser; more candidates make it worse. **W7-PROG bound: selection NEEDS a goal-conditioned (candidate-independent) component** — whose error-rank FALLS with N (0.006 → 0.001) with lower-tail hit rate 1.00.
2. **The roll-cost SURVIVES — as the context brain's uncertainty signal**, not the selector: P7 calibration ρ 0.716 (§2.5). The diagram's context-brain cell is therefore RIGHT about the same quantity the selection cell is WRONG about.
3. **"Distilled selector warm-starts"** as drawn = catalog T4 (distil roll-cost rankings into the selector). Distilling a ranking signal measured +5.98 m worse than the incumbent would distil the defect. The selection objective that exists instead is E-OBJ-1's **`softade`** (expected fan error under the scorer's own softmax — metric-aware, measured −0.0974/−0.1670 m recovery, separated; target-softening measured WORSE +0.0909 m). Distillation-for-**calibration** (C2's cost-VARIANCE half) is a different, unrefuted use.
4. **"MPC refines the top-K"** is ⬜ NOT BUILT — and E-S1-0's dose-response is the standing warning for any refine-then-rescore loop: the same weights' refined readout scored **2.8–2.95× worse** than the shipped t=0 confidence, purely for scoring off-distribution (cited in `V6Stack.emit`). A future refinement stage must re-score with a candidate-independent (goal) cost, not the roll-consistency cost.

**What the code builds instead (all currently gated OFF):** `cfg.selector ∈ {"none" (default), "goal", "mlp"}` — `GoalDistanceScorer` (+267 params, `score = −‖endpoint − ĝ‖/τ + b`, goal-echo control mandatory beside every number) with `MLPCandidateScorer` as the pre-registered **capacity control** (information-MATCHED, ~127× params; a "goal" win without it is the C6 confound); trained by `w_select` → `softade`; ε-relaxed WTA (`plan_wta_eps`) bounds the losing candidates.

**And the chain enforces the current refusal:** **SEL-1 is REFUSED** (`v6_chain.SEL1_ADMISSION`, fired 2026-08-16 against `V6F_PLANNER_DESIGN` §5.2's pre-registration with both outcomes committed: measured σ/ADE **9.9915 [7.4492, 13.5119]** vs the refusal line 3.0 — the CI's lower bound is 2.48× the threshold; a 0-parameter constant-yaw-rate goal is 3.96× better than the ridge and STILL not funded ⇒ *"these latents are the wrong surface"*, scope: REF-C, T0-DIAGNOSTIC). `assert_selector_admissible` refuses ANY selector launch until the **E-WC2-SW dump** on frozen S-W latents reaches the pre-registered `SW_LATENT_ADMISSION` thresholds (σ(2 s) ≤ 0.80 m FUNDED · > 1.41 m REFUSED stands; committed before the measurement). The default S-T therefore runs `--selector none`.

**⇒ The binding way to read the diagram's selection cell:** *"roll every candidate through P_O"* survives as the **P7 uncertainty instrument and the context brain's input**; *"cost = imagined-vs-candidate consistency"* is **refuted as a selection rule** and must be replaced in the diagram by a **goal-conditioned, candidate-independent cost** (pending the S-W latent-surface admission); *"distilled selector warm-starts + MPC refines top-K"* is unimplemented and must not be built as drawn (distillation target refuted; refinement must not re-score with the refuted cost). Diagram-update D-1 is the highest-priority PI item.

---

## 4. INPUT/ENCODER + INTERPRETATION HEADS + CROSS-CUTTING

### 4.1 Input & encoder

| element | verdict | evidence |
|---|---|---|
| front camera 120° cyl 256×640 @ 10 Hz | ✅ CONFORMS | `EncoderConfig(in_channels=9, image_size=256, image_width=640, patch_size=16)`; the chain's production argv emits `--frame-h 256 --frame-w 640 --frame-hfov 120 --projection cylindrical` (+ `--v2-subframe 176x624`); `dt = 0.1`. MEASURED |
| + proprioception (v0, controls) | ✅ CONFORMS (with the binding vision-only reading) | v0 enters (1) the unicycle INTEGRATOR ("the integration constant of a kinematic rollout, not a perception input") and (2) the lifted third action channel (`_lift3`, the SPEED_SCALE contract — the validated speed-input fix); recorded controls are the action conditioning. Neither is an encoder input — which is the 2026-08-03 vision-only rule's requirement, POST-DATING the diagram. MEASURED |
| encoder f_θ spatial cell tokens **never pooled away** (DINO-WM) | ✅ CONFORMS | `SpatialGridReadout` → `d_op = n_cells × d_readout` is a RESHAPE (`V6Stack.cells`: "pooling is where geometry goes to die"); O2/O3 act on cells directly; the readout is the geometry firewall (`d_op` derived, never set; build-time refusal on mismatch). MEASURED |
| **E-ENC arm pre-registered**: (a) shared + per-layer adapters vs (b) per-layer encoders | ✅ CONFORMS | `V6Config.shared_encoder` (True = arm a, the default) with arm (b) fully buildable (`encoder_tac/str`, `readout_tac/str`, `own_frames_*` refused when mis-configured); CLI `--per-layer-encoders`; decision metric in-code: "per-layer P-battery pass rate at MATCHED TOTAL PARAMS; a tie goes to the common encoder" + `matched_param_config` does the matching MEASURED-by-instantiation. Arm (b) has not been RUN — that is the pre-registration working as designed, not a gap. MEASURED |
| SIGReg (full_relaxed) anti-collapse | ✅ CONFORMS | §1.2 O6 row (naming: the implemented call is `position_relaxed(sigreg, z, free_dims)`; `free_dims=0` default = the full form). MEASURED |
| **latent spectrum monitored per layer** | 🟥/⬜ partial-wrong | operative-only; catalog X4 (per-layer SIGReg + spectrum) NOT BUILT. Fix F-4. MEASURED |
| labels may supervise readouts and goal heads only — never a trunk loss | ✅ CONFORMS (header reading) | §2.1 last row + D-8 wording item. |
| ViT-5 recipe (live run) | — (not in the transcribed diagram) | noted for completeness: `vit5_encoder` is a DECLARED arm (changes param count); the live config E runs it at 768×12 with registers+RoPE (INHERITED, handover doc). |

### 4.2 Interpretation heads — frozen latents (`HIERARCHY_VOCABULARY` §6 is the status table the diagram compresses)

| head | verdict | evidence |
|---|---|---|
| perception agent slots (bbox cx,cy,yaw,l,w · state v,yaw-rate,occluded · class & size) | 🟨 **PARTIAL — the MODULE is built; it has never been trained** | ⬜→🟨 2026-08-16. BUILT: `tanitad/models/agent_slots.py` (`AgentSlotDecoder`, DETR-style queries over spatial tokens, Hungarian match cost, `SLOT_FIELDS`/`SLOT_SLICES`), wired behind `V6Config.agent_slots` and **DEFAULT-OFF**. MEASURED by me 2026-08-16: default build **87,893,449 params / 405 state_dict keys** (unchanged); with `agent_slots=True` **91,100,894 / 467** ⇒ the decoder is **3,207,445 params**, inside §6's "~2–4 M" band. Pinned by `tests/test_v6_agent_slots.py`, and the default forward emits no new key (`test_v6_gstr_port.py::test_default_forward_is_bit_identical_and_emits_no_new_key`). ⛔ **WHAT REMAINS, and it is the load-bearing half:** (a) **no train-corpus obstacle join** — `obstacle.offline` is joined by a pod-side step (`build_obstacle_join.py`, `…/2026-08-03-obstacle-offline-join`) that has not been run over the canonical 2 376-episode train corpus, so there are no slot TARGETS to train against; (b) **the head has never been trained or evaluated** — every number about it is a parameter count, not a capability. ⇒ presence ≠ capability; do not read 🟨 as "perception works". Needs: the corpus join + the prereg'd training arm + ~1 GPU-day (§7 item 4). Fix F-18. Evidence class: MEASURED (module presence, param counts, default inertness) / **NOT MEASURED (any perception quality)**. What would change it: a completed train-corpus join and a trained arm with slot metrics |
| BEV occupancy readout (P8) | ✅ CONFORMS | `scripts/train_p8_occupancy.py` (attempt-2, P4 visible/occluded split machinery in-file) + `tests/test_p8_v6.py` — the v6 port the brief said "was just ported" is real. MEASURED (presence + tests) |
| kinematic readout (speed R² 0.99 · yaw-rate 0.86 · curvature 0.84 on v5f) | ✅ CONFORMS | P1 in `scripts/probe_latent_state.py`; the R² figures are INHERITED (diagram/registry; §6 calls it "P1-proven"). MEASURED (instrument) / INHERITED (numbers) |
| lead state (gap · closing speed · TTC) — absent in v5f (measured); v6 targets it label-free via O2/O3/O4 | ✅ CONFORMS (as a target statement) | the head is NOT BUILT (§6: "after LF0 locates the information"); the locator (`scripts/lf0_bev_lead.py`) and the label/eval side (`taniteval/tools/build_lead_block.py`, lead-join keys in `eval_four_families.py`) exist; O2 (near-field cells), O3 (occlusion blocks), O4 (interaction oversampling) are exactly the three label-free levers the diagram names, all implemented (§1.2). The "absent in v5f" measurement is INHERITED. |
| uncertainty + probe-gradient saliency (P9) | 🟨 PARTIAL | uncertainty = P7 ✅ (§2.5). P9: the REUSE SEAM exists (`probe_latent_state.py` dumps `probe_arrays.pt` — "the P9 probe-gradient saliency pass rebuilds nothing"; full-set fit kept for probe DIRECTION) but no standalone P9 pass/script was found (2 probes: grep "P9" over `scripts/` + `taniteval/tools/` → dump seam only; §6 status "rides P8 harness"). Fix F-19 |
| scenario/domain tags from the VLM pipeline — **eval strata only, never an input** | ✅ CONFORMS | no `scena`/VLM import anywhere in `v6.py`/`train_v6_staged.py`; the training batch carries only frames/actions/poses/future_*; O4 is actions-only by construction with the eval-strata rule quoted in its docstring; `GoalHead` refuses undeclared conditioning paths (no `**kwargs`, `d_cond=0` head refuses a cond — pinned by `test_goal_head_refuses_an_undeclared_conditioning_path`). X1's "audit as CI check" lives as these signature pins. MEASURED |

### 4.3 Cross-cutting (X-measures)

| element | verdict | evidence |
|---|---|---|
| **X3 gradient isolation** — planner/goal heads NEVER into any encoder; higher→lower only through stop-grad/EMA | ✅ CONFORMS (the strongest row in the audit) | `ISOLATION_MATRIX` as data + `V6Stack.assert_isolation` as a REAL autograd probe over the DECLARED `planner_side` surface (linear non-symmetric reduction so the zero-init emission head cannot fake isolation; every parameter made differentiable FOR THE PROBE so a frozen stage cannot produce a vacuous pass; probes all three edges and names live parameters). The surface is total-by-test (`test_planner_surface_is_total`); mis-wired arms are pre-registered controls (`--no-isolate-planner`/`--no-isolate-uplink` behind the ack flag) and the probe FAILS on them by test. Encoder-protected set includes the READOUTS ("a gradient that stops at the readout has still sculpted the state"). Build-time enforcement: `build_stack_from_args` runs `assert_isolation` before any GPU time. MEASURED |
| X1 disjointness audits | ✅ CONFORMS (as tests) | goal-admissibility stated per module with the binding check ("could this have been computed from the situation classifier's output?"); no situation-classifier port exists; signature pins + the `encode()` refusal of an undeclared categorical channel ("an undeclared conditioning path is exactly what the disjointness audit (X1) forbids"). A standalone pre-launch audit SCRIPT (the catalog's wording) does not exist — the test suite is the audit. Acceptable reading; noted in F-3's instrument. MEASURED |
| X2 seam metrics (paired estimator only) | 🟨 PARTIAL | §2.4 seam row: band metrics ✅ in-trainer; the per-seam paired-CI instrument ⬜. Fix F-16 |
| X4 per-layer SIGReg/spectrum | ⬜ NOT BUILT | fix F-4. |
| X5 staging | ✅ CONFORMS | §1.1. |

---

## 5. CODE-FIX LIST (prioritised — 1–3 are the GPU-day protectors)

| # | priority | fix | size / risk |
|---|---|---|---|
| **F-1** | ⛔ **P0 — BEFORE S-T LAUNCHES** | **Build the `g_str → P_T` conditioning port** (§1.3). The binding diagram, §5, and the trainer-design ASCII all spec `P_T(z_tac, a_tac \| g_str)`; the code conditions only the tactical GOAL HEAD. Recommended shape: a **zero-init additive conditioning adapter** (e.g. `predictor_tac_gport: Linear(d_goal_embed → d_tac-predictor-conditioning)`) rather than widening `FTac.in_proj` — widening changes existing state_dict SHAPES and a shape mismatch bypasses `STAGE_MAY_INTRODUCE` adjudication entirely (`load_state_dict(strict=False)` still RAISES on shapes — measured, quoted in `trainer_argv`'s `--n-candidates` note). A NEW module rides the designed introduction path: add `("predictor_tac_gport.", "layer_tac")` to `_GROUP_PREFIXES`, extend `STAGE_MAY_INTRODUCE["S-T"] += ("predictor_tac_gport.",)`, feed `e_g_str` (detached under the planner cut? NO — g_str→P_T is a WM-side downlink like g_tac→P_O: pass `self._cut(e_g_str, cut)` for symmetry with the intent port), extend `planner_side`/`uplink_side` declarations + `test_planner_surface_is_total`, and zero-init so S-T's t1 loss is continuous at introduction. **Escalating rather than hot-editing**: this changes training geometry and the isolation surface — it needs its own tests and a dry-ladder pass, not an audit-time edit. |
| **F-2** | **P1 — with S-T's gate assembly** | Complete the S-T four-families gate: this audit added the two missing families to `reported` + the both-horizons criterion (§7); remaining: (a) verify/extend `taniteval/tools/eval_four_families.py` to emit every family at BOTH 0–2 s and 0–6 s (UNVERIFIED sub-item, §1.3), (b) the PI/integrator decision on promoting `LONGITUDINAL_family` to `required` with a numeric criterion (none is pre-registered — inventing one was out of audit scope). |
| **F-3** | **P1 — the moment S2 lands** | Build the **goal-provenance audit instrument** (S-S gate; surface entry added in §7): config-audit + X3-probe evidence that every g_str label path terminates in `goal_head_str`; label-artifact hashes in the run row; the situation-classifier disjointness check. Then **promote `goal_provenance` from `reported` to `required`** in `STAGE_GATE_SPEC["S-S"]` (the promotion trigger is recorded in the in-code comment). Owner should live beside the S2 wiring (DataEng S2-gap stream — escalate integration to the orchestrator, do not leave it in this doc only). |
| F-4 | P2 | X4: per-layer spectrum (cheap: push `z_tac`/`z_str` through `spectrum_report`/`SpectrumAccumulator` at `--spectrum-every`; note d_tac=512/d_str=256 lower the admissibility ceiling arithmetic) and per-layer SIGReg as a DECLARED arm (a new loss term changes training — not a default flip). |
| F-5 | P2 | Promote **P8** into the S-W gate `required` once a real P8 artifact exists for the live run (diagram says gate; code says reported — the decision needs the instrument's maturity, PI call; pair with D-3). |
| F-6 | P3 | Align `--o5-k` default (20) with the §4b 6 s contract the live run already uses (60) — or keep 20 and document the live-run override in the runbook; either way remove the silent divergence between default and contract. |
| F-7 | P3 | T2 manoeuvre contrastives (time-reversal + lane-mirror negatives on `z_tac`) — label-free, S-T-stage loss + weight. |
| F-8 | P3 | T5 temporal-consistency selection loss — needs consecutive-window batches (sampler change) + plan-switch-rate logging (its gate row asks for LATERAL yaw/curvature MAE at selection level). |
| F-9 | P3 | T3 interaction curriculum (multi-agent entropy from the P8 occupancy readout) — gated on P8 maturity. |
| F-10 | P3 | S3 domain-stratified mix — VLM strata as SAMPLER input (admissible: data mix, not a model input; declare it). |
| F-11 | P3 | S1 multi-tick strategic rollout (8–30 s = 4–15 strategic ticks) — currently 1-tick; the gate reports `S1_ade_8_30s` against a capability the loss never exercises. |
| F-12 | P4 | `v6.py` `PARAM_BUDGET` comment still says "sub-300M is a programme INVARIANT (CLAUDE.md)" — stale vs the PI's 350 M raise; reword to "the DEFAULT budget; the live budget is a declared `--param-budget` decision (config E: 350 M)". (CLAUDE.md itself is out of this agent's scope.) |
| F-13 | P4 | `FOLLOW_MAIN_ROAD` default: implement the inference-time default injection (when no route is set / the head abstains) — currently a documented convention only. |
| F-14 | P4 | SPEED_BAND derivation: integrate the sign/OCR + corridor-prior source (`…/2026-08-04-target-speed/code/vt_band_from_vision.py`, currently untracked incoming) into the g_tac label path — coordinate with the target-speed stream owner. |
| F-15 | P4 | Diffusion proposals (§2.4): control-sequence denoiser with temporally correlated noise, as a DECLARED arm introduced post-S-W via `STAGE_MAY_INTRODUCE`, pre-registered against the query fan. Do not build before the selection question (F-1/SEL-1) settles — proposals and selection are one experiment surface. |
| F-16 | P4 | X2 instrument: per-seam paired episode-cluster CI (the ctx→tactical +0.0148 lesson) + a 2 s-boundary continuity verifier on the emitted winner (verify-never-repair). |
| F-17 | P4 | Fallback trigger (§2.5): P7-calibrated band + runtime comparator + defined fallback emission + T1 operating-point eval. |
| F-18 | P4 | PERCEPTION-AGENTS slot decoder (§4.2) — prereg exists in §6/§7 of the vocabulary doc; ~1 GPU-day estimate INHERITED. |
| F-19 | P4 | P9 saliency pass consuming the banked `probe_arrays.pt` dump. |

## 6. DIAGRAM-UPDATE LIST (separate — these go to the PI; the code is right or the world moved)

| # | diagram cell | update |
|---|---|---|
| **D-1** | **Selection** | Replace the roll-consistency selection cell with the §3 reading: roll-cost → **P7 uncertainty (context brain input)** only; selection = **goal-conditioned candidate-independent cost** (SEL-1, currently REFUSED pending the E-WC2-SW dump at the pre-registered σ(2 s) ≤ 0.80 m threshold); "distilled selector warm-starts / MPC refines top-K" must not be built as drawn (distillation target refuted +5.98 m; refined-readout rescoring measured 2.8–2.95× worse). |
| **D-2** | Header "sub-300M" | The live line is config E, **336,542,025 params at the PI-raised 350 M budget** ("the bigger optimized variant, I think 330 Mio"). Either restate the header ("≤ 350 M, PI 2026-08-13") or record sub-300M as the ORIGINAL target with the raise as a dated decision. |
| D-3 | S-W gate | P8 is `reported`, not `required`, until the just-ported P8 instrument produces its first real artifact (pair with F-5 promotion). |
| D-4 | S-W gate "rank retention ≥ 0.8×" | The naked criterion is MEASURED noise-prone (fires 9–38 % on nothing, power 0.11 at n=48); the binding form is `o6_rank_verdict`'s three clauses (admissible ceiling ≥ 1024 via pooling · CI-based retention vs 0.8 · absolute floor 64 · can return INCONCLUSIVE). Keep the 0.8 number, add the estimator. |
| D-5 | Emission `a = a_max·tanh, κ = κ_max·tanh` | Stale: fp32 tanh gradient is EXACTLY 0 from raw ≥ 10 (measured; a gnorm-354,076 spike is on the live run's record). The shipped bound is `_squash` (identity in-range, C1 rational tail); tanh survives only as the bit-exact legacy arm. |
| D-6 | Staged-training row | Add the backward-invalidation edge the code enforces: S-S moves `e_g_tac` (the frozen selector's only input), so the S-S gate REQUIRES `sel_gap_revalidated` + `TACTICAL_revalidated` — S-T's certificate does not survive S-S. |
| D-7 | Header "four layers each with its own predictor and action space" | The context/fallback brain has neither (by the diagram's own context-row content) — reword to "three predictive layers + a context/monitoring layer". |
| D-8 | Encoder cell "labels may supervise readouts and goal heads only — never a trunk loss" | Align to the header's "no PERCEPTION label, map or reward": catalog-REQUIRED O1 supervises through the metric readout with EGO odometry from step 0 of S-W (labels-may-use-ego, PI 2026-08-03), which the literal encoder-cell wording would forbid. |
| D-9 | Tactical a_tac examples | Naming drift: EVADE lives in the g_tac GOAL vocabulary (`EVADE_IN_CORRIDOR`), not the LAT action list; WAIT → `HOLD` (action) / `WAIT_FOR_ONCOMING` (goal); GAP→`GAP_TARGET`, STOP→`STOP_POINT`, LIGHT_REACT→`TRAFFIC_LIGHT_REACT`. Cosmetic, but the diagram is binding — it should name the shipped tokens. |

## 7. EDITS MADE IN THIS AUDIT (both data-only, both strengthening-direction, tests green)

`stack/scripts/train_v6_staged.py`, `STAGE_GATE_SPEC` only:
1. **S-T:** `reported` += `LONGITUDINAL_family`, `STRATEGIC_family` (owners → `eval_four_families.py`); `criteria` += `four_families_horizons` (the §4b both-horizons requirement + the 2026-08-02 per-family-declaration rule).
2. **S-S:** `reported` += `goal_provenance` (owner names the to-be-built config-audit; in-code comment records the diagram provenance and the promote-to-required trigger = S2 landing).

Behaviour-safe by construction: `stage_gate_dict` adjudicates `required` only; `reported` probes are auto-stubbed "not-run" by `run_stage_gate` and never flip a verdict. **Verified: `pytest tests/test_v6_staged.py tests/test_v6_stage_revalidation.py tests/test_v6_chain.py -q` → 144 passed, 0 failed** (2026-08-16, this audit; the full 3,346-test suite is the committer's gate). No other code was modified.

---

## Deliverable manifest

| artifact | where |
|---|---|
| this matrix + fix lists | `TanitAD Research Hub/Architecture & Inference/Implementation/incoming/2026-08-16-diagram-conformance/DIAGRAM_CONFORMANCE.md` (repo, staged) |
| gate-surface edits | `stack/scripts/train_v6_staged.py` (repo, staged — `STAGE_GATE_SPEC` S-T/S-S reported additions, §7) |
| escalations for the orchestrator | **F-1 must land before any S-T launch** (GPU-day protector); F-3's instrument belongs with the DataEng S2-gap stream; D-1/D-2 need the PI. |

*Nothing lives on a pod or a worktree; both artifacts are in the working tree and staged on `agent/arch-inf-20260803`.*
