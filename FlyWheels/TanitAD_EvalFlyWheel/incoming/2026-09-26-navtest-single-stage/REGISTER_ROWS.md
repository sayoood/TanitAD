# Rows for `Project Steering/GOALS_AND_CLAIMS.md` — W8, 2026-09-26 (for the Master Mind to insert)

⛔ **Not edited in place, on purpose:** this worktree's `GOALS_AND_CLAIMS.md` (blob `740e4a59…`) is OLDER than
the branch tip's (`agent/arch-inf-20260803:` blob `baeaf548…`, queried through the mirror). Editing the stale base
would revert the tip's newer rows on commit — the revert hazard `CLAUDE.md` documents. The rows below go into the
tip version, in the NavSim harness block (beside `D-NAVSIM-REUSE-FLOORS-1` / `D-NAVSIM-ARTIFACT-CRITERIA-1`).

| id | claim | status | evidence |
|---|---|---|---|
| **D-NAVSIM-NAVTEST1S-1** | ⭐ **`navsim_v2 --split navtest_single_stage` is WIRED** — NAVSIM v2 EPDMS on navtest through the devkit's ONE-STAGE runner (`run_pdm_score_one_stage.py`), non-reactive log-replay traffic (the runner's default, passed explicitly), logs from the FULL D: set (the C: dir holds navhard's 76 and `dataloader.py:34-36` skips an absent log in silence), headline = the `average_all_frames` row, HUMAN a legal reference, interval = `navsim_log_cluster_bootstrap` with the single-stage aggregation. The `:228` refusal is lifted for this split only. A 218-token whole-log subset ran COMPLETE end-to-end (HUMAN 95.13 / STOP 57.98 / CV 26.40 EPDMS — a SUBSET, `_scratch`, **not a result**); criteria_check 0 violations ×3; 29 tests; full taniteval suite 1,888 passed / 0 failed | **SUPPORTED (built, tested, smoke COMPLETE) — the 12,146-token run is PENDING** (cache building RAM-gated; scheduled after the refcv6 FINAL by an agent-free waiter) | `FlyWheels/TanitAD_EvalFlyWheel/incoming/2026-09-26-navtest-single-stage/RESULT.md`; `taniteval/taniteval/bench/navsim/single_stage.py`; `taniteval/tests/test_bench_navsim_navtest_single_stage.py` |
| **D-NAVSIM-V1V2-INPUT-1** | ⛔ **NAVSIM v1.1 and v2 (0a380a9) compute NC and DAC with the SAME function but NOT the same inputs: the metric cache's observation window is 5.0 s in v1.1 and 4.0 s in v2, and an object observed EXACTLY ONCE in the window is placed at EVERY step (a "ghost").** A pre-registered exact per-token NC identity FAILED on 1/218 smoke tokens (CV: v1 0.5, v2 1.0): 5 objects seen only at +5.0 s in the log are ghosts at all 41 steps in v1 and absent in v2. Discriminating experiment: the **v2 function on v1.1's inputs reproduces W3's v1.1 NC and DAC exactly on 654/654 token×arm pairs** (CV/STOP/HUMAN), and the v2 function on v2 inputs reproduces our run exactly (control). Ghosts run BOTH ways: of 218 smoke tokens only 9 carry identical observations. DAC (observation-free) agrees exactly on 418 × 3. ⇒ Any v1-vs-v2 NC/TTC comparison must be read through the observation difference, never as a scorer change | **SUPPORTED (MEASURED; three independent probes: both caches, the log, a counterfactual re-score)** | `…/2026-09-26-navtest-single-stage/raw/diag_937ca624/`, `raw/counterfactual_smoke218_*.json`, `raw/e_obs_class_smoke218.json`; v1 `metric_cache_processor.py:101`, v2 `:109`, `:165-166`/`:177-178` |
| **D-NAVSIM-1S-SUBSET-AGG-1** | ⚠️ **The devkit's one-stage aggregation CRASHES on a token subset with no adjacent frames** (`run_pdm_score_one_stage.py:222`, `pd.concat([])` of an empty adjacency mapping) — W3's random sub200 (≈2 tokens/log) scored 200/200 and then died. Not a full-split risk (0.5 s frames); **subsets must be whole logs** (which also keeps every token's EC — hence its `score` — identical to the full split) | **SUPPORTED (MEASURED)** | `raw/smoke200_run/`; PREREG AMENDMENT A1 |
| **D-NAVSIM-PREAGG-COUNT-1** | ⛔ **`scoring.py` counted the pre-aggregation dump's LINES, not records**: `ego_simulated_states` cells span lines (24,600 lines for 200 records), so `AGGREGATION_FAILED` never fired and a fully-scored arm read as a plain FAIL | **SUPPORTED and FIXED** (csv records; pinned by a multi-line-cell test) | `taniteval/taniteval/bench/navsim/scoring.py` |
| **D-BENCH-CRITERIA-CP1252-1** | ⛔ **`BenchContext.run_criteria` let `criteria_check.py` crash under a cp1252 parent** (`⛔` at print → rc 1, NO json — the check silently never ran; it made the existing offline twin fail in a non-UTF-8 shell) | **SUPPORTED and FIXED** (`PYTHONUTF8=1` in the child env) | `taniteval/taniteval/bench/contract.py` |
| **RETRACTION-CANDIDATE (W8 PREREG §2)** | My pre-registration read *"NC function text identical + t0-collider removal the only input change"* as *"NC identical outside E-T0"*. Wrong: the observation window changed underneath the identical function. **Class: identical function text read as identical metric — an input pipeline changed underneath it.** The pre-registered C-NC verdict stays FAILED on the smoke; AMENDMENT A2 (hashed before any full-split score) adds class-aware rules + the function-identity test | for `RETRACTION_LOG.md` | `PREREG.md` A2 |
